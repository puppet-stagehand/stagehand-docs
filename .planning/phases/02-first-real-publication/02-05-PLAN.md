---
phase: 02-first-real-publication
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - infra/environments/testpilots/variables.tf
autonomous: false
requirements: [PUB-06, PUB-07]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A real, same-repository pull request touching infra/** produces a real, value-free OpenTofu plan summary for all three roots, through a plan Environment, with the job-level same-repository guard running before the Environment is attached."
    - "No AWS account identifier, credential, state file, saved plan, terraform.tfvars, or backend.hcl value is committed anywhere in this phase's history."
  artifacts:
    - "infra/environments/testpilots/variables.tf: one documentation comment on hosted_zone_id"
    - "A merged, real pull request proving infrastructure.yml's plan job runs end-to-end"
  key_links:
    - "02-02's configured *-plan Environments -> a real infra/** PR -> infrastructure.yml's plan job -> an uploaded, value-free tofu-plan-* artifact per root"
---

<objective>
Prove PUB-06 by opening a real, same-repository pull request that touches `infra/**` and observing
`infrastructure.yml`'s plan job actually run — the code path is already implemented per
02-RESEARCH.md Pattern 3; this plan verifies it behaviorally now that 02-02's plan Environments
exist. Then run the phase-closing PUB-07 sweep: confirm nothing sensitive was ever committed across
every plan in this phase.

Purpose: PUB-06 (a same-repo PR touching infra/** produces a real, value-free plan summary through a
plan Environment) and PUB-07 (no AWS account identifier, credential, state file, saved plan,
terraform.tfvars, or backend.hcl value committed at any point during this phase's work).
Output: One real, observed, successful infrastructure plan run, and a clean PUB-07 sweep across the
whole phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-first-real-publication/02-RESEARCH.md
@.github/workflows/infrastructure.yml
@docs/operations/github-environments.md
@infra/environments/testpilots/variables.tf
</context>

<artifacts_this_phase_produces>
- `infra/environments/testpilots/variables.tf`: one added documentation comment on `hosted_zone_id` — the real, safe, reviewable infra change this plan's proof PR carries
- A closed-out, phase-wide PUB-07 sweep recorded in the SUMMARY
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Open a real same-repo PR touching infra/**</name>
  <read_first>infra/environments/testpilots/variables.tf, .github/workflows/infrastructure.yml</read_first>
  <precondition>02-02's six GitHub Environments exist and are configured (testpilots-plan/beta-plan/stable-plan hold real plan-role ARNs), and 02-03's testpilots apply has completed, so the testpilots plan job reads real, already-applied state rather than a from-scratch create plan.
  <files>infra/environments/testpilots/variables.tf</files>
  <action>
    On a new branch, add one genuine, useful documentation comment above the `hosted_zone_id`
    variable in `infra/environments/testpilots/variables.tf` noting it must be the same zone 02-01
    created (cross-reference by phase/plan). Run `tofu fmt -check -recursive infra` and fix formatting
    if needed. Commit and push, then `gh pr create` against `main` from the same repository (not a
    fork) with a title/body describing this as the PUB-06 proof PR.

    This PR touches `infra/**`, so `infrastructure.yml`'s `plan` job (matrix over `testpilots`,
    `beta`, `stable`) will queue behind each of the three `-plan` Environments' required-reviewer
    protection. Watch the PR's checks (`gh pr checks`) and confirm the `validate` job (unprotected)
    runs immediately and passes.
  </action>
  <verify>
    <automated>gh pr checks --json name,state 2&gt;/dev/null | grep -c '"name":"Validate OpenTofu"'</automated>
  </verify>
  <acceptance_criteria>
    - The PR is opened from a branch in `puppet-stagehand/stagehand-docs` itself (same-repository, not a fork) — this is what PUB-06's job-level guard checks.
    - `tofu fmt -check -recursive infra` passes on the new commit.
    - The `validate` job on the PR completes successfully.
    - The three `plan` matrix jobs (testpilots/beta/stable) are visible as queued/pending, blocked on their respective `-plan` Environment's required reviewer.
  </acceptance_criteria>
  <done>A real same-repo PR touching infra/** is open, validate passed, and all three plan jobs are queued behind their protected Environments — ready for the human review step this Environment protection exists to require.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Approve the plan-Environment deployment reviews and confirm the plan jobs ran</name>
  <what-built>
    A real pull request (link in the run output) touching `infra/environments/testpilots/variables.tf`,
    with `infrastructure.yml`'s three `plan` matrix jobs queued behind `testpilots-plan`,
    `beta-plan`, and `stable-plan`'s required-reviewer protection.
  </what-built>
  <how-to-verify>
    1. Open the PR in GitHub and review the one-line diff — confirm it is exactly the documentation
       comment described in Task 1, nothing else.
    2. Approve each of the three pending deployment reviews (Actions tab → the queued run → Review
       deployments → approve `testpilots-plan`, `beta-plan`, `stable-plan`). Note: 02-02's Task 1
       documented that a solo maintainer approving their own PR's deployment may be blocked by
       "prevent self-review" — if so, this is the friction that plan flagged, not a bug in this plan;
       use a second GitHub identity if one exists, or make the deliberate, recorded call to add one
       before this proof can complete.
    3. Once approved, confirm each `plan` job completes and uploads a `tofu-plan-{root}` artifact.
  </how-to-verify>
  <resume-signal>Type "approved" once all three plan jobs have run and uploaded their artifacts, or describe what blocked it.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Confirm the plan artifacts are value-free, merge, and run the PUB-07 sweep</name>
  <read_first>.github/workflows/infrastructure.yml (the `jq` pipeline that builds plan-summary.txt)</read_first>
  <files>(no repository files — verification and a merge of Task 1's branch)</files>
  <action>
    Download each of the three `tofu-plan-{root}` artifacts (`gh run download {run-id}`) and inspect
    `plan-summary.txt` for each: confirm it contains only resource addresses and change-action counts
    (`count\t{action}\t{n}` and `resource\t{address}\t{action}` lines per infrastructure.yml's `jq`
    pipeline) — no attribute values, no ARNs, no bucket names, no secrets. Confirm no binary `tfplan`
    file was uploaded anywhere (only `plan-summary.txt`).

    Merge the PR (it is a real, useful documentation improvement — no need to close it unmerged).

    Run the phase-closing PUB-07 sweep across the whole of this phase's work (all five plans'
    commits, not just this one): `git log --all -p -- infra/bootstrap/terraform.tfvars
    infra/environments/*/backend.hcl '*.tfstate*' '*.tfplan' tfplan` (expect no output — these paths
    were never committed); `git grep -nE '[0-9]{12}' -- . ':!node_modules' ':!*.lock'` filtered to
    exclude comment-only/example matches, reviewed by hand for any real AWS account ID; confirm no
    `AKIA`-prefixed string or other credential-shaped literal appears anywhere in tracked history
    touched by this phase (`git log --all -p -- . | grep -c 'AKIA'` returns `0`).
  </action>
  <verify>
    <automated>git log --all --diff-filter=A --name-only -- infra/bootstrap/terraform.tfvars 'infra/environments/*/backend.hcl' '*.tfstate*' | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - All three `plan-summary.txt` artifacts contain only resource addresses and action counts — no attribute values.
    - No binary `tfplan` artifact was ever uploaded.
    - `git log --all --diff-filter=A --name-only` for `terraform.tfvars`, `backend.hcl`, `*.tfstate*`, `*.tfplan` returns zero files across this phase's commits.
    - `git log --all -p | grep -c 'AKIA'` returns `0`.
    - Any 12-digit numeric match from the account-ID grep is manually reviewed and confirmed to not be a real AWS account identifier (or is absent entirely).
  </acceptance_criteria>
  <done>PUB-06 is proven end-to-end with real artifacts inspected for content, not just job success; PUB-07's sweep is clean across the whole phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pull request → plan-role AWS credentials | The exact control PUB-06 exists to prove works: same-repo guard, then Environment protection, before any AWS credential is issued. |
| Repository history → public GitHub | PUB-07's boundary — this is a public repository; anything committed here is published. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Elevation of Privilege | A fork PR reaching a plan Environment | critical | accept | Already mitigated by infrastructure.yml's job-level `github.event.pull_request.head.repo.full_name == github.repository` guard, evaluated before Environment attachment (02-RESEARCH Pattern 3, verified) — this plan proves it, does not newly implement it. |
| T-02-17 | Information Disclosure | A raw/binary Terraform plan uploaded as a CI artifact | high | mitigate | Task 2 downloads and inspects every artifact by hand to confirm only the `jq`-summarized, value-free text exists — not just that the job "succeeded." |
| T-02-18 | Information Disclosure | An AWS account ID, credential, or state file committed anywhere in this phase | critical | mitigate | Task 2's `git log --all` sweep runs across every commit this phase produced, not just the working tree, closing the "committed and later removed" gap a working-tree-only grep would miss. |
</threat_model>

<verification>
- A real same-repo PR's plan jobs ran end-to-end after human deployment approval and uploaded three value-free plan-summary artifacts.
- The artifacts are inspected by hand and contain no attribute values, ARNs, or secrets.
- `git log --all` across this phase's commits contains no `terraform.tfvars`, `backend.hcl`, `*.tfstate*`, `*.tfplan`, or `AKIA`-prefixed string.
</verification>

<success_criteria>
PUB-06 is proven with a real, human-reviewed pull request and inspected artifacts. PUB-07's sweep is
clean across every commit this phase produced.
</success_criteria>

<output>
Create `.planning/phases/02-first-real-publication/02-05-SUMMARY.md` when done
</output>
