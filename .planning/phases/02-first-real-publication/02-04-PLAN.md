---
phase: 02-first-real-publication
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - scripts/check-live-deployment.ts
  - tests/unit/check-live-deployment.test.ts
  - .github/workflows/deploy.yml
  - scripts/deploy-site.sh
  - docs/operations/github-environments.md
autonomous: true
requirements: [PUB-04, PUB-05, GATE-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A missing GitHub Environment variable in the deploy job now fails the run instead of reporting green with nothing uploaded."
    - "A merge to main runs Deploy site through an executed Upload site step for testpilots, for real."
    - "After a real deploy, an automated post-deploy step confirms the live distribution serves every documented route, both JSON endpoints, the branded 404, and the exact deployed commit."
    - "testpilots.puppetstagehand.com's own initial route surface is proven reachable and correct via CloudFront's default domain, with the custom-hostname path still explicitly recorded as blocked on the deferred cutover."
  artifacts:
    - "scripts/check-live-deployment.ts (exports a testable verifyLiveDeployment function)"
    - "tests/unit/check-live-deployment.test.ts"
    - ".github/workflows/deploy.yml: hard-fail gate, commit-stamp step, live-verification step"
    - "scripts/deploy-site.sh: /deployed-commit.txt added to the invalidation path list"
  key_links:
    - "02-03's captured content_bucket_name / distribution_id / deployment_role_arn / distribution_domain_name -> this plan's testpilots GitHub Environment variables -> deploy.yml's real role assumption and upload"
    - "deploy.yml's new 'Stamp deployed commit' step -> dist/deployed-commit.txt -> uploaded by the existing deploy-site.sh sync -> read back by check-live-deployment.ts's commit-stamp assertion"
---

<objective>
Close the two remaining gaps 02-RESEARCH.md identified as this phase's one genuine code change:
`deploy.yml`'s soft-skip that reports green while publishing nothing (GATE-02), and the missing
live-verification step that would catch it. Wire testpilots's real deploy variables from 02-03, land
a real deploy of `main`, and prove the live site answers correctly — via CloudFront's own default
domain, since the custom hostname stays deliberately unreachable this phase.

Purpose: PUB-04 (the deploy job's Upload site step actually executes with correct cache-control),
PUB-05 (testpilots serves every documented route, both JSON endpoints, and the branded 404), GATE-02
(a post-deploy step asserts the deployed environment answers and serves the exact commit).
Output: A hardened deploy.yml, a tested live-verification script, and one real, verified `Deploy
site` run against testpilots.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-first-real-publication/02-RESEARCH.md
@.github/workflows/deploy.yml
@scripts/deploy-site.sh
@scripts/check-built-routes.ts
@scripts/check-links.ts
@src/pages/404.astro
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
- `scripts/check-live-deployment.ts`: exports `verifyLiveDeployment(options)` — asserts every required route + JSON endpoint returns 200, a nonexistent path returns 404 with the branded body, and `/deployed-commit.txt` matches the expected SHA, with bounded retry/backoff for first-deploy CloudFront propagation
- `tests/unit/check-live-deployment.test.ts`: unit tests with a stubbed `fetch`
- `.github/workflows/deploy.yml`: `Check deployment configuration` now `exit 1` on missing variables (not a soft skip); new `Stamp deployed commit` step; new `Verify live deployment` step
- `scripts/deploy-site.sh`: `/deployed-commit.txt` added to the CloudFront invalidation path list
- `docs/operations/github-environments.md`: `SITE_CHECK_URL`'s real testpilots value documented
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the live-verification script test-first</name>
  <read_first>
    scripts/check-built-routes.ts (the exact required-route list this script's route set mirrors),
    scripts/check-links.ts (fetch/error-handling and logging conventions to match),
    src/pages/404.astro (the "Page not found" marker the branded-404 assertion checks for),
    .planning/codebase/CONVENTIONS.md (naming, error handling, logging patterns)
  </read_first>
  <behavior>
    - `verifyLiveDeployment({ siteUrl, expectedSha, fetchImpl })` resolves without throwing when every
      check passes: all 9 routes (`/`, `/tiers/`, `/compatibility/`, `/docs/`, `/docs/getting-started/`,
      `/docs/security/`, `/support/`, `/data/tiers.json`, `/data/compatibility.json`) return 200; a
      fixed nonexistent path (e.g. `/this-route-does-not-exist/`) returns 404 with a body containing
      "Page not found"; `/deployed-commit.txt` returns 200 with a body (trimmed) equal to `expectedSha`.
    - Throws an `Error` listing every failing check by path when any route returns a non-200, the
      nonexistent path does not return 404 or lacks the branded marker, or the commit stamp does not
      match — never throws on only the first failure found (aggregate, per CONVENTIONS.md's error
      pattern).
    - Retries each check up to a bounded number of attempts with backoff before counting it a failure
      (first-deploy CloudFront propagation per 02-RESEARCH.md Pitfall 5) — a transient failure that
      succeeds on a later attempt is not reported as a failure.
    - Accepts an injectable `fetchImpl` (defaulting to global `fetch`) so unit tests run with a stub,
      never a real network call.
  </behavior>
  <files>scripts/check-live-deployment.ts, tests/unit/check-live-deployment.test.ts</files>
  <action>
    RED: write `tests/unit/check-live-deployment.test.ts` first, stubbing `fetch` (or an injected
    `fetchImpl`) with `vi.fn()` to return controlled `Response`-like objects (status + text/body) per
    scenario from the behavior spec above — all-pass, one-route-fails, 404-check-fails,
    commit-mismatch, and a transient-then-success retry case. Run `npx vitest run
    tests/unit/check-live-deployment.test.ts` and confirm it fails with "Cannot find module" or an
    assertion failure against a not-yet-existing export — not a syntax error.

    GREEN: implement `scripts/check-live-deployment.ts` exporting `verifyLiveDeployment` per the
    behavior spec, following CONVENTIONS.md (named export, `camelCase`, options object with
    `fetchImpl` default, thrown `Error` aggregating every failure, one `console.log` success summary
    line on pass). Add a thin CLI wrapper at the bottom (`if (import.meta.url === ...)` or a
    top-level-await direct call, matching `scripts/check-built-routes.ts`'s pattern) reading
    `SITE_URL` and `EXPECTED_SHA` from `process.env` and calling `verifyLiveDeployment`, so `tsx
    scripts/check-live-deployment.ts` runs it directly from a CI step. Run the test file again until
    green.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/check-live-deployment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - RED commit exists showing the test failing for the right reason (missing export/module), not a syntax error.
    - GREEN: `npx vitest run tests/unit/check-live-deployment.test.ts` passes, covering all-pass, a failing route, the 404-marker check, a commit mismatch, and a transient-then-success retry.
    - `verifyLiveDeployment` is a named export; no default export (per CONVENTIONS.md).
    - Zero real network calls in the test file — `fetchImpl`/`fetch` is fully stubbed.
  </acceptance_criteria>
  <done>`scripts/check-live-deployment.ts` exists, is unit-tested with a stubbed fetch, and is ready to be invoked for real from deploy.yml against a live URL.</done>
</task>

<task type="auto">
  <name>Task 2: Harden deploy.yml and wire the live-verification step</name>
  <read_first>.github/workflows/deploy.yml, scripts/deploy-site.sh, docs/operations/github-environments.md</read_first>
  <files>.github/workflows/deploy.yml, scripts/deploy-site.sh, docs/operations/github-environments.md</files>
  <action>
    In `.github/workflows/deploy.yml`'s `Check deployment configuration` step: replace the
    `else` branch that sets `configured=false` and only writes a step-summary line with a branch that
    writes the same diagnostic to `$GITHUB_STEP_SUMMARY` and then `exit 1` — a missing required
    variable now fails the run (GATE-02; matches 02-RESEARCH.md Pattern 1). Leave the `if:
    steps.deployment_configuration.outputs.configured == 'true'` guards on every later step in place
    (harmless once `configured` can only ever be `true` for a run that reaches them, and keeps this
    diff minimal).

    Add a `Stamp deployed commit` step immediately after `Build site` and before `Configure AWS
    credentials`, gated by the same `if:` condition: `run: echo "${{ inputs.git_sha || github.sha }}"
    > dist/deployed-commit.txt`.

    In `scripts/deploy-site.sh`, add `/deployed-commit.txt` to the `aws cloudfront create-invalidation
    --paths` list (alongside the existing `/index.html`, `/404.html`, etc.) so a stale cached copy is
    never read back by the live-verification step moments after upload.

    Add a `Verify live deployment` step after `Upload site`, gated by the same `if:` condition:
    `run: npx tsx scripts/check-live-deployment.ts`, with `env: { SITE_URL: "https://${{
    vars.SITE_CHECK_URL }}", EXPECTED_SHA: "${{ inputs.git_sha || github.sha }}" }` — note
    `SITE_CHECK_URL` is documented in github-environments.md as a bare hostname (matching the shape
    of other bare values like `CONTENT_BUCKET`), so the step prefixes `https://` itself.

    Update `docs/operations/github-environments.md`'s `SITE_CHECK_URL` row (added by 02-02) to record
    that its real testpilots value is the `distribution_domain_name` captured in 02-03, and that it
    stays pointed at the CloudFront default domain until the deferred DNS cutover (D-01/D-03) makes
    the custom hostname reachable, at which point a future phase can repoint it.
  </action>
  <verify>
    <automated>grep -c "exit 1" .github/workflows/deploy.yml &amp;&amp; grep -c "deployed-commit.txt" scripts/deploy-site.sh &amp;&amp; grep -c "check-live-deployment" .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - `deploy.yml`'s `Check deployment configuration` step's failure branch ends in `exit 1`, not just a step-summary write.
    - `deploy.yml` has a `Stamp deployed commit` step writing `dist/deployed-commit.txt` between `Build site` and `Configure AWS credentials`.
    - `deploy.yml` has a `Verify live deployment` step after `Upload site`, invoking `scripts/check-live-deployment.ts` with `SITE_URL` and `EXPECTED_SHA`.
    - `scripts/deploy-site.sh`'s invalidation path list includes `/deployed-commit.txt`.
    - `docs/operations/github-environments.md` documents `SITE_CHECK_URL`'s real-value source and its deferred-cutover caveat.
  </acceptance_criteria>
  <done>deploy.yml fails loudly on misconfiguration, stamps every deploy with its commit, and automatically verifies the live result — GATE-02 is implemented, not just documented.</done>
</task>

<task type="auto">
  <name>Task 3: Populate testpilots's deploy variables and land a real deploy</name>
  <read_first>infra/environments/testpilots/main.tf, docs/operations/github-environments.md</read_first>
  <precondition>02-03's testpilots apply completed: content_bucket_name, distribution_id, deployment_role_arn, and distribution_domain_name are all captured.</precondition>
  <files>(no repository files — this task sets live GitHub Environment variables and observes a real workflow run)</files>
  <action>
    Set `testpilots`'s remaining deploy variables via `gh api --method POST
    repos/puppet-stagehand/stagehand-docs/environments/testpilots/variables` (or PATCH if present):
    `CONTENT_BUCKET` = 02-03's `content_bucket_name`, `CLOUDFRONT_DISTRIBUTION_ID` = 02-03's
    `distribution_id`, `AWS_DEPLOY_ROLE_ARN` = 02-03's `deployment_role_arn`, `SITE_CHECK_URL` =
    02-03's `distribution_domain_name` (bare hostname, no `https://` prefix). Do not set these on
    `beta` or `stable` — neither is applied this phase.

    Land this plan's own commits (Task 1 and Task 2's changes) on `main` through the normal commit
    flow this project uses. That push triggers `Deploy site` for real against `testpilots`
    (`on: push: branches: [main]`, defaulting to the `testpilots` environment). Watch it:
    `gh run watch $(gh run list --workflow="Deploy site" --limit 1 --json databaseId --jq
    '.[0].databaseId')`.

    Confirm from the run's own logs/step list (not just the exit code) that: `Check deployment
    configuration` produced `configured=true`; `Upload site` actually executed (not skipped); `Verify
    live deployment` ran `check-live-deployment.ts` and passed. If the live-verification step fails
    on first attempt, read the failure output before retrying anything — a transient CloudFront-
    propagation failure on testpilots's very first real content upload is plausible (02-RESEARCH.md
    Pitfall 5) and the script already retries internally, but a persistent failure is a real
    misconfiguration to fix, not to route around.
  </action>
  <verify>
    <automated>gh run list --workflow="Deploy site" --limit 1 --json conclusion --jq '.[0].conclusion' | grep -q success</automated>
  </verify>
  <acceptance_criteria>
    - `testpilots`'s GitHub Environment has `CONTENT_BUCKET`, `CLOUDFRONT_DISTRIBUTION_ID`, `AWS_DEPLOY_ROLE_ARN`, `SITE_CHECK_URL` all set from 02-03's real outputs.
    - The most recent `Deploy site` run against `testpilots` concluded `success`, with `Upload site` and `Verify live deployment` both actually executed (visible in the run's step list, not inferred).
    - `curl -s "https://{distribution_domain_name}/data/compatibility.json"` (or the equivalent check inside the script's own run) returns 200 with valid content, proving real bytes are behind CloudFront now, not an empty bucket.
    - `curl -s "https://{distribution_domain_name}/deployed-commit.txt"` returns the exact SHA that was deployed.
  </acceptance_criteria>
  <done>A real Deploy site run against testpilots executed Upload site, passed the new live-verification step, and testpilots now serves real content reachable via its CloudFront default domain — PUB-04, PUB-05 (via the default domain), and GATE-02 are all proven true, not just implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → AWS (OIDC) | Now exercised for real for the first time; a hard-fail gate replaces a soft-skip that previously masked a broken credential path. |
| Internet → CloudFront default domain | The first real, publicly-reachable endpoint this project serves content from. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Repudiation | Silent green run with zero bytes uploaded | high | mitigate | Task 2's hard-fail gate (GATE-02) turns a missing/misconfigured deploy variable into a failed run instead of a false-positive success. |
| T-02-14 | Tampering | Deployed content not matching the reviewed commit | medium | mitigate | The commit-stamp file plus `check-live-deployment.ts`'s exact-match assertion proves the live host serves the exact commit that was deployed, not a stale or substituted build. |
| T-02-15 | Information Disclosure | Live-verification script logging secrets or full response bodies on failure | low | mitigate | The script's thrown Error lists failing paths/statuses only, never full response bodies or credential material. |
</threat_model>

<verification>
- `deploy.yml`'s configuration check exits non-zero on a missing required variable.
- A real `Deploy site` run against `testpilots` concludes `success` with `Upload site` and `Verify live deployment` both executed.
- `check-live-deployment.ts` is unit-tested and, run for real in CI, confirms every route, both JSON endpoints, the branded 404, and the exact deployed commit.
- `curl` against the CloudFront default domain returns real content, not the pre-content 403/404 from 02-03.
</verification>

<success_criteria>
GATE-02 is closed: a broken deploy now fails loudly. PUB-04 and PUB-05 are proven true against
testpilots's CloudFront default domain, with the custom-hostname gap still correctly recorded as
deferred rather than silently resolved or dropped.
</success_criteria>

<output>
Create `.planning/phases/02-first-real-publication/02-04-SUMMARY.md` when done
</output>
