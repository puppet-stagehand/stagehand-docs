---
phase: 02-first-real-publication
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - docs/operations/github-environments.md
autonomous: true
requirements: [PUB-02]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All six GitHub Environments (testpilots, beta, stable, testpilots-plan, beta-plan, stable-plan) exist with the exact branch policy, reviewer, and self-review rules github-environments.md specifies."
    - "No plan Environment (testpilots-plan/beta-plan/stable-plan) holds an apply or deploy role ARN, and no Environment anywhere holds an AWS access-key secret."
    - "Every Environment variable this phase can set without a testpilots apply (AWS_REGION, the matching plan or apply role ARN, OIDC_PROVIDER_ARN, HOSTED_ZONE_ID, TOFU_STATE_BUCKET) is set from 02-01's real bootstrap outputs — never copied between environments."
  artifacts:
    - "Six real GitHub Environments on puppet-stagehand/stagehand-docs, each fully configured per github-environments.md's tables"
  key_links:
    - "02-01's captured infrastructure_plan_role_arns / infrastructure_apply_role_arns / github_oidc_provider_arn / state_bucket_names -> this plan's Environment variables -> consumed for real by infrastructure.yml's plan/apply jobs and deploy.yml"
---

<objective>
Create and fully configure all six GitHub Environments for real, reading every role ARN, the OIDC
provider ARN, and the state bucket names from 02-01's captured bootstrap outputs. Populate every
variable this phase can set now; the three deploy-scoped variables for `testpilots`
(`CONTENT_BUCKET`, `CLOUDFRONT_DISTRIBUTION_ID`, `AWS_DEPLOY_ROLE_ARN`) wait for 02-04, since they
only exist after 02-03 applies `testpilots`.

Purpose: PUB-02 — all six Environments exist with the specified branch policies, reviewer rules, and
variable sets; no plan Environment holds apply/deploy authority; no AWS access-key secret exists
anywhere.
Output: Six configured GitHub Environments, ready for infrastructure.yml and deploy.yml to actually
run against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-first-real-publication/02-RESEARCH.md
@docs/operations/github-environments.md
@.github/workflows/deploy.yml
@.github/workflows/infrastructure.yml
</context>

<artifacts_this_phase_produces>
- Six real GitHub Environments: `testpilots`, `beta`, `stable`, `testpilots-plan`, `beta-plan`, `stable-plan` — each with branch policy, reviewer, and variable configuration
- `docs/operations/github-environments.md` updated with a short note that the six Environments now exist for real, plus the new `SITE_CHECK_URL` variable row (a placeholder row here; 02-04 sets the actual value once testpilots is applied)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create and configure all six GitHub Environments</name>
  <read_first>
    docs/operations/github-environments.md (the two protection tables and the two variable tables —
    this task implements them exactly, does not re-derive them),
    .planning/phases/02-first-real-publication/02-RESEARCH.md (Code Examples: `gh api` shape — flagged
    `[ASSUMED]`, verify against the first real response before scripting the remaining five)
  </read_first>
  <precondition>infra/bootstrap/.captured-outputs.json from 02-01 exists and contains 3 plan-role ARNs, 3 apply-role ARNs, the OIDC provider ARN, 3 bucket names, and the real hosted_zone_id.</precondition>
  <files>(no repository files — this task's output is live GitHub Environment configuration)</files>
  <action>
    Resolve the trusted reviewer's numeric GitHub user id once: `gh api users/matthewrstone --jq .id`
    (CODEOWNERS names `@matthewrstone` as the repository's sole owner — use this identity for every
    reviewer slot below; if this is a mistake, that is a config-check finding to raise, not something
    to silently substitute).

    Configure `testpilots-plan` FIRST as the pilot environment, then read the real API response back
    (`gh api repos/puppet-stagehand/stagehand-docs/environments/testpilots-plan`) and confirm the
    fields actually landed (branch policy type, reviewer list, `prevent_self_review`) before
    replicating the same shape to the other five — 02-RESEARCH.md's Open Question 3 flags the exact
    `gh api` field names as unverified this session; do not blind-script all six from an assumed
    shape.

    For each of `testpilots`, `beta`, `stable` (apply/deploy Environments), via
    `gh api --method PUT repos/puppet-stagehand/stagehand-docs/environments/{name}`:
    - Deployment branch policy: selected branches only, `main` — no tags, no other refs.
    - Reviewers: `testpilots` → none required (optional per the table); `beta` → matthewrstone
      required; `stable` → matthewrstone required.
    - `prevent_self_review`: `testpilots` → true (recommended); `beta` → true (recommended);
      `stable` → true (required).

    For each of `testpilots-plan`, `beta-plan`, `stable-plan`, via the same PUT endpoint:
    - Deployment branch policy: selected branches AND tags, with the single custom rule
      `refs/pull/*/merge` — no `main`, no tags, no other refs (`POST .../deployment-branch-policies`
      with `{"name": "refs/pull/*/merge"}` per the doc's existing prescription; verify the exact
      field/endpoint shape against the real Settings → Environments UI or `gh api --help` output for
      this endpoint if the first attempt 4xxs — this is the one area 02-RESEARCH.md flags as
      unverified).
    - Reviewers: matthewrstone, required on all three.
    - `prevent_self_review`: true (required) on all three.

    **Known friction (do not silently work around):** this repository currently has one named
    administrator (`@matthewrstone`, per CODEOWNERS). "Prevent self-review" blocks the same GitHub
    identity that triggered a run from approving its own pending deployment — for a solo maintainer
    this means `beta`, `stable`, and all three `-plan` Environments will have no one able to approve a
    run they themselves triggered until a second trusted reviewer exists. This is the intended
    behavior of ADR-0002's multi-administrator design, not a bug to route around — configure exactly
    per the tables above and surface this plainly in the SUMMARY rather than disabling
    `prevent_self_review` to make a solo workflow easier.

    Set variables via `gh api --method POST repos/puppet-stagehand/stagehand-docs/environments/{name}/variables -f name={VAR} -f value={value}` (or `--method PATCH .../variables/{VAR}` if it already exists):

    Plan Environments (`testpilots-plan`, `beta-plan`, `stable-plan`) — set exactly:
    `AWS_REGION=us-east-2`, `AWS_INFRASTRUCTURE_PLAN_ROLE_ARN` = that environment's plan-role ARN
    from `infrastructure_plan_role_arns`, `OIDC_PROVIDER_ARN` = `github_oidc_provider_arn`,
    `HOSTED_ZONE_ID` = the real zone id from 02-01, `TOFU_STATE_BUCKET` = that environment's bucket
    name from `state_bucket_names`.

    Apply/deploy Environments (`testpilots`, `beta`, `stable`) — set exactly:
    `AWS_REGION=us-east-2`, `AWS_INFRASTRUCTURE_APPLY_ROLE_ARN` = that environment's apply-role ARN
    from `infrastructure_apply_role_arns`, `OIDC_PROVIDER_ARN` = `github_oidc_provider_arn`,
    `HOSTED_ZONE_ID` = the real zone id, `TOFU_STATE_BUCKET` = that environment's bucket name. Do
    NOT set `CONTENT_BUCKET`, `CLOUDFRONT_DISTRIBUTION_ID`, `AWS_DEPLOY_ROLE_ARN`, or `SITE_CHECK_URL`
    on any of the three yet — those depend on 02-03's testpilots apply and land in 02-04.

    Never copy an ARN between environments (a plan-role ARN must never land in an apply/deploy
    Environment and vice versa; each environment's own bucket/zone/role values only).
  </action>
  <verify>
    <automated>gh api repos/puppet-stagehand/stagehand-docs/environments --jq '.environments | map(.name) | sort' | grep -c -E 'beta|beta-plan|stable|stable-plan|testpilots|testpilots-plan'</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/puppet-stagehand/stagehand-docs/environments --jq '.environments | length'` returns `6`.
    - For each of the three apply/deploy Environments, `gh api .../environments/{name}/variables --jq '.variables | map(.name)'` contains exactly `AWS_REGION`, `AWS_INFRASTRUCTURE_APPLY_ROLE_ARN`, `OIDC_PROVIDER_ARN`, `HOSTED_ZONE_ID`, `TOFU_STATE_BUCKET` — no `CONTENT_BUCKET`, no `CLOUDFRONT_DISTRIBUTION_ID`, no `AWS_DEPLOY_ROLE_ARN`.
    - For each of the three plan Environments, the variable set is exactly `AWS_REGION`, `AWS_INFRASTRUCTURE_PLAN_ROLE_ARN`, `OIDC_PROVIDER_ARN`, `HOSTED_ZONE_ID`, `TOFU_STATE_BUCKET` — no `AWS_INFRASTRUCTURE_APPLY_ROLE_ARN` anywhere in a `-plan` Environment.
    - No Environment has any entry under `gh api .../environments/{name}/secrets` (variables only — no AWS access-key secret exists anywhere).
    - No plan-role ARN value appears as any apply/deploy Environment's `AWS_INFRASTRUCTURE_APPLY_ROLE_ARN`/`AWS_DEPLOY_ROLE_ARN`, and no apply-role ARN appears as any plan Environment's `AWS_INFRASTRUCTURE_PLAN_ROLE_ARN` (cross-check by value, not just by variable name).
  </acceptance_criteria>
  <done>All six Environments exist, fully configured per github-environments.md's tables, with every currently-available variable set from 02-01's real outputs and no cross-environment ARN leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the configuration and update the runbook</name>
  <read_first>docs/operations/github-environments.md</read_first>
  <files>docs/operations/github-environments.md</files>
  <action>
    Read back every Environment's full configuration (`gh api repos/puppet-stagehand/stagehand-docs/environments/{name}`
    for branch policy/reviewers/`prevent_self_review`, plus `.../variables` for the variable set) and
    diff it against github-environments.md's tables. Fix any mismatch found before moving on.

    Update `docs/operations/github-environments.md`: add one short paragraph at the top of "Create
    the environments" stating the six Environments now exist for real as of this phase, applied via
    the procedure above, and note the known solo-maintainer self-review friction documented in Task 1
    plainly (do not remove or soften it). Add one new row to the "deployment/apply Environment"
    variable table for `SITE_CHECK_URL` — "HTTPS origin the post-deploy live-check targets; the
    CloudFront default domain until the DNS cutover (D-01/D-03) makes the custom hostname reachable"
    — 02-04 sets its actual value; this task only documents the row.
  </action>
  <verify>
    <automated>grep -c "SITE_CHECK_URL" docs/operations/github-environments.md</automated>
  </verify>
  <acceptance_criteria>
    - Every Environment's live branch-policy, reviewer, and `prevent_self_review` configuration matches github-environments.md's tables exactly (verified by direct `gh api` read-back, not assumed from Task 1's apply calls succeeding).
    - `docs/operations/github-environments.md` states the six Environments exist for real and documents the solo-maintainer self-review friction.
    - `docs/operations/github-environments.md`'s apply/deploy variable table includes a `SITE_CHECK_URL` row.
  </acceptance_criteria>
  <done>Live configuration is confirmed correct by direct read-back (not just successful API calls), and the runbook matches reality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Environment protection → AWS role assumption | The only control standing between an untrusted PR and real AWS write authority for the plan-role path; misconfigured branch/reviewer rules here are the whole point of ADR-0002. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Elevation of Privilege | Plan Environment holding an apply/deploy role ARN | critical | mitigate | Task 1's acceptance criteria mechanically assert the plan-Environment variable set excludes `AWS_INFRASTRUCTURE_APPLY_ROLE_ARN`; Task 2 re-verifies by direct read-back. |
| T-02-06 | Information Disclosure | AWS access-key secret stored in a GitHub Environment | critical | mitigate | Task 1's acceptance criteria assert zero entries under every Environment's `secrets` endpoint — only `vars` are ever used (matches the existing OIDC-only design). |
| T-02-07 | Repudiation | An Environment auto-created by a workflow reference before this plan configures it | medium | accept | 02-RESEARCH.md Pitfall 6 (`[ASSUMED]`, medium confidence) — mitigated structurally by this plan running strictly before any workflow in this phase references any of the six Environment names for real (Wave 2, before 02-04's Wave-3 deploy trigger). |
| T-02-08 | Elevation of Privilege | Solo-maintainer self-review deadlock resolved by disabling `prevent_self_review` | high | mitigate | Task 1 explicitly forbids this workaround; the friction is surfaced in the runbook and SUMMARY instead of silently weakened. |
</threat_model>

<verification>
- `gh api repos/puppet-stagehand/stagehand-docs/environments --jq '.environments | length'` returns 6.
- Each Environment's variable set matches github-environments.md's tables exactly, confirmed by direct API read-back.
- No Environment has any `secrets` entries.
- `docs/operations/github-environments.md` reflects the real, applied state.
</verification>

<success_criteria>
All six GitHub Environments exist, fully configured with the correct branch policies, reviewers, and
variables, verified by direct read-back rather than assumed from successful API calls — with the
runbook updated to match.
</success_criteria>

<output>
Create `.planning/phases/02-first-real-publication/02-02-SUMMARY.md` when done
</output>
