---
phase: 02-first-real-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/bootstrap/terraform.tfvars (untracked, gitignored)
  - docs/operations/aws-bootstrap.md
autonomous: false
requirements: [PUB-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An administrator can run `aws sts get-caller-identity` and see a non-root, short-lived identity before any real AWS write happens in this phase."
    - "A real Route 53 hosted zone for puppetstagehand.com exists in the AWS account, inert (not yet delegated by the live domain), ready to receive per-environment records."
    - "`infra/bootstrap/` has been applied for real: three state buckets, the shared GitHub OIDC provider, and six named IAM roles (three plan, three apply) exist in AWS with the ARNs recorded outside git."
    - "The bootstrap saved plan file no longer exists on disk after this plan completes."
    - "docs/operations/aws-bootstrap.md tells the next operator that the hosted zone must exist (with its real creation command) before `tofu -chdir=infra/bootstrap plan` is run."
  artifacts:
    - "infra/bootstrap/terraform.tfvars (gitignored, real bucket names + real hosted_zone_id — never committed)"
    - "Real AWS: 1 Route 53 hosted zone, 3 S3 state buckets, 1 GitHub OIDC provider, 6 IAM roles"
  key_links:
    - "hosted_zone_id (real Route 53 zone) -> infra/bootstrap/terraform.tfvars -> bootstrap tofu apply -> the same zone ID every later apply (testpilots) reuses"
    - "bootstrap tofu apply outputs (infrastructure_plan_role_arns, infrastructure_apply_role_arns, state_bucket_names, github_oidc_provider_arn) -> captured here -> consumed by 02-02 (GitHub Environments) and 02-03 (testpilots apply)"
---

<objective>
Get a non-root AWS identity authenticated, create the Route 53 hosted zone for
`puppetstagehand.com` (a real prerequisite this repository's OpenTofu does not create), and apply
`infra/bootstrap/` for real for the first time in this project — capturing every output the rest of
this phase depends on. This is the phase's tracer: the one slice every later plan (GitHub
Environments, the testpilots apply, deploy hardening) is downstream of.

Purpose: PUB-01 — an authorized administrator applies bootstrap from a reviewed saved plan,
captures every output, deletes the saved plan, and puts the bootstrap state in an approved,
accountable custody location.
Output: A real Route 53 hosted zone, three real S3 state buckets, a real GitHub OIDC provider, and
six real IAM roles — with every ARN and bucket name captured for 02-02 and 02-03 to consume.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-first-real-publication/02-CONTEXT.md
@.planning/phases/02-first-real-publication/02-RESEARCH.md
@docs/operations/aws-bootstrap.md
@infra/bootstrap/variables.tf
@infra/bootstrap/outputs.tf
@infra/bootstrap/terraform.tfvars.example
</context>

<artifacts_this_phase_produces>
This plan produces (all real AWS state, not repository files, except where noted):
- Route 53 public hosted zone for `puppetstagehand.com` (id captured, not yet delegated by the live domain)
- `infra/bootstrap/terraform.tfvars` (local file, gitignored — never committed; holds real bucket names + the real `hosted_zone_id`)
- Real AWS: 3 S3 state buckets (`state_bucket_names` output), 1 GitHub OIDC provider (`github_oidc_provider_arn` output), 3 `stagehand-<env>-infrastructure-plan` IAM roles (`infrastructure_plan_role_arns` output), 3 `stagehand-<env>-infrastructure-apply` IAM roles (`infrastructure_apply_role_arns` output)
- `docs/operations/aws-bootstrap.md` updated with the hosted-zone-creation prerequisite and a note that this apply has now happened for real
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Authenticate a non-root AWS identity</name>
  <action>Confirm (or establish) a non-root AWS identity before any real apply in this phase runs.</action>
  <instructions>
    I ran `aws sts get-caller-identity` and read its `Arn` field. Per D-04/D-05 (02-CONTEXT.md,
    LOCKED): every real `tofu apply` in this phase MUST run under a short-lived, non-root identity —
    never the AWS account root user. `docs/operations/aws-bootstrap.md`'s "Before you begin" section
    names `aws sso login` (AWS IAM Identity Center) as the organization's approved short-lived
    method.

    If the `Arn` already does NOT end in `:root` (e.g. it is an assumed-role or federated-user ARN),
    this checkpoint is already satisfied — reply "done" immediately, no further action needed.

    If the `Arn` DOES end in `:root` — 02-RESEARCH.md's Pitfall 2 found this was the case for the
    only currently-authenticated identity as of 2026-08-26 — STOP. Do not run any `tofu plan` or
    `tofu apply` under this identity. Authenticate with `aws sso login` (or your org's approved
    short-lived method) against the account that owns `puppetstagehand.com`, then re-run
    `aws sts get-caller-identity` and confirm the `Arn` no longer ends in `:root` before replying
    "done".
  </instructions>
  <verification>I will re-run `aws sts get-caller-identity` after you reply and confirm the `Arn` does not end in `:root` before proceeding to Task 2.</verification>
  <resume-signal>Type "done" once a non-root `aws sts get-caller-identity` succeeds.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Create the hosted zone and apply bootstrap for real</name>
  <read_first>
    docs/operations/aws-bootstrap.md, infra/bootstrap/variables.tf, infra/bootstrap/outputs.tf,
    infra/bootstrap/terraform.tfvars.example, .planning/phases/02-first-real-publication/02-CONTEXT.md
    (D-01 through D-03 — the DNS-cutover boundary this task must not cross)
  </read_first>
  <precondition>Task 1's checkpoint resolved "done": `aws sts get-caller-identity` shows a non-root Arn.</precondition>
  <files>infra/bootstrap/terraform.tfvars</files>
  <action>
    Re-verify the precondition first: run `aws sts get-caller-identity` again and confirm the `Arn`
    still does not end in `:root` before touching anything else.

    Create the Route 53 hosted zone (this repository's OpenTofu never creates it — confirmed by
    `grep -rn 'aws_route53_zone"' infra/` returning nothing; `hosted_zone_id` is an externally
    supplied variable everywhere it is used):

    `aws route53 create-hosted-zone --name puppetstagehand.com --caller-reference "stagehand-bootstrap-$(date +%s)" --hosted-zone-config Comment="Stagehand-managed zone; NOT yet delegated by the live domain (D-01/D-02/D-03, 02-CONTEXT.md)"`

    Capture the returned `HostedZone.Id` and strip the `/hostedzone/` prefix — this is the real
    `hosted_zone_id`. Creating this zone does NOT touch `puppetstagehand.com`'s live NS delegation
    at Cloudflare (D-02) — it is an inert, unreferenced AWS resource until something later points
    the domain's NS records at it, which this phase never does (D-01, D-03).

    Copy `infra/bootstrap/terraform.tfvars.example` to `infra/bootstrap/terraform.tfvars` (gitignored
    — confirmed by `.gitignore`'s `terraform.tfvars` entry). Set `hosted_zone_id` to the real zone ID
    just captured. Choose three globally-unique S3 bucket names (the example's
    `puppet-stagehand-{env}-tofu-state` names are a reasonable starting point; S3 bucket names are
    global, so if a name is already taken by another AWS account, append a short disambiguating
    suffix) and set `state_bucket_names`.

    Initialize, plan, and review before applying:
    ```
    tofu -chdir=infra/bootstrap init
    tofu -chdir=infra/bootstrap plan -out=bootstrap.tfplan
    tofu -chdir=infra/bootstrap show bootstrap.tfplan
    ```
    Read the full plan output. Confirm it creates exactly: 3 `aws_s3_bucket` resources (state), 1
    `aws_iam_openid_connect_provider`, 3 `aws_iam_role.infrastructure_plan` +
    `aws_iam_role_policy.infrastructure_plan`, 3 `aws_iam_role.infrastructure_apply` +
    `aws_iam_role_policy.infrastructure_apply` — nothing else, no destroys, no replacements.

    Apply and capture every output:
    ```
    tofu -chdir=infra/bootstrap apply bootstrap.tfplan
    tofu -chdir=infra/bootstrap output -raw github_oidc_provider_arn
    tofu -chdir=infra/bootstrap output -json state_bucket_names
    tofu -chdir=infra/bootstrap output -json infrastructure_plan_role_arns
    tofu -chdir=infra/bootstrap output -json infrastructure_apply_role_arns
    rm -f infra/bootstrap/bootstrap.tfplan
    ```
    Write the captured JSON (OIDC provider ARN, 3 bucket names, 6 role ARNs, the real
    `hosted_zone_id`) to a local, gitignored scratch file (e.g. `infra/bootstrap/.captured-outputs.json`
    — add it to `.gitignore` if not already covered by an existing ignore pattern) so 02-02 and 02-03
    can read it without re-running `tofu output`. Never write these values into a tracked file.
  </action>
  <verify>
    <automated>tofu -chdir=infra/bootstrap output -json infrastructure_plan_role_arns | jq -e 'keys | length == 3' &amp;&amp; tofu -chdir=infra/bootstrap output -json infrastructure_apply_role_arns | jq -e 'keys | length == 3'</automated>
  </verify>
  <acceptance_criteria>
    - `aws sts get-caller-identity` at the start of this task shows a non-root Arn.
    - A real Route 53 hosted zone exists for `puppetstagehand.com` (`aws route53 get-hosted-zone --id &lt;id&gt;` succeeds).
    - `tofu -chdir=infra/bootstrap apply` completed with 0 errors, 0 destroys, 0 replacements.
    - `infrastructure_plan_role_arns` and `infrastructure_apply_role_arns` each have exactly 3 keys (`testpilots`, `beta`, `stable`), each value a valid IAM role ARN.
    - `infra/bootstrap/bootstrap.tfplan` does not exist on disk after this task (`ls infra/bootstrap/bootstrap.tfplan` fails).
    - `git status --porcelain infra/bootstrap/terraform.tfvars` shows nothing tracked (the file is gitignored).
  </acceptance_criteria>
  <done>Bootstrap is applied for real; the hosted zone, 3 state buckets, the OIDC provider, and 6 IAM roles all exist in AWS with every ARN/name captured for 02-02 and 02-03.</done>
</task>

<task type="auto">
  <name>Task 3: Place bootstrap state in custody and update the runbook</name>
  <read_first>docs/operations/aws-bootstrap.md (the existing "Store `terraform.tfstate`..." paragraph this task extends, not replaces)</read_first>
  <files>docs/operations/aws-bootstrap.md</files>
  <action>
    Update `docs/operations/aws-bootstrap.md`'s "1. Create the shared bootstrap resources" section
    with one new paragraph, placed immediately before the existing `cp infra/bootstrap/terraform.tfvars.example ...`
    code block, stating: the Route 53 hosted zone for `puppetstagehand.com` does not exist by
    default and must be created first — `aws route53 create-hosted-zone --name puppetstagehand.com
    --caller-reference "&lt;unique-value&gt;"` — and its returned zone ID is the value `hosted_zone_id`
    expects. State plainly that creating this zone does not delegate the live domain to it; that
    remains a separate, deliberate, human-driven step (cross-reference the phase 2 CONTEXT.md D-01
    through D-03 decisions by name so a future reader understands why the zone can exist inert).

    Do not touch the existing "Safety boundary" section's wording beyond this addition — it
    currently correctly states no scaffold task performs an AWS apply or DNS cutover; that sentence
    stops being true starting with this plan, so add one sentence noting that as of this phase,
    bootstrap has been applied for real (do not claim testpilots or beta or stable have been applied
    — those are separate plans/phases).
  </action>
  <verify>
    <automated>grep -c "create-hosted-zone" docs/operations/aws-bootstrap.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operations/aws-bootstrap.md` names the exact `aws route53 create-hosted-zone` command before the `terraform.tfvars` copy step.
    - The doc explicitly states the zone is created inert, with no NS delegation change, citing the phase 2 CONTEXT.md decisions by ID.
    - The "Safety boundary" section still states no DNS cutover has happened, and now also states bootstrap has been applied for real.
  </acceptance_criteria>
  <done>The runbook tells the next reader exactly how to reproduce this task's prerequisite, and accurately reflects that bootstrap is no longer unapplied.</done>
  <verify><human-check>Confirm `infra/bootstrap/terraform.tfstate` has been copied to your organization's approved encrypted, access-controlled, versioned custody location (per the existing "Store `terraform.tfstate`..." paragraph), and that one accountable owner has been designated for it. Claude cannot verify an external custody system — confirm this by hand.</human-check></verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation → AWS | The first real write path in this project; a root-user credential here has no permission boundary. |
| Local disk → git | `terraform.tfvars`, the saved plan, and `terraform.tfstate` all carry account-scoped or sensitive values and must never cross into a tracked file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | AWS root-user apply | critical | mitigate | Task 1's `checkpoint:human-action` blocks every later task in this plan until `aws sts get-caller-identity` shows a non-root Arn (D-04/D-05, LOCKED). |
| T-02-02 | Information Disclosure | `terraform.tfvars`, saved plan, `terraform.tfstate` | high | mitigate | All three are gitignored; the saved plan is deleted immediately after apply (Task 2); captured outputs go to a gitignored scratch file, never a tracked one. |
| T-02-03 | Tampering | Route 53 hosted zone creation reachable from a full-domain NS cutover | high | mitigate | Task 2's zone creation is explicitly scoped to not touch the live domain's NS delegation (D-01/D-02); no task in this plan runs a registrar or Cloudflare-side change. |
| T-02-04 | Repudiation | Bootstrap state with no accountable owner | medium | mitigate | Task 3's `<verify><human-check>` requires a named custodian and an approved custody location before this plan is considered complete. |
</threat_model>

<verification>
- `aws sts get-caller-identity` shows a non-root identity before Task 2 begins.
- `tofu -chdir=infra/bootstrap output -json infrastructure_plan_role_arns` and `infrastructure_apply_role_arns` each return exactly 3 keyed ARNs.
- `infra/bootstrap/bootstrap.tfplan` does not exist after Task 2.
- `git status --porcelain` shows no tracked change to `terraform.tfvars`, `terraform.tfstate`, or any saved plan file.
- `docs/operations/aws-bootstrap.md` documents the hosted-zone prerequisite and the real-apply fact.
</verification>

<success_criteria>
Bootstrap is applied for real under a confirmed non-root identity; the hosted zone, 3 state buckets,
1 OIDC provider, and 6 IAM roles exist in AWS; every output is captured for 02-02/02-03; the saved
plan is deleted; the runbook reflects reality; bootstrap state custody is confirmed by a human.
</success_criteria>

<output>
Create `.planning/phases/02-first-real-publication/02-01-SUMMARY.md` when done
</output>
