---
phase: 04.2-tester-downloads
plan: 02
type: execute
wave: 2
depends_on: ["04.2-01"]
files_modified:
  - src/content/docs/user-guide.md
  - src/content/docs/installer-registry-distribution.md
  - src/content/docs/installer-support.md
  - tests/unit/docs-collection.test.ts
  - scripts/check-built-routes.ts
  - scripts/deploy-site.sh
  - tests/unit/deploy-scripts.test.ts
autonomous: true
requirements: [DOC-02, DOC-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "src/content/docs/user-guide.md exists, contains none of the four sections the source marks '— Deferred in Stagehand 1.0', no named-engineer ownership sentence, and no reference to the puppet-console GitHub Pages docs site (D-08)."
    - "src/content/docs/installer-support.md exists, contains zero internal/ Go package-path references and zero literal Go code blocks, while retaining the reader-actionable credential/troubleshooting guidance the resolved scope decision calls for (D-09)."
    - "src/content/docs/installer-registry-distribution.md exists with the two docs/superpowers/specs/... path references removed or replaced with plain prose (Pitfall 6, D-09)."
    - "The puppet-core-9-v1 platform-lock-contract table exists exactly once site-wide, in user-guide.md — installer-support.md's port points back to it instead of repeating it."
    - "All three new docs build as normal /docs/{slug}/ routes on all three environments (no visibleOn restriction, unlike testers-guide.md); npm run test:unit / check:routes / check:invalidation all pass with the closed-enumeration lists updated."
  artifacts:
    - src/content/docs/user-guide.md
    - src/content/docs/installer-registry-distribution.md
    - src/content/docs/installer-support.md
  key_links:
    - "Each new doc's slug -> tests/unit/docs-collection.test.ts's closed slug array -> scripts/check-built-routes.ts's requiredOutputs -> scripts/deploy-site.sh's invalidation --paths (Pitfall 7)"
    - "installer-support.md's platform-lock recovery guidance -> user-guide.md's 'Puppet package and runtime protection' section, referenced rather than duplicated"
---

<objective>
Port and curate the general User Guide (DOC-02, from `puppet-console`'s `docs/USER-GUIDE.md`, D-08)
and the two reader-facing `puppet-installer` docs (DOC-03: `registry-distribution-guide.md` near
verbatim, `support-guide.md` heavily curated per the just-resolved scope decision, D-09) into this
site's general documentation area, visible on all three environments — unlike Phase 04.1's
password-gated, testpilots/beta-only Tester's Guide.

All three docs land flat in `src/content/docs/` (not the `installer/` subdirectory RESEARCH.md
floated) because `tests/unit/docs-collection.test.ts`'s `loadDocsFrontmatter()` uses a
non-recursive `readdirSync` over that one directory — a subdirectory's files would silently never
reach that test's frontmatter assertions. The two installer docs use an `installer-` filename
prefix instead, for the same grouping clarity without the readdirSync pitfall.

Purpose: DOC-02 (User Guide live on every environment), DOC-03 (reader-facing installer docs live,
maintainer-facing ones excluded).
Output: `src/content/docs/user-guide.md`, `src/content/docs/installer-registry-distribution.md`,
`src/content/docs/installer-support.md`, all wired into the same closed-enumeration contracts
`docs-collection.test.ts`/`check-built-routes.ts`/`deploy-site.sh` already enforce.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-tester-downloads/04.2-CONTEXT.md
@.planning/phases/04.2-tester-downloads/04.2-RESEARCH.md
@src/content.config.ts
@src/content/docs/testers-guide.md
@src/content/docs/security.md
@tests/unit/docs-collection.test.ts
@scripts/check-built-routes.ts
@scripts/deploy-site.sh
@tests/unit/deploy-scripts.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Tracer: port and fully wire the User Guide (DOC-02) end-to-end</name>
  <files>src/content/docs/user-guide.md, tests/unit/docs-collection.test.ts, scripts/check-built-routes.ts, scripts/deploy-site.sh, tests/unit/deploy-scripts.test.ts</files>
  <read_first>
    /Users/matthew/Code/puppet-console/docs/USER-GUIDE.md (the full 991-line source — read
    completely before curating), src/content/docs/testers-guide.md (the established curated-port
    precedent and frontmatter shape), src/content.config.ts, tests/unit/docs-collection.test.ts,
    scripts/check-built-routes.ts, scripts/deploy-site.sh
  </read_first>
  <action>
    Curate `src/content/docs/user-guide.md` from the full source. This source is already written
    for the actual console end-user (unlike `TESTER-GUIDE.md`'s original internal-audience
    mismatch) — most of it ports close to verbatim. Concretely:

    - Frontmatter: `title: "Puppet Stagehand — User's Guide"`, a one-sentence `description`,
      `order: 5` (next unused value after `testers-guide`'s 4), `updated` set to today's date. No
      `visibleOn` field — this guide is visible on all three environments (D-08), unlike
      `testers-guide.md`.
    - Opening blockquote: keep the "who this is for" audience framing; drop the second paragraph
      ("Status: this guide grows with the console... every phase of development is required to
      check this document...") — an internal contributor-process note, not reader content.
    - Drop the "This guide and the Tester's Guide are also published as a browsable, searchable
      docs site — generated straight from this exact markdown by CI... Read it at
      [puppetlabs-seteam.github.io/puppet-console]" sentence in "The Five-Minute Version" — it
      points at a DIFFERENT docs site (the source repo's own GitHub Pages build); this
      `stagehand-docs` site is now itself the reader-facing docs site, and that sentence would be
      actively misleading here.
    - In "Node Dependency Graph (Puppet Labs)", drop the sentence "**Matthew Stone owns this Labs
      flag** and is the person responsible for deciding when (or whether) it's mature enough to
      graduate out of Labs..." — a named individual's internal feature ownership must never ship to
      a public site; keep the rest of that paragraph (how an administrator turns the Labs flag on
      or off).
    - Drop these four sections in full, heading and body — each is explicitly marked "— Deferred in
      Stagehand 1.0" in the source and describes a feature not reachable in the shipped product
      (mirrors D-07's precedent from Phase 04.1 for `TESTER-GUIDE.md`'s deferred-to-v2 sections):
      "Discovering classes and groups from another instance", "Data (Hiera)", "Self-Update",
      "Teams, Roles, and Approvals".
    - Correspondingly drop every Glossary row whose term or definition is marked "(deferred to
      v2)" — at minimum: Discovery/import wizard, Merge suggestion, Self-update, Update channel,
      Manifest, Snapshot, Rollback, Lifecycle stage, Health check, Signed manifest, Downgrade
      attack, Replay attack.
    - Drop the "Docs site" Glossary row (same GitHub-Pages cross-reference problem as above).
    - Drop the closing "*This guide is maintained alongside the codebase...*" footer — an internal
      contributor-process note.
    - Keep the `&lt;!-- platform-lock-contract:start --&gt;...&lt;!-- platform-lock-contract:end --&gt;` block
      ("Puppet package and runtime lock contract") in full — this doc is where it canonically
      lives; Task 2's `installer-support.md` will point back here rather than repeating this exact
      table.
    - Keep every Mermaid fenced code block as-is — this site's Markdown pipeline has no Mermaid
      renderer plugin (confirmed: no new packages per RESEARCH.md's Package Legitimacy Audit), so
      it renders as a plain code block, which is an acceptable, non-blocking degradation, not a bug
      to fix. Do not add a Mermaid rendering library — that would be new, unresearched scope.
    - Everything else — the full console feature walkthrough (Dashboard through Patchbot
      installation and ownership), the non-deferred Glossary entries, and the EULA appendix —
      ports as-is; do not trim any currently-supported feature's content.
    - Before finishing, `grep -in` the curated file for `matthew`, `internal/`, `.planning`,
      `docs/qa`, and any DSN/token/password-shaped string, confirming zero matches (mirrors
      04.1-03's precedent verification).

    Wire the new entry into every closed-enumeration list in the same change: add `'user-guide'` in
    alphabetically-sorted position to `tests/unit/docs-collection.test.ts`'s slug array; add
    `'docs/user-guide/index.html'` to `scripts/check-built-routes.ts`'s `requiredOutputs`; add
    `'/docs/user-guide/index.html'` to `scripts/deploy-site.sh`'s `create-invalidation --paths`
    list; update `tests/unit/deploy-scripts.test.ts`'s exact-string assertion to match.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/docs-collection.test.ts tests/unit/deploy-scripts.test.ts</automated>
  </verify>
  <done>src/content/docs/user-guide.md exists, curated per the rules above, and builds as a normal, fully-wired /docs/user-guide/ route visible on all three environments.</done>
</task>

<task type="auto">
  <name>Task 2: Port and curate the two reader-facing installer docs (DOC-03)</name>
  <files>src/content/docs/installer-registry-distribution.md, src/content/docs/installer-support.md, tests/unit/docs-collection.test.ts, scripts/check-built-routes.ts, scripts/deploy-site.sh, tests/unit/deploy-scripts.test.ts</files>
  <read_first>
    /Users/matthew/Code/puppet-installer/docs/registry-distribution-guide.md,
    /Users/matthew/Code/puppet-installer/docs/support-guide.md (both full sources — read
    completely before curating)
  </read_first>
  <action>
    Curate `src/content/docs/installer-registry-distribution.md` from
    `registry-distribution-guide.md` (194 lines, already reader-appropriate). Frontmatter: `title`
    reflecting its own heading ("Registry-Hosted Container Distribution"), a one-sentence
    `description`, `order: 6`, `updated` today. Content changes, otherwise near-verbatim:
    - Simplify the opening paragraph's "is implemented on the branch this guide lives on, and
      merges into `main` along with it — so if you're reading this after that merge..." framing —
      that is `puppet-installer`'s own internal branch/merge-timing detail, irrelevant to a public
      reader. State plainly what's shipped today (Parts 1–2) vs. planned (Part 3), matching the
      tone the source's own "What's deliberately not included here" section already uses.
    - Remove the two `` (`docs/superpowers/specs/2026-08-19-...-design.md`) `` parenthetical path
      references (Pitfall 6) — replace each with plain prose (e.g. "documented in an internal
      design note") or drop the parenthetical outright. Do not carry the literal string
      `docs/superpowers/specs/` into the port.
    - Keep every Mermaid diagram as-is (same non-blocking-degradation note as Task 1).

    Curate `src/content/docs/installer-support.md` from `support-guide.md` (507 lines) — this one
    needs real curation, not a near-verbatim port, per the resolved scope decision (D-09): trim
    internal Go package paths and PS-engineer-only troubleshooting internals; keep what is
    genuinely useful for an external reader troubleshooting the Puppet Installer. Frontmatter:
    `title` (e.g. "Installer Support &amp; Troubleshooting"), a one-sentence `description` reframed for
    a reader (not "Internal reference for Support and PS engineers..."), `order: 7`, `updated`
    today. Concretely:

    KEEP, reframed into second-person reader voice (not "the customer"/"a ticket"):
    - The quick-reference symptom table — keep every row's Symptom and remediation guidance; strip
      internal-code detail from the "Check" column (function names like `credentials.Validate()`,
      package paths like `internal/preflight`, `modes.Run`) and replace with plain-English steps.
    - "1. What this tool is and isn't" — keep the conceptual overview of the five install modes and
      the `Plan()`/`Apply()` preview-then-execute contract (genuinely useful troubleshooting
      framing: "check what the preview showed first"); strip every `internal/modes/...`
      parenthetical.
    - "2. The credential model" — keep the Forge-key-vs-PE-license-id concept and which modes need
      one; DROP the literal Go code block and the `internal/config/config.go:75`-style file:line
      citations entirely. Keep "Repository and Stagehand credentials," "Valid credential contexts,"
      and "Lifecycle secret recovery" close to verbatim — these are genuinely operator-actionable
      setup/recovery steps. For the "what a forge key unlocks" numbered list, drop the "(this
      repo)"/"(the console app, `puppet-core-console` repo)" internal-repo-attribution
      parentheticals.
    - "3. Common failure points by mode" — keep the failure-mode descriptions and remediation per
      mode; strip `internal/preflight`, `modes.Run`, `internal/vendored`,
      `scripts/vendor-modules.sh`-style internal references, replacing with plain descriptions of
      the symptom and what to check.
    - "Platform-lock diagnosis and controlled recovery" — keep the operator-facing recovery
      guidance (what a stop condition means, don't bypass, don't unlock every role, preserve
      evidence) but do NOT repeat the `&lt;!-- platform-lock-contract:start/end --&gt;` table — that
      exact table already lives in `user-guide.md`'s "Puppet package and runtime protection"
      section (Task 1); add one sentence pointing there instead of duplicating ~60 lines of
      identical table content.
    - "4. Secrets" — reframe from Support-engineer ticket-handling policy into reader-facing "what
      the installer does with your secrets" trust framing (never asks for X, redacts Y in logs and
      previews, cleans up temporary secrets after a run, generated recovery values must be saved by
      you) — matches this site's existing `/docs/security.md` trust-boundary voice.
    - "5. Vendored artifacts" — keep only the "distribution channels are still evolving" caveat
      (registry availability isn't guaranteed for every Puppet version yet); drop the
      `go:embed`/`internal/consolebin`/`internal/hierascopebin` build-mechanics detail.

    DROP entirely:
    - The literal Go code block, every `internal/...` path, every `file.go:NNN` citation.
    - The closing "*Written 2026-08-19/20... check the current codebase or ask engineering.*"
      footer.
    - The "Console-only lifecycle" mode's "most Puppet-native... not shell scripting"
      internal-architecture aside — keep only its PE-vs-Core routing behavior and the
      generated-secrets-recovery pointer.

    Add a brief "where to report a problem" note distinguishing installer/console issues (file at
    `puppet-stagehand/stagehand-release`) from docs-site issues (this repository's own tracker,
    per `/support/`) — D-03.

    Before finishing, `grep -in 'internal/'` the curated `installer-support.md` and confirm zero
    matches.

    Wire both new entries into every closed-enumeration list in the same change, alongside Task 1's
    `user-guide` entry: add `'installer-registry-distribution'` and `'installer-support'` in
    alphabetically-sorted position to `tests/unit/docs-collection.test.ts`'s slug array; add both
    `docs/installer-registry-distribution/index.html` and `docs/installer-support/index.html` to
    `scripts/check-built-routes.ts`'s `requiredOutputs`; add both
    `/docs/installer-registry-distribution/index.html` and `/docs/installer-support/index.html` to
    `scripts/deploy-site.sh`'s invalidation list; update `tests/unit/deploy-scripts.test.ts`'s
    assertion again to match the now-complete list.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run check:routes &amp;&amp; npm run check:invalidation &amp;&amp; npx vitest run</automated>
  </verify>
  <done>All three DOC-02/DOC-03 docs are live, curated per the rules above, and the full local build/test sweep is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `puppet-console`/`puppet-installer` source repositories -> this public repository | The port must not carry over an internal named-individual reference, internal file path, or credential-shaped string from the source docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-05 | Information Disclosure | Curated content copied from `USER-GUIDE.md`/`support-guide.md`/`registry-distribution-guide.md` | medium | mitigate | Task 1/Task 2's curation directives explicitly drop the named-engineer ownership sentence, every `internal/...` Go package path, every `file.go:NNN` citation, and the `docs/superpowers/specs/...` internal spec-path references; each task's action ends with an explicit grep sweep of the curated file before it is considered done. |
| T-04.2-06 | Information Disclosure | `installer-support.md`'s ported "what's safe to ask a customer to share" section | low | mitigate | Reframed into reader-facing trust language (what the installer itself does with your secrets) rather than an internal support-ticket-handling policy voice — no new secret-handling behavior is introduced, only documentation of already-shipped installer behavior. |
</threat_model>

<verification>
- `npx vitest run tests/unit/docs-collection.test.ts tests/unit/deploy-scripts.test.ts` after each task.
- `npm run build && npm run check:routes && npm run check:invalidation` — all three docs build as registered routes.
- `grep -in 'internal/\|matthew\|docs/superpowers/specs' src/content/docs/user-guide.md src/content/docs/installer-*.md` — empty.
</verification>

<success_criteria>
- `user-guide.md`, `installer-registry-distribution.md`, and `installer-support.md` all exist, are curated per D-08/D-09, and are live on all three environments via the normal docs route (no `visibleOn` restriction).
- No internal-only reference (named engineer, `internal/` Go path, `docs/superpowers/specs/` path, unrelated GitHub Pages docs-site link) survives into any of the three files.
- The platform-lock-contract table exists exactly once site-wide.
</success_criteria>

<output>
Create `.planning/phases/04.2-tester-downloads/04.2-02-SUMMARY.md` when done
</output>
