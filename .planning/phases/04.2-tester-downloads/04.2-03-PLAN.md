---
phase: 04.2-tester-downloads
plan: 03
type: execute
wave: 3
depends_on: ["04.2-01", "04.2-02"]
files_modified:
  - .planning/WINDOWS.md
autonomous: true
requirements: [DOWN-01, DOWN-02, DOWN-03, DOWN-04, DOC-02, DOC-03, GATE-07]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "npm run verify is fully green with Plans 04.2-01 and 04.2-02's changes present together."
    - "Roadmap success criteria 3 (downloads page reachable without the shared password) and 6 (User Guide + reader-facing installer docs live on all three environments, maintainer docs excluded) are demonstrated true — not merely asserted from code review."
    - "A WINDOWS.md entry records that DOWN-01/DOWN-02/DOWN-04's live verification against the real puppet-stagehand/stagehand-release repo is deferred until the cross-repo prerequisite lands, mirroring the AUTH-06/Phase 04.1 precedent (WINDOWS.md entry #2) rather than silently marking the phase fully closed."
  artifacts: []
  key_links:
    - "Plan 04.2-01's fixture-proven downloads page + Plan 04.2-02's live docs -> the real, deployed /downloads/ and /docs/{user-guide,installer-registry-distribution,installer-support}/ routes, proven together for the first time in this plan"
---

<objective>
Close Phase 04.2: run the full standing verification gate with every change from this phase present
together, and prove the two roadmap success criteria that are reachable without the cross-repo
prerequisite (criteria 3 and 6 — DOWN-03's ungated reachability, and DOC-02/DOC-03's docs being
live). Per the resolved scope decision, fixture-based proof (GATE-07, Plan 04.2-01's tests) is
sufficient to close this phase for DOWN-01/DOWN-02/DOWN-04 — do NOT hold the phase open waiting on
`puppet-stagehand/stagehand-release`'s creation (D-04). Mirror Phase 04.1's AUTH-06 precedent
exactly: log the deferred live verification as an open WINDOWS.md item, re-verified once the
cross-repo prerequisite lands.

Purpose: confirm DOWN-01 through DOWN-04, DOC-02, DOC-03, and GATE-07 are genuinely true either
live (criteria 3/6) or by fixture-proof-with-a-tracked-deferral (criteria 1/2/4/5), and that
nothing from this phase leaked a credential for the private installer repo into the repository.
Output: a green `npm run verify`, a SUMMARY recording the live proof, and a new WINDOWS.md entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/WINDOWS.md
@.planning/phases/04.2-tester-downloads/04.2-01-SUMMARY.md
@.planning/phases/04.2-tester-downloads/04.2-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full npm run verify with every phase change present</name>
  <files>(none — verification only; any fix is mechanical drift in files already owned by prior plans)</files>
  <action>
    Run `npm run verify` from a clean working tree with all of Plans 04.2-01 and 04.2-02's changes
    present. If anything fails, fix the specific drift (e.g. a formatting or lint issue from
    combining independently-authored files) without reopening any of this phase's design decisions
    — this task closes the phase, it does not replan it. Re-run until green.
  </action>
  <verify>
    <automated>npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify` exits 0.
    - No fix in this task touches the build-time-fetch architecture, the honest-unavailable-state
      behavior, or the content curation scope decided in prior plans — only mechanical integration
      drift (formatting, lint, closed-enumeration lists) is in scope here.
  </acceptance_criteria>
  <done>npm run verify is green with the complete phase's changes present together.</done>
</task>

<task type="auto">
  <name>Task 2: Prove criteria 3 and 6 live, and log the deferred cross-repo verification</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>.planning/ROADMAP.md (Phase 04.2's six numbered success criteria and Cross-Repo Prerequisite note, verbatim), .planning/WINDOWS.md (entry #2 — the AUTH-06 precedent this task mirrors)</read_first>
  <action>
    Against the three real, live hostnames (assuming this phase's changes have been merged and
    deployed by the time this task runs — if not yet deployed, run the equivalent checks against a
    local `npm run build` + `npm run preview` instead, and note in the SUMMARY that the live-host
    checks below still need re-running post-deploy):

    1. `curl -sI "https://testpilots.puppet-stagehand.com/downloads/"`,
       `.../beta.puppet-stagehand.com/downloads/"`, and `.../www.puppet-stagehand.com/downloads/"`
       each return `200` with NO `WWW-Authenticate` challenge and no `401` — proving DOWN-03/D-06
       (criterion 3).
    2. `curl -sI` each of `/docs/user-guide/`, `/docs/installer-registry-distribution/`, and
       `/docs/installer-support/` on all three hostnames — each returns `200` with no gate
       challenge — proving DOC-02/DOC-03 are live on every environment (criterion 6). Confirm
       `releasing.md`/`secrets.md`/`production-readiness.md` were never added to
       `src/content/docs/` at all (maintainer-facing docs correctly excluded).
    3. Record both results in the SUMMARY with their real command output, not just a claim they
       were run.

    Then log the deferred live verification for criteria 1, 2, 4, and 5 (DOWN-01/DOWN-02/DOWN-04 —
    everything that depends on `puppet-stagehand/stagehand-release` actually existing and holding a
    real release per channel) as an open WINDOWS.md item, mirroring entry #2's exact style:

    `gsd-tools windows append --kind deviation --phase 04.2 --file src/lib/data/downloads.ts
    --description "DOWN-01/DOWN-02/DOWN-04 live verification deferred: the downloads page's
    build-time loader is proven against fixtures only (GATE-07); puppet-stagehand/stagehand-release
    does not exist yet (D-07, confirmed via gh repo view returning 'Could not resolve to a
    Repository'), so no real release has ever been fetched/rendered/checksum-verified end-to-end.
    Re-verify once the cross-repo prerequisite (repo creation, workflow retarget from
    souldonetworks to puppet-stagehand, token wiring, first real release per channel) lands — see
    ROADMAP.md's Phase 04.2 Cross-Repo Prerequisite note."`

    Confirm the new entry appears in `.planning/WINDOWS.md`'s table and JSON block with `status:
    open`.
  </action>
  <verify>
    <automated>grep -q "DOWN-01/DOWN-02/DOWN-04 live verification deferred" .planning/WINDOWS.md &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Criteria 3 and 6's checks are recorded in the SUMMARY with real command output.
    - `.planning/WINDOWS.md` contains the new open deviation entry for phase 04.2.
  </acceptance_criteria>
  <done>Criteria 3 and 6 are demonstrated true, and the deferred live verification for criteria 1/2/4/5 is tracked openly rather than silently assumed complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This plan's own scope | Verification-only — no new code, so no new trust boundary is introduced; this plan re-confirms Plan 04.2-01/04.2-02's boundaries hold once combined. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-07 | Information Disclosure | Full working tree + git history | low | mitigate | Task 1's full `npm run verify` pass plus Plan 04.2-01 Task 3's grep sweep (mirroring PUB-07/AUTH-02's established convention) for the private `puppetlabs-seteam/puppet-installer` repo name and any credential-shaped string confirms nothing leaked across all three of this phase's plans combined. |
</threat_model>

<verification>
- `npm run verify` — full standing gate, green.
- Live/local `curl` checks for criteria 3 and 6.
- `.planning/WINDOWS.md` carries the new open deviation entry for the deferred criteria 1/2/4/5.
</verification>

<success_criteria>
- `npm run verify` passes with the whole phase's changes present.
- Criteria 3 and 6 are proven true against real (or locally built/previewed) environments.
- The phase closes honestly: criteria 1/2/4/5's real-world verification is tracked as an open,
  re-visitable WINDOWS.md item rather than asserted complete.
</success_criteria>

<output>
Create `.planning/phases/04.2-tester-downloads/04.2-03-SUMMARY.md` when done
</output>
