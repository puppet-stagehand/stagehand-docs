---
phase: 04.2-tester-downloads
plan: 02
subsystem: docs
tags: [astro, content-collections, docs-porting, markdown, curation]

requires:
  - phase: 04.1-gated-tester-access
    provides: testers-guide.md's curated-port precedent (frontmatter shape, deferred-section
      handling, grep-sweep-before-done discipline) and the docs-collection/check-built-routes/
      deploy-site closed-enumeration wiring pattern
  - phase: 04.2-tester-downloads
    plan: "01"
    provides: the downloads/index.html route and its own closed-enumeration wiring, which this
      plan's additions land alongside without disturbing
provides:
  - src/content/docs/user-guide.md — DOC-02's curated port of puppet-console's USER-GUIDE.md,
    visible on all three environments (no visibleOn restriction), the canonical (only) home of the
    puppet-core-9-v1 platform-lock-contract table site-wide
  - src/content/docs/installer-registry-distribution.md — DOC-03's near-verbatim port of
    puppet-installer's registry-distribution-guide.md
  - src/content/docs/installer-support.md — DOC-03's heavily curated port of
    puppet-installer's support-guide.md, reframed from an internal Support/PS runbook into
    reader-facing troubleshooting guidance, pointing back to user-guide.md for the platform-lock
    table instead of repeating it
  - Two new external-link allowlist entries in scripts/check-built-links.ts (help.puppet.com's
    Bolt install page, stagehand-release's issue tracker)
affects: [04.2-03]

actuals:
  tokens: 47564
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Curate-don't-verbatim-port for cross-repo doc sources (second application of Phase 04.1's
      TESTER-GUIDE.md precedent) — drop internal-audience sentences/paths/citations, keep
      reader-actionable content, grep-sweep the curated file before calling a task done"
    - "Single canonical home for a duplicated table across docs: user-guide.md keeps the full
      platform-lock-contract table; installer-support.md links to it instead of repeating ~60
      lines of identical content"

key-files:
  created:
    - src/content/docs/user-guide.md
    - src/content/docs/installer-registry-distribution.md
    - src/content/docs/installer-support.md
  modified:
    - tests/unit/docs-collection.test.ts
    - scripts/check-built-routes.ts
    - scripts/deploy-site.sh
    - tests/unit/deploy-scripts.test.ts
    - scripts/check-built-links.ts

key-decisions:
  - "Dropped the 'Teams, Roles, and Approvals — Deferred in Stagehand 1.0' section from
    user-guide.md in full, per the plan's explicit four-section drop list, then fixed the three
    resulting dangling cross-references elsewhere in the doc (Signing-in section, Code Management's
    deploy-picker note, and the glossary's Team module repo row) so no reader-facing 'see X below'
    pointer is left aimed at removed content."
  - "installer-support.md's platform-lock section points to user-guide.md's 'Puppet package and
    runtime protection' section by prose reference rather than an anchor-fragment link, since this
    site's markdown pipeline wasn't verified to expose stable heading-slug anchors and a broken
    fragment would be worse than a plain page link."
  - "Added a 'Where to report a problem' section to installer-support.md distinguishing
    installer/console issues (puppet-stagehand/stagehand-release) from docs-site issues (this
    repo's own /support/ tracker), satisfying D-03's requirement that only installer/console
    issue links redirect to the new repo."

requirements-completed: [DOC-02, DOC-03]

coverage:
  - id: D1
    description: "user-guide.md exists, curated per D-08 (no deferred-in-1.0 sections, no named-
      engineer ownership sentence, no cross-repo GitHub Pages docs-site reference), and is wired
      into every closed-enumeration list alongside the other docs routes"
    requirement: DOC-02
    verification:
      - kind: unit
        ref: "tests/unit/docs-collection.test.ts"
        status: pass
      - kind: integration
        ref: "npm run build && npm run check:routes && npm run check:invalidation"
        status: pass
      - kind: other
        ref: "grep -in 'internal/|matthew|docs/superpowers/specs' src/content/docs/user-guide.md"
        status: pass
    human_judgment: false
  - id: D2
    description: "installer-support.md exists with zero internal/ Go package-path references and
      zero literal Go code blocks, while retaining reader-actionable credential/troubleshooting
      guidance (D-09's resolved scope decision)"
    requirement: DOC-03
    verification:
      - kind: other
        ref: "grep -in 'internal/' src/content/docs/installer-support.md"
        status: pass
      - kind: unit
        ref: "tests/unit/docs-collection.test.ts"
        status: pass
    human_judgment: false
  - id: D3
    description: "installer-registry-distribution.md exists with the two docs/superpowers/specs/...
      path references removed and replaced with plain prose (Pitfall 6, D-09)"
    requirement: DOC-03
    verification:
      - kind: other
        ref: "grep -in 'docs/superpowers/specs' src/content/docs/installer-registry-distribution.md"
        status: pass
    human_judgment: false
  - id: D4
    description: "The puppet-core-9-v1 platform-lock-contract table exists exactly once site-wide,
      in user-guide.md — installer-support.md's port points back to it instead of repeating it"
    requirement: DOC-02
    verification:
      - kind: other
        ref: "grep -rl 'platform-lock-contract:start' src/content/docs/ (one match: user-guide.md)"
        status: pass
    human_judgment: false
  - id: D5
    description: "All three new docs build as normal /docs/{slug}/ routes on all three environments
      (no visibleOn restriction), with npm run test:unit/check:routes/check:invalidation and the
      closed-enumeration lists (docs-collection.test.ts, check-built-routes.ts, deploy-site.sh,
      deploy-scripts.test.ts) all updated together"
    requirement: DOC-02
    verification:
      - kind: integration
        ref: "npm run build && npm run check:routes && npm run check:invalidation"
        status: pass
      - kind: unit
        ref: "npx vitest run (114/114)"
        status: pass
    human_judgment: false
  - id: D6
    description: "Curated content reads correctly as public-facing prose — no residual internal
      voice, awkward cross-reference, or broken deep link a human skim would catch that an
      automated grep sweep would not"
    human_judgment: true
    rationale: "Automated grep sweeps prove the absence of specific forbidden strings (internal/,
      matthew, docs/superpowers/specs) but cannot judge overall reader-voice quality or whether the
      curation choices (what to keep vs. trim from support-guide.md's 507 lines) struck the right
      balance the resolved D-09 scope decision called for. A human read of the three rendered pages
      is the appropriate check for this."

duration: 15min
completed: 2026-08-31
status: complete
---

# Phase 04.2 Plan 02: Documentation Porting (User Guide + Installer Docs) Summary

**Curated ports of the User Guide and two reader-facing installer docs (DOC-02/DOC-03) — user-guide.md keeps the puppet-core-9-v1 platform-lock-contract table as its one canonical site-wide home, and installer-support.md is reframed from a 507-line internal Support/PS runbook into second-person reader troubleshooting guidance with zero `internal/` Go package-path references.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-08-31T15:41:00Z
- **Completed:** 2026-08-31T15:47:16Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments

- `src/content/docs/user-guide.md`: curated port of `puppet-console`'s 991-line `USER-GUIDE.md` —
  drops the four sections explicitly marked "— Deferred in Stagehand 1.0" (discovery/import wizard,
  Data/Hiera editor, Self-Update, Teams/Roles/Approvals) in full, the named-engineer Labs-flag
  ownership sentence, and the cross-repo GitHub Pages docs-site cross-reference (this site is now
  itself the reader-facing docs site). No `visibleOn` restriction — live on all three environments,
  unlike Phase 04.1's password-gated Tester's Guide.
- Fixed the three dangling "see Teams, roles, and approvals" cross-references left behind by
  dropping that section, so no reader-facing pointer aims at removed content.
- `src/content/docs/installer-registry-distribution.md`: near-verbatim port of
  `registry-distribution-guide.md` — simplified the branch/merge internal-timing framing in the
  opener and removed both `docs/superpowers/specs/...` internal spec-path references (Pitfall 6).
- `src/content/docs/installer-support.md`: heavily curated port of `support-guide.md` per D-09's
  resolved scope decision — the quick-reference symptom table's "Check" column was rewritten from
  internal function/package-path citations to plain-English steps; every `internal/...` Go path and
  `file.go:NNN` citation, the literal Go code block, and the "Internal reference for Support and PS
  engineers" framing were dropped; Secrets was reframed into second-person "what the installer does
  with your secrets" trust language matching `/docs/security.md`'s voice; the platform-lock section
  points back to `user-guide.md`'s table instead of repeating ~60 lines of identical content; added
  a "Where to report a problem" section distinguishing installer/console issues
  (`puppet-stagehand/stagehand-release`) from docs-site issues (this repo's own `/support/`, D-03).
- Closed-enumeration lists (`docs-collection.test.ts`'s slug array, `check-built-routes.ts`'s
  `requiredOutputs`, `deploy-site.sh`'s CloudFront invalidation `--paths`,
  `deploy-scripts.test.ts`'s exact-string assertion) all updated together for all three new docs.
- Full local sweep green: `npm run build && npm run check:routes && npm run check:invalidation &&
  npx vitest run` (114/114) `&& npm run lint`.

## Task Commits

Each task was committed atomically:

1. **Tracer: port and fully wire the User Guide (DOC-02) end-to-end** — `df24f1e` (feat)
2. **Task 2: Port and curate the two reader-facing installer docs (DOC-03)** — `ef20cfe` (feat)

**Plan metadata:** commit follows this SUMMARY

## Files Created/Modified

- `src/content/docs/user-guide.md` - curated User Guide port, canonical home of the
  platform-lock-contract table
- `src/content/docs/installer-registry-distribution.md` - curated registry-distribution guide port
- `src/content/docs/installer-support.md` - curated, reframed installer support/troubleshooting
  guide
- `tests/unit/docs-collection.test.ts` - added `user-guide`, `installer-registry-distribution`,
  `installer-support` to the closed slug array
- `scripts/check-built-routes.ts` - added all three docs' `index.html` outputs to `requiredOutputs`
- `scripts/deploy-site.sh` - added all three docs' paths to the CloudFront invalidation list
- `tests/unit/deploy-scripts.test.ts` - updated the exact invalidation-command assertion
- `scripts/check-built-links.ts` - added the two new external links (Bolt install page,
  stagehand-release issues) this plan's docs introduce to `allowedNonClaimLinks`

## Decisions Made

- Dropped "Teams, Roles, and Approvals" from `user-guide.md` in full (per the plan's explicit
  four-section drop list) and fixed the resulting dangling cross-references rather than leaving
  "see X below" pointers at removed content.
- Pointed `installer-support.md`'s platform-lock section at `user-guide.md` by plain prose link
  (not an anchor fragment), since heading-slug anchor stability wasn't verified for this markdown
  pipeline.
- Kept `puppetlabs-edgeops`/`puppetlabs-cem_linux` module names and the `POST /api/security/readiness`
  / `POST /api/control-repo/proposal` endpoint references in `installer-support.md` — these are
  public Puppet Forge module identifiers and customer-facing REST API endpoints, not internal Go
  paths, so they were judged in-scope reader content rather than something the DROP rules covered.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added two new external links to `check-built-links.ts`'s allowlist**
- **Found during:** Task 2's full local sweep (`npx vitest run`)
- **Issue:** `tests/unit/built-link-policy.test.ts` asserts every external link in the built site
  against an exact allowlist (`allowedNonClaimLinks`) in `scripts/check-built-links.ts`. This plan's
  new docs introduce two links not previously on that allowlist: the Bolt manual-install page
  (`help.puppet.com/bolt/current/topics/bolt_installing.htm`) and the new `stagehand-release` issue
  tracker (D-03's own requirement). Neither file was in this plan's declared `files_modified`, but
  leaving them off the allowlist fails the plan's own required `<verify>` step (`npx vitest run`).
- **Fix:** Added both URLs to `allowedNonClaimLinks`.
- **Files modified:** `scripts/check-built-links.ts`
- **Verification:** `npx vitest run` — full suite green (114/114).
- **Committed in:** `ef20cfe` (Task 2 commit)

**2. [Rule 1 - Bug] Reformatted the three new/changed docs with prettier**
- **Found during:** `npm run format:check` (part of the project's `npm run verify` gate)
- **Issue:** The curated markdown (frontmatter quote style, table column widths) didn't match this
  project's Prettier formatting convention.
- **Fix:** Ran `npx prettier --write` on the three files; no content changed, only formatting.
- **Files modified:** `src/content/docs/user-guide.md`, `src/content/docs/installer-registry-distribution.md`,
  `src/content/docs/installer-support.md`
- **Verification:** `npm run format:check` — clean.
- **Committed in:** `ef20cfe` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 formatting bug)
**Impact on plan:** Both fixes necessary for the plan's own `<verify>` and this project's `npm run
verify` gate to stay green. No scope creep — neither touched curation content or design decisions.

## Issues Encountered

None beyond the two deviations above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All three DOC-02/DOC-03 docs are live, registered routes with a green local build/test sweep
  (`npm run build && npm run check:routes && npm run check:invalidation && npx vitest run` all
  pass, plus `npm run lint`/`format:check` spot-checked green).
- The platform-lock-contract table exists exactly once site-wide, in `user-guide.md`, as required.
- D6 (reader-voice quality of the curated content, especially `installer-support.md`'s curation
  depth against D-09's resolved scope decision) is flagged `human_judgment: true` — grep sweeps
  prove the absence of forbidden strings but not overall prose quality; a human read of the three
  rendered pages is the appropriate final check before Phase 04.2-03 or ship.
- Plan 04.2-03 (noted in this phase's dependency graph as depending on this plan) can proceed;
  nothing in this plan blocks it.

## Self-Check: PASSED

- All 3 new doc files confirmed present on disk (`src/content/docs/user-guide.md`,
  `src/content/docs/installer-registry-distribution.md`, `src/content/docs/installer-support.md`).
- Both referenced commit hashes (`df24f1e`, `ef20cfe`) confirmed present in `git log --oneline --all`.
- Plan-level `<verification>` re-run and passing: `npx vitest run tests/unit/docs-collection.test.ts
  tests/unit/deploy-scripts.test.ts` (23/23), `npm run build && npm run check:routes && npm run
  check:invalidation` (all green), `grep -in 'internal/|matthew|docs/superpowers/specs'
  src/content/docs/user-guide.md src/content/docs/installer-*.md` (empty).
- Full `npx vitest run` (114/114 passing across 17 files) and `npm run lint` clean.
- Platform-lock-contract table confirmed to exist exactly once site-wide
  (`grep -rl 'platform-lock-contract:start' src/content/docs/` → one match: `user-guide.md`).

---
*Phase: 04.2-tester-downloads*
*Completed: 2026-08-31*
