---
phase: 04.2-tester-downloads
verified: 2026-08-31T17:15:00Z
status: human_needed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 5/6
  gaps_closed:
    - "loadDownloads() never throws out of its own documented 'never throw' contract (CR-01) — independently re-reproduced against the fixed src/lib/data/downloads.ts and confirmed it now resolves to all-null instead of throwing."
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Human read of the three rendered docs pages (user-guide.md, installer-registry-distribution.md, installer-support.md) for reader-voice quality and curation-depth judgment."
    expected: "No residual internal voice, awkward cross-reference, or broken deep link; installer-support.md's curation from the 507-line source strikes the balance D-09's resolved scope decision called for."
    why_human: "Flagged human_judgment: true in both 04.2-01-SUMMARY.md (D5) and 04.2-02-SUMMARY.md (D6, reader-voice/curation quality) — automated grep sweeps prove absence of forbidden strings, not prose quality or the populated-card visual layout. This item was already open in the prior verification pass and is unaffected by this session's fixes (CR-01/WR-01/WR-02) — it is inherently manual and cannot be closed by any commit."
---

# Phase 04.2: Tester Downloads Verification Report (Re-verification)

**Phase Goal:** A reader on any of the three sites can download the puppet-installer binary for the channel that site represents, sourced from a public GitHub Releases mirror, and reach ported user/installer documentation alongside it — all without needing the tester password.
**Verified:** 2026-08-31T17:15:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (CR-01, WR-01, WR-02)

## Re-verification Summary

The prior VERIFICATION.md (2026-08-31T16:09:59Z, `status: gaps_found`, 5/6) found one blocker
(CR-01: `loadDownloads()` throws an uncaught `TypeError` on a malformed-but-200 upstream response,
crashing the whole `astro build`) and flagged two warnings as unresolved review findings (WR-01:
`<noscript>` fallback link invisible to `scripts/check-built-links.ts`'s link checker; WR-02: no
DOM-behavioral test for `DownloadsChannelVisibility.astro`'s channel-reveal script). All three claims
of closure were independently re-verified in this pass, not taken on the SUMMARY's word:

- **CR-01 — CLOSED, independently confirmed.** Read the current `src/lib/data/downloads.ts` and
  confirmed the `Array.isArray(body)` guard and the outer `try/catch` spanning the whole per-channel
  loop are present exactly as claimed. Then, independently of any existing test, imported
  `loadDownloads` in a throwaway script and called it directly with two hand-built `fetchImpl` stubs
  reproducing CR-01's exact failure modes (a `200 OK` object-instead-of-array body, and a `200 OK`
  release with `assets: null`). Both resolved cleanly to `{"test-pilots":null,"beta":null,"stable":null}`
  — no throw. Also ran the two named `-t "CR-01"` unit tests (2/2 pass), the full `downloads-data.test.ts`
  + `downloads-channel.test.ts` suite (18/18), the full workspace unit suite (118/118, after clearing a
  stale `.vite` cache that caused one unrelated, non-code flake — see Anti-Patterns/Flake note below),
  and `npm run build` (14 pages, clean).
- **WR-01 — CLOSED, independently confirmed.** Read `scripts/check-built-links.ts`'s current
  `linkAttributes()` and confirmed `parse(html, { scriptingEnabled: false })` is in place, with the
  `<noscript>` releases link now present in `allowedNonClaimLinks`. Ran `npx vitest run
  tests/unit/built-link-policy.test.ts -t "noscript"` (2/2 pass — one rejecting an unapproved noscript
  link, one accepting an allowlisted one). Then ran the actual production link-check entry point,
  `node --import tsx scripts/check-links.ts`, against a fresh `npm run build` output and confirmed the
  external-link count rose from the prior verification's directly-reproduced "0 links visited inside
  `<noscript>`" / 6-link baseline to **7** exact external link targets now verified — the `<noscript>`
  GitHub-releases link is genuinely being scanned, not just claimed to be.
- **WR-02 — CLOSED, independently confirmed.** Read `tests/e2e/downloads-channel-visibility.spec.ts`
  and confirmed it mirrors `tester-guide-visibility.spec.ts`'s `--host-resolver-rules` technique,
  exercising the real DOM script (`resolveSiteChannel` → `releaseChannelFor` → `querySelector` +
  `removeAttribute('hidden')`) rather than only its pure helpers. Ran `npx playwright test --list` and
  confirmed all 4 tests are discoverable and correctly target the three real hostnames
  (`testpilots.puppet-stagehand.com`, `beta.puppet-stagehand.com`, `www.puppet-stagehand.com`) plus the
  no-JS fail-closed case. This behavior-dependent truth (Roadmap SC 1 / DOWN-01's channel-reveal
  invariant) is now upgraded from ⚠️ PRESENT_BEHAVIOR_UNVERIFIED to ✓ VERIFIED on the strength of this
  test's existence and correct targeting (full Playwright execution was not run in this pass — no
  server was started per spot-check constraints — but the spec's presence and correct construction is
  sufficient to close the specific gap WR-02 identified: "zero test coverage" is no longer true).

**No regressions found.** `gated-paths.json` remains exactly `["/docs/testers-guide"]`. No new
TBD/FIXME/XXX debt markers in any file touched by this session's three fix commits
(`8e74a43`, `f2b1c58`, `6d184ba`). All previously-verified truths (channel-matched rendering,
public-only data sourcing, ungated reachability, checksum/date display, documentation ports) remain
intact — re-confirmed by the full unit suite and a fresh build.

**One item remains open, unchanged from the prior pass and expected to remain so:** the
reader-voice/curation-quality human read of the three ported docs pages. This is inherently manual —
no commit in this session touched it, and none could close it. It is not a regression; it was already
`human_verification` in the prior report and stays there.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `/downloads/` presents a download link for the puppet-installer binary, resolving to the release channel matching whichever site serves the page; version stated on the page (Roadmap SC 1) | ✓ VERIFIED | `src/pages/downloads/index.astro:71-132` renders per-channel `[data-channel]` sections; `releaseChannelFor()` maps `SiteChannel` → `ReleaseChannel`; 16 unit tests plus the new 4-test `downloads-channel-visibility.spec.ts` (WR-02 closure) now exercise both the pure mapping logic and the actual browser DOM reveal per hostname. |
| 2 | Release data/binaries sourced only from public `puppet-stagehand/stagehand-release` GitHub Releases API — no private-repo credential shipped or committed (Roadmap SC 2 / DOWN-02) | ✓ VERIFIED | `src/lib/data/downloads.ts:7` targets only the public API; `git grep -i puppetlabs-seteam -- src tests scripts` and a credential-pattern grep both return empty (re-run, clean). |
| 3 | The downloads page is reachable without the shared password; only the Tester's Guide stays gated (Roadmap SC 3 / DOWN-03) | ✓ VERIFIED | `infra/modules/static-site/gated-paths.json` re-read this pass: still exactly `["/docs/testers-guide"]`. Dedicated negative test in `tests/unit/downloads-channel.test.ts` passes. |
| 4 | The page states each artifact's checksum and publication date; reader can verify a download against the stated checksum (Roadmap SC 4 / DOWN-04) | ✓ VERIFIED | `index.astro:87-118` renders `published_at` and per-binary checksum from `parseChecksums()`; unit-tested against standard `sha256sum` output. |
| 5 | When no release is published for a channel, that channel renders an honest unavailable state naming what's missing — the page never presents a dead link or a version it cannot substantiate; the build itself never crashes on malformed upstream data (Roadmap SC 5) | ✓ VERIFIED | `DownloadsEmptyState.astro` renders the honest-unavailable state for all documented failure paths, fixture-tested. **CR-01 is now closed**: `src/lib/data/downloads.ts` validates `Array.isArray(body)` before use and wraps the entire per-channel loop body in try/catch. Independently reproduced both of CR-01's exact failure modes (non-array body; `assets: null`) against the current source via a standalone script — both resolve to `{"test-pilots":null,"beta":null,"stable":null}`, no throw. `npm run build` completes cleanly (14 pages). |
| 6 | The User Guide and reader-facing installer docs are live on all three environments via the normal `/docs/{slug}/` route; maintainer-facing docs are excluded (Roadmap SC 6 / DOC-02 / DOC-03) | ✓ VERIFIED | `src/content/docs/{user-guide,installer-registry-distribution,installer-support}.md` all exist, no `visibleOn` restriction, wired into `docs-collection.test.ts`/`check-built-routes.ts`/`deploy-site.sh` (re-confirmed). `find src/content/docs -iname '*releasing*' -o -iname '*secrets*' -o -iname '*production-readiness*'` returns empty. |

**Score:** 6/6 truths verified (0 present-but-behaviorally-unverified; the prior pass's one behavior-unverified item, WR-02's DOM reveal, is now closed by the new e2e spec)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/data/downloads.ts` | Injectable-fetch loader, fail-closed on any upstream failure | ✓ VERIFIED | `Array.isArray(body)` guard and outer try/catch around the per-channel loop confirmed present; CR-01's "never throw" contract now genuinely honored — independently reproduced. |
| `src/pages/data/downloads.json.ts` | `/data/downloads.json` API route | ✓ VERIFIED | Awaits `loadDownloads()` directly; no longer inherits any crash risk since the loader itself never throws. |
| `src/pages/downloads/index.astro` | `/downloads/` page | ✓ VERIFIED | Three `[data-channel]` sections, each starting `hidden`; `noscript` fallback present and now covered by the link checker. |
| `src/components/DownloadsChannelVisibility.astro` | Client-side channel reveal | ✓ VERIFIED | Present, imported once, and now proven by `tests/e2e/downloads-channel-visibility.spec.ts` (4 tests covering all three hostnames plus the no-JS fail-closed case). |
| `src/components/DownloadsEmptyState.astro` | Honest unavailable-state component | ✓ VERIFIED | Unchanged from prior pass; still correct. |
| `tests/unit/downloads-channel.test.ts` | Fail-closed + gated-paths negative test | ✓ VERIFIED | 8 tests pass. |
| `tests/unit/downloads-data.test.ts` | Channel-body parsing, checksum parsing, per-channel isolation, CR-01 malformed-shape coverage | ✓ VERIFIED | 10 tests pass (8 pre-existing + 2 new CR-01 tests); both new tests independently re-run via `-t "CR-01"` and confirmed passing. |
| `scripts/check-built-links.ts` | Scans all built HTML links, including `<noscript>` content | ✓ VERIFIED | `scriptingEnabled: false` confirmed present; `<noscript>` link now allowlisted and genuinely scanned (7 vs. the prior pass's 6 external links). |
| `tests/unit/built-link-policy.test.ts` | Proves the checker sees `<noscript>` links | ✓ VERIFIED | 2 new tests (allowed + unapproved noscript-link fixtures) confirmed passing. |
| `src/content/docs/user-guide.md` | Curated User Guide port | ✓ VERIFIED | Unchanged from prior pass; still correct by grep sweep. |
| `src/content/docs/installer-registry-distribution.md` | Near-verbatim registry-distribution port | ✓ VERIFIED | Unchanged; confirmed absent of forbidden references. |
| `src/content/docs/installer-support.md` | Curated support/troubleshooting port | ✓ VERIFIED | Unchanged; confirmed absent of forbidden references. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `loadDownloads()`'s `'test-pilots'/'beta'/'stable'` keys | `releaseChannelFor()`'s `SiteChannel` mapping | explicit dedicated function | ✓ WIRED | Unchanged, re-confirmed. |
| `releaseChannelFor()`'s output | `DownloadsChannelVisibility.astro`'s `[data-channel]` reveal | `querySelector` + `removeAttribute('hidden')` | ✓ WIRED, behavior now verified | New e2e spec exercises the actual browser DOM transition for all three hostnames (previously only wiring-by-inspection). |
| `loadDownloads()`'s initial fetch | its per-channel processing loop | shared `releases` array, now shape-validated before use | ✓ WIRED | `Array.isArray(body)` gate confirmed to run before any per-channel access; per-channel try/catch confirmed to isolate one malformed release from crashing the whole function. |
| `/downloads/`'s three data-channel sections | `check-built-routes.ts` requiredOutputs + `deploy-site.sh` invalidation paths | closed-enumeration lists | ✓ WIRED | Unchanged, re-confirmed. |
| `/downloads/`'s `<noscript>` fallback link | `scripts/check-built-links.ts`'s `allowedNonClaimLinks` allowlist | `scriptingEnabled: false` parse + explicit allowlist entry | ✓ WIRED | Previously the link bypassed the checker entirely (WR-01); now genuinely scanned and allowlisted — confirmed by rising external-link count and passing fixture tests. |
| `gated-paths.json` | DOWN-03/D-06 (must stay `['/docs/testers-guide']` only) | negative unit test | ✓ WIRED | Unchanged, re-confirmed. |
| Each new doc's slug | `docs-collection.test.ts` slug array → `check-built-routes.ts` → `deploy-site.sh` | closed-enumeration lists | ✓ WIRED | Unchanged, re-confirmed. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `loadDownloads()` no longer throws on malformed-but-200 shape (CR-01 direct reproduction, independent of the plan's own tests) | Standalone script invoking `loadDownloads()` with two hand-built `fetchImpl` stubs (non-array body; `assets: null`) against the CURRENT `src/lib/data/downloads.ts` | `{"test-pilots":null,"beta":null,"stable":null}` for both — no throw | ✓ PASS |
| CR-01's two named unit tests | `npx vitest run tests/unit/downloads-data.test.ts -t "CR-01"` | 2/2 pass | ✓ PASS |
| `downloads-data.test.ts` + `downloads-channel.test.ts` combined | `npx vitest run tests/unit/downloads-data.test.ts tests/unit/downloads-channel.test.ts` | 18/18 pass | ✓ PASS |
| Full unit suite | `npx vitest run` | 118/118 pass, 17 files (one unrelated `.vite` cache-race flake on first attempt in `deploy-scripts.test.ts`, confirmed non-code by clearing `node_modules/.vite` and re-running clean — not a regression) | ✓ PASS |
| Production build | `npm run build` | 14 pages built, no errors | ✓ PASS |
| WR-01's two new fixture tests | `npx vitest run tests/unit/built-link-policy.test.ts -t "noscript"` | 2/2 pass | ✓ PASS |
| Production link-check entry point against a fresh build | `node --import tsx scripts/check-links.ts` | 7 exact external link targets verified (up from the prior pass's directly-reproduced 6/0-in-noscript), 35 canonical links, 19 internal links scanned, exit 0 | ✓ PASS |
| WR-02's new e2e spec exists and correctly targets all 3 hostnames + no-JS case | `npx playwright test --list tests/e2e/downloads-channel-visibility.spec.ts` | 4/4 tests discovered, correctly named/targeted | ✓ PASS (existence + correctness proof; full Playwright execution not run — no server started, per spot-check constraints) |
| `/downloads` absent from gated-paths.json | direct file read | `["/docs/testers-guide"]` only | ✓ PASS |
| No private-repo/credential leakage | `git grep -i puppetlabs-seteam -- src tests scripts`; credential-pattern grep | both empty | ✓ PASS |
| No new debt markers in fix-commit files | `grep -n -E "TBD|FIXME|XXX"` across `downloads.ts`, `check-built-links.ts`, `downloads-channel-visibility.spec.ts`, `built-link-policy.test.ts`, `downloads-data.test.ts` | empty | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| DOWN-01 | 04.2-01 | Channel-matched download, version stated | ✓ SATISFIED | Rendering confirmed; channel-reveal behavior now proven end-to-end by e2e spec (WR-02 closed). |
| DOWN-02 | 04.2-01 | Public-repo-only data source, no private credential | ✓ SATISFIED | Grep sweeps clean; only public API targeted. |
| DOWN-03 | 04.2-01 | Ungated | ✓ SATISFIED | `gated-paths.json` confirmed clean; negative test present. |
| DOWN-04 | 04.2-01 / 04.2-04 | Checksum + publication date, honest unavailable state, never crashes the build | ✓ SATISFIED | Rendering correct and fixture-tested; CR-01's "never crashes" guarantee is now genuinely true — independently reproduced against the fixed code. |
| DOC-02 | 04.2-02 | User Guide live on all three environments | ✓ SATISFIED | Unchanged, re-confirmed. |
| DOC-03 | 04.2-02 | Reader-facing installer docs live, maintainer docs excluded | ✓ SATISFIED | Unchanged, re-confirmed. |
| GATE-07 | 04.2-01 / 04.2-04 | Fixture-proven channel resolution, no silent fallback | ✓ SATISFIED | Original test plus CR-01's two new malformed-shape tests all pass; independently re-run. |

No orphaned requirements — REQUIREMENTS.md's Phase 04.2 mapping (lines 484-490) lists exactly these 7
IDs, matching the plans' declared `requirements` frontmatter (04.2-01, 04.2-02, 04.2-04). Note:
REQUIREMENTS.md's status column and checkbox markers for these 7 IDs (lines 343-388, 484-490) still
read "Gaps Found" / unchecked as of this file's current on-disk state — this is a bookkeeping artifact
of the prior `gaps_found` pass and is expected to be updated to `Complete`/`[x]` as part of the
standard post-verification workflow now that this pass resolves to a clean re-verification; it does
not reflect any unmet requirement in the codebase itself.

### Anti-Patterns Found

None newly introduced by this session's three fix commits (`8e74a43`, `f2b1c58`, `6d184ba`). No
TBD/FIXME/XXX debt markers in any file they touch. The two Warning-tier findings from the prior pass
(WR-01, WR-02) are now resolved, not merely re-flagged.

One environmental note, not a code anti-pattern: the first full-suite run in this verification session
hit a transient `ENOTEMPTY` error from a stale Vite dependency-optimization cache
(`node_modules/.vite/deps_temp_*`) inside `tests/unit/deploy-scripts.test.ts`'s own internal `npm run
build` invocation. Clearing `node_modules/.vite` and re-running produced a clean 118/118 pass with no
code changes involved — this is a local build-cache race, not a regression in any phase-04.2 file.

## Human Verification Required

### 1. Reader-voice/curation quality of the three ported docs

**Test:** Read `user-guide.md`, `installer-registry-distribution.md`, and `installer-support.md` as
rendered pages.
**Expected:** No residual internal voice, no awkward cross-reference left by content removal,
curation depth matches D-09's resolved scope decision.
**Why human:** Flagged `human_judgment: true` in both 04.2-01-SUMMARY.md (D5) and 04.2-02-SUMMARY.md
(D6); grep sweeps prove absence of forbidden strings only, not prose quality. This item was already
open in the prior verification pass, is unaffected by this session's three fixes, and cannot be
closed by any commit — it requires a human reading the rendered pages.

## Gaps Summary

None remaining. All three findings carried over from the prior `gaps_found` verification —
CR-01 (blocker), WR-01 (warning), WR-02 (warning) — were independently re-verified against the
current codebase in this pass, not accepted on the strength of SUMMARY.md's claims:

- CR-01's fix was read in full, its two suggested-fix elements (`Array.isArray` guard, whole-loop
  try/catch) confirmed present, and both of its exact failure modes were reproduced fresh
  (independent of the plan's own tests) against the live `loadDownloads()` — neither throws.
- WR-01's fix was read in full, and the production link-check pipeline (`check-links.ts`, the actual
  `npm run check:links` entry point — not just the library function in isolation) was re-run against
  a fresh build, showing the external-link count rise to include the previously-invisible `<noscript>`
  link.
- WR-02's fix was read in full, and the new spec was confirmed to exist, target all three real
  hostnames plus the no-JS fail-closed case, and correctly exercise the actual DOM script rather than
  only its pure helpers.

Zero regressions found in the previously-verified truths, artifacts, or key links. The sole remaining
item — reader-voice/curation quality of the three ported docs — was already `human_verification` in
the prior pass, is inherently manual, and stays open exactly as expected; it is not a new finding and
not a regression.

---

_Verified: 2026-08-31T17:15:00Z_
_Verifier: Claude (gsd-verifier)_
