---
phase: 03-real-documentation-content
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
  - src/data/tiers.yaml
  - tests/unit/json-endpoints.test.ts
  - src/pages/support/index.astro
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-06]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor reading / can reach /tiers/ directly from the hero, not only via top navigation — the home page currently has no tier path at all."
    - "The three home-page capability descriptions read as real product positioning grounded in facts already established elsewhere on this site (Bolt, SSH, the compatibility register), not placeholder marketing filler."
    - "A visitor on /tiers/ reads real, comparative entitlement language for all four tiers (OpenVox, Puppet Core, Puppet Enterprise, PE Advanced) that describes what each Puppet product tier means for the customer, without the site itself asserting or verifying that entitlement (CONT-06)."
    - "/data/tiers.json's rendered JSON still matches /tiers/'s rendered copy exactly (same loadTiers() source), and the unit test asserting the exact JSON contract is updated to the new content, not left describing stale copy."
    - "/support/ states the product's current lifecycle stage (pre-1.0, actively developed, current release v0.2.3) in customer terms, reusing the site's existing non-entitlement, non-assertive voice."
  artifacts:
    - src/pages/index.astro (View tiers CTA + rewritten capability copy)
    - src/data/tiers.yaml (real per-tier summary/audience/features)
    - src/pages/support/index.astro (new Product lifecycle section)
  key_links:
    - "src/data/tiers.yaml -> src/lib/data/tiers.ts's loadTiers() -> both src/pages/tiers/index.astro AND src/pages/data/tiers.json.ts -> tests/unit/json-endpoints.test.ts's exact-string assertion (single source, must not disagree, per CONT-02)"
    - "src/pages/index.astro's new 'View tiers' CTA -> /tiers/ (direct hero path to tier selection, satisfying the phase goal's 'work out which tier they need')"
---

<objective>
Replace the site's remaining placeholder-grade marketing and product copy with real, customer-ready
content on the three routes this phase's remaining requirements target: the home page's positioning and
tier path (CONT-01), the tiers page's comparative entitlement language (CONT-02), and the support page's
product lifecycle statement (CONT-03). This plan is independent expansion work — it shares no files with
03-01 (the docs-registration tracer) or 03-02 (the GATE-03 checker) and can execute in parallel with both.

Purpose: PROJECT.md records the current home/tiers/support copy as placeholder-grade, and the UI-SPEC
identifies exactly two structural gaps (no tier-path CTA on the home hero; no lifecycle section on
support) plus one content gap (tier features are single placeholder bullets, not real comparative
entitlement copy). This plan closes all three under CONT-06's explicit boundary: no credential
collection, no entitlement assertion, no third-party script, no analytics, no premium behavior presented
as shipped.
Output: real copy on `/`, `/tiers/`, and `/support/`, with `/data/tiers.json`'s test contract kept in
sync with the new tier data.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-real-documentation-content/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home page — real capability copy and the "View tiers" CTA</name>
  <files>src/pages/index.astro</files>
  <read_first>
    src/pages/index.astro, .planning/phases/03-real-documentation-content/03-UI-SPEC.md ("Home"
    Page-Specific Contract and Copywriting Contract sections)
  </read_first>
  <action>
    Per the UI-SPEC's exact copywriting contract (D-source: UI-SPEC "Secondary CTA" row), insert a new
    `<a class="btn btn-outline-light" href="/tiers/">View tiers</a>` inside `.control-hero__actions` as
    the second of three buttons, positioned between the existing "Get started" (`.btn.btn-primary`) and
    "Check compatibility" (`.btn.btn-outline-light`) anchors. Do not change either existing button's
    class, href, or position. Leave the hero `<h1>`, eyebrow, and lede paragraph unchanged — the UI-SPEC
    marks these already-shipped.

    Rewrite the three `capabilities` array description strings (title text may stay or be lightly
    tightened; keep exactly 3 entries — do not add a 4th without also updating `.capability-grid`'s
    3-column CSS, which is out of scope this task) to real, grounded product positioning. Ground every
    claim in facts already established on this site: "Environment visibility" should describe reading
    Puppet/OpenVox environment state from one place rather than piecing together separate sessions or
    reports; "Bolt-native execution" should name Puppet Bolt's own task/plan execution model over SSH
    (the same execution path documented in `getting-started.md`/`security.md`) instead of a second,
    unnamed automation surface; "Explicit support boundaries" should reference knowing exactly what each
    tier includes and what is still primary-evidence-verified in the compatibility register before
    relying on it. Do not invent Stagehand-specific feature claims (dashboards, specific report formats,
    specific CLI flags) that appear nowhere else in this codebase. Do not present PCP/orchestrator
    behavior as available.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -5</automated>
  </verify>
  <done>The home hero has a working "View tiers" CTA between "Get started" and "Check compatibility", and all 3 capability descriptions are rewritten to grounded, non-placeholder copy.</done>
  <acceptance_criteria>
    - `.control-hero__actions` contains exactly 3 `<a>` elements in order: Get started, View tiers, Check
      compatibility; the new link's `href` is `/tiers/` and its class is `btn btn-outline-light`.
    - `capabilities` still has exactly 3 entries; none of the 3 descriptions is the pre-existing
      placeholder text verbatim.
    - No description references a specific CLI command, dashboard, or report format not documented
      elsewhere in this repository.
    - No description implies PCP/orchestrator behavior ships today.
    - `npx astro check` reports no new type errors.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tiers data — real comparative entitlement copy for all four tiers</name>
  <files>src/data/tiers.yaml, tests/unit/json-endpoints.test.ts</files>
  <read_first>
    src/data/tiers.yaml, src/data/schema/tiers.schema.json, src/components/TierCard.astro,
    tests/unit/json-endpoints.test.ts (the `expectedTiers` exact-string literal),
    .planning/phases/03-real-documentation-content/03-UI-SPEC.md ("Tiers" Page-Specific Contract)
  </read_first>
  <action>
    Rewrite `summary`, `audience`, and `features` for all four tiers in `src/data/tiers.yaml`. Ground the
    copy in facts already established in this codebase and in well-known, real Puppet-ecosystem facts —
    do not invent Stagehand-specific premium features gated per tier, since access control and Forge
    payment enforcement explicitly happen in Puppet Console, not this site (per `tiers/index.astro`'s
    existing enforcement-boundary disclaimer, which stays unchanged). For each tier, write a `summary`
    describing what that Puppet product tier is for the customer (e.g., openvox: the community-governed,
    open-source Puppet continuation; puppet-core: the foundational commercial-adjacent Puppet product;
    puppet-enterprise: Puppet's commercial platform with console, RBAC, and orchestration reporting on
    top of core Puppet; pe-advanced: the higher Puppet Enterprise package tier). For `features`, write at
    least 1 (this satisfies the UI-SPEC's flagged "zero-one-many" unresolved gap as a content-authoring
    discipline requirement — do not touch `tiers.schema.json`'s `minItems` absence to enforce this; that
    schema change is explicitly out of this task's scope per the UI-SPEC's default assumption), and
    prefer 2 real, comparative bullets per tier covering: (a) the environment-visibility and
    Bolt-native-execution guidance this documentation site provides for that product (true for every
    tier equally — do not claim it differs by tier), and (b) the support channel that tier's entitlement
    maps to (public issue tracker for `community`; the reader's own commercial support channel for
    `commercial`/`advanced`, per `support/index.astro`'s existing channel language — do not invent a
    priority-support or SLA claim not documented anywhere in this repository). Keep every `id`,
    `entitlement`, and `name` field exactly as currently defined — only `audience`, `summary`, and
    `features` change.

    Update `tests/unit/json-endpoints.test.ts`'s `expectedTiers` array to match the new `tiers.yaml`
    content exactly, field for field, in the same tier order — this is a byte-exact `toBe()` string
    comparison against `/data/tiers.json`'s serialized output, so any mismatch (including whitespace
    inside string values) fails the test.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/json-endpoints.test.ts tests/unit/data-validation.test.ts</automated>
  </verify>
  <done>src/data/tiers.yaml has real, grounded per-tier summary/audience/features copy (>=1 feature each) for all 4 tiers, and tests/unit/json-endpoints.test.ts's expectedTiers literal matches it exactly.</done>
  <acceptance_criteria>
    - All 4 tiers keep their existing `id`/`entitlement`/`name`; every `features` array has at least 1
      entry containing real, non-placeholder text (not the pre-existing single-bullet placeholder).
    - No `features` entry asserts a Stagehand-specific premium capability that exists nowhere else in
      this repository (e.g., no invented SLA tiers, no invented dashboards).
    - `npx vitest run tests/unit/json-endpoints.test.ts` passes — `/data/tiers.json`'s exact serialized
      output matches the updated `expectedTiers` literal.
    - `npx vitest run tests/unit/data-validation.test.ts` still passes (the 4-tier-ID-set assertions are
      unaffected by this content-only change).
    - `npm run validate:data` passes against `tiers.schema.json` unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">Treating the schema's missing minItems as a content-authoring discipline (every tier keeps >=1 real feature by review, not by schema enforcement) rather than tightening tiers.schema.json is a fully reversible scope choice — the schema can still gain a minItems constraint in a later phase without this task's content needing to change.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Support page — add the Product lifecycle section</name>
  <files>src/pages/support/index.astro</files>
  <read_first>
    src/pages/support/index.astro, src/styles/components/_tier-card.scss (the `.tier-intro` rule at
    line 73, reused verbatim), .planning/phases/03-real-documentation-content/03-UI-SPEC.md ("Support"
    Page-Specific Contract), .planning/REQUIREMENTS.md (DOWN section, for the sourced release-version
    fact)
  </read_first>
  <action>
    Insert a new `<section class="tier-intro" aria-labelledby="product-lifecycle-title">` between the
    existing `<header class="register-hero support-page__hero">...</header>` block and the existing
    `<section class="support-page" aria-labelledby="support-paths-title">` block. Reuse the `.tier-intro`
    class as-is (do not write new CSS — it already supplies the `clamp(3.5rem, 8vw, 6.5rem) 0` padding,
    border-bottom, and raised-navy background this new section needs) but do not use
    `.tier-intro__grid`/`.tier-intro__levels` (those are for the two-column tier-mapping layout on
    `/tiers/`, not needed here). Inside, a `<div class="container">` containing a
    `<p class="section-kicker">PRODUCT LIFECYCLE</p>` (exact casing, matching the existing kicker
    convention) and an `<h2 id="product-lifecycle-title">` with real lifecycle-stage copy, followed by a
    `<p>` stating: Stagehand is in active pre-1.0 development (the sourced fact from REQUIREMENTS.md:
    latest release `v0.2.3`) — do not invent a marketing-style maturity label ("GA", "production-ready")
    not evidenced anywhere in this repository. If the section includes a caution about unavailable
    behavior, reuse the exact `.support-page__warning` callout markup (amber left-border, 8%-opacity
    tint) already used lower on this page — do not invent a second warning treatment. State that premium
    PCP/orchestrator behavior is unavailable until an approved Stagehand release compatibility record
    documents it (reuse this exact commitment from `getting-started.md`/`security.md`, worded to fit this
    page's flow). Do not assert any entitlement or premium behavior as available today (CONT-06). The two
    existing channel cards below (`.support-page__grid`) stay copy-stable — only touch them if this new
    section changes what belongs in "where to report," which it should not.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -5</automated>
  </verify>
  <done>src/pages/support/index.astro has a new Product lifecycle section between the hero header and the existing support-page section, reusing the tier-intro and support-page__warning patterns with no new CSS.</done>
  <acceptance_criteria>
    - The new section sits between the existing hero header and the existing "Choose a channel" section,
      uses the `tier-intro` class with no new SCSS added, and has exactly one `<h2>` (no new `<h1>`).
    - The section states the product is in pre-1.0 / active development and cites `v0.2.3` as the
      current release, without an invented maturity label.
    - If a caution is present, it uses the existing `support-page__warning` markup verbatim, not a new
      warning treatment.
    - No entitlement assertion, credential field, third-party script, or download/installer reference
      appears in the new section.
    - `npx astro check` reports no new type errors.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Content author (git PR) -> rendered public HTML | Home, tiers, and support copy are committed by a reviewed PR and rendered statically — no runtime user input on any of these three pages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | `src/pages/index.astro` capability copy, `src/data/tiers.yaml` feature bullets, `src/pages/support/index.astro` lifecycle copy | medium | mitigate | Every task's acceptance criteria explicitly forbid inventing Stagehand-specific feature/SLA/maturity claims not evidenced elsewhere in this repository; CODEOWNERS review on the PR is the primary control. |
| T-03-07 | Tampering | Tier `entitlement` language creeping into an assertion the static site cannot back | medium | mitigate | Task 2's action explicitly forbids per-tier premium-capability claims and requires reusing the existing "does not grant or enforce product access" framing already on `/tiers/`; Task 3's action explicitly forbids presenting PCP/orchestrator behavior as shipped. |
</threat_model>

<verification>
- `npx astro check` reports no new type errors after all 3 tasks.
- `npx vitest run tests/unit/json-endpoints.test.ts tests/unit/data-validation.test.ts` passes.
- `npm run build` succeeds and produces `dist/index.html`, `dist/tiers/index.html`, `dist/support/index.html`
  reflecting the new copy.
</verification>

<success_criteria>
A first-time visitor can reach `/tiers/` directly from the home hero, reads real comparative entitlement
language for all four tiers there, and learns the product's current lifecycle stage on `/support/` — all
without a credential field, an entitlement assertion, a third-party script, or a premium feature
presented as shipped.
</success_criteria>

<output>
Create `.planning/phases/03-real-documentation-content/03-03-SUMMARY.md` when done
</output>
