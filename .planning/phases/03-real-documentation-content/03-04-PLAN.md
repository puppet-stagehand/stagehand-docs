---
phase: 03-real-documentation-content
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified: []
autonomous: true
requirements: [CONT-06, CONT-07]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "npm run verify is green on the working tree with all three prior plans' changes merged — not just green in each plan's own isolated context."
    - "Axe reports zero serious/critical violations on /, /tiers/, /compatibility/, /docs/, and /docs/first-run/ with the real content from this phase in place."
    - "GATE-03's check:invalidation step (built in 03-02) reports zero missing routes against the real build produced by 03-01's new page."
    - "None of the content this phase added — across 03-01's docs page and 03-03's home/tiers/support pages — collects a credential, asserts an entitlement, loads a third-party script, adds analytics, or presents unshipped premium behavior as available today (CONT-06), confirmed by an automated sweep, not a claim without inspection."
  artifacts: []
  key_links:
    - "This plan depends on 03-01, 03-02, and 03-03 all completing first — it is the phase's integration point, proving the combination of independently-executed plans, not just each plan in isolation."
---

<objective>
Close the phase with the two checks that can only be run once every other plan's content exists
together: a full, real `npm run verify` pass over the merged working tree, and an automated CONT-06
boundary sweep across every file this phase touched. Neither check is meaningful run against any single
plan's isolated changes — 03-01, 03-02, and 03-03 execute independently and in parallel, so this plan is
the first point at which their combination is actually exercised.

Purpose: the phase's success criteria (ROADMAP.md Phase 3) require `npm run verify` green with the new
content in place, axe clean on the full audited route set including the new doc page, and no boundary
violation anywhere in the new content — none of which any single upstream plan can prove alone.
Output: a confirmed-green `npm run verify` run and a documented, zero-match boundary sweep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full npm run verify pass over the merged tree</name>
  <files></files>
  <read_first>package.json (the full `verify` script chain)</read_first>
  <action>
    Run `npm run verify` on the working tree with 03-01, 03-02, and 03-03's changes all present. This
    exercises, in order: format check, lint, `astro check`, `validate:data`, unit tests (including
    03-01's updated `docs-collection.test.ts`, 03-02's new `invalidation-coverage.test.ts`, and 03-03's
    updated `json-endpoints.test.ts`), the production and e2e-fixture builds, `check:routes`,
    03-02's new `check:invalidation`, `check:links`, and the full Playwright e2e suite (including
    `accessibility.spec.ts` with 03-01's added `/docs/first-run/` route). If any step fails, diagnose
    whether the failure is a genuine cross-plan regression (e.g., a test literal one plan updated that a
    sibling plan's change also touches) and fix it minimally — do not alter any prior plan's content or
    scope decisions to make a step pass; if a fix would require changing scope, stop and report the
    conflict instead of silently narrowing it. Re-run `npm run verify` until it exits 0.
  </action>
  <verify>
    <automated>npm run verify</automated>
  </verify>
  <done>npm run verify exits 0 on the working tree with 03-01, 03-02, and 03-03's changes all merged.</done>
  <acceptance_criteria>
    - `npm run verify` exits 0 on the merged working tree.
    - `npx playwright test tests/e2e/accessibility.spec.ts --project=production` (already run as part of
      `verify`'s `test:e2e` step) reports zero serious/critical violations across all 5 audited routes.
    - `npm run check:invalidation` (already run as part of `verify`) reports zero missing routes.
    - No fix applied in this task altered a prior plan's copy content or scope — only mechanical
      regressions (literal test-string drift, formatting) were corrected, and each such fix is named in
      this plan's SUMMARY.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Automated CONT-06 boundary sweep across this phase's new and changed content</name>
  <files></files>
  <read_first>
    src/content/docs/first-run.md, src/content/docs/getting-started.md, src/pages/index.astro,
    src/data/tiers.yaml, src/pages/support/index.astro
  </read_first>
  <action>
    Run a set of targeted, zero-match grep checks over exactly the five files this phase's content plans
    touched (listed in read_first), scoped to structural HTML/markdown patterns rather than prose
    mentions of the underlying concepts (security.md already legitimately discusses credentials in prose
    without collecting them — that file is out of this sweep's scope since 03-01/03-03 did not modify
    it). Check, across those five files: (1) no credential-collecting form markup — no `type="password"`,
    no opening `input` tag, no opening `form` tag; (2) no entitlement-assertion phrasing — no
    case-insensitive match for "your plan includes", "you are entitled", "unlocked for your account";
    (3) no third-party runtime script or embed — no `script` tag with an external `src`, no `iframe` tag;
    (4) no analytics/telemetry marker — no `gtag(`, no `google-analytics`, no `data-analytics`; (5) no premature
    reference to the tester-download surface that Phase 04.2 owns — no reference to a container-registry
    pull command or an installer download link/button (that capability does not exist on this site yet).
    Report the exact grep command and its (expected-empty) output for each of the 5 checks in this plan's
    SUMMARY so the sweep is independently reproducible, not just asserted.
  </action>
  <verify>
    <automated>! grep -rniE 'type="password"|<input|<form|<script src=|<iframe|gtag\(|google-analytics|data-analytics' src/content/docs/first-run.md src/content/docs/getting-started.md src/pages/index.astro src/data/tiers.yaml src/pages/support/index.astro</automated>
  </verify>
  <done>All 5 CONT-06 boundary greps (credential markup, entitlement phrasing, third-party script/embed, analytics markers, premature download-surface references) return zero matches across the 5 phase-touched content files.</done>
  <acceptance_criteria>
    - The credential-markup grep (`type="password"`, `<input`, `<form`) returns zero matches across
      the five files.
    - The third-party-script grep (`<script src=`, `<iframe`) returns zero matches across the five
      files.
    - The analytics-marker grep (`gtag(`, `google-analytics`, `data-analytics`) returns zero matches
      across the five files.
    - A case-insensitive grep for the phrases "your plan includes", "you are entitled", and "unlocked for
      your account" returns zero matches across the five files.
    - A case-insensitive grep for the container-registry pull verb-noun pair and for the standalone path
      segment that names the tester-download route (both reserved for Phase 04.2) returns zero matches
      across the five files.
    - Every check's exact command and empty output is recorded in this plan's SUMMARY.md.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Merged working tree (03-01 + 03-02 + 03-03) -> `npm run verify` | The first point at which all three independently-executed plans' changes are exercised together; a regression here is by definition a cross-plan interaction, not a single plan's own bug. |
| This phase's new/changed content -> CONT-06's boundary | The site's static-only, non-entitlement-asserting, credential-free boundary is a standing security/privacy constraint (PROJECT.md), not just a style preference — a violation here would be a real trust-boundary breach if it reached production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | Cross-plan merge of 03-01/03-02/03-03 | medium | mitigate | Task 1 runs the full `npm run verify` chain against the actual merged tree rather than trusting each plan's isolated `npm run verify` run; any test-literal collision (e.g., two plans both touching a shared hardcoded array) surfaces here before it reaches `main`. |
| T-03-09 | Elevation of Privilege | CONT-06 boundary (entitlement/credential/analytics/third-party-script) | high | mitigate | Task 2's five automated, zero-match greps are the closing gate for every CONT-06 acceptance criterion asserted individually in 03-01 and 03-03 — this task is what proves those individual claims held once all content is final, not a re-assertion without evidence. |
</threat_model>

<verification>
- `npm run verify` exits 0 on the fully merged working tree (Task 1).
- All five CONT-06 boundary greps return zero matches across the five phase-touched content files (Task 2).
- This plan's SUMMARY.md records every grep command run and its empty output.
</verification>

<success_criteria>
The phase's ROADMAP success criteria are all independently verifiable from this plan's evidence: `npm run
verify` green with real content in place, axe clean on all 5 audited routes, GATE-03's invalidation check
passing, and zero CONT-06 boundary violations anywhere in this phase's new or changed content.
</success_criteria>

<output>
Create `.planning/phases/03-real-documentation-content/03-04-SUMMARY.md` when done
</output>
