---
phase: 01-infrastructure-role-ownership
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - docs/operations/aws-bootstrap.md
  - docs/operations/github-environments.md
  - docs/adr/0002-github-environment-model.md
  - docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md
  - docs/superpowers/plans/2026-08-22-stagehand-docs-site.md
autonomous: true
requirements: [INFRA-05, DRIFT-01, DRIFT-02, DRIFT-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An operator following aws-bootstrap.md and github-environments.md is no longer told to provision the plan and apply roles by hand; both runbooks describe the OpenTofu-owned path and name the outputs the ARNs come from."
    - "Both runbooks still state that bootstrap is applied by a human under CODEOWNERS review on /infra/ plus a second administrator's review of the trust and permission policies, and the Safety boundary section is unchanged."
    - "The bootstrap runbook's capture block gains the two new role-ARN captures and the saved-plan cleanup remains its last command, so no capture can be attempted after the plan file is deleted."
    - "github-environments.md names the exact set of CloudFront create actions AWS provides no resource type for, states plainly that they are granted unscoped, and names the compensating controls — rather than continuing to promise scoping the OpenTofu does not deliver."
    - "A reader of ADR-0002 is pointed to ADR-0003 for ownership of the infrastructure plan and apply role ARNs, and ADR-0002's Decision section is byte-identical to before."
    - "No source document still describes the site as having three GitHub Environments, and no source document still pins a TypeScript version the repository does not use."
    - "grep -rn for the stale TypeScript version across docs/ returns nothing, and grep for the stale ownership pointer sentence in ADR-0002 returns nothing."
  artifacts:
    - path: "docs/operations/aws-bootstrap.md"
      provides: "the OpenTofu-owned role path, the two new output captures, and the retained human-apply and custody language"
      contains: "infrastructure_plan_role_arns"
    - path: "docs/operations/github-environments.md"
      provides: "the bootstrap-output path replacing manual provisioning, the retained subject list and scoping specification, and the named unscopable CloudFront exception"
      contains: "cloudfront:CreateDistribution"
    - path: "docs/adr/0002-github-environment-model.md"
      provides: "a References entry pointing at ADR-0003 for role ownership"
      contains: "0003-infrastructure-iam-role-ownership.md"
  key_links:
    - from: "docs/operations/github-environments.md"
      to: "infra/bootstrap/iam-github-actions.tf"
      via: "the runbook's scoping prose is the specification the OpenTofu satisfies; where they disagree one is amended"
      pattern: "infra/bootstrap"
    - from: "docs/operations/aws-bootstrap.md"
      to: "infra/bootstrap/outputs.tf"
      via: "the capture block reads the two new outputs by name"
      pattern: "infrastructure_(plan|apply)_role_arns"
    - from: "docs/adr/0002-github-environment-model.md"
      to: "docs/adr/0003-infrastructure-iam-role-ownership.md"
      via: "References entry closing the stale ownership pointer"
      pattern: "0003-infrastructure-iam-role-ownership"
  prohibitions:
    - "The runbook amendment must narrow the least-privilege claim to what AWS actually permits and name the exception. It must never resolve the prose-versus-reality gap by deleting the least-privilege commitment, softening it into an aspiration, or leaving the residual unscoped surface unnamed."
    - "No Decision section of an ADR marked locked is modified. A locked ADR's stale References pointer may be corrected; its decisions may not be edited, reinterpreted, or quietly widened under cover of a documentation fix."
---

<objective>
Close the gap ADR-0003 rule 3 forbids: make the prose agree with the OpenTofu. Two runbooks stop
telling an administrator to hand-craft six least-privilege IAM roles and start telling them to read
six ARNs out of an apply. Three stale sentences elsewhere in the repository stop contradicting
locked decisions.

Purpose: ADR-0003 rule 5 kept the manual provisioning path operative "until the OpenTofu exists".
It now exists. Leaving the manual instruction in place would give an operator two contradictory
procedures for the account's most privileged CI identities. And leaving
`github-environments.md`'s scoping promise untouched while the apply role grants five CloudFront
creates on an unrestricted resource would recreate exactly the prose-versus-reality drift the ADR
exists to prevent — in the direction that flatters the implementation.

Output: both operations runbooks rewritten in place, ADR-0002's stale References pointer closed, and
the design specification and implementation plan reconciled with the locked decisions and the
working tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-role-ownership/01-RESEARCH.md
@.planning/phases/01-infrastructure-role-ownership/01-PATTERNS.md
@.planning/phases/01-infrastructure-role-ownership/01-01-SUMMARY.md
@.planning/phases/01-infrastructure-role-ownership/01-02-SUMMARY.md
@docs/adr/0003-infrastructure-iam-role-ownership.md
@docs/adr/0002-github-environment-model.md
</context>

<artifacts_this_phase_produces>
Every symbol below is **created by this phase** and does not exist in the tree today. The runbook and
ADR prose in this plan references them by name; drift verification must treat them as new.

**New files** (plans 01-01, 01-02)
- `infra/bootstrap/locals.tf`, `infra/bootstrap/iam-github-actions.tf`,
  `infra/bootstrap/tests/iam-github-actions.tftest.hcl`

**New OpenTofu locals** — `local.required_tags`, `local.site`, `local.github_repository`

**New OpenTofu variables** — `var.github_repository`, `var.hosted_zone_id`
(the latter supplied at apply time via `TF_VAR_hosted_zone_id`)

**New OpenTofu resources and data sources** — `data.aws_caller_identity.current`,
`data.aws_partition.current`, `aws_iam_role.infrastructure_plan`,
`aws_iam_role_policy.infrastructure_plan`, `aws_iam_role.infrastructure_apply`,
`aws_iam_role_policy.infrastructure_apply`

**New OpenTofu outputs** — `infrastructure_plan_role_arns`, `infrastructure_apply_role_arns`

**New `tofu test` run names** — `binds_each_plan_role_to_exactly_one_plan_environment`,
`scopes_each_plan_role_to_its_own_state_and_lock_object`,
`publishes_one_plan_role_arn_per_environment`,
`binds_each_apply_role_to_exactly_one_apply_environment`,
`scopes_each_apply_role_to_its_own_environment_resources`,
`forbids_escalation_actions_in_every_role_policy`, `publishes_one_apply_role_arn_per_environment`

**New shell symbols in `scripts/check-tofu-tags.sh`** (plan 01-03) — `bootstrap_dir`, the bootstrap
`tags =` rule, the `providers.tf` `default_tags` rule, the revised final success message

**New requirement ID** — `OPS-13` in `.planning/REQUIREMENTS.md`

**New AWS role names** — `stagehand-<environment>-infrastructure-plan`,
`stagehand-<environment>-infrastructure-apply`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite both operations runbooks onto the OpenTofu-owned role path and name the unscopable exception</name>
  <precondition>The active Node.js satisfies the package.json engines range of at least 24 and below 25 — `node -v` reports v24.x; if it does not, run `nvm use` before any npm command, because `.npmrc` sets engine-strict and npm will otherwise refuse outright.</precondition>
  <reversibility rating="costly">Amending a published least-privilege promise is a change to a stated security posture that operators and reviewers rely on; walking it back would require re-establishing the tighter claim in the implementation first.</reversibility>
  <read_first>
    - `docs/operations/github-environments.md` — the whole file, especially the section that currently instructs an administrator to provision both roles by hand, the trust-subject bullet list, the plan-role and apply-role scoping paragraphs, and the closing second-administrator sentence
    - `docs/operations/aws-bootstrap.md` — the whole file, especially the introductory paragraph describing what bootstrap creates, the numbered capture block and the paragraph explaining why the saved-plan cleanup comes last, the output recording sentence, the custody paragraphs, and the closing Safety boundary section
    - `infra/bootstrap/outputs.tf` — the exact output names the capture commands must use
    - `infra/bootstrap/iam-github-actions.tf` — the actual scoping delivered, so the prose describes what shipped rather than what was hoped for
    - `docs/adr/0003-infrastructure-iam-role-ownership.md` — rules 3, 4, and 5: the runbook is the specification, bootstrap stays human-applied, and the manual path is retired only once the OpenTofu exists
    - `.planning/phases/01-infrastructure-role-ownership/01-RESEARCH.md` — § Documentation Edit Map (exact line ranges and quoted current text), Open Question 1
    - `.planning/phases/01-infrastructure-role-ownership/01-PATTERNS.md` — the prose conventions each runbook already follows
    - `.prettierignore` — confirms `docs/operations/` is Prettier-formatted, so `npm run format:check` covers these edits
  </read_first>
  <files>docs/operations/github-environments.md, docs/operations/aws-bootstrap.md</files>
  <action>
**`docs/operations/github-environments.md`.** Replace the manual-provisioning instruction — the
passage stating that neither the bootstrap nor the site stack creates the two infrastructure role
ARNs and that an authorized administrator must provision both after bootstrap — with the
bootstrap-output path. State that the bootstrap root now declares all six roles, that their ARNs are
the `infrastructure_plan_role_arns` and `infrastructure_apply_role_arns` outputs, and that the
operator copies one ARN into each matching GitHub Environment. Keep the section's imperative mood
addressed to the administrator.

**Keep** the trust-subject bullet list and the two scoping paragraphs. ADR-0003 rule 3 makes them the
specification the OpenTofu satisfies, so deleting them would remove the thing the code is checked
against. **Keep** the closing sentence requiring a second administrator to review the trust and
permission policies before the ARNs are stored in GitHub, verbatim. **Keep** the existing prohibition
sentences at the end of each block.

Amend the apply-role scoping paragraph — the one currently promising scoping by known ARNs, hosted
zone, resource-name prefixes, and tag conditions. Follow that paragraph's existing structure: state
the scoping that applies, then state the exception and why. Three corrections are required and each
must be stated plainly rather than softened.

First, the tag-condition clause. The resource-tag condition-key family is supported by zero
CloudFront, ACM, Route 53, and IAM actions, and by no S3 bucket action, so the promise as written
cannot be kept by any policy. Replace it with what the implementation actually uses as its
least-privilege levers: exact ARNs, resource-name prefixes, the hosted-zone ARN, and two
service-specific condition keys — the ACM domain-names key on the certificate request, and the
Route 53 normalized-record-name and record-type keys on record changes.

Second, the residual unscoped surface. Name the five actions explicitly:
`cloudfront:CreateDistribution`, `cloudfront:CreateCachePolicy`,
`cloudfront:CreateResponseHeadersPolicy`, `cloudfront:CreateOriginAccessControl`, and
`cloudfront:CreateFunction`. State that AWS defines no IAM resource type for them, so they are
granted on an unrestricted resource, and that this is a limitation of the service rather than a
choice. Do not describe it as temporary or as a future enhancement — nothing in this repository can
change it.

Third, the compensating controls for that residual. Name all three, and name them as controls that
remain in force rather than as reassurance: bootstrap is applied by a human and by no CI job;
CODEOWNERS review is required on `/infra/`; and a second administrator reviews the trust and
permission policies before the ARNs are stored. These are the same three controls ADR-0003 rule 4
already requires, and they are the reason the residual is acceptable.

This paragraph is a visible narrowing of a documented security promise, and it is what user decision
D-A directs: amend the runbook and state the gap plainly. Narrow the claim to what AWS permits; do
not delete the commitment, do not soften it into an aspiration, and do not leave the unscoped set
unnamed. An operator reading it must finish knowing exactly which five actions are unscoped and
exactly what stands in their place. Per D-A, do not attempt to recover the promise by adding
create-time tag conditions to those five actions, and do not split the apply role.

**`docs/operations/aws-bootstrap.md`.** Rewrite the introductory paragraph that currently says the
bootstrap does not create the plan and apply roles and directs the reader elsewhere to provision them
separately. It now creates six. Keep the sentence stating that the bootstrap requires initial local
administrative authority verbatim — ADR-0003 rule 4 depends on it.

Extend the numbered capture block with two `output -json` captures, one for each new role-ARN output,
written in the same one-line-per-command style as the existing `state_bucket_names` captures. Place
them before the saved-plan cleanup command: that cleanup must remain the last command in the block,
for the reason the following paragraph already gives. Extend the recording sentence so it names the
six role ARNs alongside the existing outputs, and keep its instruction to record them without display
quotes.

Add one sentence telling the operator which GitHub Environment each captured ARN belongs in — plan
role ARNs into the matching `-plan` Environment, apply role ARNs into the matching unsuffixed
Environment — and that no ARN is ever copied between environments. The six Environments do not exist
yet; that is Phase 2 work, and the runbook should read as instruction for when they do.

Change nothing in the custody, restore-test, or tag-audit sections, and change nothing in the closing
Safety boundary section: no AWS apply has been performed by this phase and that statement is still
true.

Run `npm run format:check` after editing; both files are Prettier-formatted.
  </action>
  <verify>
    <automated>npm run format:check && grep -q 'infrastructure_plan_role_arns' docs/operations/aws-bootstrap.md && grep -q 'infrastructure_apply_role_arns' docs/operations/aws-bootstrap.md && grep -q 'cloudfront:CreateDistribution' docs/operations/github-environments.md && grep -q 'Safety boundary' docs/operations/aws-bootstrap.md</automated>
    <human-check>
Read both runbooks end to end as an operator who has never seen this repository, and confirm three
things a grep cannot:
1. No step anywhere in either file tells you to create the plan or apply role by hand. There is
   exactly one procedure, not two.
2. The human-apply requirement, the CODEOWNERS review on `/infra/`, and the second administrator's
   review of the trust and permission policies are all still stated, and none reads as optional.
3. The amended apply-role scoping paragraph leaves you knowing exactly which five CloudFront actions
   are granted unscoped and exactly what compensates for them — and it does not read as though
   least-privilege scoping was abandoned rather than narrowed.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm run format:check` exits 0.
    - `grep -c 'infrastructure_plan_role_arns' docs/operations/aws-bootstrap.md` returns at least 1 and `grep -c 'infrastructure_apply_role_arns' docs/operations/aws-bootstrap.md` returns at least 1.
    - `grep -c 'cloudfront:CreateDistribution' docs/operations/github-environments.md` returns at least 1, and the same file also contains `cloudfront:CreateCachePolicy`, `cloudfront:CreateResponseHeadersPolicy`, `cloudfront:CreateOriginAccessControl`, and `cloudfront:CreateFunction`.
    - `grep -c 'second administrator' docs/operations/github-environments.md` returns at least 1.
    - `grep -c 'initial local administrative authority' docs/operations/aws-bootstrap.md` returns 1 — the ADR-0003 rule 4 sentence is verbatim intact.
    - `grep -c 'Safety boundary' docs/operations/aws-bootstrap.md` returns 1 and `git diff docs/operations/aws-bootstrap.md` shows no change within that section.
    - `grep -n 'rm -f infra/bootstrap/bootstrap.tfplan' docs/operations/aws-bootstrap.md` reports a line number greater than the line numbers of both new `output -json` capture commands.
    - `grep -c 'must provision both after bootstrap' docs/operations/github-environments.md` returns 0 — the manual-provisioning instruction is gone.
    - `grep -c 'aws:ResourceTag' docs/operations/github-environments.md` returns 0 — the unkeepable tag-condition promise is gone rather than restated.
  </acceptance_criteria>
  <done>An operator following either runbook is told exactly one way to obtain the six role ARNs — read them from the bootstrap apply — is still told bootstrap is human-applied under CODEOWNERS plus second-administrator review, and finishes knowing precisely which five CloudFront actions are unscoped, why, and what compensates.</done>
</task>

<task type="auto">
  <name>Task 2: Close the three stale sentences that contradict a locked decision or the working tree</name>
  <precondition>The active Node.js satisfies the package.json engines range of at least 24 and below 25 — `node -v` reports v24.x; if it does not, run `nvm use` before any npm command, because `.npmrc` sets engine-strict and npm will otherwise refuse outright.</precondition>
  <reversibility rating="reversible">Four single-sentence corrections in documentation; each is a one-line revert.</reversibility>
  <read_first>
    - `docs/adr/0002-github-environment-model.md` — the whole file. Its front matter carries `locked: true`; only the final entry of the References list is in scope
    - `docs/adr/0003-infrastructure-iam-role-ownership.md` — its References list is the format analog: backticked path, em dash, lowercase descriptive fragment, no trailing period
    - `docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md` — the GitHub and deployment flow section, whose closed enumeration of Environment names contradicts ADR-0002 rule 3
    - `docs/superpowers/plans/2026-08-22-stagehand-docs-site.md` — the Tech Stack line, the Global Constraints bullet naming the Environment set, and the dependency block in the packaging section
    - `package.json` — the authoritative TypeScript pin the working tree actually uses
    - `.prettierignore` — confirms `docs/superpowers/` is excluded from Prettier while `docs/adr/` is not
    - `.planning/phases/01-infrastructure-role-ownership/01-RESEARCH.md` — § Documentation Edit Map, which quotes each target sentence verbatim with its file and line
  </read_first>
  <files>docs/adr/0002-github-environment-model.md, docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md, docs/superpowers/plans/2026-08-22-stagehand-docs-site.md</files>
  <action>
**DRIFT-01 — ADR-0002 References.** The last entry in ADR-0002's References list is a three-line
prose paragraph stating that ownership of the two infrastructure role ARNs is unsettled and
deferring to the operations guide pending a future ADR. That ADR now exists. Replace the whole
paragraph with a single References entry in the format ADR-0003's own References list uses —
backticked path to `docs/adr/0003-infrastructure-iam-role-ownership.md`, an em dash, and a lowercase
descriptive fragment naming it as the ADR that settled ownership of the plan and apply roles, with
no trailing period.

ADR-0002 carries `locked: true`. The References section is a pointer, not a decision, and correcting
a stale pointer is in scope. Do not touch the Status, Context, Decision, Consequences, or
Alternatives rejected sections. Do not renumber or reword the other References entries.

**DRIFT-02 — the design specification.** In the GitHub and deployment flow section, one sentence
enumerates the GitHub Environment names as a closed three-value set. Read literally it forbids the
three plan Environments the infrastructure workflow requires, which is the exact
misreading ADR-0002 exists to prevent. Amend it to the six-Environment model per ADR-0002 rule 3:
three apply Environments named for the three Stagehand environments, restricted to `main`; and three
plan Environments carrying the `-plan` suffix, restricted to the pull-request merge-ref branch rule.
Keep the amendment to the length of the surrounding prose; this section is a summary, and the full
specification lives in ADR-0002 and the operations guide. Add a pointer to ADR-0002 so a reader who
needs the reviewer and variable rules knows where to go.

**DRIFT-02, second instance — the implementation plan's Global Constraints.** One bullet in the
Global Constraints list states the Environment name set without qualifying which namespace it binds.
This is the exact sentence ADR-0002 rule 2 says it amends, and it is in scope for this phase per user
decision D-B, which ruled it a source document under DRIFT-01/02/03 rather than an out-of-scope
artifact. Rewrite it to bind the three-value enum to the OpenTofu environment namespace
only — the `var.environment` values and the `environment` tag values — using ADR-0002 rule 2's own
wording, and note that the GitHub Environment namespace has six members per ADR-0002 rule 3.

**DRIFT-03 — the TypeScript pin.** The implementation plan records a TypeScript version in two
places: the Tech Stack line and a dependency block in the packaging section. `package.json` pins
`6.0.3`, and the working tree is authoritative for a pinned toolchain version. Correct both
occurrences to `6.0.3`. Leave every other version in that Tech Stack line alone — only the TypeScript
pin was verified as drifted, and correcting an unverified version would substitute one unchecked
claim for another.

`docs/superpowers/` is excluded from Prettier by `.prettierignore`, so those three edits are not
format-checked; `docs/adr/` is not excluded, so run `npm run format:check` after the ADR edit.

**Recorded scope decision — `.planning/intel/constraints.md` is deliberately left unchanged.** It
carries the design specification's original sentence verbatim as an ingest record, with the ADR-0002
override already attached beside it. It is an audit artifact of what the source documents said at
ingest time, not a source document, and rewriting an audit record to match a later correction
destroys the provenance the ingest exists to preserve. Success criterion 5 constrains source
documents; this is not one. Do not edit it, and state this decision in the plan summary so the
omission is a recorded choice rather than an oversight.
  </action>
  <verify>
    <automated>npm run format:check && ! grep -rn "7\.0\.2" docs/ && ! grep -n "until a separate ADR records an owner" docs/adr/0002-github-environment-model.md && grep -q "0003-infrastructure-iam-role-ownership.md" docs/adr/0002-github-environment-model.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "7\.0\.2" docs/` returns nothing (exit 1).
    - `grep -c "6.0.3" docs/superpowers/plans/2026-08-22-stagehand-docs-site.md` returns 2.
    - `grep -n "until a separate ADR records an owner" docs/adr/0002-github-environment-model.md` returns nothing (exit 1).
    - `grep -c "0003-infrastructure-iam-role-ownership.md" docs/adr/0002-github-environment-model.md` returns 1.
    - `git diff docs/adr/0002-github-environment-model.md` shows changed lines only within the `## References` section — no hunk touches `## Status`, `## Context`, `## Decision`, `## Consequences`, or `## Alternatives rejected`.
    - `grep -c "locked: true" docs/adr/0002-github-environment-model.md` returns 1 — the front matter is unchanged.
    - `grep -c '\-plan' docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md` returns at least 1 — the six-Environment model is now described there.
    - `grep -c '\-plan' docs/superpowers/plans/2026-08-22-stagehand-docs-site.md` returns at least 1 — the Global Constraints bullet now distinguishes the two namespaces.
    - `git diff --name-only` does not include `.planning/intel/constraints.md`.
    - `npm run format:check` exits 0.
  </acceptance_criteria>
  <done>ADR-0002 points a reader to ADR-0003 for role ownership with its Decision section untouched; no source document still describes the site as having three GitHub Environments; and no source document still pins a TypeScript version the repository does not use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runbook prose → an administrator's hands | An operator with local administrative credentials acts on this prose. A wrong or contradictory instruction is executed against a real AWS account with no gate behind it. |
| Documented security promise → reviewer expectation | A reviewer approving `/infra/` changes checks the diff against what the runbook says the roles may do. Prose that over-promises makes the review structurally unable to catch the gap. |
| Locked ADR → future contributor | A contributor resolving an apparent contradiction by document precedence acts on whichever text they read. A locked decision quietly edited is a control removed without a decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-20 | Repudiation | `docs/operations/github-environments.md` apply-role scoping paragraph | high | mitigate | The paragraph is amended to name the five unscopable CloudFront creates explicitly and to name the three compensating controls, so a reviewer's expectation matches what the policy grants and the gap cannot hide behind a promise nobody checked. Asserted by grep criteria on all five action names. |
| T-01-21 | Elevation of Privilege | Two contradictory provisioning procedures for the six privileged roles | high | mitigate | The manual-provisioning instruction is removed, not merely supplemented, so an operator cannot hand-craft a role that diverges from the reviewed OpenTofu and then have `tofu plan` fight it. Asserted by a negative grep on the manual instruction. |
| T-01-22 | Tampering | A locked ADR's Decision section edited under cover of a documentation fix | high | mitigate | Only ADR-0002's References list is in scope; the acceptance criteria require the diff to touch no other section and the `locked: true` front matter to be unchanged. |
| T-01-23 | Elevation of Privilege | The human-apply, CODEOWNERS, and second-administrator controls dropped during the rewrite | critical | mitigate | All three are retained by explicit instruction, the ADR-0003 rule 4 sentence is kept verbatim, and each is covered by a grep acceptance criterion plus the human-check. These three controls are the entire compensating basis for threat T-01-09. |
| T-01-24 | Information Disclosure | A real AWS account identifier or ARN pasted into a runbook example | medium | mitigate | The runbook shows `output -json` capture commands, never captured values; requirement PUB-07 and `.gitignore` already forbid committing an account identifier. No example ARN is added. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase; RESEARCH § Package Legitimacy Audit records zero new packages. `npm run format:check` runs against the existing lockfile with no install step. |
</threat_model>

<verification>
- `npm run format:check` exits 0 (run under Node 24 per the task preconditions)
- `grep -rn "7\.0\.2" docs/` returns nothing
- `grep -n "until a separate ADR records an owner" docs/adr/0002-github-environment-model.md` returns nothing
- `git diff docs/adr/0002-github-environment-model.md` touches only the References section
- `git diff --name-only` does not include `.planning/intel/constraints.md`
- The Safety boundary section of `docs/operations/aws-bootstrap.md` is unchanged
</verification>

<success_criteria>
- Both operations runbooks describe the OpenTofu-owned role path and no manual provisioning procedure remains
- Both runbooks still state human apply, CODEOWNERS review on `/infra/`, and second-administrator review
- The five unscopable CloudFront create actions are named in `github-environments.md` together with their compensating controls
- ADR-0002's References points to ADR-0003 and its Decision section is byte-identical
- No source document claims three GitHub Environments or pins a TypeScript version the repository does not use
</success_criteria>

<flagged_assumptions>
**Unresolved edge-probe rows, surfaced rather than auto-resolved.** The deterministic edge probe
returned `unclassified` for each of these four requirements and produced no usable predicate. Per the
spec-less fallback rules an `unclassified` row is never auto-resolved with a backstop, so each is
carried here as an explicit flagged assumption:

- **INFRA-05 / `unclassified`** — assumed: *"the runbooks describe the OpenTofu-owned path" has no
  automatable predicate beyond the presence and absence greps in Task 1.* Prose intent is checked by
  the `<human-check>` block, which will be harvested into the phase UAT at end of phase. If that
  human check is skipped, INFRA-05 is unverified, not verified.
- **DRIFT-01 / `unclassified`** — assumed: *closing the stale ownership pointer is fully discharged by
  the absence of the old sentence plus the presence of the ADR-0003 path.* A reader following the new
  pointer and finding it unhelpful would not be detected by either check.
- **DRIFT-02 / `unclassified`** — assumed: *two source documents carry the three-Environment claim
  (the design specification and the implementation plan's Global Constraints) and no third exists.*
  Derived from the researcher's exhaustive Documentation Edit Map, not from an independent sweep. A
  third occurrence elsewhere in `docs/` would not be caught by this plan's criteria.
- **DRIFT-03 / `unclassified`** — assumed: *the working tree's `package.json` pin is authoritative and
  the implementation plan is the drifted document, rather than the reverse.* Grounded in the project's
  stated pinned-toolchain constraint. If the intent was actually to upgrade TypeScript, this plan
  corrects the wrong side of the drift.

**Recorded scope decision:** `.planning/intel/constraints.md` carries the design specification's
original sentence verbatim as an ingest audit record with the ADR-0002 override already attached. It
is deliberately left unchanged — see Task 2.
</flagged_assumptions>

<output>
Create `.planning/phases/01-infrastructure-role-ownership/01-04-SUMMARY.md` when done
</output>
