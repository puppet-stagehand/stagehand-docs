---
phase: 05-production-launch
plan: 08
type: execute
wave: 7
depends_on: ["05-07", "05-09"]
files_modified:
  - docs/operations/RELEASE-EVIDENCE.md
  - docs/operations/aws-bootstrap.md
autonomous: false
requirements: [LAUN-02]

user_setup:
  - service: domain-registrar
    why: "puppetstagehand.com's authoritative NS records must move from Cloudflare to the Route 53 hosted zone before any of Stagehand's DNS records (www, apex redirect) become publicly reachable. This is the deferred cutover from Phase 2's 02-CONTEXT.md D-01/D-03, and this phase's central, one-way decision."
    dashboard_config:
      - task: "At the domain's registrar, replace puppetstagehand.com's NS records with the four Route 53 nameservers this plan's Task 1 surfaces."

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A customer can load https://www.puppetstagehand.com/ and read the tiers, the compatibility register, and the documentation, for real, after the registrar NS flip."
    - "https://puppetstagehand.com/ and any path beneath it redirect to the www host with the path and query string intact, verified against the real public internet, not just CloudFront's default domain."
    - "The registrar NS flip was performed by the maintainer directly, never simulated or performed by the plan's executor, and was verified with dig after the fact."
  artifacts:
    - "docs/operations/RELEASE-EVIDENCE.md: a new confirmation row recording the post-cutover apex-redirect check as genuinely passed, with today's date"
    - "docs/operations/aws-bootstrap.md: the closing 'Safety boundary' paragraph updated to record the real cutover, mirroring its existing 'As of phase 2, infra/bootstrap/ has been applied for real' precedent"
  key_links:
    - "This plan's gathered Route 53 nameserver values -> the human's registrar-level NS change -> dig NS puppetstagehand.com resolving to Route 53 -> www.puppetstagehand.com and the apex redirect becoming publicly reachable"
    - "05-09's confirmed private-vulnerability-reporting + security@ delivery test (LAUN-04) -> this plan's precondition -> the cutover is not attempted before the production host can be safely made customer-facing"
  prohibitions:
    - requirement_id: LAUN-02
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT record the DNS cutover or apex redirect as confirmed in RELEASE-EVIDENCE.md or SUMMARY.md without an actual, real dig/curl check performed against the public internet after the human's NS change — no assumption that the change 'should have worked' substitutes for verification."
---

<objective>
Execute the phase's central, one-way decision (05-CONTEXT.md D-01/D-02/D-03, LOCKED): gather the
exact Route 53 nameserver values, hand them to the maintainer in a precise runbook step, and — only
after the maintainer performs the actual registrar-level NS flip — verify for real that
`www.puppetstagehand.com` serves the site and `puppetstagehand.com` redirects to it with path and
query intact. This plan does NOT perform or simulate the registrar change itself (D-03, LOCKED); it
prepares the exact inputs, waits for the human action, and proves the result.

Purpose: LAUN-02 — the deliberate DNS cutover, completed for real, with the apex redirect proven
against the actual public internet.
Output: `www.puppetstagehand.com` is live; `puppetstagehand.com` redirects correctly; both are
recorded, honestly, in RELEASE-EVIDENCE.md.

**Note on checkpoint type (no preceding `checkpoint:decision`):** Task 2's one-way reversibility
rating intentionally has no preceding `checkpoint:decision` — the choice itself (plan
documents the exact runbook, human executes the registrar flip; see 05-CONTEXT.md D-01/D-02) was
already made by the human during `/gsd-discuss-phase` and is LOCKED. Per the reversibility
taxonomy's own anti-pattern guidance ("gating a decision already made — keep the rating for the
record; do not insert a checkpoint to re-ask"), and per D-03's explicit, LOCKED instruction that
this step MUST be a `checkpoint:human-action` (not a decision prompt — there is no option menu to
choose from, only a physical action only the maintainer can take), Task 2 below is that mandated
`checkpoint:human-action` and doubles as the human confirmation this one-way door requires.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-production-launch/05-CONTEXT.md
@.planning/phases/05-production-launch/05-RESEARCH.md
@docs/operations/aws-bootstrap.md
@docs/operations/RELEASE-EVIDENCE.md
@.planning/phases/02-first-real-publication/02-CONTEXT.md
</context>

<artifacts_this_phase_produces>
- `docs/operations/RELEASE-EVIDENCE.md`: a new "confirmation" Promotions row for `stable` recording the apex-redirect check as genuinely passed post-cutover
- `docs/operations/aws-bootstrap.md`: the "Safety boundary" closing paragraph updated to state the real cutover happened, in the file's own established precedent style
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Gather the exact Route 53 nameserver values</name>
  <read_first>docs/operations/aws-bootstrap.md</read_first>
  <precondition>docs/operations/RELEASE-EVIDENCE.md's "Security advisory delivery test" table has a real, non-blank Result value (05-09's LAUN-04 checkpoint is confirmed, not just dispatched) — LAUN-04 must close before the production host serves customers.</precondition>
  <files>(no repository files)</files>
  <action>
    Run `aws route53 get-hosted-zone --id {hosted_zone_id} --query
    'DelegationSet.NameServers' --output json` and capture the exact four nameserver values
    (typically `ns-####.awsdns-##.{com,net,org,co.uk}` form). Also run `dig +short NS
    puppetstagehand.com` now, before the cutover, and record the current (Cloudflare) values for
    comparison in Task 3.
  </action>
  <verify>
    <automated>aws route53 get-hosted-zone --id {hosted-zone-id} --query 'DelegationSet.NameServers' --output json | grep -c awsdns</automated>
  </verify>
  <acceptance_criteria>
    - Exactly four Route 53 nameserver values are captured, none fabricated or guessed.
    - The current (pre-cutover) `dig NS puppetstagehand.com` result is recorded for later comparison.
  </acceptance_criteria>
  <done>The exact NS values the registrar needs are known and ready to hand to the maintainer.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Flip puppetstagehand.com's registrar NS records</name>
  <reversibility rating="one-way">Flipping the live registrar's NS records away from Cloudflare takes down the currently-serving, unrelated puppetlabs-seteam.github.io site if done incorrectly, and DNS propagation delay makes a bad change slow to undo and outside git's reach entirely (D-01, 05-CONTEXT.md, LOCKED).</reversibility>
  <action>Replace puppetstagehand.com's NS records at the domain registrar with the four Route 53 nameservers from Task 1.</action>
  <instructions>
    This is the one step in this entire phase I cannot perform myself, and the one genuinely
    irreversible-in-practice action in this project (D-01/D-02/D-03, 05-CONTEXT.md, LOCKED). I have
    already: applied stable's full AWS infrastructure (05-05/05-06/05-07), verified its content is
    correct via its CloudFront default domain, and confirmed private vulnerability reporting plus
    the security@ mailbox test are both closed (05-09) — so this flip is the last step before
    `puppetstagehand.com` becomes customer-facing.

    At the domain's registrar (wherever `puppetstagehand.com` is registered — not Cloudflare's own
    dashboard, the actual registrar account), replace the current NS records with exactly these
    four values:

    {the four Route 53 nameservers captured in Task 1, listed explicitly}

    This will eventually move `puppetstagehand.com` and every subdomain (including
    `testpilots.puppetstagehand.com` and `beta.puppetstagehand.com`, which currently do not resolve
    at all) to Route 53's authority, and will take down `puppetlabs-seteam.github.io`'s current
    presence on the apex and `www` unless it has already been migrated or is intentionally being
    replaced — confirm this is intended before proceeding. DNS propagation after an NS change can
    take anywhere from minutes to 48 hours depending on the registrar and prior TTLs.

    After making the change, run `dig NS puppetstagehand.com` yourself and confirm it returns the
    four Route 53 nameservers above (not the Cloudflare ones this plan recorded in Task 1). Do not
    tell me "done" until that dig confirms the new values — I will independently re-verify it in
    Task 3, but you should see it resolve first.
  </instructions>
  <verification>Once you confirm, I will poll `dig NS puppetstagehand.com` (read-only) with bounded retries, then proceed to verify the live site in Task 3.</verification>
  <resume-signal>Type "done" once dig confirms the four Route 53 nameservers are live for puppetstagehand.com, or describe what happened instead if it's not resolving as expected.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify the cutover and record it honestly</name>
  <read_first>docs/operations/RELEASE-EVIDENCE.md, docs/operations/aws-bootstrap.md</read_first>
  <precondition>Task 2's checkpoint resolved "done": dig NS puppetstagehand.com returns the Route 53 nameservers.</precondition>
  <files>docs/operations/RELEASE-EVIDENCE.md, docs/operations/aws-bootstrap.md</files>
  <action>
    Independently confirm `dig +short NS puppetstagehand.com` returns the four Route 53 nameserver
    values (not Cloudflare's). Then run `SITE_URL=https://www.puppetstagehand.com
    EXPECTED_SHA={the SHA already verified in 05-07} npx tsx scripts/check-live-deployment.ts`
    against the real public hostname for the first time in this project's history.

    Prove the apex redirect for real, against the public internet, with a path and query string
    (not just `/`): `curl -sI "https://puppetstagehand.com/tiers/?ref=cutover"` and confirm a `301`
    with `location: https://www.puppetstagehand.com/tiers/?ref=cutover` — path and query preserved
    exactly, per LAUN-02's requirement text.

    Append a new "confirmation" row to RELEASE-EVIDENCE.md's Promotions table (never edit 05-07's
    existing `stable` row) — same date-of-cutover, `stable`, the same SHA, all content checks
    re-marked pass, and "Apex redirect (stable only)" now genuinely `pass`, with a Notes cell
    stating "post-cutover confirmation; registrar NS flip completed {date}". Update
    `docs/operations/aws-bootstrap.md`'s closing "Safety boundary" paragraph to add one sentence in
    its existing style (mirroring "As of phase 2, infra/bootstrap/ has been applied for real...")
    stating the registrar NS flip is now complete and `puppetstagehand.com` resolves through Route
    53.
  </action>
  <verify>
    <automated>dig +short NS puppetstagehand.com | grep -c awsdns</automated>
  </verify>
  <acceptance_criteria>
    - `dig +short NS puppetstagehand.com` independently confirms all four Route 53 nameservers (not zero, not a mix showing partial propagation treated as complete).
    - `check-live-deployment.ts` against the real `https://www.puppetstagehand.com` exits 0.
    - `curl -sI "https://puppetstagehand.com/tiers/?ref=cutover"` returns `301` with the path and query preserved exactly in `location`.
    - RELEASE-EVIDENCE.md's new row is a pure append (05-07's row untouched); apex-redirect column now genuinely `pass`.
    - `docs/operations/aws-bootstrap.md`'s Safety boundary paragraph records the real cutover.
  </acceptance_criteria>
  <done>www.puppetstagehand.com serves customers for real; the apex redirect is proven against the public internet with path/query intact; the cutover is honestly recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain registrar NS delegation → Route 53 hosted zone | The single most consequential trust boundary in this project — an incorrect change here affects a currently-live, unrelated site and every Stagehand hostname simultaneously. |
| Public internet → stable CloudFront distribution (post-cutover) | The first moment any Stagehand hostname is reachable by a random, unauthenticated visitor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-18 | Tampering / Denial of Service | Registrar NS hijack or a bad NS flip taking down the currently-live, unrelated GitHub Pages site | critical | mitigate | Entirely procedural: Task 2 is a `checkpoint:human-action` naming the exact four values, explicitly confirming intent before the change, and requiring the maintainer's own `dig` confirmation before this plan proceeds; no code control is possible for an out-of-band registrar action by design. |
| T-05-19 | Repudiation | Recording the cutover or apex redirect as confirmed without an actual post-change check | high | mitigate | Task 3's acceptance criteria require real `dig`/`curl` output against the public internet, not an assumption; this plan's prohibition makes the same rule explicit. |
</threat_model>

<verification>
- `dig NS puppetstagehand.com` returns the Route 53 nameservers, confirmed independently by both the maintainer and this plan.
- `www.puppetstagehand.com` serves the verified commit for real.
- `puppetstagehand.com` (with a path and query) redirects to the equivalent `www` URL with both intact, verified against the public internet.
- RELEASE-EVIDENCE.md and aws-bootstrap.md both honestly record the real cutover.
</verification>

<success_criteria>
The registrar NS flip is performed by the maintainer alone, verified independently afterward, and
`www.puppetstagehand.com` now serves customers with a working, path/query-preserving apex redirect —
LAUN-02 is proven true against the real public internet, not assumed.
</success_criteria>

<output>
Create `.planning/phases/05-production-launch/05-08-SUMMARY.md` when done
</output>
