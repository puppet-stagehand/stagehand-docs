---
phase: 05-production-launch
plan: 07
type: execute
wave: 6
depends_on: ["05-06"]
files_modified:
  - docs/operations/RELEASE-EVIDENCE.md
autonomous: true
requirements: [LAUN-02, LAUN-03]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "stable's CloudFront default domain serves every documented route, both JSON data endpoints, the branded 404, and exactly beta's promoted SHA, proving the content/route half of LAUN-02 before the DNS cutover happens."
    - "RELEASE-EVIDENCE.md's stable promotion row honestly marks the apex-redirect check as not-yet-performed rather than a fabricated pass, because the public DNS chain does not reach stable yet."
  artifacts:
    - "docs/operations/RELEASE-EVIDENCE.md: one new, real Promotions row for the beta -> stable promotion, apex-redirect column explicitly marked pending"
  key_links:
    - "05-06's dispatched stable run reaching a terminal success state -> this plan's live-verification pass (via CloudFront default domain) -> the RELEASE-EVIDENCE.md row"
---

<objective>
Prove beta's SHA is genuinely live on stable via stable's CloudFront default domain, then record
that real promotion honestly — including the apex-redirect check, which genuinely cannot be
performed yet because the public DNS chain does not reach stable until Plan 05-08's cutover.

Purpose: LAUN-02 (stable serves the exact promoted SHA on its content/route surface) and LAUN-03
(release evidence recorded honestly, including an honestly-pending check).
Output: A verified stable deployment (pre-cutover) and its real evidence row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-production-launch/05-CONTEXT.md
@docs/operations/release.md
@scripts/check-live-deployment.ts
@docs/operations/RELEASE-EVIDENCE.md
</context>

<artifacts_this_phase_produces>
- `docs/operations/RELEASE-EVIDENCE.md`: second real Promotions row (environment=stable, apex-redirect column marked pending)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify stable's live deployment (pre-cutover)</name>
  <read_first>scripts/check-live-deployment.ts, docs/operations/release.md</read_first>
  <precondition>05-06's stable dispatch reached a terminal success state.</precondition>
  <files>(no repository files)</files>
  <action>
    Run `SITE_URL=https://{stable distribution_domain_name} EXPECTED_SHA={the SHA dispatched in
    05-06} npx tsx scripts/check-live-deployment.ts`. This confirms every route, both JSON
    endpoints, the branded 404, and the commit stamp via stable's CloudFront default domain — the
    only reachable path before the DNS cutover (RESEARCH.md Pattern 2). Do not attempt to reach
    `www.puppetstagehand.com` or test the apex redirect here — that requires the public DNS chain to
    resolve to Route 53, which has not happened yet; attempting it now would either fail loudly
    (fine) or, worse, silently succeed against Cloudflare's unrelated site, which would be a false
    positive. Confirm with `dig +short www.puppetstagehand.com` returning empty (still Cloudflare)
    before treating this task's scope as complete.
  </action>
  <verify>
    <automated>SITE_URL=https://{stable-distribution-domain-name} EXPECTED_SHA={dispatched-sha} npx tsx scripts/check-live-deployment.ts</automated>
  </verify>
  <acceptance_criteria>
    - `check-live-deployment.ts` against stable's real `distribution_domain_name` exits 0 and confirms all routes, both JSON endpoints, the branded 404, and the exact SHA.
    - `dig +short www.puppetstagehand.com` is confirmed empty/unresolved at task time (still pre-cutover), and this is stated in the SUMMARY.
  </acceptance_criteria>
  <done>stable genuinely serves beta's exact SHA across every documented route via its CloudFront default domain; the apex redirect is confirmed not yet testable.</done>
</task>

<task type="auto">
  <name>Task 2: Record the real (partial) promotion in RELEASE-EVIDENCE.md</name>
  <read_first>docs/operations/RELEASE-EVIDENCE.md</read_first>
  <files>docs/operations/RELEASE-EVIDENCE.md</files>
  <action>
    Append one new row to the "Promotions" table with: today's date (UTC), `stable`, the real SHA,
    pass/fail for Home/Tiers/Compat/Docs/Support/tiers.json/compat.json/404 (all "pass" per Task 1),
    and the "Apex redirect (stable only)" column set to `pending — awaiting DNS cutover (see Plan
    05-08)`, NOT "pass" — the check has genuinely not been performed yet, and D-05/D-07
    (05-CONTEXT.md, LOCKED) forbid marking a check as passed before it is actually run. Add a Notes
    cell linking the `Deploy site` run ID from 05-06. Never edit or delete beta's existing row from
    05-03 — this is a new, separate append.
  </action>
  <verify>
    <automated>grep -c "| stable |" docs/operations/RELEASE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new row added for `stable`; beta's prior row is untouched.
    - The apex-redirect column reads a pending/not-yet-performed state, never "pass".
    - `git diff docs/operations/RELEASE-EVIDENCE.md` shows only an appended line.
  </acceptance_criteria>
  <done>RELEASE-EVIDENCE.md carries one real, honest promotion record for stable, with the apex-redirect check correctly marked as still pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification script output → committed evidence record | The evidence log's integrity depends on this plan recording exactly what it did and did not confirm. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-17 | Repudiation | Marking the apex-redirect check "pass" before the DNS cutover has happened | high | mitigate | Task 2's action and acceptance criteria explicitly require a pending state, never "pass", for that column at this point in the phase. |
</threat_model>

<verification>
- `check-live-deployment.ts` passes against stable's real CloudFront default domain with the exact dispatched SHA.
- `docs/operations/RELEASE-EVIDENCE.md` gains exactly one new, honest Promotions row with the apex-redirect check correctly marked pending.
</verification>

<success_criteria>
stable's pre-cutover content is proven live and honestly recorded, with the one check that
genuinely cannot be performed yet marked as such rather than fabricated.
</success_criteria>

<output>
Create `.planning/phases/05-production-launch/05-07-SUMMARY.md` when done
</output>
