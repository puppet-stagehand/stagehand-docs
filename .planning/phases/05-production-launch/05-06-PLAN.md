---
phase: 05-production-launch
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified: []
autonomous: false
requirements: [LAUN-02]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Stable's GitHub Environment carries its own real CONTENT_BUCKET, CLOUDFRONT_DISTRIBUTION_ID, AWS_DEPLOY_ROLE_ARN, and SITE_CHECK_URL variables, none copied from testpilots or beta."
    - "A real Deploy site dispatch to stable, carrying beta's exact deployed SHA, reaches a concluded run (success or a named, escalated blocker) rather than silently hanging."
  artifacts:
    - "Live GitHub Environment variables on stable: CONTENT_BUCKET, CLOUDFRONT_DISTRIBUTION_ID, AWS_DEPLOY_ROLE_ARN, SITE_CHECK_URL"
    - "A concluded (or checkpoint-resolved) Deploy site run targeting stable"
  key_links:
    - "05-05's captured content_bucket_name/distribution_id/deployment_role_arn/distribution_domain_name -> this plan's stable GitHub Environment variables -> deploy.yml's real role assumption and upload"
    - "beta's current live /deployed-commit.txt SHA (verified by 05-03) -> the git_sha input to the stable dispatch -> scripts/assert-promotable-commit.sh's ancestor-of-main check"
  prohibitions:
    - requirement_id: LAUN-02
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT disable or weaken prevent_self_review or reduce the required-reviewer count on stable to unblock this dispatch's approval — ADR-0002 rule 3 is LOCKED and applies to stable specifically; a stuck approval is escalated via Task 3's checkpoint, never routed around."
---

<objective>
Wire stable's real deploy variables from 05-05, and promote beta's exact, already-verified SHA to
stable through the protected `Deploy site` workflow — completing the mechanical promotion pipeline
for stable before the DNS cutover (05-08) makes it customer-facing. This mirrors 05-02's beta
dispatch exactly, reusing whatever was learned there about the self-review approval question.

Purpose: LAUN-02 — stable receives beta's exact SHA, unaltered, through its protected Environment
(release.md's promotion order: testpilots -> beta -> stable).
Output: stable's GitHub Environment fully configured for deployment; a `Deploy site` run against
stable either succeeded or is confirmed approved and progressing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-production-launch/05-CONTEXT.md
@.planning/phases/05-production-launch/05-RESEARCH.md
@.github/workflows/deploy.yml
@docs/operations/github-environments.md
@docs/operations/release.md
@.planning/phases/05-production-launch/05-02-PLAN.md
</context>

<artifacts_this_phase_produces>
- Live GitHub Environment variables on `stable`: `CONTENT_BUCKET`, `CLOUDFRONT_DISTRIBUTION_ID`, `AWS_DEPLOY_ROLE_ARN`, `SITE_CHECK_URL` (all new)
- One real `Deploy site` workflow run dispatched against `stable`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Set stable's deploy variables</name>
  <read_first>docs/operations/github-environments.md ("Set the required variables" section), infra/environments/stable/main.tf</read_first>
  <precondition>05-05's stable apply completed: content_bucket_name, distribution_id, deployment_role_arn, and distribution_domain_name are all captured.</precondition>
  <files>(no repository files — sets live GitHub Environment variables)</files>
  <action>
    Set stable's four deploy variables via `gh api --method POST
    repos/puppet-stagehand/stagehand-docs/environments/stable/variables` (each is new): `CONTENT_BUCKET`
    = 05-05's `content_bucket_name`, `CLOUDFRONT_DISTRIBUTION_ID` = 05-05's `distribution_id`,
    `AWS_DEPLOY_ROLE_ARN` = 05-05's `deployment_role_arn`, `SITE_CHECK_URL` = 05-05's
    `distribution_domain_name` (bare hostname). This is the pre-cutover value per RESEARCH.md
    Pattern 2 — it stays pointed at the CloudFront default domain until 05-08's DNS cutover makes
    `www.puppetstagehand.com` reachable; note in the SUMMARY that a future operator repoints
    `SITE_CHECK_URL` to `www.puppetstagehand.com` after the cutover (out of scope for this plan —
    05-08 itself verifies the post-cutover state directly, not via this variable). Do not touch
    `beta`'s or `testpilots`'s variables.
  </action>
  <verify>
    <automated>gh api repos/puppet-stagehand/stagehand-docs/environments/stable/variables --jq '.variables[].name' | sort | grep -c '^\(AWS_DEPLOY_ROLE_ARN\|CLOUDFRONT_DISTRIBUTION_ID\|CONTENT_BUCKET\|SITE_CHECK_URL\)$'</automated>
  </verify>
  <acceptance_criteria>
    - `gh api .../environments/stable/variables` lists all four names with the exact values captured in 05-05 (spot-check against 05-05's outputs — never copy a value from testpilots's or beta's own outputs).
    - `beta` and `testpilots`'s existing variables are unchanged.
  </acceptance_criteria>
  <done>stable's GitHub Environment carries its own real, non-copied deploy variables.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch beta's exact SHA to stable and attempt approval</name>
  <read_first>docs/operations/release.md ("Promote a release"), .github/workflows/deploy.yml</read_first>
  <files>(no repository files)</files>
  <action>
    Read beta's currently deployed SHA directly from its live site: `curl -s
    "https://{beta distribution_domain_name}/deployed-commit.txt"` (fall back to `gh api
    repos/puppet-stagehand/stagehand-docs/deployments?environment=beta --jq '.[0].sha'`). Per
    `release.md`'s release invariant, this must be the SAME SHA 05-03 already verified live on beta
    — do not use a different or newer commit.

    Dispatch: `gh workflow run "Deploy site" --ref main -f environment=stable -f
    git_sha={beta-sha}`. Poll `gh run list --workflow="Deploy site" --limit 1 --json
    databaseId,status,conclusion` until `status` is no longer `queued`.

    If the `deploy` job is `waiting`, attempt approval as the same identity, exactly as in 05-02
    Task 2: `gh api repos/puppet-stagehand/stagehand-docs/actions/runs/{run_id}/pending_deployments
    --jq '.[].environment.id'`, then `gh api --method POST
    repos/puppet-stagehand/stagehand-docs/actions/runs/{run_id}/pending_deployments -f
    environment_ids[]={id} -f state=approved -f comment="Automated promotion per
    docs/operations/release.md"`. `stable` has `prevent_self_review=true` (ADR-0002 rule 3, LOCKED
    — cannot be disabled here even if it blocks this call). Record the exact outcome; do not retry
    repeatedly or touch protection rules regardless of outcome.
  </action>
  <verify>
    <automated>gh run list --workflow="Deploy site" --limit 1 --json status --jq '.[0].status' | grep -Ev '^queued$'</automated>
  </verify>
  <acceptance_criteria>
    - The SHA dispatched is read from beta's own live deployment (the one 05-03 verified), never hardcoded or a different/newer commit.
    - A `Deploy site` run against `stable` exists and its `status`/`conclusion`/approval-attempt outcome are recorded.
    - No Environment protection rule on `stable` was modified in this task.
  </acceptance_criteria>
  <done>A real Deploy site run targets stable with beta's exact SHA, and the approval attempt's real outcome is known and recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Approve the pending stable deployment if still blocked</name>
  <action>If Task 2's automated approval attempt was rejected by GitHub's self-review protection, get the pending deployment approved by the maintainer.</action>
  <instructions>
    I dispatched `Deploy site` to `stable` with SHA `{sha}` and attempted to approve the resulting
    pending deployment via the API. {Report the exact outcome from Task 2, and whether 05-02's
    earlier attempt on beta resolved whether a manual UI click bypasses self-review for this
    account.}

    Please open the pending run in the GitHub UI (Actions → Deploy site → the run for `stable`) and
    click **Review deployments → Approve and deploy**.

    Per ADR-0002 rule 3 (LOCKED), `prevent_self_review` must never be disabled on `stable` — if the
    UI approval also fails here, the only clean fix remains adding a second trusted GitHub
    collaborator as an additional reviewer, which is your decision, not mine.
  </instructions>
  <verification>Once you confirm, I will poll `gh run list --workflow="Deploy site" --limit 1 --json status,conclusion` until the run reaches a terminal state.</verification>
  <resume-signal>Type "approved" once you've approved the deployment in the UI, or describe how you'd like to proceed instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions dispatch → protected stable Environment approval | The most sensitive approval gate in this project — `stable` is the eventual customer-facing environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-15 | Elevation of Privilege | `prevent_self_review` on stable | critical | mitigate | Task 2 attempts approval once, records the outcome, and never touches the protection rule; Task 3 escalates to the maintainer, and ADR-0002 rule 3 (LOCKED) is cited explicitly as non-negotiable for stable. |
| T-05-16 | Tampering | Promoting a SHA other than the one beta actually verified | high | mitigate | Task 2 reads beta's SHA from its own live `/deployed-commit.txt`, matching 05-03's verified value, rather than accepting any other commit; `assert-promotable-commit.sh` independently re-verifies ancestry. |
</threat_model>

<verification>
- stable's four deploy variables are live, real, and not copied from another environment.
- A `Deploy site` run against stable reaches a terminal state (success or a maintainer-resolved approval).
</verification>

<success_criteria>
stable's deploy pipeline is fully configured and beta's exact, verified SHA has been dispatched to
it through the real, protected approval path.
</success_criteria>

<output>
Create `.planning/phases/05-production-launch/05-06-SUMMARY.md` when done
</output>
