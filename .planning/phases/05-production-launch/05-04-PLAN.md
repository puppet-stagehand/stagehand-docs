---
phase: 05-production-launch
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docs/operations/RELEASE-EVIDENCE.md
autonomous: true
requirements: [GATE-05, LAUN-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "npm run verify runs infra/modules/static-site/tests/redirect.test.mjs's all 6 existing tests, not a synthetic zero-count pass, and CI already executes this via validate.yml's existing npm run verify invocation."
    - "docs/operations/RELEASE-EVIDENCE.md exists with Promotions, Rollbacks, and Security advisory delivery test tables, all empty until a real event happens."
    - "RELEASE-EVIDENCE.md rows are never edited or deleted — a check that needs to be redone gets a new appended row, never a merge or overwrite (edge-probe LAUN-03 adjacency)."
    - "An environment with no real promotion yet keeps its Promotions/Rollbacks rows empty rather than a fabricated or placeholder entry (edge-probe LAUN-03 empty-input case)."
    - "Row order in RELEASE-EVIDENCE.md is append/insertion order, never resorted or deduplicated by any field (edge-probe LAUN-03 ordering)."
    - "RELEASE-EVIDENCE.md has exactly one human operator editing it serially per the release runbook — no concurrent-write guarantee is claimed or required (edge-probe LAUN-03 concurrency)."
  artifacts:
    - "package.json: new test:redirect script, spliced into the verify chain"
    - "docs/operations/RELEASE-EVIDENCE.md (new file)"
  key_links:
    - "package.json's verify chain -> npm run test:redirect -> infra/modules/static-site/tests/redirect.test.mjs's existing 6 tests -> .github/workflows/validate.yml's unchanged npm run verify invocation"
  prohibitions:
    - requirement_id: GATE-05
      category: values
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT wire test:redirect in a way that could report success while discovering zero real tests (e.g. a bare directory argument silently matching 0 files) — the exact glob must be verified to actually run all 6 existing tests, never a synthetic zero-count pass treated as green."
    - requirement_id: LAUN-03
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT fabricate a RELEASE-EVIDENCE.md row for a promotion, rollback, or security-delivery test that did not actually happen — every row records a real, dated event this project genuinely performed (D-07, LOCKED)."
---

<objective>
Wire the existing, currently-dead `redirect.test.mjs` into `npm run verify` and CI with the exact
working glob (GATE-05), and scaffold the append-only `docs/operations/RELEASE-EVIDENCE.md` log every
later plan in this phase appends real rows to (LAUN-03). Both are small, self-contained, and have no
dependency on the beta/stable applies or the DNS cutover — CONTEXT.md explicitly leaves GATE-05's
sequencing to the planner, and this is the earliest point it can land.

Purpose: GATE-05 (the apex→www redirect guarantee is enforced automatically, not checked by hand)
and the LAUN-03 scaffold every promotion/rollback this phase performs records real evidence into.
Output: A green `npm run verify` that now actually runs the redirect test suite, and a committed,
empty release-evidence log ready for real rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-production-launch/05-CONTEXT.md
@.planning/phases/05-production-launch/05-RESEARCH.md
@.planning/phases/05-production-launch/05-PATTERNS.md
@package.json
@infra/modules/static-site/tests/redirect.test.mjs
@.github/workflows/validate.yml
@docs/operations/release.md
</context>

<artifacts_this_phase_produces>
- `package.json`: `"test:redirect": "node --test infra/modules/static-site/tests/*.test.mjs"` script, spliced into `verify` immediately after `test:unit`
- `docs/operations/RELEASE-EVIDENCE.md`: header prose + "Promotions" / "Rollbacks" / "Security advisory delivery test" tables, all rows empty
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire redirect.test.mjs into the verify chain</name>
  <read_first>package.json, infra/modules/static-site/tests/redirect.test.mjs</read_first>
  <files>package.json</files>
  <action>
    Add `"test:redirect": "node --test infra/modules/static-site/tests/*.test.mjs"` to
    `package.json`'s `scripts` block (the explicit glob, per RESEARCH.md Pitfall 5 — a bare
    directory argument such as `infra/modules/static-site/tests/` silently discovers 0 real tests
    and reports one synthetic failing test instead). Splice `npm run test:redirect` into the
    `verify` script immediately after `npm run test:unit` and before `npm run build` — both are
    fast, non-build-dependent checks, and `build`/`test:e2e` stay last as already ordered. Do not
    convert `redirect.test.mjs` to vitest (D-10, LOCKED) — leave its `node:test` runner untouched.
  </action>
  <verify>
    <automated>node --test infra/modules/static-site/tests/*.test.mjs 2>&amp;1 | grep -E "^# (tests|pass|fail) " </automated>
  </verify>
  <acceptance_criteria>
    - `package.json` has the exact `test:redirect` script using the glob form, not a bare directory.
    - `node --test infra/modules/static-site/tests/*.test.mjs` reports `# tests 6`, `# pass 6`, `# fail 0` — never a synthetic 1-test run.
    - `verify`'s script string contains `npm run test:redirect` positioned between `test:unit` and `build`.
  </acceptance_criteria>
  <done>test:redirect exists, runs all 6 real tests, and is part of the verify chain.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm CI already covers the new script (D-11) and run full verify</name>
  <read_first>.github/workflows/validate.yml</read_first>
  <files>(no new files — confirmation only)</files>
  <action>
    Confirm `.github/workflows/validate.yml`'s `site` job invokes `npm run verify` verbatim (no
    narrower subset job that would skip the new script) — grep the workflow file rather than assume.
    Since `test:redirect` is now inside `verify`, CI already executes it on every PR and push to
    `main` with zero workflow changes required, satisfying D-11.

    Run `npm run verify` in full (the whole chain, not just the new script) to confirm the addition
    does not regress lint/format/build/e2e or any other existing gate.
  </action>
  <verify>
    <automated>grep -c "npm run verify" .github/workflows/validate.yml</automated>
  </verify>
  <acceptance_criteria>
    - `validate.yml`'s `site` job step literally runs `npm run verify` (not a subset), confirmed by grep, not by assumption.
    - `npm run verify` (the full chain) exits 0 locally with `test:redirect` included.
  </acceptance_criteria>
  <done>CI enforces the redirect guarantee automatically; the full verify chain stays green with the new script included.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold RELEASE-EVIDENCE.md</name>
  <read_first>docs/operations/release.md, docs/operations/github-environments.md</read_first>
  <files>docs/operations/RELEASE-EVIDENCE.md</files>
  <action>
    Create `docs/operations/RELEASE-EVIDENCE.md` with: a short header framing sentence in
    `release.md`'s imperative, present-tense style stating this is the append-only log a release
    operator records every promotion, rollback, and security-delivery test into; an explicit rule
    that a prior row is never edited or deleted, and a check that needs to be redone gets a new
    appended row (never a merge — this is the answer to LAUN-03's adjacency question); a
    "Promotions" table with columns Date (UTC) | Environment | SHA | Home | Tiers | Compat | Docs |
    Support | tiers.json | compat.json | 404 | Apex redirect (stable only) | Notes, currently empty
    (no fabricated rows — this answers LAUN-03's empty-input question); a "Rollbacks" table with
    columns Date (UTC) | Environment | Incident | Known-good SHA | Dispatch run | Restored? | Notes,
    currently empty; and a "Security advisory delivery test" table with columns Date (UTC) | Channel
    | Test performed | Result | Recorded by, seeded with one row naming the channel
    `security@puppetstagehand.com` and every other cell blank until 05-09 fills it in for real.
  </action>
  <verify>
    <automated>test -f docs/operations/RELEASE-EVIDENCE.md &amp;&amp; grep -c "^## " docs/operations/RELEASE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with exactly three `##` sections: Promotions, Rollbacks, Security advisory delivery test.
    - No table contains a populated data row yet (every table body is empty except the seeded channel name in the security table).
    - Header prose explicitly states rows are append-only and never edited/deleted.
  </acceptance_criteria>
  <done>RELEASE-EVIDENCE.md exists, committed, and ready for every later plan's real rows.</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged Assumptions (edge-probe fallback, GATE-05)

The spec-less edge-probe fallback classified GATE-05 as `unclassified`. Acknowledged: GATE-05 wires
an existing, already-fully-tested redirect implementation into the verification chain — it adds no
new redirect logic and therefore no new encoding/adjacency/ordering/concurrency surface. The one
real risk this requirement carries (a wiring bug that silently discovers zero tests) is not a
generic edge case; it is handled explicitly by this plan's GATE-05 prohibition and Task 1's
acceptance criteria requiring `# tests 6`.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm script chain → CI enforcement | The redirect guarantee's entire enforcement now depends on this one script wiring being correct. |
| RELEASE-EVIDENCE.md → operator trust | Every later plan's evidence entries depend on this scaffold's append-only contract holding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Repudiation | `test:redirect` silently discovering 0 tests and reporting green | medium | mitigate | Task 1's acceptance criteria hard-require `# tests 6`, not just a zero exit code. |
| T-05-09 | Repudiation | A future plan editing/deleting a prior RELEASE-EVIDENCE.md row instead of appending | medium | mitigate | The scaffold's own header prose states the append-only rule explicitly, and every later plan in this phase (05-03, 05-07, 05-08, 05-09, 05-10, 05-11) is written to assert a pure-append diff. |
</threat_model>

<verification>
- `node --test infra/modules/static-site/tests/*.test.mjs` reports 6 real tests, all passing.
- `npm run verify` (full chain) is green with `test:redirect` included.
- `validate.yml` is confirmed to already run `npm run verify` verbatim.
- `docs/operations/RELEASE-EVIDENCE.md` exists with all three tables scaffolded and empty.
</verification>

<success_criteria>
GATE-05 is closed with zero workflow changes needed, and the release-evidence log is ready for real
data from every remaining plan in this phase.
</success_criteria>

<output>
Create `.planning/phases/05-production-launch/05-04-SUMMARY.md` when done
</output>
