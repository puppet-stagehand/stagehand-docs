---
phase: 05-production-launch
plan: 09
type: execute
wave: 2
depends_on: ["05-04"]
files_modified:
  - docs/operations/RELEASE-EVIDENCE.md
  - SECURITY.md
autonomous: false
requirements: [LAUN-04]

user_setup:
  - service: security-mailbox
    why: "security@puppetstagehand.com must be provisioned, monitored, and delivery-tested before the production host serves customers (D-09, github-environments.md 'Enable private security reports')."
    dashboard_config:
      - task: "Send a test message to security@puppetstagehand.com and confirm receipt; report the result back."

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "gh api repos/puppet-stagehand/stagehand-docs/private-vulnerability-reporting returns {\"enabled\":true}, confirmed by a fresh GET after the enabling call, not assumed from the PUT/POST response alone."
    - "A real send-and-receive test to security@puppetstagehand.com was performed and its result — pass or fail — is recorded honestly in RELEASE-EVIDENCE.md, never presented as verified without an actual test."
    - "SECURITY.md's fallback-mailbox language is only updated to claim verification once the real test in this plan actually passed."
  artifacts:
    - "RELEASE-EVIDENCE.md: Security advisory delivery test row filled with a real date, real result, and who recorded it"
    - "SECURITY.md: fallback-mailbox caveat updated only if the test passed"
  key_links:
    - "gh api PUT .../private-vulnerability-reporting -> the immediate GET re-check -> the must_haves.truths assertion above"
    - "The human's mailbox test result -> RELEASE-EVIDENCE.md's Security advisory delivery test row -> Plan 05-08's precondition, which blocks the DNS cutover until this row is real and non-blank"
  prohibitions:
    - requirement_id: LAUN-04
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT mark security@puppetstagehand.com delivery as verified in SECURITY.md or RELEASE-EVIDENCE.md without an actual send-and-receive test having been performed and reported by the maintainer — a documented-but-untested channel is not 'done' (RESEARCH.md Known Threat Patterns)."
---

<objective>
Close LAUN-04's two real gaps: private vulnerability reporting is currently OFF on the live
repository (`{"enabled":false}`, verified live during research), and `security@puppetstagehand.com`
has never had a delivery test. Enable the toggle deterministically via the GitHub API and re-verify
it; get the maintainer to run the one test Claude has no access to perform (an actual mailbox
send/receive), and record the honest result either way.

Purpose: LAUN-04 — private vulnerability reporting enabled, a non-maintainer can reach it, and
security@ delivery tested and recorded, before the production host serves customers (D-09 gates
Plan 05-08's DNS cutover on this plan completing).
Output: Private vulnerability reporting is really on; the mailbox delivery test result is recorded
honestly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-production-launch/05-CONTEXT.md
@.planning/phases/05-production-launch/05-RESEARCH.md
@SECURITY.md
@docs/operations/github-environments.md
@docs/operations/RELEASE-EVIDENCE.md
</context>

<artifacts_this_phase_produces>
- Live repository setting: `private-vulnerability-reporting` flipped from `false` to `true`
- `docs/operations/RELEASE-EVIDENCE.md`: the "Security advisory delivery test" row filled with a real result
- `SECURITY.md`: fallback-mailbox caveat language updated only on a real pass
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Enable and re-verify private vulnerability reporting</name>
  <read_first>docs/operations/github-environments.md ("Enable private security reports")</read_first>
  <files>(no repository files — live GitHub repository setting)</files>
  <action>
    Confirm the current state: `gh api repos/puppet-stagehand/stagehand-docs/private-vulnerability-reporting`
    (expected `{"enabled":false}` per RESEARCH.md's live verification). Enable it: `gh api --method
    PUT repos/puppet-stagehand/stagehand-docs/private-vulnerability-reporting` — this is the
    standard GitHub REST pattern (mirroring the analogous `vulnerability-alerts` PUT/DELETE toggle);
    if this call errors (wrong verb/path), try the repository Settings UI path documented in
    `github-environments.md` (Settings → Security → Code security → enable private vulnerability
    reporting) as a fallback and note in the SUMMARY which path worked. Immediately re-run the GET
    check — do not treat the PUT/POST response alone as proof it took effect: `gh api
    repos/puppet-stagehand/stagehand-docs/private-vulnerability-reporting` must now return
    `{"enabled":true}`.

    Confirm a non-maintainer's view is unaffected by trust level: this setting is a repository-wide
    toggle, not per-user, so a non-maintainer visiting **Security → Advisories → Report a
    vulnerability** on the public repository now sees the option — this cannot be tested from an
    authenticated session alone; note in the SUMMARY that this is inferred from the setting being a
    repository-wide flag (per GitHub's documented behavior), not independently browser-tested. This
    whole task is D-08's (05-CONTEXT.md, LOCKED) deterministic, executor-checkable verification that
    GitHub's private Security Advisory path is enabled.
  </action>
  <verify>
    <automated>gh api repos/puppet-stagehand/stagehand-docs/private-vulnerability-reporting --jq '.enabled'</automated>
  </verify>
  <acceptance_criteria>
    - The GET check returns `true` immediately after the enabling call — a fresh re-check, not the mutating call's own response.
    - No account-level or organization-level setting was changed, only this repository's toggle.
  </acceptance_criteria>
  <done>private-vulnerability-reporting is confirmed enabled on the live repository.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Test security@puppetstagehand.com delivery</name>
  <action>Send a real test message to security@puppetstagehand.com and confirm it was received and by whom.</action>
  <instructions>
    Private vulnerability reporting is now confirmed enabled via GitHub's own mechanism. The one
    remaining gap is `security@puppetstagehand.com`, SECURITY.md's documented fallback — I have no
    access to real mailbox infrastructure to test it myself (D-09, 05-CONTEXT.md, LOCKED).

    Please: (1) confirm the mailbox is provisioned and actively monitored, (2) send a real test
    message to `security@puppetstagehand.com`, and (3) confirm receipt — who received it and
    roughly when.

    Report back honestly: if delivery failed or the mailbox isn't provisioned yet, say so — I will
    record that as a failed test and this phase's DNS cutover (Plan 05-08) stays blocked until a
    real, passing test exists, per D-09's explicit gate. There is no path to marking this "done"
    without an actual test.
  </instructions>
  <verification>I cannot independently verify mailbox delivery; I will record exactly what you report, honestly, in RELEASE-EVIDENCE.md.</verification>
  <resume-signal>Report the real test result: "delivered, received by {who}, at {when}" for a pass, or describe the failure for a fail.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the real result and update SECURITY.md if it passed</name>
  <read_first>docs/operations/RELEASE-EVIDENCE.md, SECURITY.md</read_first>
  <files>docs/operations/RELEASE-EVIDENCE.md, SECURITY.md</files>
  <action>
    Fill in RELEASE-EVIDENCE.md's seeded "Security advisory delivery test" row (from 05-04) with
    today's date, "email" as the channel detail, a one-line description of the test performed, the
    real result (`pass` or `fail`) exactly as the maintainer reported it in Task 2, and who recorded
    it. Do not add a second row unless a retest is genuinely needed later (per the append-only rule,
    a retest would get its own new row, not overwrite this one).

    ONLY if Task 2's result was a genuine pass: update `SECURITY.md`'s fallback-mailbox paragraph,
    replacing "Repository administrators must provision, monitor, and test delivery to this address
    before the repository is published; this policy does not claim that setup is already complete
    ... Until the address has been verified, do not send technical details" with a statement that
    delivery was verified on the date recorded in RELEASE-EVIDENCE.md's Security advisory delivery
    test table — keep the rest of the file (private-advisory-first instructions, sensitive-data
    section) unchanged, a targeted prose edit only. If Task 2's result was a fail, make NO change to
    SECURITY.md and state explicitly in the SUMMARY that LAUN-04 is not yet closed and Plan 05-08 is
    correctly blocked by its precondition until a passing retest happens.
  </action>
  <verify>
    <automated>grep -A2 "security@puppetstagehand.com" docs/operations/RELEASE-EVIDENCE.md | grep -Ev "^\s*\|\s*\|" | grep -c .</automated>
  </verify>
  <acceptance_criteria>
    - RELEASE-EVIDENCE.md's Security advisory delivery test row has a non-blank Date, Result, and Recorded-by cell — never left blank after this task.
    - The recorded Result matches exactly what the maintainer reported in Task 2 (no upgrading a "fail" to a "pass", no inventing detail the maintainer didn't state).
    - SECURITY.md is modified only if the recorded Result is `pass`; on `fail`, SECURITY.md is byte-identical to before this task.
  </acceptance_criteria>
  <done>The real security-delivery-test result is recorded honestly, and SECURITY.md reflects reality — updated only on a genuine pass.</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged Assumptions (edge-probe fallback, LAUN-04)

The spec-less edge-probe fallback classified LAUN-04 as `unclassified`. Acknowledged: LAUN-04 is a
binary-state repository setting (enabled/disabled) plus a single pass/fail delivery test — there is
no combinatorial input space, ordering, or concurrency concern for a single maintainer flipping a
single toggle and sending one test email. The one real risk (Assumption A1 in RESEARCH.md — the
exact enable-endpoint verb might be wrong) is handled by Task 1's fallback-to-UI path and its
mandatory fresh-GET re-verification, not by a generic edge case.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub repository security settings → public disclosure path | Enabling this setting changes how external security researchers can privately reach the maintainer. |
| Maintainer-reported test result → committed evidence record | This plan's entire LAUN-04 closure depends on trusting the maintainer's honest report of the mailbox test — there is no independent verification path available to Claude. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-20 | Repudiation / Information Disclosure | Broken vulnerability-reporting channel silently failing a real future report | high | mitigate | Task 2's real send-and-receive test is the actual mitigation; Task 3 refuses to mark SECURITY.md verified on anything less than a genuine pass. |
| T-05-21 | Tampering | Wrong API verb/endpoint silently not enabling the setting | medium | mitigate | Task 1 requires a fresh GET re-check after the mutating call, never trusting the mutating call's own response as proof. |
</threat_model>

<verification>
- `gh api .../private-vulnerability-reporting` returns `{"enabled":true}` on a fresh GET.
- RELEASE-EVIDENCE.md's security-delivery-test row is filled with the maintainer's real, reported result.
- SECURITY.md is updated if and only if that result was a genuine pass.
</verification>

<success_criteria>
Private vulnerability reporting is genuinely on, and the security@ fallback's delivery has either
been proven for real or is honestly recorded as still failing — with Plan 05-08's DNS cutover
correctly gated on this plan producing a real pass.
</success_criteria>

<output>
Create `.planning/phases/05-production-launch/05-09-SUMMARY.md` when done
</output>