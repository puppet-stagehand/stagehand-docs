---
phase: 05-production-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/environments/beta/main.tf
  - infra/environments/beta/backend.hcl (untracked, gitignored)
autonomous: false
requirements: [LAUN-01]

user_setup:
  - service: cloudflare
    why: "puppetstagehand.com's live DNS is still authoritative at Cloudflare (D-01/D-04, LOCKED); ACM cannot validate beta's certificate without one CNAME added there."
    dashboard_config:
      - task: "Add the single ACM validation CNAME this plan's Task 1 surfaces, in Cloudflare's existing DNS management for puppetstagehand.com — no NS delegation change."

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The beta environment root is applied for real: a validated ACM certificate, a CloudFront distribution, a private content bucket, a deploy IAM role, and Route 53 alias records all exist in AWS for beta.puppetstagehand.com."
    - "Beta's ACM certificate validated without the plan ever touching puppetstagehand.com's live NS delegation at Cloudflare — only one narrowly-scoped CNAME was added."
    - "Beta's own CloudFront default domain answers HTTPS requests today, independent of the deferred registrar cutover (D-01/D-03)."
    - "Reaching beta.puppetstagehand.com by its literal custom hostname is explicitly recorded as blocked on the deferred, human-driven DNS cutover — not silently claimed working, not silently dropped."
  artifacts:
    - "infra/environments/beta/backend.hcl (gitignored)"
    - "infra/environments/beta/main.tf: new distribution_domain_name output"
    - "Real AWS: 1 validated ACM certificate, 1 CloudFront distribution, 1 private S3 content bucket, 1 deploy IAM role, Route 53 alias records inside the shared hosted zone"
  key_links:
    - "Bootstrap's hosted_zone_id/github_oidc_provider_arn (Phase 1/2) -> infra/environments/beta backend/apply -> content_bucket_name / distribution_id / deployment_role_arn / distribution_domain_name outputs -> consumed by 05-02's GitHub Environment variables"
    - "The one Cloudflare CNAME a human adds in Task 2 -> ACM DNS validation completing -> the beta CloudFront distribution (and everything downstream) actually getting created"
  prohibitions:
    - requirement_id: LAUN-01
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT disable or weaken prevent_self_review, or reduce the required-reviewer count, on the beta or stable GitHub Environments to unblock a stuck dispatch approval — ADR-0002 rule 3 is LOCKED and requires self-review prevention on stable; the correct response to a stuck approval is a checkpoint, never a loosened protection rule."
---

<objective>
Apply the `beta` environment root for real for the first time: request and validate its ACM
certificate, stand up its CloudFront distribution, content bucket, and deploy role, and create its
Route 53 alias records — all without crossing the DNS-cutover boundary D-01/D-02/D-03
(05-CONTEXT.md, LOCKED) draw around this phase. This is the phase's tracer: the thinnest real,
end-to-end path through every layer the phase's promotion pipeline touches (Terraform apply, ACM/DNS
validation, live reachability) before Plan 05-02 wires the deploy/dispatch half.

Purpose: LAUN-01 — the beta environment is applied, ready to receive testpilots's exact SHA.
Output: A real, working CloudFront distribution serving (empty, pre-content) beta — reachable today
via its own default domain, with the custom-hostname path explicitly recorded as blocked on the
deferred cutover.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-production-launch/05-CONTEXT.md
@.planning/phases/05-production-launch/05-RESEARCH.md
@docs/operations/aws-bootstrap.md
@infra/environments/beta/main.tf
@infra/environments/beta/variables.tf
@infra/environments/beta/backend.hcl.example
@infra/modules/static-site/acm.tf
@infra/modules/static-site/dns.tf
@infra/modules/static-site/outputs.tf
@.planning/phases/02-first-real-publication/02-03-PLAN.md
@.planning/phases/02-first-real-publication/02-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `infra/environments/beta/main.tf`: one added `output "distribution_domain_name"` block (identical shape to testpilots's)
- `infra/environments/beta/backend.hcl` (gitignored — real S3 backend config)
- Real AWS: `aws_acm_certificate.site` (validated) + `aws_acm_certificate_validation.site`, `aws_cloudfront_distribution.site`, private `aws_s3_bucket.content`, `aws_iam_role.deploy`, Route 53 `site_ipv4`/`site_ipv6` alias records — all tagged `project=stagehand, environment=beta`
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged Assumptions (edge-probe fallback, LAUN-01)

The spec-less edge-probe fallback classified LAUN-01 as `unclassified` (no SPEC exists for this
phase to lift edge coverage from). Acknowledged, not silently dropped: LAUN-01 is a single
environment/single-artifact promotion operation (one SHA, one target environment) with no
combinatorial input space — the generic edge categories (encoding/adjacency/ordering/concurrency)
do not apply. The one genuine risk this requirement carries — a stuck approval on a protected
Environment — is handled explicitly in 05-02's checkpoint, not by this probe.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Add beta's missing output and request its ACM certificate</name>
  <read_first>
    infra/modules/static-site/acm.tf, infra/environments/testpilots/main.tf (the working
    distribution_domain_name output pattern), infra/environments/beta/main.tf,
    infra/environments/beta/backend.hcl.example, docs/operations/aws-bootstrap.md (section 2)
  </read_first>
  <files>infra/environments/beta/main.tf, infra/environments/beta/backend.hcl</files>
  <action>
    Re-verify the non-root apply identity first: `aws sts get-caller-identity` must not show a
    `:root` Arn — if it does, stop and surface a checkpoint rather than proceeding (mirrors Phase
    2's D-04/D-05 precedent).

    Add one output to `infra/environments/beta/main.tf`, immediately after the existing
    `deployment_role_arn` output block: `output "distribution_domain_name" { value =
    module.site.distribution_domain_name }` — copied verbatim from `testpilots/main.tf`'s existing
    block. The module already computes this value (`infra/modules/static-site/outputs.tf`); it is
    only unsurfaced at beta's root. Do not touch `stable/main.tf` in this plan.

    Copy `infra/environments/beta/backend.hcl.example` to `backend.hcl` (gitignored) and set
    `bucket` to the real `beta` state bucket name from bootstrap's `state_bucket_names` output.
    Export `TF_VAR_hosted_zone_id` and `TF_VAR_github_oidc_provider_arn` from the real bootstrap
    outputs (shell exports only, never written to a tracked file).

    Initialize, then request only the ACM certificate via a targeted apply — cheap and reversible —
    to obtain the real DNS validation record before committing to a full apply that would otherwise
    block on that same validation (RESEARCH.md Pattern 1 / Pitfall 1):
    `tofu -chdir=infra/environments/beta init -backend-config=backend.hcl`, then
    `tofu -chdir=infra/environments/beta apply -target=module.site.aws_acm_certificate.site`, then
    `tofu -chdir=infra/environments/beta show -json | jq -r '.values.root_module.child_modules[]?.resources[]? | select(.address=="module.site.aws_acm_certificate.site") | .values.domain_validation_options'`.
    Capture the exact `resource_record_name`, `resource_record_value`, and `resource_record_type`
    (always `CNAME`) — beta has exactly one domain on its cert (`beta.puppetstagehand.com`), so
    expect exactly one validation record, unlike stable's two.
  </action>
  <verify>
    <automated>tofu -chdir=infra/environments/beta state show module.site.aws_acm_certificate.site &gt;/dev/null 2&gt;&amp;1 &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `infra/environments/beta/main.tf` has the new `distribution_domain_name` output block; `tofu -chdir=infra/environments/beta validate` passes.
    - `aws_acm_certificate.site` exists in state with a non-empty `domain_validation_options` set containing exactly one entry.
    - The exact CNAME record name/value is captured (not just observed and discarded).
    - `git status --porcelain infra/environments/beta/backend.hcl` shows nothing tracked.
  </acceptance_criteria>
  <done>The ACM certificate for beta.puppetstagehand.com exists and its real DNS validation record is known.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Add the ACM validation record at Cloudflare</name>
  <action>Add one narrowly-scoped CNAME record at Cloudflare so beta's requested ACM certificate can validate without any NS delegation change.</action>
  <instructions>
    I requested the ACM certificate for `beta.puppetstagehand.com`. AWS will not issue it until it
    can see one DNS record proving control of that name:

    Name: {captured resource_record_name from Task 1}
    Type: CNAME
    Value: {captured resource_record_value from Task 1}

    Add exactly this one record in Cloudflare's existing DNS management for `puppetstagehand.com`.
    This is deliberately NOT an NS delegation change and does not touch the apex or `www` records —
    Cloudflare stays 100% authoritative for the whole domain throughout, and the live GitHub Pages
    site those records currently serve is untouched. D-01 (05-CONTEXT.md, LOCKED) forbids a
    full-domain NS cutover; this record does not perform that cutover — `beta.puppetstagehand.com`
    will still not resolve for a random visitor afterward, because Cloudflare has no A/AAAA record
    for that name. Only the ACM validator (which polls the standard public DNS chain, still rooted
    at Cloudflare) needs to see this one record.

    If you disagree with this approach, reply with your preferred alternative instead of adding the
    record, and I will stop here.
  </instructions>
  <verification>Once you confirm the record is added, I will poll `dig +short CNAME "{resource_record_name}"` (read-only) with bounded retries until it resolves to the expected value.</verification>
  <resume-signal>Type "done" once the CNAME record is added at Cloudflare, or describe your preferred alternative instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Complete beta's apply and verify via its CloudFront default domain</name>
  <read_first>infra/modules/static-site/cloudfront.tf, infra/modules/static-site/dns.tf</read_first>
  <precondition>Task 2's checkpoint resolved "done": the ACM validation CNAME is confirmed publicly resolving via dig.</precondition>
  <files>(no new repository files — completes the apply started in Task 1)</files>
  <action>
    Re-verify the non-root identity precondition again (defense in depth).

    Run the full apply to completion (no `-target` this time):
    `tofu -chdir=infra/environments/beta plan -out=tfplan`, review it (0 destroys, 0 replacements
    beyond what Task 1 already targeted), `tofu -chdir=infra/environments/beta apply tfplan`, then
    `rm -f infra/environments/beta/tfplan`. This can take several minutes for ACM validation to
    complete now that the CNAME is visible — expected, not a hang.

    Capture every output: `content_bucket_name`, `distribution_id`, `deployment_role_arn`,
    `distribution_domain_name` (via `tofu -chdir=infra/environments/beta output -raw &lt;name&gt;`
    each) into a gitignored scratch file (e.g. `infra/environments/beta/.captured-outputs.json`) —
    05-02 consumes these.

    Verify reachability via CloudFront's own default domain only — NOT the custom hostname, which
    stays deliberately unresolvable this phase: `curl -sI "https://{distribution_domain_name}/"`
    should return a real TLS-terminated HTTP response (likely 403/404 — no content uploaded yet,
    that is 05-02/05-03's job).

    Record explicitly (in the SUMMARY): `https://beta.puppetstagehand.com/` does NOT resolve for a
    normal visitor yet, and will not until the separate, deliberate, human-driven DNS cutover
    (D-01/D-03) happens for the shared hosted zone — expected, not a defect.
  </action>
  <verify>
    <automated>tofu -chdir=infra/environments/beta output -raw distribution_id | grep -q . &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `tofu -chdir=infra/environments/beta apply` completed with 0 errors, 0 destroys, 0 unexpected replacements.
    - `content_bucket_name`, `distribution_id`, `deployment_role_arn`, `distribution_domain_name` are all captured non-empty values.
    - `curl -sI "https://{distribution_domain_name}/"` returns an HTTP response (any status), proving the distribution is live and TLS-terminating.
    - `infra/environments/beta/tfplan` does not exist on disk after this task.
    - The SUMMARY explicitly states the custom hostname is not yet publicly reachable and why.
  </acceptance_criteria>
  <done>beta is applied for real; its distribution is reachable today via its own default domain; the custom-hostname gap is recorded as expected and deferred, not silently dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare-managed live DNS zone → ACM validation | The one point this plan touches a currently-live, unrelated production asset (the GitHub Pages site); scoped to a single additive record. |
| Public internet → beta CloudFront distribution | The second real internet-facing endpoint this project creates (after testpilots). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | AWS root-user apply of beta | critical | mitigate | Tasks 1 and 3 both independently re-verify the non-root identity precondition before touching AWS. |
| T-05-02 | Tampering | Cloudflare DNS zone for the live `puppetstagehand.com` site | high | mitigate | Task 2 is a `checkpoint:human-action` naming the exact record to add, explicitly scoped away from NS delegation and the apex/`www` records; the human may decline and propose an alternative. |
| T-05-03 | Information Disclosure | `backend.hcl`, captured outputs scratch file | high | mitigate | Both gitignored; Task 1's acceptance criteria assert nothing tracked. |
| T-05-04 | Denial of Service | Premature exposure of an empty/misconfigured distribution before content exists | low | accept | The distribution answers 403/404 until 05-02/05-03 upload real content; no customer traffic depends on it and the custom hostname is not publicly reachable yet. |
</threat_model>

<verification>
- ACM certificate for beta.puppetstagehand.com is ISSUED without touching Cloudflare's NS delegation.
- CloudFront distribution, content bucket, deploy role, and Route 53 alias records all exist.
- `distribution_domain_name` answers HTTPS requests today.
- The custom hostname's non-reachability is explicitly recorded as expected, not silently dropped or silently claimed working.
</verification>

<success_criteria>
beta is applied for real under a confirmed non-root identity, its ACM certificate validated through
one narrowly-scoped, human-approved DNS record rather than any NS delegation change, and its
reachability is proven via its CloudFront default domain — with the custom-hostname gap explicitly
recorded as deferred.
</success_criteria>

<output>
Create `.planning/phases/05-production-launch/05-01-SUMMARY.md` when done
</output>
