---
phase: 05-production-launch
plan: 10
type: execute
wave: 8
depends_on: ["05-03", "05-08", "05-09"]
files_modified:
  - docs/operations/RELEASE-EVIDENCE.md
autonomous: false
requirements: [LAUN-05]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "beta is promoted to a genuinely newer commit than the one 05-03 recorded, creating a real, distinct 'known-good vs current' scenario for Plan 05-11's rollback to prove against — never a same-SHA no-op dressed up as a promotion."
    - "This promotion, like every dispatch this phase, goes through the real protected-Environment approval path, escalated to a human checkpoint only if Claude's own approval attempt is blocked."
  artifacts:
    - "docs/operations/RELEASE-EVIDENCE.md: a new Promotions row for beta recording this newer-SHA promotion"
  key_links:
    - "testpilots's live /deployed-commit.txt, now necessarily different from beta's 05-03 SHA because several of this phase's own plans have landed commits on main in between -> this task's dispatched SHA"
---

<objective>
Set up a genuine "current release differs from a known-good release" scenario on `beta`, by
promoting testpilots's now-current (and, by this point in the phase, necessarily newer than 05-03's)
SHA to beta. This is real, ordinary promotion work — not a fabricated incident — that Plan 05-11
then rolls back from, proving LAUN-05 against two real, distinct commits rather than a same-SHA
no-op.

Purpose: LAUN-05 setup — beta now has a second real deployment, distinct from 05-03's, so a genuine
rollback to the earlier one is possible and meaningful.
Output: beta serves a newer, real SHA; the promotion is recorded honestly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-production-launch/05-CONTEXT.md
@docs/operations/release.md
@.planning/phases/05-production-launch/05-02-PLAN.md
@.planning/phases/05-production-launch/05-03-PLAN.md
</context>

<artifacts_this_phase_produces>
- `docs/operations/RELEASE-EVIDENCE.md`: a new Promotions row for beta (the "current" release Plan 05-11 will roll back from)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Confirm a genuinely different SHA exists, and dispatch it to beta</name>
  <read_first>docs/operations/release.md ("Promote a release")</read_first>
  <precondition>05-03's Promotions row exists in RELEASE-EVIDENCE.md with beta's known-good SHA recorded.</precondition>
  <files>(no repository files)</files>
  <action>
    Read testpilots's currently deployed SHA: `curl -s
    "https://{testpilots distribution_domain_name}/deployed-commit.txt"`. Compare it against 05-03's
    recorded beta SHA (read from RELEASE-EVIDENCE.md's Promotions table). By this point in the
    phase, several plans' own commits (05-04's package.json/RELEASE-EVIDENCE.md changes, 05-09's
    SECURITY.md changes, etc.) have landed on `main` and triggered testpilots's automatic
    redeploys, so these two SHAs should differ. If they are identical — the honest, fail-loud
    outcome, not a silent proceed — stop and report this in the SUMMARY rather than fabricating a
    scenario; do not invent a fake SHA or skip ahead to Plan 05-11.

    If they differ, dispatch: `gh workflow run "Deploy site" --ref main -f environment=beta -f
    git_sha={testpilots-current-sha}`. Poll to a non-`queued` status. If the deploy job is
    `waiting`, attempt approval exactly as in 05-02 Task 2 (`gh api ...
    /pending_deployments`, `state=approved`); record whichever outcome results without retrying
    repeatedly or touching protection rules.
  </action>
  <verify>
    <automated>gh run list --workflow="Deploy site" --limit 1 --json status --jq '.[0].status' | grep -Ev '^queued$'</automated>
  </verify>
  <acceptance_criteria>
    - The task explicitly compares testpilots's current SHA against 05-03's recorded beta SHA and states the result (same/different) in the SUMMARY before proceeding.
    - If the SHAs are identical, the task stops and reports this honestly rather than fabricating a rollback scenario.
    - If different, a real `Deploy site` run targets beta with the new SHA, and its status/approval outcome are recorded.
  </acceptance_criteria>
  <done>beta either now has a genuinely newer SHA dispatched (with approval status recorded), or the task honestly reports that no newer SHA yet exists and stops.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Approve the pending beta deployment if still blocked</name>
  <action>If Task 1's automated approval attempt was rejected, get the pending deployment approved by the maintainer.</action>
  <instructions>
    Same pattern as 05-02/05-06's checkpoints: I dispatched a new SHA to `beta` and attempted to
    approve it via the API. {Report the exact outcome from Task 1, and reference whether an earlier
    checkpoint this phase already established that a manual UI click succeeds for this account.}

    Please open Actions → Deploy site → the run for `beta` and click **Review deployments → Approve
    and deploy**.
  </instructions>
  <verification>Once you confirm, I will poll the run to a terminal state.</verification>
  <resume-signal>Type "approved" once approved in the UI, or describe how you'd like to proceed instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify the new SHA is live and record the promotion</name>
  <read_first>docs/operations/RELEASE-EVIDENCE.md</read_first>
  <precondition>Task 1 or Task 2 resolved the dispatch to a terminal success state.</precondition>
  <files>docs/operations/RELEASE-EVIDENCE.md</files>
  <action>
    Run `SITE_URL=https://{beta distribution_domain_name} EXPECTED_SHA={the newer SHA} npx tsx
    scripts/check-live-deployment.ts` and confirm it passes. Append a new Promotions row to
    RELEASE-EVIDENCE.md for `beta` with today's date, the new SHA, and real per-check results
    (never editing 05-03's earlier beta row) — a Notes cell states this promotion establishes the
    "current" release that Plan 05-11 will demonstrate rolling back from.
  </action>
  <verify>
    <automated>grep -c "| beta |" docs/operations/RELEASE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `check-live-deployment.ts` against beta's real domain, with the new SHA, exits 0.
    - RELEASE-EVIDENCE.md now has two distinct `beta` Promotions rows (05-03's and this one), with different SHAs, neither edited.
  </acceptance_criteria>
  <done>beta genuinely serves a newer SHA than 05-03's, honestly recorded as a second, distinct promotion.</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged Assumptions (edge-probe fallback, LAUN-05)

The spec-less edge-probe fallback classified LAUN-05 as `unclassified`. Acknowledged: LAUN-05 is a
single operational dispatch through an existing, already-tested promotion path
(`assert-promotable-commit.sh`, `check-live-deployment.ts`) — it introduces no new combinatorial
input space. The one genuine risk unique to a rollback proof — accidentally "proving" a rollback
against a no-op (identical before/after SHA) — is handled explicitly by this task's fail-loud
same-SHA check, not by a generic edge case.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions dispatch → protected beta Environment approval | Repeats the same approval boundary exercised in 05-02/05-06. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-22 | Repudiation | A fabricated "rollback scenario" built on a same-SHA no-op | medium | mitigate | Task 1 explicitly compares SHAs and stops honestly rather than proceeding if they're identical. |
| T-05-23 | Elevation of Privilege | `prevent_self_review` on beta | high | mitigate | Task 1 attempts approval once and records the outcome; Task 2 escalates rather than weakening the control. |
</threat_model>

<verification>
- The two beta SHAs (05-03's and this plan's) are confirmed genuinely different before proceeding.
- The new SHA is verified live via check-live-deployment.ts.
- RELEASE-EVIDENCE.md records both promotions honestly, as separate rows.
</verification>

<success_criteria>
beta now carries a real, distinct "current" release, setting up a genuine rollback proof in Plan
05-11 rather than a synthetic one.
</success_criteria>

<output>
Create `.planning/phases/05-production-launch/05-10-SUMMARY.md` when done
</output>
