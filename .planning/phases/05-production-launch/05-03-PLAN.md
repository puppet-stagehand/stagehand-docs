---
phase: 05-production-launch
plan: 03
type: execute
wave: 3
depends_on: ["05-02", "05-04"]
files_modified:
  - docs/operations/RELEASE-EVIDENCE.md
autonomous: true
requirements: [LAUN-01, LAUN-03]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "beta.puppetstagehand.com's CloudFront default domain serves every documented route, both JSON data endpoints, the branded 404, and exactly testpilots's promoted SHA (via /deployed-commit.txt)."
    - "The first real Promotions row in docs/operations/RELEASE-EVIDENCE.md records an actual promotion this plan performed, with a real date, real SHA, and per-check pass/fail — never a fabricated or templated row for a promotion that didn't happen."
  artifacts:
    - "docs/operations/RELEASE-EVIDENCE.md: one new, real Promotions row for the testpilots -> beta promotion"
  key_links:
    - "05-02's dispatched beta run reaching a terminal success state -> this plan's live-verification pass -> the RELEASE-EVIDENCE.md row's pass/fail columns"
---

<objective>
Prove testpilots's SHA is genuinely live on beta, then record that real promotion — with real
per-check results, not a template — in `docs/operations/RELEASE-EVIDENCE.md`.

Purpose: LAUN-01 (beta serves the exact promoted SHA) and the first real entry toward LAUN-03
(release evidence recorded per deployment).
Output: A verified beta deployment and its first real, honest evidence row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-production-launch/05-CONTEXT.md
@docs/operations/release.md
@scripts/check-live-deployment.ts
@docs/operations/RELEASE-EVIDENCE.md
</context>

<artifacts_this_phase_produces>
- `docs/operations/RELEASE-EVIDENCE.md`: first real row in the "Promotions" table (environment=beta)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify beta's live deployment</name>
  <read_first>scripts/check-live-deployment.ts, docs/operations/release.md ("Promote a release" check list)</read_first>
  <precondition>05-02's beta dispatch reached a terminal state (success, confirmed via Task 2/3 of 05-02).</precondition>
  <files>(no repository files)</files>
  <action>
    Run `SITE_URL=https://{beta distribution_domain_name} EXPECTED_SHA={the SHA dispatched in
    05-02} npx tsx scripts/check-live-deployment.ts`. This already checks every route in
    `release.md`'s list (`/`, `/tiers/`, `/compatibility/`, `/docs/`, `/docs/getting-started/`,
    `/docs/security/`, `/support/`, both JSON endpoints), the branded 404, and the commit stamp —
    do not re-implement any of this by hand.

    If the script reports any failure, do not proceed to Task 2 — investigate and fix the real
    cause (a genuine misconfiguration, per RESEARCH.md Pitfall 5's warning against routing around a
    real failure) before recording anything.
  </action>
  <verify>
    <automated>SITE_URL=https://{beta-distribution-domain-name} EXPECTED_SHA={dispatched-sha} npx tsx scripts/check-live-deployment.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/check-live-deployment.ts`, run with `SITE_URL`/`EXPECTED_SHA` set to beta's real `distribution_domain_name` and the real dispatched SHA, exits 0 and its console summary confirms all routes, both JSON endpoints, the branded 404, and the exact SHA.
  </acceptance_criteria>
  <done>beta genuinely serves testpilots's exact SHA across every documented route and endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Record the real promotion in RELEASE-EVIDENCE.md</name>
  <read_first>docs/operations/RELEASE-EVIDENCE.md</read_first>
  <files>docs/operations/RELEASE-EVIDENCE.md</files>
  <action>
    Append one new row to the "Promotions" table (never edit or delete the existing empty header
    row structure) with: today's date (UTC), `beta`, the real 40-character SHA, pass/fail for each
    of Home/Tiers/Compat/Docs/Support/tiers.json/compat.json/404 (all "pass" if Task 1's script
    succeeded — record the real per-check result, do not write "pass" for a check the script itself
    did not run), the "Apex redirect (stable only)" column left blank (not applicable to beta), and
    a Notes cell linking the `Deploy site` run ID from 05-02. This is a real event this plan
    performed — D-07 (05-CONTEXT.md, LOCKED) forbids fabricating a row for a promotion that has not
    actually happened, and this one has.
  </action>
  <verify>
    <automated>grep -c "| beta |" docs/operations/RELEASE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new row added to the Promotions table for `beta`; no prior row edited or removed.
    - The row's SHA matches the SHA verified live in Task 1 exactly (40 lowercase hex characters).
    - `git diff docs/operations/RELEASE-EVIDENCE.md` shows only an appended line, no changes to existing lines.
  </acceptance_criteria>
  <done>RELEASE-EVIDENCE.md carries one real, honest promotion record for beta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification script output → committed evidence record | The evidence log's integrity depends entirely on this plan recording only what the script actually confirmed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Repudiation | A release-evidence row marked "pass" for a check that was never actually run | high | mitigate | Task 2's action explicitly forbids writing "pass" for anything Task 1's script didn't confirm; acceptance criteria require the diff to be a pure append. |
</threat_model>

<verification>
- `check-live-deployment.ts` passes against beta's real CloudFront default domain with the exact dispatched SHA.
- `docs/operations/RELEASE-EVIDENCE.md` gains exactly one new, accurate Promotions row.
</verification>

<success_criteria>
beta's promotion is proven live and honestly recorded — the first real entry in the phase's
evidence log.
</success_criteria>

<output>
Create `.planning/phases/05-production-launch/05-03-SUMMARY.md` when done
</output>
