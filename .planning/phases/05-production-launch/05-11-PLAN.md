---
phase: 05-production-launch
plan: 11
type: execute
wave: 9
depends_on: ["05-10"]
files_modified:
  - docs/operations/RELEASE-EVIDENCE.md
autonomous: false
requirements: [LAUN-05]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A known-good SHA is redeployed to beta through the normal, protected Deploy site dispatch path — the exact same mechanism every promotion in this phase used — and the previous pages are genuinely restored, verified live, not assumed."
    - "No S3 object is edited by hand at any point in this rollback — the restoration happens entirely through the dispatch -> build -> upload path scripts/deploy-site.sh already implements."
    - "The rollback is recorded in RELEASE-EVIDENCE.md's Rollbacks table with the incident, the known-good SHA, the dispatch run, and confirmation the pages were restored — verified, not claimed."
  artifacts:
    - "docs/operations/RELEASE-EVIDENCE.md: a new Rollbacks row recording this real rollback"
  key_links:
    - "05-03's recorded known-good beta SHA -> this plan's rollback dispatch target -> scripts/assert-promotable-commit.sh's ancestor check -> scripts/deploy-site.sh's normal upload path -> check-live-deployment.ts confirming restoration"
  prohibitions:
    - requirement_id: LAUN-05
      category: values
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT mark the rollback proof complete based solely on a green Deploy site run — the actual restored content (via check-live-deployment.ts against the known-good SHA) must be independently verified live before RELEASE-EVIDENCE.md records 'Restored? yes'."
---

<objective>
Prove LAUN-05 end to end: redeploy beta's earlier, known-good SHA (05-03's) through the exact same
protected dispatch path every other promotion in this phase used, and independently verify the
previous pages are genuinely restored — never by editing an S3 object by hand, and never by trusting
a green workflow run alone.

Purpose: LAUN-05 — a rollback is proven end to end via the protected dispatch path, restores the
previous pages, and the incident plus selected SHA are recorded.
Output: beta reverts to 05-03's known-good SHA; the restoration is verified live and recorded
honestly in RELEASE-EVIDENCE.md's Rollbacks table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-production-launch/05-CONTEXT.md
@docs/operations/release.md ("Roll back beta or stable")
@scripts/assert-promotable-commit.sh
@scripts/check-live-deployment.ts
@docs/operations/RELEASE-EVIDENCE.md
</context>

<artifacts_this_phase_produces>
- `docs/operations/RELEASE-EVIDENCE.md`: a new Rollbacks row recording this real, verified rollback
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch the known-good SHA back to beta</name>
  <read_first>docs/operations/release.md ("Roll back beta or stable")</read_first>
  <precondition>05-10's newer SHA is confirmed live on beta (the "current, to-be-rolled-back-from" state).</precondition>
  <files>(no repository files)</files>
  <action>
    Read 05-03's recorded known-good SHA from RELEASE-EVIDENCE.md's earliest `beta` Promotions row.
    Per `release.md`'s rollback procedure: dispatch `gh workflow run "Deploy site" --ref main -f
    environment=beta -f git_sha={05-03-sha}` — the same `Deploy site` workflow, the same protected
    Environment, no different mechanism. `scripts/assert-promotable-commit.sh` will independently
    re-verify this older SHA is still a full, ancestor-of-main commit before the deploy job runs;
    `scripts/deploy-site.sh` performs the actual content sync — no S3 object is ever touched by
    hand, satisfying `release.md`'s explicit invariant.

    Poll to a non-`queued` status; if `waiting`, attempt approval exactly as in prior dispatches
    this phase, recording the outcome without retrying repeatedly or touching protection rules.
  </action>
  <verify>
    <automated>gh run list --workflow="Deploy site" --limit 1 --json status --jq '.[0].status' | grep -Ev '^queued$'</automated>
  </verify>
  <acceptance_criteria>
    - The dispatched SHA is 05-03's exact recorded known-good SHA, read from RELEASE-EVIDENCE.md, never re-derived or guessed.
    - A real `Deploy site` run targets beta with that SHA via the normal dispatch path; its status/approval outcome is recorded.
    - No S3 object was touched by any means other than `scripts/deploy-site.sh`'s normal sync.
  </acceptance_criteria>
  <done>The known-good SHA is dispatched back to beta through the real, protected path, and the run's outcome is recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Approve the pending rollback deployment if still blocked</name>
  <action>If Task 1's automated approval attempt was rejected, get the pending rollback deployment approved by the maintainer.</action>
  <instructions>
    Same pattern as every earlier dispatch this phase: I dispatched the rollback to `beta` and
    attempted to approve it via the API. {Report the exact outcome from Task 1.}

    Please open Actions → Deploy site → the run for this rollback to `beta` and click **Review
    deployments → Approve and deploy**.
  </instructions>
  <verification>Once you confirm, I will poll the run to a terminal state and then independently verify the restored content in Task 3.</verification>
  <resume-signal>Type "approved" once approved in the UI, or describe how you'd like to proceed instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify restoration and record the rollback</name>
  <read_first>docs/operations/RELEASE-EVIDENCE.md</read_first>
  <precondition>Task 1 or Task 2 resolved the rollback dispatch to a terminal success state.</precondition>
  <files>docs/operations/RELEASE-EVIDENCE.md</files>
  <action>
    Independently verify the restoration — do not infer it from a green workflow run alone
    (this plan's prohibition, and RESEARCH.md's explicit "a rollback proof's verification step" note
    reusing `check-live-deployment.ts` with `SITE_URL` pointed at beta): run `SITE_URL=https://{beta
    distribution_domain_name} EXPECTED_SHA={05-03-sha} npx tsx scripts/check-live-deployment.ts` and
    confirm it passes — this proves beta's `/deployed-commit.txt` and every route now match the
    known-good SHA again, not 05-10's newer one.

    Append a new row to RELEASE-EVIDENCE.md's Rollbacks table (D-06, 05-CONTEXT.md, LOCKED — the
    rollback proof appends its own entry to this same file, never a separate rollback log): today's
    date (UTC), `beta`, an incident description ("planned rollback demonstration per LAUN-05 —
    reverting from 05-10's promotion back to 05-03's known-good release, no real production
    incident"), the known-good SHA, the `Deploy site` run ID/link from Task 1, and `Restored? yes` —
    only write `yes` because Task 3's own live check just independently confirmed it, not because
    the dispatch succeeded.
  </action>
  <verify>
    <automated>SITE_URL=https://{beta-distribution-domain-name} EXPECTED_SHA={known-good-sha} npx tsx scripts/check-live-deployment.ts</automated>
  </verify>
  <acceptance_criteria>
    - `check-live-deployment.ts` against beta's real domain, with the known-good SHA, exits 0 — proving the actual content reverted, not just that the workflow reported success.
    - RELEASE-EVIDENCE.md's Rollbacks table gains exactly one new row with a non-fabricated incident description, the real known-good SHA, a real dispatch run reference, and `Restored? yes` written only because Task 3 verified it independently.
    - No prior row in RELEASE-EVIDENCE.md (Promotions or Rollbacks) was edited or deleted.
  </acceptance_criteria>
  <done>The rollback is proven end to end — beta genuinely serves the known-good SHA again, verified live, not assumed — and honestly recorded.</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged Assumptions (edge-probe fallback, LAUN-05)

The spec-less edge-probe fallback classified LAUN-05 as `unclassified`. Acknowledged here as in
05-10: this is a single, already-tested dispatch mechanism reused for a rollback rather than a
forward promotion — no new combinatorial input space, and the one genuine risk (trusting a green
run instead of the actual restored content) is handled by this plan's prohibition and Task 3's
independent live check, not by a generic edge case.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions dispatch → protected beta Environment approval | The final approval exercise this phase, reusing the same mechanism as every prior promotion. |
| Workflow "success" status → actual restored content | The precise gap this plan's prohibition exists to close — a green run is not proof of restoration on its own. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-24 | Tampering / Repudiation | Manual S3 edit used to "fix" or fake a rollback instead of the real dispatch path | medium | mitigate | Task 1 uses only the normal `Deploy site` dispatch and `scripts/deploy-site.sh`'s existing sync; acceptance criteria explicitly assert no other S3 write path was used, matching `release.md`'s "never edit S3 objects manually" invariant. |
| T-05-25 | Repudiation | Recording "Restored? yes" based on a green workflow run without checking actual content | high | mitigate | Task 3's action and acceptance criteria require `check-live-deployment.ts`'s independent pass against the known-good SHA before writing `yes`; this plan's own prohibition makes the rule explicit. |
| T-05-26 | Elevation of Privilege | `prevent_self_review` on beta | high | mitigate | Task 1 attempts approval once and records the outcome; Task 2 escalates rather than weakening the control. |
</threat_model>

<verification>
- The rollback dispatch reaches a terminal success state through the real, protected path.
- `check-live-deployment.ts` independently confirms beta now serves the known-good SHA again.
- RELEASE-EVIDENCE.md's Rollbacks table gains one new, honest, verified row.
</verification>

<success_criteria>
LAUN-05 is proven true against real infrastructure: a known-good SHA was redeployed through the
normal protected dispatch path, the previous pages were genuinely restored (independently verified,
not assumed), no S3 object was ever edited by hand, and the incident is honestly recorded — closing
the last open requirement in this phase.
</success_criteria>

<output>
Create `.planning/phases/05-production-launch/05-11-SUMMARY.md` when done
</output>
