---
phase: 05-production-launch
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: []
autonomous: false
requirements: [LAUN-01]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Beta's GitHub Environment carries its own real CONTENT_BUCKET, CLOUDFRONT_DISTRIBUTION_ID, AWS_DEPLOY_ROLE_ARN, and SITE_CHECK_URL variables, none copied from testpilots or stable."
    - "A real Deploy site dispatch to beta, carrying testpilots's exact deployed SHA, reaches a concluded run (success or a named, escalated blocker) rather than silently hanging."
    - "Every dispatch to a protected GitHub Environment in this phase is followed by an explicit human-action checkpoint when Claude's own approval attempt is blocked — never silently assumed approved."
  artifacts:
    - "Live GitHub Environment variables on beta: CONTENT_BUCKET, CLOUDFRONT_DISTRIBUTION_ID, AWS_DEPLOY_ROLE_ARN, SITE_CHECK_URL"
    - "A concluded (or checkpoint-resolved) Deploy site run targeting beta"
  key_links:
    - "05-01's captured content_bucket_name/distribution_id/deployment_role_arn/distribution_domain_name -> this plan's beta GitHub Environment variables -> deploy.yml's real role assumption and upload"
    - "testpilots's current live /deployed-commit.txt SHA -> the git_sha input to the beta dispatch -> scripts/assert-promotable-commit.sh's ancestor-of-main check"
  prohibitions:
    - requirement_id: LAUN-01
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT disable or weaken prevent_self_review, or reduce the required-reviewer count, on beta or stable to unblock this dispatch's approval — ADR-0002 rule 3 is LOCKED; a stuck approval is escalated via Task 3's checkpoint, never routed around by loosening the protection rule."
---

<objective>
Wire beta's real deploy variables from 05-01, dispatch testpilots's exact, already-verified SHA to
beta through the protected `Deploy site` workflow, and get that dispatch to a concluded state —
automating the approval attempt and escalating to the maintainer only if GitHub's self-review
protection actually blocks it (RESEARCH.md Pitfall 3 / Open Question 1).

Purpose: LAUN-01 — beta receives testpilots's exact SHA, unaltered, through its protected
Environment.
Output: beta's GitHub Environment fully configured for deployment; a `Deploy site` run against beta
either succeeded or is confirmed approved and progressing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-production-launch/05-CONTEXT.md
@.planning/phases/05-production-launch/05-RESEARCH.md
@.github/workflows/deploy.yml
@docs/operations/github-environments.md
@docs/operations/release.md
@scripts/assert-promotable-commit.sh
</context>

<artifacts_this_phase_produces>
- Live GitHub Environment variables on `beta`: `CONTENT_BUCKET`, `CLOUDFRONT_DISTRIBUTION_ID`, `AWS_DEPLOY_ROLE_ARN`, `SITE_CHECK_URL` (all new — beta had none of these before this plan)
- One real `Deploy site` workflow run dispatched against `beta`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Set beta's deploy variables</name>
  <read_first>docs/operations/github-environments.md ("Set the required variables" section), infra/environments/beta/main.tf</read_first>
  <precondition>05-01's beta apply completed: content_bucket_name, distribution_id, deployment_role_arn, and distribution_domain_name are all captured.</precondition>
  <files>(no repository files — sets live GitHub Environment variables)</files>
  <action>
    Set beta's four deploy variables via `gh api --method POST
    repos/puppet-stagehand/stagehand-docs/environments/beta/variables` (each is new — beta has
    never had deploy variables set, so POST/create, not PATCH/update):
    `CONTENT_BUCKET` = 05-01's `content_bucket_name`, `CLOUDFRONT_DISTRIBUTION_ID` = 05-01's
    `distribution_id`, `AWS_DEPLOY_ROLE_ARN` = 05-01's `deployment_role_arn`, `SITE_CHECK_URL` =
    05-01's `distribution_domain_name` (bare hostname, no `https://` prefix — matches the shape
    documented for testpilots). Do not touch `stable`'s or `testpilots`'s variables.

    Confirm each write with `gh api repos/puppet-stagehand/stagehand-docs/environments/beta/variables
    --jq '.variables[].name'` and check all four names are present.
  </action>
  <verify>
    <automated>gh api repos/puppet-stagehand/stagehand-docs/environments/beta/variables --jq '.variables[].name' | sort | grep -c '^\(AWS_DEPLOY_ROLE_ARN\|CLOUDFRONT_DISTRIBUTION_ID\|CONTENT_BUCKET\|SITE_CHECK_URL\)$'</automated>
  </verify>
  <acceptance_criteria>
    - `gh api .../environments/beta/variables` lists all four names with the exact values captured in 05-01 (spot-check `CONTENT_BUCKET`/`CLOUDFRONT_DISTRIBUTION_ID` against 05-01's captured outputs — never copy a value from testpilots or stable's own outputs, per github-environments.md's explicit anti-pattern).
    - `stable` and `testpilots`'s existing variables are unchanged (`gh api .../environments/testpilots/variables` and `.../stable/variables` diff empty against their pre-task state).
  </acceptance_criteria>
  <done>beta's GitHub Environment carries its own real, non-copied deploy variables.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch testpilots's exact SHA to beta and attempt approval</name>
  <read_first>docs/operations/release.md ("Promote a release"), .github/workflows/deploy.yml</read_first>
  <files>(no repository files — dispatches a workflow run and attempts a live API approval)</files>
  <action>
    Read testpilots's currently deployed SHA directly from the live site: `curl -s
    "https://{testpilots distribution_domain_name}/deployed-commit.txt"` (fall back to `gh api
    repos/puppet-stagehand/stagehand-docs/deployments?environment=testpilots --jq
    '.[0].sha'` if the stamp file is unavailable). This is the exact, already-verified SHA
    `docs/operations/release.md`'s release invariant requires beta to receive unaltered.

    Dispatch: `gh workflow run "Deploy site" --ref main -f environment=beta -f
    git_sha={testpilots-sha}`. Poll `gh run list --workflow="Deploy site" --limit 1 --json
    databaseId,status,conclusion` until `status` is no longer `queued`.

    If the `deploy` job's own status is `waiting` (pending deployment review), attempt to approve
    it as the same authenticated identity: `gh api
    repos/puppet-stagehand/stagehand-docs/actions/runs/{run_id}/pending_deployments --jq
    '.[].environment.id'` to find the pending environment id, then `gh api --method POST
    repos/puppet-stagehand/stagehand-docs/actions/runs/{run_id}/pending_deployments -f
    environment_ids[]={id} -f state=approved -f comment="Automated promotion per
    docs/operations/release.md"`. This is a genuine attempt at automation, not a formality — per
    RESEARCH.md Pitfall 3, `beta` has `prevent_self_review=true` and the only collaborator
    (`matthewrstone`) is both the dispatching identity and the only possible reviewer, so this call
    is expected to plausibly fail with a 4xx self-review rejection. Do not retry it repeatedly or
    attempt to route around the rejection (e.g. by touching Environment protection rules) — record
    the exact outcome (approved, or the specific rejection message) and hand off to Task 3
    regardless of which outcome occurred.
  </action>
  <verify>
    <automated>gh run list --workflow="Deploy site" --limit 1 --json status --jq '.[0].status' | grep -Ev '^queued$'</automated>
  </verify>
  <acceptance_criteria>
    - The SHA dispatched is read from testpilots's own live deployment, never hardcoded or guessed.
    - A `Deploy site` run against `beta` exists and its `status`/`conclusion` (and, if attempted, the approval call's exact response) are recorded — not silently assumed.
    - No Environment protection rule (`prevent_self_review`, required reviewers) was modified in this task, regardless of the approval attempt's outcome.
  </acceptance_criteria>
  <done>A real Deploy site run targets beta with testpilots's exact SHA, and the approval attempt's real outcome (approved-and-running, or blocked) is known and recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Approve the pending beta deployment if still blocked</name>
  <action>If Task 2's automated approval attempt was rejected by GitHub's self-review protection, get the pending deployment approved by the maintainer.</action>
  <instructions>
    I dispatched `Deploy site` to `beta` with SHA `{sha}` and attempted to approve the resulting
    pending deployment via the API. {If approved automatically: "The API approval succeeded — this
    checkpoint is a formality; confirm the run is now proceeding." If rejected: "GitHub rejected the
    API approval because `beta` has `prevent_self_review=true` and you are both the dispatching
    identity and the only configured reviewer (docs/operations/github-environments.md's documented,
    intentional solo-maintainer friction)."}

    Please open the pending run in the GitHub UI (Actions → Deploy site → the run for `beta`) and
    click **Review deployments → Approve and deploy**. It is currently unconfirmed whether GitHub's
    self-review check also blocks a manual UI click from the same account that dispatched the run
    (RESEARCH.md Open Question 1) — try it now; this attempt resolves that question for the rest of
    this phase's remaining dispatches (05-06, 05-10, 05-11 all repeat this same step).

    If the UI approval also fails: per ADR-0002 rule 3 (LOCKED), `prevent_self_review` must not be
    disabled on `stable`, and per RESEARCH.md's anti-patterns this project should not weaken it on
    `beta` either just to route around this. The only clean fix is adding a second trusted GitHub
    collaborator as an additional reviewer on `beta`/`stable` (and their `-plan` counterparts) —
    that is your decision to make, not mine; tell me if you want to do that and I will help wire the
    reviewer once you've added them.
  </instructions>
  <verification>Once you confirm, I will poll `gh run list --workflow="Deploy site" --limit 1 --json status,conclusion` until the run reaches a terminal state, and report the result.</verification>
  <resume-signal>Type "approved" once you've approved the deployment in the UI (state whether the manual click succeeded or also failed), or describe how you'd like to proceed instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions dispatch → protected Environment approval | The first real exercise of `beta`'s deployment-approval gate. |
| GitHub API token → pending-deployment review | Claude's own credential attempts a privileged approval action; must fail closed, not bypass the control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Elevation of Privilege | `prevent_self_review` on beta/stable Environments | high | mitigate | Task 2 attempts approval once and records the outcome without retry-looping or touching protection rules; Task 3 escalates to the maintainer instead of weakening the control (ADR-0002 rule 3, LOCKED, applies to stable and is treated as the same standard for beta). |
| T-05-06 | Repudiation | A dispatch left in an unknown/unrecorded state | medium | mitigate | Task 2's acceptance criteria require the run's real status/conclusion to be recorded, not assumed. |
</threat_model>

<verification>
- beta's four deploy variables are live, real, and not copied from another environment.
- A `Deploy site` run against beta reaches a terminal state (success or a maintainer-resolved approval).
- The self-review-vs-manual-UI-approval question (Open Question 1) is resolved for real and the answer is recorded for reuse by later dispatches this phase.
</verification>

<success_criteria>
beta's deploy pipeline is fully configured and testpilots's exact SHA has been dispatched to it
through the real, protected approval path — with the phase's biggest completion risk (solo-maintainer
self-review) resolved empirically rather than assumed.
</success_criteria>

<output>
Create `.planning/phases/05-production-launch/05-02-SUMMARY.md` when done
</output>
