---
phase: 04-evidence-bearing-compatibility-register
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified: []
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, GATE-04]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "npm run verify is green on the working tree with all five prior plans' changes merged — including the three-way build:e2e chain, the three Playwright projects (production, fixture-matrix, fixture-matrix-scale), and the reworked check:e2e-isolation gate — not just green in each plan's own isolated context."
    - "src/data/compatibility.yaml still holds exactly records: [] at phase close — this plan confirms, it does not fabricate, that no record was published without completing the real docs/operations/compatibility-claims.md review (COMP-01), matching this phase's explicit scope boundary."
    - "docs/operations/compatibility-claims.md is unchanged by this phase — the human evidence-review process COMP-01 depends on was already complete before this phase started, and nothing here altered it."
    - "The reworked isolation checker (COMP-03) and the still-empty production register (COMP-02) are proven together in the same real npm run verify run, not asserted separately."
  artifacts: []
  key_links:
    - "This plan depends on 04-01 through 04-05 all completing first — it is the phase's integration point, proving the combination of independently-executed plans (three build targets, the reworked isolation gate, the two corrected test files, the token/prose fixes), not just each plan in isolation."
---

<objective>
Close the phase with the two checks that can only be run once every other plan's content exists together:
a full, real `npm run verify` pass over the merged working tree (now exercising three Astro build targets,
three Playwright projects, and the reworked isolation gate together for the first time), and an explicit
confirmation that this phase did not fabricate a compatibility claim to satisfy any test or requirement.

Purpose: the phase's ROADMAP success criteria require `npm run verify` green with the isolation gate now
proving "no fixture-derived record" rather than "empty," the matrix readable/filterable/accessible at
realistic volume, `/data/compatibility.json` matching the rendered page, and — critically — the honest
empty state still rendering with no fixture record ever promoted to fill it. No single upstream plan can
prove all of this together; 04-01 through 04-05 execute independently (04-01 tracer, then 04-02/04-03 in
parallel, 04-04/04-05 independent of all of them), so this plan is the first point their combination is
actually exercised.
Output: a confirmed-green `npm run verify` run and a documented, reproducible confirmation that
`src/data/compatibility.yaml` is still empty by honest omission, not by design shortcut.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full npm run verify pass over the merged tree</name>
  <files></files>
  <read_first>package.json (the full `verify` script chain, and the updated `build:e2e`/`check:e2e-isolation` scripts from 04-01/04-02)</read_first>
  <action>
    Run `npm run verify` on the working tree with 04-01 through 04-05's changes all present. This exercises,
    in order: format check, lint (including 04-05's SCSS token change), `astro check`, `validate:data`
    (including 04-01's new scale-fixture-flag unit test and 04-02's reworked isolation-checker unit test),
    the production build, `check:routes`, `check:invalidation`, `check:links`, and the full `test:e2e` chain
    — which now builds three targets (`dist/`, `.e2e-dist/`, `.scale-dist/`), runs all three Playwright
    projects (`production` including 04-04's branched empty/populated tests, `fixture-matrix` unchanged,
    `fixture-matrix-scale` with 04-01/04-03's 7 tests), and finally `check:e2e-isolation` against all three
    real build outputs together for the first time. If any step fails, diagnose whether the failure is a
    genuine cross-plan regression (e.g., a Playwright project misrouting a spec file, a test literal one
    plan updated that a sibling plan's change also touches) and fix it minimally — do not alter any prior
    plan's scope decisions to make a step pass; if a fix would require changing scope, stop and report the
    conflict instead of silently narrowing it. Re-run `npm run verify` until it exits 0.
  </action>
  <verify>
    <automated>npm run verify</automated>
  </verify>
  <done>npm run verify exits 0 on the working tree with 04-01 through 04-05's changes all merged.</done>
  <acceptance_criteria>
    - `npm run verify` exits 0 on the merged working tree.
    - `npm run check:e2e-isolation`'s success output (already run as part of `verify`) reports `production=0 (0 fixture-derived), e2e=5, scale=27`.
    - `env -u NO_COLOR playwright test tests/e2e/fixture-matrix-scale.spec.ts --project=fixture-matrix-scale` (already run as part of `verify`) reports all 7 tests passing.
    - No fix applied in this task altered a prior plan's scope decisions — only mechanical regressions (literal test-string drift, formatting, cross-project spec routing) were corrected, and each such fix is named in this plan's SUMMARY.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Confirm the published register is still honestly empty and the evidence-review process is untouched</name>
  <files></files>
  <read_first>src/data/compatibility.yaml, docs/operations/compatibility-claims.md, docs/adr/0001-compatibility-scaffold.md</read_first>
  <action>
    Run two targeted, reproducible checks and record both exact commands and outputs in this plan's
    SUMMARY: (1) confirm `src/data/compatibility.yaml` still contains no record entries — grep the file for
    any line matching a YAML list-item record start (`- id:`) and confirm zero matches, and confirm the
    file still contains the literal `records: []`; this is a phase-close confirmation, not a permanent gate
    (the permanent gate is 04-02's isolation checker) — a future phase legitimately adding a real, reviewed
    record will need to update this specific assertion, and that is expected, not a regression. (2) confirm
    `docs/operations/compatibility-claims.md` is byte-identical to its state before this phase started
    (`git diff --stat` against the commit that opened this phase shows no changes to that file) — COMP-01's
    review process was already complete before this phase, and this phase's scope was explicitly the
    build-isolation mechanism and test corpus around it, never the review process itself or a fabricated
    claim to exercise it.
  </action>
  <verify>
    <automated>! grep -q '^\s*- id:' src/data/compatibility.yaml && grep -q 'records: \[\]' src/data/compatibility.yaml</automated>
  </verify>
  <done>src/data/compatibility.yaml is confirmed empty by honest omission (no record was fabricated this phase to satisfy any test or requirement), and docs/operations/compatibility-claims.md is confirmed unchanged.</done>
  <acceptance_criteria>
    - The grep for `- id:` list-item records in `src/data/compatibility.yaml` returns zero matches.
    - `src/data/compatibility.yaml` still contains the literal `records: []`.
    - `docs/operations/compatibility-claims.md` shows no diff against the phase's starting commit.
    - Both exact commands and their outputs are recorded in this plan's SUMMARY.md.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Merged working tree (04-01 through 04-05) -> `npm run verify` | The first point at which all five independently-executed plans' changes are exercised together across three real build targets and three Playwright projects — a regression here is by definition a cross-plan interaction, not any single plan's own bug. |
| `src/data/compatibility.yaml`'s honest-empty state -> ADR-0001 rule 1 (LOCKED) | Confirming this phase did not fabricate a record to make a test pass is itself a control against the exact failure mode ADR-0001 exists to prevent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09 | Tampering | Cross-plan merge of 04-01 through 04-05 | medium | mitigate | Task 1 runs the full `npm run verify` chain against the actual merged tree — including all three Playwright projects together for the first time — rather than trusting each plan's isolated run; any cross-project spec-routing or test-literal collision surfaces here before it reaches `main`. |
| T-04-10 | Spoofing | An unreviewed compatibility claim reaching `src/data/compatibility.yaml` | critical | mitigate | Task 2's explicit, reproducible empty-register confirmation is the closing gate proving this phase never fabricated a record to satisfy COMP-01/COMP-02/COMP-03/COMP-05 — the permanent technical control is 04-02's isolation checker; this task is the evidence that control was never needed against this phase's own work. |
</threat_model>

<verification>
- `npm run verify` exits 0 on the fully merged working tree (Task 1).
- `check:e2e-isolation` reports `production=0 (0 fixture-derived), e2e=5, scale=27` (Task 1).
- `src/data/compatibility.yaml` is confirmed empty by honest omission, and `docs/operations/compatibility-claims.md` is confirmed unchanged (Task 2).
- This plan's SUMMARY.md records every command run and its output.
</verification>

<success_criteria>
The phase's ROADMAP success criteria are all independently verifiable from this plan's evidence: `npm run
verify` green with the reworked isolation gate and all three build targets proven together, the matrix
readable/filterable/accessible at realistic volume, `/data/compatibility.json` matching the rendered page,
and the honest empty state still rendering with zero fabricated records.
</success_criteria>

## Artifacts This Plan Produces

- No new files — this plan produces verification evidence recorded in its SUMMARY.md.

<output>
Create `.planning/phases/04-evidence-bearing-compatibility-register/04-06-SUMMARY.md` when done
</output>
