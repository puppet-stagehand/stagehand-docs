---
phase: 04-evidence-bearing-compatibility-register
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - src/lib/data/compatibility.ts
  - tests/unit/data-validation.test.ts
  - tests/fixtures/data/compatibility-scale.yaml
  - .gitignore
  - playwright.config.ts
  - package.json
  - tests/e2e/fixture-matrix-scale.spec.ts
autonomous: true
requirements: [GATE-04, COMP-01]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A third, independent build target exists alongside production (dist/) and the ADR-0001-locked E2E fixture (.e2e-dist/): running STAGEHAND_SCALE_FIXTURES=1 npm run build produces .scale-dist/data/compatibility.json with exactly 27 records, without touching production's dist/ or the E2E fixture's .e2e-dist/ output."
    - "The scale fixture is never mistaken for either the published register or the ADR-0001 e2e fixture — it lives at tests/fixtures/data/compatibility-scale.yaml, is not ADR-0001-governed, and its evidence_urls all point at this repository's own issue tracker, never a claim a reader could mistake for reviewed evidence."
    - "loadCompatibility()'s HTTPS, duplicate-identity, and 365-day-freshness validation runs unconditionally on every data source including the new scale branch — COMP-01's evidence-review invariant is not weakened or bypassed to make the scale fixture pass."
    - "A real Playwright project (fixture-matrix-scale) serves .scale-dist on its own port (4323) and a real test proves the populated matrix renders all 27 records with zero serious/critical axe violations — this is GATE-04's first concrete proof, not just wiring."
  artifacts:
    - tests/fixtures/data/compatibility-scale.yaml
    - tests/e2e/fixture-matrix-scale.spec.ts
  key_links:
    - "astro.config.mjs's outDir branch -> STAGEHAND_SCALE_FIXTURES env var -> .scale-dist/ -> scripts/serve-static-build.ts .scale-dist 4323 -> playwright.config.ts's fixture-matrix-scale project baseURL http://127.0.0.1:4323"
    - "src/lib/data/compatibility.ts's loadCompatibility() scaleDataPath/scaleValidationDate branch, checked before the existing e2e branch so the two flags stay mutually exclusive -> tests/fixtures/data/compatibility-scale.yaml"
    - "package.json's build:e2e script chains all three builds (plain, E2E, scale) with both flags explicitly unset for the plain build, so a stray exported env var can never leak fixture data into production"
---

<objective>
Extend the repository's existing env-var-gated build-target pattern (already used twice for production vs.
the ADR-0001-locked E2E fixture) a third time for GATE-04's realistic-volume compatibility fixture, and
prove the full pipeline end to end with one real Playwright test against the populated matrix. This is the
phase's tracer: every other Phase 4 plan that touches the scale fixture (04-02's isolation-checker rework,
04-03's remaining GATE-04 assertions) depends on this plan's artifacts existing first.

Purpose: GATE-04 requires a realistic-volume fixture (>=24 records, target ~30) proving the matrix's
layout, responsiveness, and accessibility at scale — the ADR-0001-locked 5-record E2E fixture is
explicitly forbidden from growing past 5 records (ADR-0001 rule 2), so a second, independent,
non-ADR-governed fixture and build target are required. The 04-UI-SPEC.md names the exact minimum bar
(>=24 records, >=10 distinct platforms, all 4 tiers x all 5 statuses represented >=3 times each, a mix of
empty/populated limitations, one 60+-character limitation sentence, one unusually long platform name) and
leaves the build-pipeline mechanism to research/planning; 04-RESEARCH.md recommends replicating the
existing STAGEHAND_E2E_FIXTURES pattern exactly.
Output: `tests/fixtures/data/compatibility-scale.yaml` (27 records), a third Astro build target
(`.scale-dist/`), a third Playwright project (`fixture-matrix-scale`) serving it on port 4323, and one
real, passing end-to-end test proving the populated matrix renders correctly and is axe-clean at volume.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-evidence-bearing-compatibility-register/04-UI-SPEC.md
@.planning/phases/04-evidence-bearing-compatibility-register/04-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wire the STAGEHAND_SCALE_FIXTURES data/build branch end to end and author the 27-record scale fixture</name>
  <files>astro.config.mjs, src/lib/data/compatibility.ts, tests/unit/data-validation.test.ts, tests/fixtures/data/compatibility-scale.yaml, .gitignore</files>
  <read_first>
    astro.config.mjs, src/lib/data/compatibility.ts, src/lib/data/load-yaml.ts,
    src/data/schema/compatibility.schema.json, tests/fixtures/data/compatibility-e2e.yaml,
    tests/unit/data-validation.test.ts (the "uses a fixed validation date only for the exact E2E fixture
    flag" test — the pattern to mirror), .gitignore
  </read_first>
  <action>
    Before making any change, run `STAGEHAND_SCALE_FIXTURES=1 ASTRO_TELEMETRY_DISABLED=1 npm run build`
    and confirm it currently produces `dist/data/compatibility.json` with 0 records (the flag is not read
    yet, so it silently falls through to the plain production path) — this is the RED baseline this task
    corrects to a real, independent `.scale-dist/` output.

    In `astro.config.mjs`, change the `outDir` expression from the existing single ternary
    (`process.env.STAGEHAND_E2E_FIXTURES === '1' ? './.e2e-dist' : './dist'`) to a nested ternary checked
    in this exact order: `process.env.STAGEHAND_SCALE_FIXTURES === '1' ? './.scale-dist' :
    process.env.STAGEHAND_E2E_FIXTURES === '1' ? './.e2e-dist' : './dist'` — the scale check comes first so
    the two flags can never silently combine.

    In `src/lib/data/compatibility.ts`, add `const scaleDataPath = resolve(process.cwd(),
    'tests/fixtures/data/compatibility-scale.yaml');` immediately after the existing `e2eDataPath` const,
    and `const scaleValidationDate = new Date('2026-08-26T00:00:00.000Z');` immediately after the existing
    `e2eValidationDate` const — this fixed date mirrors `e2eValidationDate`'s purpose exactly: without it,
    the scale fixture's `last_verified` dates would silently start failing the 365-day freshness check as
    real calendar time passes. Inside `loadCompatibility`, add `const useScaleFixtures =
    process.env.STAGEHAND_SCALE_FIXTURES === '1' && options.path === undefined;` as the first line, then
    change the existing `useE2eFixtures` line to also require `!useScaleFixtures` (so the two flags stay
    mutually exclusive, scale taking precedence): `const useE2eFixtures = !useScaleFixtures &&
    process.env.STAGEHAND_E2E_FIXTURES === '1' && options.path === undefined;`. Update the `source`
    assignment to check `useScaleFixtures` first: `options.path ?? (useScaleFixtures ? scaleDataPath :
    useE2eFixtures ? e2eDataPath : dataPath)`. Update the `currentDay` assignment the same way:
    `options.today ?? (useScaleFixtures ? scaleValidationDate : useE2eFixtures ? e2eValidationDate : new
    Date())`. Do not touch any line of the schema-validation call, the HTTPS check, the duplicate-identity
    check, or the freshness-window arithmetic below this branch — this task only changes which file and
    which pinned "today" feed that unchanged validation pipeline, never the validation itself (COMP-01).

    In `tests/unit/data-validation.test.ts`, add one new test in the `describe('compatibility data
    validation', ...)` block, immediately after the existing "uses a fixed validation date only for the
    exact E2E fixture flag" test, named "uses a fixed validation date only for the exact scale-fixture
    flag": mirror that test's exact shape (save/restore `process.env.STAGEHAND_SCALE_FIXTURES`,
    `vi.useFakeTimers()`/`vi.setSystemTime(new Date('2028-08-22T00:00:00.000Z'))` to prove the real registry
    would otherwise be treated as far in the future) — set `process.env.STAGEHAND_SCALE_FIXTURES = '1'` and
    assert `loadCompatibility()` returns a 27-length array; then set it to `'true'` (not the exact string
    `'1'`) and assert `loadCompatibility({ path: fixture('compatibility-scale.yaml') })` throws
    `'Compatibility evidence is older than 365 days'` (proving the fixed-date branch is gated on the exact
    flag value, not merely its presence, exactly like the existing E2E test proves for its own flag).

    Author `tests/fixtures/data/compatibility-scale.yaml` with `schema_version: 1` and exactly 27 records
    (`records:` list), generated deterministically for index i = 0..26 from these fixed pools, matching
    `compatibility-e2e.yaml`'s exact field order and quoting style (single-quoted version-range and date
    strings, unquoted enum values):

    platforms (12 entries, selected by i % 12, each giving platform/provider/transport): [0] AWS EC2 / aws
    / ssh, [1] Azure VM / azure / ssh, [2] Google Cloud / google / ssh, [3] VMware vSphere / vmware / pcp,
    [4] Docker / docker / ssh, [5] Bare Metal / baremetal / pcp, [6] DigitalOcean Droplet / digitalocean /
    ssh, [7] Oracle Cloud Infrastructure / oracle / ssh, [8] IBM Cloud / ibm / ssh, [9] Proxmox VE / proxmox
    / pcp, [10] Kubernetes (Self-Managed) / kubernetes / ssh, [11] AWS Outposts Rack (Customer-Managed
    On-Premises Extension of Amazon EC2) / aws / ssh — this 12th platform is deliberately unusually long,
    for the wrap-backstop check a later plan adds.

    tiers (4, i % 4): openvox, puppet-core, puppet-enterprise, pe-advanced.
    statuses (5, i % 5): supported, compatible, limited, deprecated, unsupported.
    operating_systems (8, i % 8, each record gets a single-element array): Ubuntu 24.04, Red Hat Enterprise
    Linux 9, Debian 13, Rocky Linux 9, AlmaLinux 9, Windows Server 2025, SUSE Linux Enterprise Server 15,
    Amazon Linux 2023.
    docs_path (3, i % 3): /docs/getting-started/, /docs/security/, /docs/first-run/ — all three are real,
    already-shipped routes.
    evidence_url (2, i % 2): https://github.com/puppet-stagehand/stagehand-docs/issues,
    https://github.com/puppet-stagehand/stagehand-docs/issues/new.
    last_verified (9, i % 9): '2025-09-05', '2025-10-12', '2025-11-20', '2025-12-15', '2026-01-22',
    '2026-03-10', '2026-04-18', '2026-06-05', '2026-08-24' — every one of these is safely within 365 days
    of the pinned `scaleValidationDate` (2026-08-26) and never in its future.

    Per record: `id` = `scale-{i zero-padded to 2 digits}-{tier}-{status}` (the zero-padded index alone
    guarantees global uniqueness across all 27 ids, regardless of any tier/status repeat). `puppet_versions`
    = `'>= 8.{i} < 8.{i+1}'` using the plain integer i (e.g. i=9 gives `'>= 8.9 < 8.10'`) — this alone
    guarantees every record's `platform|puppet_versions|tier|provider|transport` identity tuple is globally
    unique, regardless of any repeat in the other four fields. `stagehand_versions` = `'>= 0.1 < 0.2'` for
    every record (unchanged from the existing fixtures). `limitations`: when i == 7, the single sentence
    "This fixture record intentionally carries a limitations sentence long enough to exercise the wrap
    backstop check across both the table cell and the card layout at realistic content volume." (this is
    the required 60+-character backstop record); else when i is odd, the single sentence "Scale fixture
    record; treat this limitation as synthetic test data, not a real constraint."; else (i even, i != 7) an
    empty array `[]`.

    Two fully worked records to lock the exact shape: i=0 -> `id: scale-00-openvox-supported, platform: AWS
    EC2, puppet_versions: '>= 8.0 < 8.1', stagehand_versions: '>= 0.1 < 0.2', tier: openvox, provider: aws,
    transport: ssh, operating_systems: [Ubuntu 24.04], status: supported, limitations: [], docs_path:
    /docs/getting-started/, evidence_url: https://github.com/puppet-stagehand/stagehand-docs/issues,
    last_verified: '2025-09-05'`. i=7 -> `id: scale-07-pe-advanced-limited, platform: Oracle Cloud
    Infrastructure, puppet_versions: '>= 8.7 < 8.8', stagehand_versions: '>= 0.1 < 0.2', tier: pe-advanced,
    provider: oracle, transport: ssh, operating_systems: [Amazon Linux 2023], status: limited, limitations:
    [the long sentence above], docs_path: /docs/security/, evidence_url:
    https://github.com/puppet-stagehand/stagehand-docs/issues/new, last_verified: '2026-06-05'`.

    This composition yields 12 distinct platform values (>=10 required), each tier represented 7/7/7/6
    times (>=3 required each), and each status represented 6/6/5/5/5 times (>=3 required each), satisfying
    every minimum in the UI-SPEC's Realistic-Volume Fixture Requirement.

    In `.gitignore`, add `.scale-dist/` immediately after the existing `.e2e-dist/` line.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/data-validation.test.ts && env -u STAGEHAND_E2E_FIXTURES STAGEHAND_SCALE_FIXTURES=1 ASTRO_TELEMETRY_DISABLED=1 npm run build && node -e "process.exitCode = JSON.parse(require('fs').readFileSync('.scale-dist/data/compatibility.json','utf8')).records.length === 27 ? 0 : 1"</automated>
  </verify>
  <done>tests/fixtures/data/compatibility-scale.yaml exists with exactly 27 valid records, the new data-validation.test.ts test passes, and a real STAGEHAND_SCALE_FIXTURES=1 build produces .scale-dist/data/compatibility.json with exactly 27 records.</done>
  <acceptance_criteria>
    - `tests/fixtures/data/compatibility-scale.yaml` parses under `src/data/schema/compatibility.schema.json` with exactly 27 records, no duplicate `id`, no duplicate `platform|puppet_versions|tier|provider|transport` identity tuple.
    - The new "uses a fixed validation date only for the exact scale-fixture flag" test in `tests/unit/data-validation.test.ts` passes.
    - `env -u STAGEHAND_E2E_FIXTURES STAGEHAND_SCALE_FIXTURES=1 ASTRO_TELEMETRY_DISABLED=1 npm run build` succeeds and `.scale-dist/data/compatibility.json` contains exactly 27 records.
    - A plain `env -u STAGEHAND_E2E_FIXTURES -u STAGEHAND_SCALE_FIXTURES ASTRO_TELEMETRY_DISABLED=1 npm run build` run immediately afterward still produces `dist/data/compatibility.json` with 0 records — the scale branch does not leak into the production path.
    - `.gitignore` contains `.scale-dist/`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire the fixture-matrix-scale Playwright project and prove the populated matrix end to end</name>
  <files>playwright.config.ts, package.json, tests/e2e/fixture-matrix-scale.spec.ts</files>
  <read_first>
    playwright.config.ts, scripts/serve-static-build.ts, package.json (the `build:e2e`/`test:e2e` scripts),
    tests/e2e/fixture-matrix.spec.ts, tests/e2e/accessibility.spec.ts
  </read_first>
  <action>
    In `playwright.config.ts`, convert the `production` project's `testIgnore` from the single string
    `'fixture-matrix.spec.ts'` to the array `['fixture-matrix.spec.ts', 'fixture-matrix-scale.spec.ts']`.
    Add `testIgnore: 'fixture-matrix-scale.spec.ts'` to the existing `fixture-matrix` project (alongside
    its existing `testMatch: 'fixture-matrix.spec.ts'`). Add a third project entry after `fixture-matrix`:
    `{ name: 'fixture-matrix-scale', testMatch: 'fixture-matrix-scale.spec.ts', testIgnore:
    'fixture-matrix.spec.ts', use: { ...devices['Desktop Chrome'], baseURL: 'http://127.0.0.1:4323' } }` —
    every project now explicitly excludes every other project's spec file by name, not relying on
    `testMatch` alone to prevent cross-project bleed (RESEARCH.md Pitfall 5). Add a third `webServer`
    entry after the existing two: `{ command: 'exec env -u NO_COLOR node --import tsx
    scripts/serve-static-build.ts .scale-dist 4323', url: 'http://127.0.0.1:4323', reuseExistingServer:
    false, timeout: 120_000 }` — `scripts/serve-static-build.ts` needs no code change, it already accepts
    an arbitrary root directory and port as CLI arguments.

    In `package.json`'s `build:e2e` script, change `"env -u STAGEHAND_E2E_FIXTURES ASTRO_TELEMETRY_DISABLED=1 npm run build && STAGEHAND_E2E_FIXTURES=1 ASTRO_TELEMETRY_DISABLED=1 npm run build"` to
    `"env -u STAGEHAND_E2E_FIXTURES -u STAGEHAND_SCALE_FIXTURES ASTRO_TELEMETRY_DISABLED=1 npm run build && STAGEHAND_E2E_FIXTURES=1 ASTRO_TELEMETRY_DISABLED=1 npm run build && STAGEHAND_SCALE_FIXTURES=1 ASTRO_TELEMETRY_DISABLED=1 npm run build"` —
    the plain production build now explicitly unsets both flags (previously only unset
    `STAGEHAND_E2E_FIXTURES`), and a third chained build produces `.scale-dist/`. Do not change
    `test:e2e` or `check:e2e-isolation` — both already work correctly against three build targets via
    unchanged default arguments once `.scale-dist/` exists.

    Create `tests/e2e/fixture-matrix-scale.spec.ts` with two tests, following `tests/e2e/fixture-matrix.spec.ts`'s
    and `tests/e2e/accessibility.spec.ts`'s existing patterns exactly (same `AxeBuilder`/`@playwright/test`
    imports, same tag set): Test 1 "the scale-volume compatibility matrix renders every fixture record" —
    set viewport 1280x800, `page.goto('/compatibility/')`, assert `page.locator('.compat-table tbody tr')`
    has count 27, and assert `page.locator('#compat-results-count')` contains text "Showing 27 of 27
    verified records". Test 2 "the scale-volume compatibility matrix has no serious or critical axe
    violations" — `page.goto('/compatibility/')`, run `new AxeBuilder({ page }).withTags(['wcag2a',
    'wcag2aa', 'wcag21a', 'wcag21aa']).analyze()`, assert the `serious`/`critical`-impact violation list is
    empty — this is GATE-04's concrete "axe scan added to the fixture-matrix Playwright project family"
    requirement, and the first time the populated matrix has ever been axe-scanned in this codebase.
  </action>
  <verify>
    <automated>npm run build:e2e && env -u NO_COLOR playwright test tests/e2e/fixture-matrix-scale.spec.ts --project=fixture-matrix-scale</automated>
  </verify>
  <done>playwright.config.ts declares the fixture-matrix-scale project and third webServer entry, package.json's build:e2e chains all three builds with both flags unset for the plain build, and both new tests in fixture-matrix-scale.spec.ts pass against a real .scale-dist build.</done>
  <acceptance_criteria>
    - `playwright.config.ts` has three projects (`production`, `fixture-matrix`, `fixture-matrix-scale`) and three `webServer` entries, each project's `testIgnore` naming every other project's spec file.
    - `npm run build:e2e` produces `dist/`, `.e2e-dist/`, and `.scale-dist/` in one chained run.
    - `env -u NO_COLOR playwright test tests/e2e/fixture-matrix-scale.spec.ts --project=fixture-matrix-scale` passes both tests.
    - `env -u NO_COLOR playwright test tests/e2e/fixture-matrix.spec.ts --project=fixture-matrix` still passes unchanged (5-record parity preserved, no cross-project bleed).
  </acceptance_criteria>
  <reversibility rating="reversible">A third parallel Playwright project + webServer instance (rather than parameterizing the existing fixture-matrix project) is a direct extension of the two-instance pattern already in the tree; swapping the mechanism later would touch only playwright.config.ts and this new spec file, not any production code.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `STAGEHAND_SCALE_FIXTURES`/`STAGEHAND_E2E_FIXTURES` env vars -> which data file and which `outDir` a build uses | The entire separation between "provably real production output" and "test fixture output" depends on these two flags being read correctly and never both true, and on the plain production build explicitly unsetting both. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `astro.config.mjs`'s outDir branch / `compatibility.ts`'s data-source branch | high | mitigate | The scale check is ordered before the e2e check in both files (mutual exclusion by construction), and `package.json`'s `build:e2e` now explicitly unsets both flags (`env -u STAGEHAND_E2E_FIXTURES -u STAGEHAND_SCALE_FIXTURES`) for the plain production build, so a stray exported env var from a prior shell command can never leak fixture data into `dist/`. Task 1's acceptance criteria assert the plain build still produces 0 records immediately after a scale build in the same shell. |
| T-04-02 | Information Disclosure | `tests/fixtures/data/compatibility-scale.yaml`'s `evidence_url` values | low | accept | Every evidence URL points at this repository's own public issue tracker (already-public GitHub URLs), matching the existing E2E fixture's convention — no internal or sensitive URL is introduced. |
</threat_model>

<verification>
- `npx vitest run tests/unit/data-validation.test.ts` passes, including the new scale-fixture-flag test.
- `env -u STAGEHAND_E2E_FIXTURES STAGEHAND_SCALE_FIXTURES=1 ASTRO_TELEMETRY_DISABLED=1 npm run build` produces `.scale-dist/data/compatibility.json` with exactly 27 records.
- `npm run build:e2e` produces all three dist targets in one chained run.
- `env -u NO_COLOR playwright test tests/e2e/fixture-matrix-scale.spec.ts --project=fixture-matrix-scale` passes both tests (27-record render, zero serious/critical axe violations).
</verification>

<success_criteria>
A third, independent, non-ADR-governed build target exists end to end — data file, build config, static
server, Playwright project — and one real Playwright run proves the populated compatibility matrix renders
correctly and is axe-clean at 27 records, without altering production output or the ADR-0001-locked
5-record E2E fixture.
</success_criteria>

## Artifacts This Plan Produces

- `tests/fixtures/data/compatibility-scale.yaml` — new, 27-record, non-ADR-governed compatibility fixture.
- `tests/e2e/fixture-matrix-scale.spec.ts` — new Playwright spec, 2 tests (record-count render, axe scan).
- `src/lib/data/compatibility.ts` — `scaleDataPath`, `scaleValidationDate` consts and the `useScaleFixtures` branch in `loadCompatibility()`.
- `astro.config.mjs` — third `outDir` branch for `STAGEHAND_SCALE_FIXTURES=1`.
- `playwright.config.ts` — `fixture-matrix-scale` project and its `webServer` entry (port 4323).
- `package.json` — `build:e2e` now chains three builds.
- `.gitignore` — `.scale-dist/` entry.

<output>
Create `.planning/phases/04-evidence-bearing-compatibility-register/04-01-SUMMARY.md` when done
</output>