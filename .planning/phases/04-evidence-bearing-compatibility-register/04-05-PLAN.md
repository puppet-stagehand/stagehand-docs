---
phase: 04-evidence-bearing-compatibility-register
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/_tokens.scss
  - src/styles/components/_compatibility.scss
  - docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md
autonomous: true
requirements: [COMP-04, DRIFT-04]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The 'unsupported' status color is a named token (--stagehand-danger / $stagehand-danger) in _tokens.scss, not a bare hex literal in _compatibility.scss, following the exact same pattern as the site's other five color variables."
    - "The rendered color for .compat-status--unsupported is unchanged (#ff8b8b) — this is a token-naming fix, not a visual redesign; no other status color, accent budget, or icon changes."
    - "All three DRIFT-04 sentences in the design spec (lines 20-21, 77, 269) now agree with ADR-0001's delivery boundary — none can be read as promising seeded customer-facing compatibility records."
  artifacts: []
  key_links:
    - "src/styles/_tokens.scss's new --stagehand-danger custom property -> src/styles/components/_compatibility.scss's .compat-status--unsupported rule, replacing the bare #ff8b8b literal"
---

<objective>
Close the two remaining Phase 4 UI-SPEC/requirement gaps that are pure token/prose fixes, independent of
the fixture and isolation-checker work in this phase's other plans: formalize the one untokenized color
the UI-SPEC identifies, and land DRIFT-04's three design-spec sentence amendments.

Purpose: 04-UI-SPEC.md names `#ff8b8b` (used only by `.compat-status--unsupported`) as "a real, load-bearing
color with no token and no entry in `_tokens.scss`" and specifies the exact fix. DRIFT-04 requires the
design specification's "representative content for every route" and "customer-facing... schema-validated
structured data" language to match ADR-0001's amendment, using ADR-0001's own already-written amendment
text.
Output: `--stagehand-danger`/`$stagehand-danger` added to `_tokens.scss` and referenced from
`_compatibility.scss`, and three amended sentences in the design specification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-evidence-bearing-compatibility-register/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize the untokenized "unsupported" status color</name>
  <files>src/styles/_tokens.scss, src/styles/components/_compatibility.scss</files>
  <read_first>
    src/styles/_tokens.scss, src/styles/components/_compatibility.scss (line 266, the
    `.compat-status--unsupported` rule)
  </read_first>
  <action>
    In `src/styles/_tokens.scss`, add `$stagehand-danger: #ff8b8b;` immediately after the existing
    `$stagehand-warning: #ffbd4a;` line, in the same declaration style as the other five color variables.
    In the `:root { ... }` block, add `--stagehand-danger: #{$stagehand-danger};` immediately after the
    existing `--stagehand-warning: #{$stagehand-warning};` line, matching the existing custom-property
    pattern exactly.

    In `src/styles/components/_compatibility.scss`, change the `.compat-status--unsupported` rule's
    `color: #ff8b8b;` to `color: var(--stagehand-danger);` — this is the only line this task changes in
    that file. Do not touch `.compat-status--supported`, `.compat-status--compatible`,
    `.compat-status--limited`, or `.compat-status--deprecated` — none of them are in scope for this fix.
  </action>
  <verify>
    <automated>! grep -q '#ff8b8b' src/styles/components/_compatibility.scss && grep -q 'stagehand-danger' src/styles/_tokens.scss</automated>
  </verify>
  <done>src/styles/_tokens.scss declares $stagehand-danger/--stagehand-danger following the existing five-color pattern, and .compat-status--unsupported references var(--stagehand-danger) instead of the bare hex literal.</done>
  <acceptance_criteria>
    - `src/styles/_tokens.scss` contains both `$stagehand-danger: #ff8b8b;` and `--stagehand-danger: #{$stagehand-danger};`, positioned immediately after the existing warning-color lines.
    - `src/styles/components/_compatibility.scss` no longer contains the literal `#ff8b8b`.
    - `npm run lint` (stylelint) passes on both changed files.
    - The rendered computed color for `.compat-status--unsupported` is still `#ff8b8b` (verified by the CSS custom property resolving to the same hex value — no visual regression).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Land DRIFT-04's three design-spec sentence amendments</name>
  <files>docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md</files>
  <read_first>
    docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md (lines 18-22, 75-79, 267-270),
    docs/adr/0001-compatibility-scaffold.md (the "design specification's delivery boundary is amended
    accordingly" sentence — the source of the exact replacement language for the second amendment)
  </read_first>
  <action>
    In `docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md`, make exactly three amendments,
    using the exact replacement language 04-UI-SPEC.md's Copywriting Contract table already drafted and
    cross-checked against ADR-0001's own wording:

    1. Lines 20-21 currently read "Product tiers and compatibility claims are customer-facing and generated
    from schema-validated structured data." Replace with: "Product tiers are customer-facing and generated
    from schema-validated structured data. Compatibility claims follow the same schema-validated pipeline
    once they complete evidence review (ADR-0001); until then, the register renders its supported empty
    state."

    2. Line 77 currently reads "The initial scaffold includes representative content for every route. Bulk
    migration from `puppet-console/docs`..." (the sentence continues onto the next line unchanged). Replace
    only the first sentence with: "The initial scaffold includes representative content for every route;
    for the compatibility register specifically, representative data means representative fixture data
    exercised by the test suite, together with a rendered empty state on the published site — it does not
    mean seeded customer-facing records. Bulk migration from `puppet-console/docs`..." (keep the existing
    second sentence and everything after it unchanged).

    3. Line 269 currently reads "representative content and compatibility data, validation tests, GitHub
    Actions" as one item in the delivery-boundary list. Replace "compatibility data" with "compatibility
    test fixtures" in that line only, so it reads "representative content and compatibility test fixtures,
    validation tests, GitHub Actions" — leave every other word on the line unchanged.

    Make no other change to this document — DRIFT-04 is exactly these three sentences.
  </action>
  <verify>
    <automated>! grep -q 'Product tiers and compatibility claims are customer-facing' docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md && grep -q 'ADR-0001' docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md && ! grep -q 'representative content and compatibility data,' docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md</automated>
  </verify>
  <done>All three DRIFT-04 sentences are amended in docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md to match ADR-0001's delivery boundary, and no other content in the document changed.</done>
  <acceptance_criteria>
    - The document no longer contains the literal sentence "Product tiers and compatibility claims are customer-facing and generated from schema-validated structured data."
    - The document's delivery-boundary list item no longer reads "representative content and compatibility data," — it now reads "representative content and compatibility test fixtures,".
    - The "representative content for every route" sentence now explicitly distinguishes fixture data from seeded customer-facing records.
    - `git diff docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md` shows exactly three changed regions, nothing else in the file touched.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Design-spec prose -> reader's understanding of what the delivery boundary promises | A stale sentence that reads as promising seeded customer-facing compatibility records could be cited to justify weakening ADR-0001 rule 1 (LOCKED) under future pressure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Repudiation | Design-spec prose vs. ADR-0001's locked decision | low | mitigate | This plan amends the three specific sentences ADR-0001 and DRIFT-04 name, using ADR-0001's own already-reviewed replacement language verbatim where specified, so the spec and the locked ADR can no longer be read as contradicting each other. |
</threat_model>

<verification>
- `npm run lint` passes on the two changed SCSS files.
- The design spec's three DRIFT-04 sentences are amended exactly as specified; `git diff` shows no other change to the file.
</verification>

<success_criteria>
Every sentence in the design specification about "representative content" and compatibility-claim
generation agrees with ADR-0001's delivery boundary, and the "unsupported" status color is a named design
token instead of a bare hex literal, with zero visual change.
</success_criteria>

## Artifacts This Plan Produces

- `src/styles/_tokens.scss` — `$stagehand-danger` / `--stagehand-danger`.
- `src/styles/components/_compatibility.scss` — `.compat-status--unsupported` now references the token.
- `docs/superpowers/specs/2026-08-22-stagehand-docs-site-design.md` — three DRIFT-04 sentence amendments.

<output>
Create `.planning/phases/04-evidence-bearing-compatibility-register/04-05-SUMMARY.md` when done
</output>
