---
phase: 04-evidence-bearing-compatibility-register
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/json-endpoints.test.ts
  - tests/e2e/production-empty.spec.ts
autonomous: true
requirements: [COMP-02, COMP-05]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "tests/unit/json-endpoints.test.ts no longer hard-codes 'records: []' for GET /data/compatibility.json — it compares structurally against whatever loadCompatibility() actually returns, so it stays correct whether the published register is empty or holds real, reviewed records."
    - "tests/e2e/production-empty.spec.ts branches on the real production JSON response's records.length instead of assuming it is always 0, so the test corpus never forces a future contributor to weaken it under time pressure just to add the first real record."
    - "CompatibilityEmptyState.astro's copy and markup are untouched — this plan only corrects test assumptions, never the empty-state component itself, per the UI-SPEC's explicit instruction that the empty state is a permanently-supported rendering, not code being migrated away from."
    - "/data/compatibility.json still returns generated_at: null and schema_version: 1 regardless of records.length — proven generically, not by re-asserting the specific empty array."
  artifacts: []
  key_links:
    - "tests/unit/json-endpoints.test.ts imports loadCompatibility() from src/lib/data/compatibility.ts as its comparison source of truth, the same function src/pages/data/compatibility.json.ts's GET handler calls"
    - "tests/e2e/production-empty.spec.ts fetches /data/compatibility.json first (via page.request.get) to determine which branch's assertions to run, before asserting anything about the rendered page"
---

<objective>
Correct the two hard-coded "the production compatibility register is always empty" test assertions that
04-RESEARCH.md's Pitfall 1 identifies living outside COMP-03's named files — `tests/unit/json-endpoints.test.ts`
and `tests/e2e/production-empty.spec.ts` — so neither one silently breaks, and neither one creates pressure
to weaken validation, the instant a real reviewed compatibility record is added in a future change.

Purpose: RESEARCH.md verified these two files test the *real* `src/data/compatibility.yaml` (not a
fixture) and both encode the same stale assumption COMP-03 is deliberately retiring elsewhere. Neither is
named in COMP-03's own `required_reading` list, so without this plan they would remain silently broken
land mines. Per the phase's open question, this plan does not assume the register stays empty or becomes
populated by phase close — both branches must be correct.
Output: two rewritten test files that assert the correct structural contract (schema_version, generated_at,
content-type, byte-for-byte JSON shape for whatever `loadCompatibility()` returns) rather than a specific
record count, plus a branched e2e assertion that still fully exercises the empty-state UI when the
register is empty (today's actual state) without hard-coding that it always will be.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structurally compare tests/unit/json-endpoints.test.ts against loadCompatibility() instead of a literal empty array</name>
  <files>tests/unit/json-endpoints.test.ts</files>
  <read_first>
    tests/unit/json-endpoints.test.ts, src/pages/data/compatibility.json.ts, src/lib/data/compatibility.ts
  </read_first>
  <action>
    In `tests/unit/json-endpoints.test.ts`, add `import { loadCompatibility } from
    '../../src/lib/data/compatibility';` alongside the existing `getCompatibility`/`getTiers` imports. In
    the `describe('GET /data/compatibility.json', ...)` block, replace the existing test's body: keep the
    same response/status/content-type/trailing-newline assertions unchanged, but replace the hard-coded
    `records: []` in the expected JSON string with `records: loadCompatibility()` — call
    `loadCompatibility()` once at the top of the test (`const expectedRecords = loadCompatibility();`) and
    build the expected string as
    `` `${JSON.stringify({ schema_version: 1, generated_at: null, records: expectedRecords }, null, 2)}\n` ``,
    mirroring the existing `GET /data/tiers.json` test's exact structure directly above it in the same
    file (which already compares against `expectedTiers`, a real, non-empty, hand-maintained array — this
    test now follows that same pattern, comparing against the real loader's output instead of a hand-typed
    literal). Rename the test from "returns an honest empty production registry in the deterministic
    download contract" to "returns the real compatibility registry in the deterministic download
    contract", and update its leading comment from "Catches invented compatibility claims, build
    timestamps, wrong MIME type, or unstable bytes." to "Catches a wrapper response, a build timestamp, or
    a mismatch between the JSON endpoint and loadCompatibility()'s own output — not just the register
    being empty."
  </action>
  <verify>
    <automated>npx vitest run tests/unit/json-endpoints.test.ts</automated>
  </verify>
  <done>tests/unit/json-endpoints.test.ts's compatibility test compares against loadCompatibility()'s live output, passes against today's empty register, and would keep passing unchanged if a real record were added.</done>
  <acceptance_criteria>
    - `tests/unit/json-endpoints.test.ts` no longer contains the literal string `records: []` for the compatibility test.
    - The test still asserts `response.status === 200`, the exact content-type header, and the trailing-newline contract.
    - `npx vitest run tests/unit/json-endpoints.test.ts` passes against the current (empty) `src/data/compatibility.yaml`.
    - The `GET /data/tiers.json` test above it is unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Branch tests/e2e/production-empty.spec.ts on the real JSON response instead of assuming an empty register</name>
  <files>tests/e2e/production-empty.spec.ts</files>
  <read_first>
    tests/e2e/production-empty.spec.ts, src/components/CompatibilityEmptyState.astro,
    src/pages/compatibility/index.astro
  </read_first>
  <action>
    In `tests/e2e/production-empty.spec.ts`, rewrite the first test: fetch `const response = await
    page.request.get('/data/compatibility.json'); const body = await response.json();` before navigating,
    then `await page.goto('/compatibility/');`. If `body.records.length === 0`, run exactly the existing
    empty-state assertions unchanged (the "Verification queue is empty" heading, the "No compatibility
    claims..." text, the "contact support" link's `/support/` href, zero
    `[data-compatibility-matrix]` elements). If `body.records.length > 0`, instead assert
    `page.locator('[data-compatibility-matrix]')` has count 1 and `page.locator('[data-record-id]')` has
    count equal to `body.records.length` — do not assert any specific record content here (that is
    COMP-04/GATE-04's job in `fixture-matrix-scale.spec.ts`, not this route-level smoke test). After the
    branch, keep a structural assertion that holds regardless of branch: `expect(body).toEqual({
    schema_version: 1, generated_at: null, records: body.records });` — this proves `schema_version` and
    `generated_at` stay correct independent of record count, without hard-coding the count. Rename the test
    from "shows the exact empty state and support path" to "reflects the real production register's
    empty-or-populated state".

    Rewrite the second test ("...no serious or critical axe violations"): fetch the same
    `/data/compatibility.json` response first, then `page.goto('/compatibility/')`, then branch the same
    way — if `body.records.length === 0`, assert the "Verification queue is empty" heading is visible
    (unchanged); if `body.records.length > 0`, assert `[data-compatibility-matrix]` has count 1 instead.
    Run the existing `AxeBuilder` scan (unchanged tag set) and assert zero serious/critical violations in
    both branches — this proves the production route stays accessible whichever state it is actually in.

    Do not touch `src/components/CompatibilityEmptyState.astro` or
    `src/pages/compatibility/index.astro` in this task — both are correct and unchanged; only the test's
    assumption about which branch always runs is being corrected.
  </action>
  <verify>
    <automated>npm run build:e2e && env -u NO_COLOR playwright test tests/e2e/production-empty.spec.ts --project=production</automated>
  </verify>
  <done>tests/e2e/production-empty.spec.ts's two tests both branch on the real JSON response's records.length, both pass against today's empty register, and CompatibilityEmptyState.astro is unchanged.</done>
  <acceptance_criteria>
    - Both tests in `tests/e2e/production-empty.spec.ts` fetch `/data/compatibility.json` before branching their assertions.
    - Both tests pass against the current (empty) production register, exercising the empty-state branch exactly as the original test did.
    - `git diff src/components/CompatibilityEmptyState.astro src/pages/compatibility/index.astro` is empty (no changes to either file).
    - `env -u NO_COLOR playwright test tests/e2e/production-empty.spec.ts --project=production` passes both tests.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-corpus assumptions -> ADR-0001 rule 3 (validation is never weakened under time pressure) | A hard-coded "always empty" test assertion is exactly the kind of pressure ADR-0001 rule 3 warns against: a future contributor adding the first real record would otherwise face a failing `npm run verify` and be tempted to weaken a check to make it pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | `tests/unit/json-endpoints.test.ts` / `tests/e2e/production-empty.spec.ts` hard-coded empty assumptions | high | mitigate | Both files are rewritten in this plan to compare structurally (json-endpoints.test.ts) or branch on the real response (production-empty.spec.ts) instead of hard-coding an empty array, directly closing RESEARCH.md's Pitfall 1 before it can pressure a future contributor into weakening validation. |
</threat_model>

<verification>
- `npx vitest run tests/unit/json-endpoints.test.ts` passes.
- `env -u NO_COLOR playwright test tests/e2e/production-empty.spec.ts --project=production` passes both tests.
- `git diff src/components/CompatibilityEmptyState.astro src/pages/compatibility/index.astro` is empty.
</verification>

<success_criteria>
Neither test file assumes the published compatibility register is permanently empty; both remain correct
and green whether it stays empty or a real reviewed record is added in any future change, with zero change
to the empty-state component itself.
</success_criteria>

## Artifacts This Plan Produces

- `tests/unit/json-endpoints.test.ts` — compatibility test rewritten to compare against `loadCompatibility()`.
- `tests/e2e/production-empty.spec.ts` — both tests rewritten to branch on the real JSON response's `records.length`.

<output>
Create `.planning/phases/04-evidence-bearing-compatibility-register/04-04-SUMMARY.md` when done
</output>
