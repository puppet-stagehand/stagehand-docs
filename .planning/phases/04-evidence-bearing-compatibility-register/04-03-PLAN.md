---
phase: 04-evidence-bearing-compatibility-register
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/e2e/fixture-matrix-scale.spec.ts
autonomous: true
requirements: [GATE-04, COMP-04]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Filtering the 27-record scale fixture by Platform, Customer tier, and Support status each produces a distinct, correct 'Showing N of 27 verified records' count — proving the filter logic and the live-region result count are correct at a volume no prior test reached, not just at 5 records."
    - "Every discrete control (select, Clear filters button, evidence/docs link) in the scale-volume render keeps its 44px minimum touch target, extending the existing fixture-matrix.spec.ts assertion to 27 records."
    - "Tab order through the filter controls into the first record's evidence and docs links is linear and reaches real, focusable anchor elements at scale — no keyboard trap introduced by the higher record count."
    - "The unusually long platform name (record scale-11/scale-23, 'AWS Outposts Rack...') and the one 60+-character limitations sentence (record scale-07) both render their full text without truncation, in both the table and card layouts."
    - "The matrix still renders correctly with JavaScript disabled at scale (all 27 rows visible, filtering degrades to 'show everything')."
  artifacts: []
  key_links:
    - "tests/e2e/fixture-matrix-scale.spec.ts (extended) -> the fixture-matrix-scale Playwright project (04-01) -> .scale-dist/ built from tests/fixtures/data/compatibility-scale.yaml (04-01)"
---

<objective>
Complete GATE-04's realistic-volume proof by extending `tests/e2e/fixture-matrix-scale.spec.ts` (created
minimally in 04-01) with the remaining assertions the UI-SPEC's Realistic-Volume Fixture Requirement
names: filter correctness at multiple N/M pairs, 44px touch targets at volume, keyboard tab-order
linearity, the long-text wrap backstops, and no-JS parity — all of it proven against the real 27-record
scale fixture 04-01 built and wired.

Purpose: 04-01 proved the pipeline exists and the matrix renders + passes axe at volume; this plan proves
the specific interaction and accessibility behaviors the UI-SPEC calls out as previously untested:
"today's 5-record fixture produces at most 5 platform options... a realistic-volume fixture will grow the
platform filter's option count meaningfully," and the wrap/keyboard backstops that "visually verify once
the realistic-volume fixture is authored."
Output: `tests/e2e/fixture-matrix-scale.spec.ts` grown from 2 tests to a complete GATE-04 test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-evidence-bearing-compatibility-register/04-UI-SPEC.md
@.planning/phases/04-evidence-bearing-compatibility-register/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filter-correctness, 44px touch-target, and no-JS parity tests at scale</name>
  <files>tests/e2e/fixture-matrix-scale.spec.ts</files>
  <read_first>
    tests/e2e/fixture-matrix-scale.spec.ts (the 2 tests 04-01 created — extend, do not replace),
    tests/e2e/fixture-matrix.spec.ts (the exact patterns to mirror: filter selectOption/Clear filters
    flow, the discreteControls 44px loop, the javaScriptEnabled:false no-JS test),
    tests/fixtures/data/compatibility-scale.yaml (confirm the exact record composition: platform "AWS
    EC2" appears at index 0/12/24 = 3 records; tier "pe-advanced" appears at index 3/7/11/15/19/23 = 6
    records; status "unsupported" appears at index 4/9/14/19/24 = 5 records)
  </read_first>
  <action>
    Add a new test "filtering at scale reflects realistic result counts across platform, tier, and
    status": `page.goto('/compatibility/')`, assert `#compat-results-count` contains "Showing 27 of 27
    verified records"; select Platform "AWS EC2", assert "Showing 3 of 27 verified records"; click "Clear
    filters", select Customer tier "pe-advanced" (via its underlying value `pe-advanced`), assert "Showing
    6 of 27 verified records"; click "Clear filters", select Support status "unsupported", assert "Showing
    5 of 27 verified records"; click "Clear filters" once more, assert "Showing 27 of 27 verified records"
    again — mirror `fixture-matrix.spec.ts`'s exact `page.getByLabel(...).selectOption(...)` and `page.getByRole('button',
    { name: 'Clear filters' }).click()` calls.

    Add a new test "every discrete control keeps its 44px minimum touch target at scale": set viewport
    1280x800, `page.goto('/compatibility/')`, locate `.compat-filters select, .compat-filters button,
    .compat-card__footer a` (same selector `fixture-matrix.spec.ts` uses), and assert every control's
    bounding box height is `>= 44` — identical loop, now exercised over 27 records' worth of rendered
    footer links instead of 5.

    Add a new test "the scale-volume matrix remains readable when JavaScript is unavailable": mirror
    `fixture-matrix.spec.ts`'s `browser.newContext({ baseURL: 'http://127.0.0.1:4323', javaScriptEnabled:
    false, viewport: { width: 1280, height: 800 } })` pattern, `page.goto('/compatibility/')`, assert
    `.compat-table tbody tr` has count 27, and assert the row containing `ID / scale-00-openvox-supported`
    is visible (proves the table renders fully server-side with the client-side filter script inert).
  </action>
  <verify>
    <automated>npm run build:e2e && env -u NO_COLOR playwright test tests/e2e/fixture-matrix-scale.spec.ts --project=fixture-matrix-scale</automated>
  </verify>
  <done>tests/e2e/fixture-matrix-scale.spec.ts has 5 total tests (the 2 from 04-01 plus these 3), all passing against the real scale build.</done>
  <acceptance_criteria>
    - The filter test asserts all three N/M pairs (3 of 27, 6 of 27, 5 of 27) and returns to 27 of 27 after each Clear filters click.
    - The 44px test asserts every matched control's bounding box height is `>= 44` with no exceptions.
    - The no-JS test asserts exactly 27 visible table rows with `javaScriptEnabled: false`.
    - All 5 tests in the file pass via `env -u NO_COLOR playwright test tests/e2e/fixture-matrix-scale.spec.ts --project=fixture-matrix-scale`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add keyboard tab-order linearity and long-text wrap backstop tests at scale</name>
  <files>tests/e2e/fixture-matrix-scale.spec.ts</files>
  <read_first>
    tests/e2e/fixture-matrix-scale.spec.ts (Task 1's version — extend further),
    src/components/CompatibilityMatrix.astro (the filter form's control order: Platform select, Customer
    tier select, Support status select, then the Clear filters button lives before the controls in DOM
    order — confirm the real tab sequence from the rendered form before writing assertions),
    tests/fixtures/data/compatibility-scale.yaml (record `scale-07-pe-advanced-limited`'s exact 60+-char
    limitations sentence, and record `scale-11-pe-advanced-compatible`'s exact long platform name "AWS
    Outposts Rack (Customer-Managed On-Premises Extension of Amazon EC2)")
  </read_first>
  <action>
    Add a new test "keyboard tab order through the filters reaches the first record's links in sequence at
    scale": set viewport 1280x800, `page.goto('/compatibility/')`, `await
    page.getByLabel('Platform').focus()`, then press Tab and assert `page.getByLabel('Customer tier')` is
    focused, press Tab and assert `page.getByLabel('Support status')` is focused, press Tab and assert
    `page.getByRole('button', { name: 'Clear filters' })` is focused, press Tab and assert
    `page.getByRole('link', { name: 'Primary evidence' }).first()` is focused, press Tab and assert
    `page.getByRole('link', { name: 'Platform guidance' }).first()` is focused — confirm the actual DOM
    order in `CompatibilityMatrix.astro` matches this sequence before writing the assertions; if the real
    order differs (e.g. Clear filters precedes the selects), write the test to match the real order rather
    than this assumed one, since the goal is proving the real order is linear and trap-free, not asserting
    a specific order.

    Add a new test "long limitation text and an unusually long platform name render without truncation at
    scale": set viewport 1280x800, `page.goto('/compatibility/')`, assert
    `page.getByText('This fixture record intentionally carries a limitations sentence long enough to
    exercise the wrap backstop check across both the table cell and the card layout at realistic content
    volume.')` is visible, and assert `page.getByText('AWS Outposts Rack (Customer-Managed On-Premises
    Extension of Amazon EC2)').first()` is visible; then set viewport 320x720 and repeat both assertions —
    proving both the table (desktop) and card (mobile) layouts render the full, untruncated text at each
    breakpoint.
  </action>
  <verify>
    <automated>npm run build:e2e && env -u NO_COLOR playwright test tests/e2e/fixture-matrix-scale.spec.ts --project=fixture-matrix-scale</automated>
  </verify>
  <done>tests/e2e/fixture-matrix-scale.spec.ts has 7 total tests, all passing, covering record count, axe, filter correctness, 44px targets, no-JS parity, keyboard tab-order linearity, and long-text wrap backstops.</done>
  <acceptance_criteria>
    - The keyboard test asserts a real, verified DOM tab sequence (not an assumed one) reaching both of the first record's links.
    - The wrap-backstop test asserts both the long limitation sentence and the long platform name are visible, full-text, at both 1280px and 320px viewports.
    - All 7 tests in `tests/e2e/fixture-matrix-scale.spec.ts` pass via the same `--project=fixture-matrix-scale` command.
    - `env -u NO_COLOR playwright test tests/e2e/fixture-matrix.spec.ts --project=fixture-matrix` still passes unchanged (no cross-contamination from this plan's changes to the sibling spec file).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Keyboard/DOM tab order at 27 records vs. 5 | A higher record count changing the DOM order of interactive elements could silently introduce a keyboard trap that the 5-record fixture-matrix.spec.ts would never surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Denial of Service (accessibility) | Keyboard tab order through the populated matrix at volume | medium | mitigate | Task 2's tab-order test asserts real, verified focus sequence reaches the first record's two links without a trap; combined with 04-01's axe scan (which flags most keyboard-accessibility violations independently), this closes the specific gap GATE-04 names — the populated matrix had never been keyboard-tested at scale before this phase. |
| T-04-06 | Tampering | `playwright.config.ts` project routing (inherited from 04-01) | low | accept | This plan only adds test cases to an existing, already-isolated spec file; it does not touch `playwright.config.ts`, so the cross-project isolation risk (RESEARCH.md Pitfall 5) was already fully mitigated in 04-01 and is re-verified here by re-running the sibling `fixture-matrix` project's suite unchanged. |
</threat_model>

<verification>
- `env -u NO_COLOR playwright test tests/e2e/fixture-matrix-scale.spec.ts --project=fixture-matrix-scale` — all 7 tests pass.
- `env -u NO_COLOR playwright test tests/e2e/fixture-matrix.spec.ts --project=fixture-matrix` — unchanged, still passes.
</verification>

<success_criteria>
GATE-04 is fully satisfied: the populated compatibility matrix's layout, filter correctness, touch
targets, keyboard operability, long-text handling, no-JS readability, and accessibility are all proven at
a realistic 27-record volume, not assumed from the 5-record ADR-0001 fixture.
</success_criteria>

## Artifacts This Plan Produces

- `tests/e2e/fixture-matrix-scale.spec.ts` — grown from 04-01's 2 tests to 7: record-count render, axe
  scan, filter N/M correctness, 44px touch targets, no-JS parity, keyboard tab-order linearity, and
  long-text wrap backstops.

<output>
Create `.planning/phases/04-evidence-bearing-compatibility-register/04-03-SUMMARY.md` when done
</output>
