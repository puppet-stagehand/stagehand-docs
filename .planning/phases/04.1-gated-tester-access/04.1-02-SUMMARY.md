---
phase: 04.1-gated-tester-access
plan: 02
subsystem: infra
tags: [cloudfront-functions, cloudfront-kvs, opentofu, iam, halted]

# Dependency graph
requires:
  - phase: 04.1-gated-tester-access (04.1-01)
    provides: "the KVS container resource (kvs.tf), gate branch in redirect.js, gated-paths.json,
      and tester_gate_kvs_arn module output this plan attempted to apply for real"
provides:
  - "Confirmed, real root-cause diagnosis: the stagehand-bootstrap-operator IAM identity used for
    all prior real applies in this project (Phase 2, Phase 5) lacks cloudfront:CreateKeyValueStore
    — a permission the six existing ADR-0003 CI roles and this operator's own out-of-band IAM
    policy do not currently grant, matching Open Question #4 and STRIDE T-04.1-07 flagged in
    04.1-RESEARCH.md"
  - "A generated shared tester password, reported to the orchestrator via session narration only
    (never written to any tracked file) — ready to seed into KVS once the IAM gap is resolved"
affects: [04.1-02 (this plan, must be resumed after the IAM grant), 04.1-03, 04.1-04]

# Actuals (#2632)
actuals:
  tokens: 400
  tasks: 1
  commits: 1

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Real-AWS blocker discovered mid-apply is haltled immediately with a clean working tree
      (transient publish=false edit reverted, speculative output addition reverted) rather than
      left half-applied or worked around by self-escalating IAM permissions — matching this
      plan's own STRIDE T-04.1-07 disposition and the project's established halt-and-record
      pattern (05-08-SUMMARY.md)."

key-files:
  created: []
  modified: []

key-decisions:
  - "Did not attempt to self-grant or work around the missing cloudfront:CreateKeyValueStore IAM
    permission (e.g. by authenticating a different, more-privileged AWS SSO profile available on
    this machine). The plan's own threat register (T-04.1-07) explicitly pre-authorizes only one
    response to this exact scenario: halt and surface a checkpoint, consistent with ADR-0003's
    'bootstrap stays human-applied' pattern. Attempting privilege escalation unilaterally would
    have violated that disposition."
  - "Derived TF_VAR_hosted_zone_id and TF_VAR_github_oidc_provider_arn directly from live AWS
    (route53 list-hosted-zones, iam list-open-id-connect-providers), matching 05-01's precedent,
    since infra/bootstrap has no local Terraform state in this working tree."
  - "Reconstructed the exact currently-live TF_VAR_basic_auth_username/TF_VAR_basic_auth_password
    values from the real deployed function code (aws cloudfront get-function --stage LIVE) rather
    than leaving them at the variables' empty-string defaults, specifically to guarantee the
    targeted apply would not accidentally rotate the pre-existing whole-site enable_basic_auth
    credential as an unintended side effect of recompiling the shared function's code — this
    would have violated D-06's 'these are separate, do not conflate' requirement even though the
    apply itself failed before reaching that resource."
  - "Reverted both the transient publish=false spike edit in cloudfront.tf and the tester_gate_kvs_arn
    output addition in testpilots/main.tf before halting, so the working tree returns to exactly
    its pre-plan state (git status clean under infra/) for whichever agent resumes this plan."

patterns-established: []

requirements-completed: []

duration: ~20min active execution before halt
completed: 2026-08-28
status: halted
---

# Phase 04.1 Plan 02: Gated Tester Access — Live KVS Apply and Rotation Runbook (HALTED — Missing IAM Permission) Summary

**Task 1's password was generated and is ready; Task 2's real testpilots apply failed with
`AccessDenied: cloudfront:CreateKeyValueStore` under the project's established `stagehand-bootstrap-operator`
identity — halted per this plan's own pre-authorized STRIDE T-04.1-07 disposition, with the working
tree left clean (zero net diff) rather than half-applied.**

## Performance

- **Duration:** ~20 min active execution before halt
- **Started:** 2026-08-28 (this session)
- **Halted:** 2026-08-28, mid-Task-2 (targeted `tofu apply` failed with AccessDenied)
- **Tasks:** 1 of 3 resolved (Task 1, the checkpoint); Task 2 attempted and halted before completion; Task 3 not started
- **Files modified:** 1 (this SUMMARY.md — the halt record itself; no infra or docs files remain changed, see below)

## Accomplishments

- **Task 1 (checkpoint) resolved per the orchestrator's pre-relayed instruction**: generated a
  cryptographically random 30-character shared tester password (`openssl rand -base64 24`,
  stripped of `=+/`), computed its precomputed `"Basic " + base64(":<password>")` expected-header
  form, and staged both in a session-scoped scratchpad env file (never written to any tracked
  file or committed) for identical use across testpilots/beta/stable per the "single shared
  password" framing. The generated password is reported to the orchestrator via this session's
  own narration text only, per the checkpoint resolution's explicit instruction — never in this
  file or any other tracked artifact.
- **Re-verified the non-root apply identity** before any AWS mutation: the project's default AWS
  CLI profile had an expired SSO session (unrelated to this project, matching 05-01's exact
  precedent); switched to the established `stagehand-bootstrap` credential profile and confirmed
  `arn:aws:iam::503561411317:user/stagehand-bootstrap-operator` — non-root, matching every prior
  real apply in this repository (Phase 2, 05-01, 05-05, 05-08).
- Added the `tester_gate_kvs_arn` output to `infra/environments/testpilots/main.tf` (same one-line
  shape as the existing outputs), then reviewed a **targeted** `tofu plan` showing exactly the
  expected diff (1 to add — the KVS store; 1 to change — the function's `publish` flag and its new
  `key_value_store_associations`) before applying.
- Ran the targeted apply
  (`-target=module.site.aws_cloudfront_key_value_store.tester_gate -target=module.site.aws_cloudfront_function.redirect`)
  with `publish` transiently set to `false`, per the plan's spike-before-publish sequencing. The
  apply failed cleanly at the KVS-store creation step:
  `AccessDenied: User: arn:aws:iam::503561411317:user/stagehand-bootstrap-operator is not
  authorized to perform: cloudfront:CreateKeyValueStore on resource:
  arn:aws:cloudfront::503561411317:key-value-store/*`. Terraform's dependency graph meant the
  function resource was never touched — `aws cloudfront describe-function --stage LIVE` afterward
  confirmed no change to the live, currently-serving function.
- Confirmed this is the exact scenario `04.1-RESEARCH.md`'s Open Question #4 and this plan's own
  `<threat_model>` entry T-04.1-07 anticipated and pre-authorized a single response to: halt and
  surface a checkpoint, not self-remediate. Did not attempt to authenticate a different, more
  privileged AWS SSO profile available on this machine (`soup` / `PuppetSoupAdministrator`, same
  AWS account) to work around the gap — that would have been an unauthorized privilege escalation
  relative to the plan's explicit disposition and ADR-0003's "bootstrap stays human-applied"
  pattern, and `aws sso login` itself requires an interactive browser flow this executor cannot
  complete unattended regardless.
- **Cleaned up before halting**: reverted `cloudfront.tf`'s transient `publish = false` edit back
  to `true` (confirmed `git diff` on that file is empty) and reverted the `tester_gate_kvs_arn`
  output addition in `testpilots/main.tf` via `git checkout --`, so the working tree is byte-identical
  to its pre-plan state under `infra/` — confirmed via `git status --short infra/` showing no output.

## Task Commits

1. **Task 1: Choose the shared tester password** — no commit (checkpoint resolution produces a
   runtime value only, per the plan's own prohibition on writing it to any tracked file).
2. **Task 2: Confirm the KVS runtime API against testpilots, then apply and verify for real** —
   attempted, halted before completion. No commit (all file edits made during the attempt were
   reverted before halting; the working tree carries zero net diff from this task).
3. **Task 3: Write the rotation runbook and the AUTH-04 exclusion record** — not started. Its
   content depends on describing a live, applied KVS store; starting it before Task 2's real
   apply succeeds risks documenting a runbook against infrastructure that does not yet exist.

**Plan metadata:** this SUMMARY's commit, made immediately after this file, is the only commit
this plan run produced.

## Files Created/Modified

- `.planning/phases/04.1-gated-tester-access/04.1-02-SUMMARY.md` — this halt record.

**Explicitly NOT left modified** (reverted before halting, confirmed via `git status --short infra/`
showing no output): `infra/modules/static-site/cloudfront.tf` (transient `publish=false` spike
edit), `infra/environments/testpilots/main.tf` (the `tester_gate_kvs_arn` output addition).
`docs/operations/tester-access.md` and `scripts/serve-static-build.ts` (Task 3's scope) were never
touched.

## Decisions Made

See `key-decisions` in frontmatter: (1) no self-granted IAM escalation, halt per T-04.1-07's
pre-authorized disposition; (2) `hosted_zone_id`/`github_oidc_provider_arn` derived from live AWS,
matching 05-01's precedent; (3) the pre-existing whole-site `basic_auth_username`/`basic_auth_password`
values were reconstructed from the real deployed function code (not left at empty-string variable
defaults) specifically so the targeted apply could not accidentally rotate that separate credential
as a side effect — this was verified moot once the apply failed before reaching the function
resource, but was the correct precaution to take before attempting the apply; (4) the working tree
was fully reverted before halting rather than leaving partial, unverifiable infra edits in place.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking, then Rule 4 - Architectural] Missing `cloudfront:CreateKeyValueStore` IAM permission on the established apply identity**
- **Found during:** Task 2, first targeted `tofu apply`.
- **Issue:** `stagehand-bootstrap-operator` (the identity every prior real apply in this project
  has used — Phase 2, 05-01, 05-05, 05-08) has no identity-based policy granting
  `cloudfront:CreateKeyValueStore`. The plan's own `<threat_model>` (T-04.1-07) and
  `04.1-RESEARCH.md`'s Open Question #4 both anticipated this exact possibility.
- **Fix:** Not auto-fixed. Granting a new IAM permission to a real operator identity in a real AWS
  account is a security-sensitive, human-decidable change — explicitly outside Rule 1-3's
  auto-fix scope and matching Rule 4 (architectural). The plan's own threat register additionally
  pre-authorizes only one response to this specific gap: halt with a checkpoint. Did not attempt
  any workaround (including authenticating a separate, more-privileged AWS SSO profile available
  on this machine).
- **Files modified:** None (the attempted edits were reverted, not committed).
- **Verification:** `aws cloudfront describe-function --stage LIVE` after the failed apply
  confirmed the live, currently-serving function was untouched; `git status --short infra/` after
  reverting confirmed zero net diff.
- **Committed in:** Not committed — surfaced as a checkpoint instead (see below).

---

**Total deviations:** 1 (Rule 3/4 boundary — blocking issue correctly routed to a human decision,
not auto-fixed)
**Impact on plan:** Task 2 could not complete; Task 3 was not attempted since it depends on Task
2's real, applied KVS store. No incorrect or unverified infrastructure was left in place.

## Issues Encountered

- The default AWS CLI profile's cached SSO session was expired and unrelated to this project
  (matches 05-01's exact precedent) — resolved by switching to the established
  `stagehand-bootstrap` credential profile, no plan impact.
- `cloudfront:CreateKeyValueStore` AccessDenied under `stagehand-bootstrap-operator` — see
  Deviations above. This is the plan-halting issue.

## User Setup Required

**An IAM policy grant on the real AWS account is required before this plan can resume.** The
`stagehand-bootstrap-operator` IAM user (account `503561411317`) needs, at minimum:

- Control-plane: `cloudfront:CreateKeyValueStore`, `cloudfront:DeleteKeyValueStore`,
  `cloudfront:DescribeKeyValueStore`, `cloudfront:UpdateKeyValueStore`, `cloudfront:TagResource`
  (scoped to `arn:aws:cloudfront::503561411317:key-value-store/*`, matching this project's existing
  resource-scoped IAM patterns) — needed for the `tofu apply` half of Task 2.
- Data-plane: `cloudfront-keyvaluestore:DescribeKeyValueStore`, `cloudfront-keyvaluestore:PutKey`,
  `cloudfront-keyvaluestore:GetKey` — needed for the `aws cloudfront-keyvaluestore put-key`
  credential-seeding half of Task 2 and every future rotation in Task 3's runbook.
- `cloudfront:TestFunction` and `cloudfront:DescribeFunction` (if not already granted) — needed for
  the `aws cloudfront test-function` KVS-API-confirmation spike.

Once granted, resume this plan from Task 2 using the already-generated password reported in this
session's narration (or generate a fresh one — the prior value was never persisted anywhere
retrievable, by design).

## Next Phase Readiness

- **04.1-02 must be resumed, not treated as complete.** Its own real, applied KVS/gate deliverable
  (the plan's actual success criteria) does not exist yet on any of testpilots/beta/stable.
- 04.1-03 and 04.1-04 (if either depends on a live tester-gate credential existing) are blocked
  until this plan resumes and completes for real.
- 04.1-01's code-layer work (redirect.js gate branch, kvs.tf container, gated-paths.json) remains
  fully valid and untouched — nothing about the IAM gap invalidates it; it is exactly what Task 2
  will apply once the permission grant lands.

---
*Phase: 04.1-gated-tester-access*
*Completed: 2026-08-28*
