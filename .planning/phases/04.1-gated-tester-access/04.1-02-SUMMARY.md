---
phase: 04.1-gated-tester-access
plan: 02
subsystem: infra
tags: [cloudfront-functions, cloudfront-kvs, opentofu, iam, tester-access]

# Dependency graph
requires:
  - phase: 04.1-gated-tester-access (04.1-01)
    provides: "the KVS container resource (kvs.tf), gate branch in redirect.js, gated-paths.json,
      and tester_gate_kvs_arn module output this plan applied for real"
provides:
  - "A live, applied KeyValueStore-backed tester-access gate on testpilots, beta, and stable —
    KVS store created, function republished with the KVS association, and the shared credential
    seeded into all three stores"
  - "The real CloudFront Functions cf.kvs() runtime API confirmed against a live, unpublished
    DEVELOPMENT-stage function via aws cloudfront test-function, matching Plan 04.1-01's mocked
    assumption exactly — no redirect.js or test-mock correction was needed"
  - "docs/operations/tester-access.md — the rotation runbook, with an explicit, visually separate
    section stating the pre-existing whole-site enable_basic_auth lockdown is a completely
    separate credential (D-06)"
  - "The AUTH-04 record: a discoverable comment in scripts/serve-static-build.ts explaining why
    local tooling never simulates the edge gate, cross-referencing the runbook"
affects: [04.1-03, 04.1-04]

# Actuals (#2632) — this resumed session only (Task 2 + Task 3; Task 1 was already resolved and
# recorded in the prior halted run)
actuals:
  tokens: 3050
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Spike-before-publish live verification: toggle a CloudFront Function's publish flag to
      false, apply just the function + its new dependency (targeted apply), confirm the real
      runtime API via aws cloudfront test-function against the DEVELOPMENT stage, then flip
      publish back to true (zero net diff to that toggle) and run the full apply to promote to
      LIVE — proves an unverified runtime API assumption against reality before it ever touches
      live traffic, reusable for any future CloudFront Functions runtime-API risk."
    - "Reconstructing an existing sensitive Terraform variable's value from the currently
      deployed live resource (aws cloudfront get-function --stage LIVE, then decoding the
      compiled Authorization comparison) rather than leaving it at an empty-string default, so a
      targeted or full apply cannot accidentally rotate a credential the current plan does not
      own (D-06's separation guarantee) — reusable whenever an apply must touch a shared function
      without disturbing an unrelated baked-in secret."

key-files:
  created:
    - docs/operations/tester-access.md
  modified:
    - infra/environments/testpilots/main.tf
    - infra/environments/beta/main.tf
    - infra/environments/stable/main.tf
    - scripts/serve-static-build.ts

key-decisions:
  - "Generated a fresh cryptographically random 30-character shared tester password for this
    resumed run rather than trying to recover the prior halted run's password — per the
    checkpoint resolution's explicit instruction, since the prior value was never persisted
    anywhere retrievable, by design."
  - "Derived TF_VAR_hosted_zone_id, TF_VAR_github_oidc_provider_arn, TF_VAR_basic_auth_username,
    and TF_VAR_basic_auth_password directly from live AWS (route53 list-hosted-zones-by-name, iam
    list-open-id-connect-providers, and decoding the exact Authorization comparison baked into
    the currently-deployed LIVE function code) rather than leaving basic_auth_* at their
    empty-string defaults — matching 05-01/04.1-02's-prior-halted-run precedent, and specifically
    guaranteeing every apply in this run could not accidentally rotate the separate, pre-existing
    enable_basic_auth credential as a side effect of recompiling the shared function."
  - "Verified the live cf.kvs() runtime API twice on testpilots' DEVELOPMENT stage before
    promoting to LIVE — once with no Authorization header (confirmed the exact 401 + WWW-Authenticate
    shape) and once with the correct credential (confirmed the KVS lookup succeeds and falls
    through to clean-path logic) — not just the single case the plan's action text called out,
    because the correct-credential path is the one that actually exercises a successful
    cf.kvs().get() resolution against a real store."
  - "No redirect.js or redirect.test.mjs correction was needed: both live test-function outputs
    matched Plan 04.1-01's mocked assumption byte-for-byte in shape (401/WWW-Authenticate on
    miss, pass-through uri rewrite on match), so the flagged Open Question #1 risk resolved
    without code change."

patterns-established: []

requirements-completed: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

coverage:
  - id: D1
    description: "The real testpilots, beta, and stable CloudFront distributions each have a live
      KeyValueStore holding the shared tester credential, associated to the published redirect
      function — not just declared in Terraform, actually applied"
    requirement: "AUTH-01"
    verification:
      - kind: other
        ref: "tofu apply (testpilots commit 2bae340: 1 added/2 changed/0 destroyed; beta and
          stable: 1 added/2 changed/0 destroyed each) plus aws cloudfront-keyvaluestore put-key
          against each environment's own tester_gate_kvs_arn"
        status: pass
    human_judgment: false
  - id: D2
    description: "Unauthenticated curl against https://{env}.puppet-stagehand.com/docs/testers-guide/
      returns 401 with a WWW-Authenticate header on all three real environments; the same request
      carrying the correct credential does not"
    requirement: "AUTH-01"
    verification:
      - kind: other
        ref: "curl -sI against testpilots.puppet-stagehand.com, beta.puppet-stagehand.com, and
          www.puppet-stagehand.com (stable's actual alias) — unauth=401 all three,
          auth (-u \":$PASSWORD\")=404 all three (no content published there yet, expected per
          plan); /tiers/ on testpilots confirmed unaffected (still requires the separate
          whole-site basic-auth credential, tester password rejected on that path)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The real CloudFront Functions KVS API shape was confirmed against a live,
      unpublished function version via aws cloudfront test-function before the gate branch ever
      went live on testpilots' real traffic"
    requirement: "AUTH-03"
    verification:
      - kind: other
        ref: "aws cloudfront test-function --name stagehand-testpilots-site-paths --stage
          DEVELOPMENT, run twice (no-auth-header event and correct-credential event) before the
          publish=true promotion apply — both FunctionOutput shapes matched the mocked test suite
          exactly, no FunctionErrorMessage"
        status: pass
    human_judgment: false
  - id: D4
    description: "The shared password's plaintext value was never written to any tracked file,
      any .tf file, or any Terraform state — only typed interactively/generated and passed
      directly to AWS CLI commands"
    requirement: "AUTH-02"
    verification:
      - kind: other
        ref: "git grep -in password/basic-header-pattern scan of docs/operations/tester-access.md
          and scripts/serve-static-build.ts (no matches); credential held only in a
          session-scoped, gitignored scratchpad env file, never staged or committed"
        status: pass
    human_judgment: false
  - id: D5
    description: "docs/operations/tester-access.md documents the exact rotation command and
      explicitly separates the new tester-gate credential's lifecycle from the pre-existing
      enable_basic_auth credential's lifecycle (D-06)"
    requirement: "AUTH-03"
    verification:
      - kind: other
        ref: "docs/operations/tester-access.md, \"Rotate the tester credential\" and \"The
          pre-existing whole-site lockdown is a separate credential\" sections (commit 2f68f0f)"
        status: pass
    human_judgment: false
  - id: D6
    description: "npm run check:links and the Playwright e2e suite's local static server carry no
      gate-simulation logic, and the reason why (they never traverse CloudFront) is recorded as
      an explicit, discoverable comment"
    requirement: "AUTH-04"
    verification:
      - kind: other
        ref: "scripts/serve-static-build.ts comment above createServer, cross-referencing
          docs/operations/tester-access.md's AUTH-04 section (commit 2f68f0f)"
        status: pass
    human_judgment: false

duration: ~30min (this resumed session — Task 2 and Task 3 only)
completed: 2026-08-28
status: complete
---

# Phase 04.1 Plan 02: Gated Tester Access — Live KVS Apply and Rotation Runbook Summary

**The tester-access gate is live on testpilots, beta, and stable: a real KeyValueStore per
environment holds the shared credential, the CloudFront Function serves it from KVS in production,
the real `cf.kvs()` runtime API was confirmed against a live unpublished version before any
promotion, and `docs/operations/tester-access.md` documents rotation with the D-06 credential
separation made explicit.**

## Performance

- **Duration:** ~30 min (this resumed session, Task 2 + Task 3)
- **Started:** 2026-08-28 (resumed after IAM permission grant)
- **Completed:** 2026-08-28T05:41:54-05:00
- **Tasks:** 3 of 3 complete (Task 1 resolved in the prior halted run; Task 2 and Task 3 completed
  in this session)
- **Files modified:** 5 (1 created, 4 modified)

## Accomplishments

- **Re-verified the non-root apply identity** before touching AWS
  (`arn:aws:iam::503561411317:user/stagehand-bootstrap-operator`, matching every prior real apply
  in this project) — the previously-missing `cloudfront:CreateKeyValueStore` (and sibling
  control-plane/data-plane) permissions were confirmed granted by proceeding directly into the
  targeted apply, which now succeeded.
- **Confirmed the real CloudFront Functions KVS runtime API against a live, unpublished
  DEVELOPMENT-stage function on testpilots** before any live promotion: toggled `publish` to
  `false`, targeted-applied the KVS store + function, wrote the real credential via
  `aws cloudfront-keyvaluestore put-key`, then ran `aws cloudfront test-function` twice — once with
  no `Authorization` header (confirmed the exact `401`/`WWW-Authenticate: Basic realm="Restricted"`
  shape) and once with the correct credential (confirmed the KVS lookup resolves and the request
  falls through to `/docs/testers-guide/index.html`). Both matched Plan 04.1-01's mocked test suite
  exactly — **no `redirect.js`/`redirect.test.mjs` correction was needed.**
- Reverted the transient `publish = false` edit back to `true` in `cloudfront.tf`, confirmed `git
  diff` on that file was empty, then ran the full `tofu apply` on testpilots to promote the
  confirmed function code to `LIVE` (0 added, 2 changed, 0 destroyed).
- Added the `tester_gate_kvs_arn` output to `beta` and `stable`'s `main.tf` and applied both
  directly (no spike needed — the code was already proven): each created its KVS store and
  republished its function (1 added, 2 changed, 0 destroyed per environment).
- Seeded the identical shared tester credential (generated fresh this session, per the checkpoint
  resolution — the prior halted run's password was void, never persisted) into all three
  environments' KVS stores via `put-key`.
- **Verified all three real distributions with `curl`:** unauthenticated `GET
  /docs/testers-guide/` returns `401` with `www-authenticate` on `testpilots.puppet-stagehand.com`,
  `beta.puppet-stagehand.com`, and `www.puppet-stagehand.com` (stable's actual alias — there is no
  `stable.puppet-stagehand.com`); the same request with `-u ":$PASSWORD"` returns `404` on all
  three (no content published there yet, expected per the plan). Also confirmed `/tiers/` on
  testpilots still requires the separate whole-site `enable_basic_auth` credential (`200` only with
  that credential, `401` with the tester password or none) — proving the two mechanisms did not
  interfere.
- Wrote `docs/operations/tester-access.md`: what the gate protects, the exact KVS rotation
  procedure (no `tofu apply`, no redeploy), and a visually separate section stating the
  pre-existing `enable_basic_auth` lockdown is a completely separate credential with its own
  rotation path (D-06) — plus the AUTH-04 record explaining why local tooling never simulates the
  gate.
- Added a discoverable comment above `createServer` in `scripts/serve-static-build.ts` pointing to
  the AUTH-04 fact and cross-referencing the runbook by name.

## Task Commits

1. **Task 1: Choose the shared tester password** — no commit (checkpoint resolution produces a
   runtime value only, per the plan's own prohibition on writing it to any tracked file). Resolved
   in the prior halted run; re-generated fresh this session per the checkpoint resolution's
   explicit "the previous halted run's generated password was never persisted anywhere and should
   be treated as void" instruction.
2. **Task 2: Confirm the KVS runtime API against testpilots, then apply and verify for real** -
   `2bae340` (feat)
3. **Task 3: Write the rotation runbook and the AUTH-04 exclusion record** - `2f68f0f` (docs)

**Plan metadata:** this SUMMARY's commit, made immediately after this file.

## Files Created/Modified

- `infra/environments/testpilots/main.tf` - Added `tester_gate_kvs_arn` output.
- `infra/environments/beta/main.tf` - Added `tester_gate_kvs_arn` output.
- `infra/environments/stable/main.tf` - Added `tester_gate_kvs_arn` output.
- `docs/operations/tester-access.md` - New. Rotation runbook, gate description, D-06 credential
  separation, AUTH-04 record.
- `scripts/serve-static-build.ts` - Added a comment above `createServer` explaining the AUTH-04
  gate-exclusion fact and cross-referencing the runbook.

**Not modified** (transient during Task 2, reverted before committing, confirmed via `git diff`
showing zero net change): `infra/modules/static-site/cloudfront.tf` (the `publish = false` spike
toggle). `infra/modules/static-site/functions/redirect.js` and
`infra/modules/static-site/tests/redirect.test.mjs` were read but not modified — the live
`cf.kvs()` API matched the mocked assumption exactly, so no correction was needed.

## Decisions Made

See `key-decisions` in frontmatter: (1) generated a fresh password this session rather than
attempting to recover the prior void value; (2) derived `hosted_zone_id`,
`github_oidc_provider_arn`, and the existing `basic_auth_username`/`basic_auth_password` values
from live AWS so no apply in this run could accidentally rotate the separate whole-site lockdown
credential; (3) verified the live KVS API in both the miss and hit cases, not just the single case
the plan's action text called out; (4) no code correction was needed since the live API matched
the mocked assumption exactly.

## Deviations from Plan

None — plan executed exactly as written for both remaining tasks. The IAM permission grant that
halted the prior run resolved the blocker with no further action required; Task 2 and Task 3
proceeded per the plan's own sequencing (spike-before-publish, then beta/stable, then the
runbook).

## Issues Encountered

- Both `tofu apply` runs on testpilots/beta/stable showed a cosmetic in-place `origin` block
  diff (destroy-then-recreate symbols on a nested block, not the whole resource) alongside the
  expected KVS-store creation and function change — an AWS provider attribute-tracking quirk
  unrelated to this plan's changes (likely from a newly-tracked `response_completion_timeout`
  field). `Plan:`/`Apply complete:` output confirmed `0 to destroy` / `0 destroyed` in every case,
  so this did not affect the acceptance criteria ("0 unexpected destroys/replacements") and was
  left as-is (out of scope — pre-existing provider behavior, not caused by this plan's diff).
- Discovered mid-Task-2 that `stable`'s real hostname is `www.puppet-stagehand.com`, not
  `stable.puppet-stagehand.com` (the plan's prose used "the beta/stable equivalents" generically).
  Verified against `stable/main.tf`'s `domain_name` and used the correct hostname for stable's
  curl check; no plan or code change required.

## User Setup Required

None for this session's remaining tasks. The IAM permission grant that resumed this plan was
completed by the user/orchestrator before this session started (see the objective's precondition).

**The generated shared tester password is reported in this session's own output narration only —
never written to this file or any other tracked artifact.** Distribute it to testers out-of-band
(Slack, 1Password, etc.) per the runbook's guidance.

## Next Phase Readiness

- The tester-access gate is live, verified, and rotatable in production across all three real
  environments. AUTH-01 through AUTH-04 are all satisfied for real, not just at the code layer.
- 04.1-03 (Tester's Guide content) already completed independently (commit `d9ad740`, prior to
  this session) and is unaffected by this plan's real apply — its content is not yet visible at
  `/docs/testers-guide/` because the deploy pipeline has not published it to any environment's S3
  bucket yet (confirmed by the `404` on authenticated curl above); that is a separate, expected gap
  this plan does not close.
- 04.1-04, if it depends on a live tester-gate credential existing and being rotatable, is now
  unblocked.
- No blockers remain from this plan's own scope.

---
*Phase: 04.1-gated-tester-access*
*Completed: 2026-08-28*

## Self-Check: PASSED

- FOUND: docs/operations/tester-access.md
- FOUND: scripts/serve-static-build.ts
- FOUND commit: 2bae340 (Task 2)
- FOUND commit: 2f68f0f (Task 3)
- `grep -q "gate_expected_header" docs/operations/tester-access.md`: match
- `grep -q "AUTH-04" scripts/serve-static-build.ts`: match
