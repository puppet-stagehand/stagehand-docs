---
phase: 04.1-gated-tester-access
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - infra/environments/testpilots/main.tf
  - infra/environments/beta/main.tf
  - infra/environments/stable/main.tf
  - docs/operations/tester-access.md
  - scripts/serve-static-build.ts
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The real testpilots, beta, and stable CloudFront distributions each have a live KeyValueStore holding the shared tester credential, associated to the already-published redirect function — not just declared in Terraform, actually applied."
    - "An unauthenticated curl against https://{env}.puppet-stagehand.com/docs/testers-guide/ returns 401 with a WWW-Authenticate header on all three real environments; the same request carrying the correct credential does not."
    - "The real CloudFront Functions KVS API shape (RESEARCH.md Open Question #1) was confirmed against a live, unpublished function version via aws cloudfront test-function before the gate branch ever went live on testpilots' real traffic."
    - "The shared password's plaintext value was never written to any tracked file, any .tf file, or any Terraform state — only typed interactively and passed directly to AWS CLI commands."
    - "docs/operations/tester-access.md documents the exact rotation command and explicitly separates the new tester-gate credential's lifecycle from the pre-existing enable_basic_auth credential's lifecycle (D-06) — rotating one is documented as never implying the other rotated too."
    - "npm run check:links and the Playwright e2e suite's local static server carry no gate-simulation logic, and the reason why (they never traverse CloudFront) is recorded as an explicit, discoverable comment (AUTH-04)."
  artifacts:
    - docs/operations/tester-access.md
    - infra/environments/testpilots/main.tf (tester_gate_kvs_arn output)
    - infra/environments/beta/main.tf (tester_gate_kvs_arn output)
    - infra/environments/stable/main.tf (tester_gate_kvs_arn output)
  key_links:
    - "docs/operations/tester-access.md's documented `aws cloudfront-keyvaluestore put-key` command -> the real KVS store Plan 04.1-01 declared in Terraform, applied here for real"
    - "tofu output -raw tester_gate_kvs_arn (per environment) -> the --kvs-arn argument every rotation/seed command in the runbook uses -> no environment's ARN is ever copied into another's command by mistake"
  prohibitions:
    - requirement_id: AUTH-02
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT write the shared password's plaintext value, its base64-encoded expected-header form, or any placeholder that is actually live, to any file tracked by git (including this plan's own SUMMARY) — only pass it via shell variables/AWS CLI arguments and report success/failure, never the value itself."
---

<objective>
Apply the gate built in Plan 04.1-01 for real across all three live environments: confirm the
CloudFront Functions KVS runtime API against a real, unpublished function version before trusting it
on live traffic, seed the shared tester credential out-of-band, verify the gate against each real
distribution with `curl`, and document the rotation runbook — keeping the new credential's lifecycle
explicitly separate from the pre-existing whole-site `enable_basic_auth` lockdown's lifecycle (D-06).

Purpose: AUTH-01 (real, live enforcement, not just code), AUTH-02 (credential enters AWS only,
never the repo), AUTH-03 (rotation is a documented KVS write, no republish/redeploy), AUTH-04 (the
local-tooling gate-exclusion fact is explicitly recorded, not silently assumed).
Output: a live KeyValueStore + gate on testpilots, beta, and stable; `docs/operations/tester-access.md`;
a discoverable comment on why local tooling doesn't simulate the edge gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-gated-tester-access/04.1-CONTEXT.md
@.planning/phases/04.1-gated-tester-access/04.1-RESEARCH.md
@.planning/phases/04.1-gated-tester-access/04.1-01-SUMMARY.md
@docs/operations/aws-bootstrap.md
@infra/environments/testpilots/main.tf
@infra/environments/testpilots/backend.hcl.example
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Choose the shared tester password</name>
  <action>Get a plaintext value for the single shared tester password (AUTH-01: no username) that this plan will write to all three real KeyValueStores.</action>
  <instructions>
    I'm about to seed the tester-access gate's shared password into three real CloudFront
    KeyValueStores (testpilots, beta, stable) via `aws cloudfront-keyvaluestore put-key` — this
    password is what you'll hand out to testers out-of-band (Slack, 1Password, etc.), never
    committed anywhere in this repository.

    Reply with the exact password you want to use, or reply "generate" and I will create a
    cryptographically random one and report it back to you in this session's output (never written
    to a tracked file — copy it somewhere safe immediately).

    I plan to use the identical password across all three environments (matching AUTH-01's "single
    shared password" framing) unless you'd prefer distinct per-environment values — say so if you
    do.
  </instructions>
  <verification>No automated verification — this is a value only you (or I, on your explicit "generate" instruction) can supply.</verification>
  <resume-signal>Reply with the password, "generate", or your preferred per-environment alternative.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Confirm the KVS runtime API against testpilots, then apply and verify for real</name>
  <precondition>Task 1 resolved: a plaintext shared password value is known (human-provided or generated and reported back), never written to a tracked file.</precondition>
  <files>
    infra/environments/testpilots/main.tf, infra/environments/beta/main.tf,
    infra/environments/stable/main.tf, infra/modules/static-site/cloudfront.tf (transient
    publish-flag toggle only — no net diff), infra/modules/static-site/functions/redirect.js and
    infra/modules/static-site/tests/redirect.test.mjs (only touched if the live spike reveals the
    mocked KVS API assumption was wrong)
  </files>
  <read_first>
    infra/modules/static-site/kvs.tf, infra/modules/static-site/cloudfront.tf (Plan 04.1-01's
    `key_value_store_associations`/`replace()` additions), infra/environments/testpilots/main.tf,
    docs/operations/aws-bootstrap.md (the existing non-root-identity re-verification pattern)
  </read_first>
  <action>
    Re-verify the non-root apply identity first (`aws sts get-caller-identity` must not show a
    `:root` Arn) — if it does, stop and surface a checkpoint rather than proceeding, matching this
    project's established Phase 2/5 precedent.

    Add one output to `infra/environments/testpilots/main.tf`, immediately after the existing
    `distribution_domain_name` output: `tester_gate_kvs_arn`, valued
    `module.site.tester_gate_kvs_arn` — copied in the same one-line shape as the existing outputs.

    Compute the expected header value once: base64-encode a leading colon followed by the chosen
    password (empty username, matching `locals.tf`'s existing `basic_auth_expected_header`
    technique), and prefix it with `"Basic "` — call this `EXPECTED_HEADER` in your working shell
    session; never write it to a file.

    Before letting the new code anywhere near live traffic, confirm the real
    `cf.kvs()` runtime API shape (RESEARCH.md Open Question #1, flagged again in Plan 04.1-01):
    temporarily change `publish = true` to `publish = false` on `aws_cloudfront_function.redirect` in
    `infra/modules/static-site/cloudfront.tf` (a purely local, transient edit — you will revert it to
    `true` before this task ends, so the final committed diff to that file is zero). Run
    `tofu -chdir=infra/environments/testpilots apply -target=module.site.aws_cloudfront_key_value_store.tester_gate -target=module.site.aws_cloudfront_function.redirect`
    — this creates/updates the KVS store and pushes the new function code to its `DEVELOPMENT` stage
    only, leaving whatever code is currently `LIVE` (and serving real testpilots traffic) completely
    untouched. Write the real key via
    `aws cloudfront-keyvaluestore put-key --kvs-arn "$(tofu -chdir=infra/environments/testpilots output -raw tester_gate_kvs_arn)" --key gate_expected_header --value "$EXPECTED_HEADER" --if-match "$(aws cloudfront-keyvaluestore describe-key-value-store --kvs-arn "$(tofu -chdir=infra/environments/testpilots output -raw tester_gate_kvs_arn)" --query ETag --output text)"`.
    Then run `aws cloudfront test-function --name stagehand-testpilots-site-paths --if-match <function ETag from `aws cloudfront describe-function`> --stage DEVELOPMENT --event-object <a real event-object JSON file you construct, matching CloudFront's documented viewer-request event shape, targeting `/docs/testers-guide/` with no Authorization header>` and inspect the returned `FunctionOutput`. Confirm it reports the expected `401` response shape. If the real API differs from Plan 04.1-01's mocked assumption (different call syntax, sync vs. async mismatch, a different missing-key behavior), fix `infra/modules/static-site/functions/redirect.js` and its test mocks in `infra/modules/static-site/tests/redirect.test.mjs` now, re-run `node --test infra/modules/static-site/tests/redirect.test.mjs` to confirm the mocked suite still passes after the correction, and repeat the `test-function` call until it matches.

    Once confirmed, revert `publish` back to `true` in `cloudfront.tf` (confirm `git diff` shows zero
    net change to that file) and run the full
    `tofu -chdir=infra/environments/testpilots apply` to promote the confirmed code to `LIVE` and
    finish applying any other pending change. Then apply `beta` and `stable` directly (no spike
    needed there — the code is now proven): add the same `tester_gate_kvs_arn` output to both their
    `main.tf` files, run `tofu -chdir=infra/environments/{beta,stable} apply` for each, and write the
    same `$EXPECTED_HEADER` value to each environment's own KVS store via the same `put-key` command
    shape (each with its own `--kvs-arn`).

    Verify all three for real: `curl -sI "https://testpilots.puppet-stagehand.com/docs/testers-guide/"`
    (and the `beta`/`stable` equivalents) must return `401` with a `www-authenticate` header when no
    `-u` flag is given, and must NOT return `401` (likely `404`, since no content exists yet — that is
    expected and correct, Plan 04.1-03 adds it) when given `-u ":$PASSWORD"`. Also confirm a
    completely unrelated existing path (e.g. `/tiers/`) still requires the pre-existing whole-site
    credential exactly as before this plan, on at least one environment — proving the two mechanisms
    did not interfere.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' "https://testpilots.puppet-stagehand.com/docs/testers-guide/" | grep -q '^401$' &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `tofu -chdir=infra/environments/{testpilots,beta,stable} apply` all completed with 0 errors and 0 unexpected destroys/replacements.
    - `aws cloudfront test-function` against testpilots' `DEVELOPMENT` stage returned the expected 401 shape before any live promotion.
    - `git diff` shows zero net change to `cloudfront.tf`'s `publish` argument (still `true`).
    - Unauthenticated curl to `/docs/testers-guide/` returns 401 on all three real hostnames; authenticated curl (`-u ":$PASSWORD"`) does not.
    - No file in the working tree or any command's output printed the plaintext password or `$EXPECTED_HEADER` to a location this task then wrote to disk.
  </acceptance_criteria>
  <done>The gate is live and verified against real AWS on testpilots, beta, and stable, with the CloudFront Functions KVS API assumption confirmed (and corrected if needed) before going live.</done>
</task>

<task type="auto">
  <name>Task 3: Write the rotation runbook and the AUTH-04 exclusion record</name>
  <files>docs/operations/tester-access.md, scripts/serve-static-build.ts</files>
  <read_first>docs/operations/aws-bootstrap.md, docs/operations/github-environments.md (heading/tone conventions to match), scripts/serve-static-build.ts</read_first>
  <action>
    Create `docs/operations/tester-access.md` following this repository's existing `docs/operations/`
    heading style (a one-paragraph audience-setting intro, then task-oriented sections). Cover: (1)
    what the gate protects and why (the paths declared in `infra/modules/static-site/gated-paths.json`,
    currently just the Tester's Guide); (2) the exact rotation procedure — compute a new
    `EXPECTED_HEADER` the same way Task 2 did, fetch each environment's `ETag` via
    `aws cloudfront-keyvaluestore describe-key-value-store`, then `put-key` with that `--if-match`,
    once per environment, using each environment's own `tofu output -raw tester_gate_kvs_arn` — state
    explicitly that this requires **no** `tofu apply` targeting the function and **no** site
    redeployment; (3) a clearly separated section (do not merge it into the rotation steps above)
    stating that the pre-existing whole-site `enable_basic_auth` lockdown (`TF_VAR_basic_auth_username`/
    `TF_VAR_basic_auth_password`) is a **completely separate credential with a separate rotation
    path** requiring a Terraform variable change and a full `tofu apply` (which does republish the
    function) — per D-06, rotating one must never be assumed to rotate or affect the other; (4) the
    AUTH-04 record: state plainly that `npm run check:links` and the Playwright `production` project
    serve the local `dist/` build via `scripts/serve-static-build.ts`'s bare `http.createServer`,
    which never passes through CloudFront or this gate at all — so the gated `/docs/testers-guide/`
    path is crawled/tested locally with no credential needed, which is not "silently reporting
    success because it stopped crawling" (it never stops), and is the deliberate, recorded reason
    neither tool needs gate-simulation logic.

    Add a short comment near the top of `scripts/serve-static-build.ts` (above the `createServer`
    call) pointing to this exact fact and cross-referencing `docs/operations/tester-access.md`'s
    AUTH-04 section by name, so a future reader who wonders "why doesn't this server enforce the
    gate?" finds the answer at the point of the question, not just in a separate runbook.
  </action>
  <verify>
    <automated>grep -q "gate_expected_header" docs/operations/tester-access.md &amp;&amp; grep -q "enable_basic_auth" docs/operations/tester-access.md &amp;&amp; grep -q "AUTH-04" scripts/serve-static-build.ts &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operations/tester-access.md` exists with distinct sections for rotation and for the
      separate whole-site-lockdown credential, and never implies rotating one rotates the other.
    - `scripts/serve-static-build.ts` carries a discoverable comment explaining why it has no gate
      logic, referencing the runbook.
    - `git grep -i` over the new runbook and the new comment finds no plaintext password or encoded
      header value — only key *names* and command shapes.
  </acceptance_criteria>
  <done>Rotating the tester credential and understanding why local tooling bypasses the edge gate are both documented, discoverable, and kept clearly separate from the pre-existing whole-site lockdown's own lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator's local shell -> real AWS KVS data plane | Where the plaintext password exists, briefly, outside any file. |
| Real, currently-serving testpilots traffic -> a not-yet-verified async code path | The spike-before-publish sequencing exists specifically to keep this boundary safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-05 | Information Disclosure | Shell history, this plan's own SUMMARY, `docs/operations/tester-access.md` | high | mitigate | Task 1 elicits the password via conversational reply (not a shell arg logged to a file); Task 2/3's acceptance criteria explicitly forbid writing the plaintext or encoded value anywhere; the runbook documents command *shapes* with `$EXPECTED_HEADER`/`$PASSWORD` placeholders only. |
| T-04.1-06 | Denial of Service | Publishing untested async gate code directly to testpilots' live traffic | high | mitigate | The `publish=false` → `test-function` → `publish=true` sequence in Task 2 confirms the real KVS API against an unpublished `DEVELOPMENT` stage before any live promotion; blast radius is further scoped since only `/docs/testers-guide*` requests ever reach the new branch (`isGatedPath` gates entry to it). |
| T-04.1-07 | Elevation of Privilege | `aws cloudfront-keyvaluestore` commands run under the operator's existing broad AWS identity, not one of the six ADR-0003 CI roles | medium | accept | Consistent with ADR-0003's "bootstrap stays human-applied" pattern — rotation is explicitly a human-run, out-of-band operation (AUTH-03), not a CI role's job. If the identity lacks `cloudfront-keyvaluestore:PutKey`/`DescribeKeyValueStore`, Task 2 halts and surfaces a checkpoint rather than silently working around it. |
| T-04.1-08 | Tampering | Old `enable_basic_auth` and new tester-gate credential lifecycles conflated by a future operator | medium | mitigate | `docs/operations/tester-access.md` documents them in visually distinct sections with an explicit "these are separate, do not conflate" statement (D-06). |
</threat_model>

<verification>
- All three real environments' distributions return 401 unauthenticated / non-401 authenticated at `/docs/testers-guide/`.
- The CloudFront Functions KVS API was confirmed (or corrected) against a real, unpublished function version before going live.
- `docs/operations/tester-access.md` and `scripts/serve-static-build.ts` both exist/updated with no committed secret value.
</verification>

<success_criteria>
The gate is live, verified, and rotatable in production across all three real environments, with the
KVS runtime-API risk resolved against reality (not just a mock) before it ever touched live traffic,
and the two credential lifecycles (new tester gate, old whole-site lockdown) are documented as
explicitly separate per D-06.
</success_criteria>

<output>
Create `.planning/phases/04.1-gated-tester-access/04.1-02-SUMMARY.md` when done
</output>
