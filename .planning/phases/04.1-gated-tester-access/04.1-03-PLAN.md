---
phase: 04.1-gated-tester-access
plan: 03
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - src/lib/site-channel.ts
  - tests/unit/site-channel.test.ts
  - src/content.config.ts
  - src/content/docs/testers-guide.md
  - tests/unit/docs-collection.test.ts
  - scripts/check-built-routes.ts
  - scripts/deploy-site.sh
  - src/layouts/DocsLayout.astro
autonomous: true
requirements: [DOC-01, AUTH-06]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "src/lib/resolveSiteChannel(hostname) returns 'testpilots' for testpilots.puppet-stagehand.com, 'beta' for beta.puppet-stagehand.com, 'stable' for both www.puppet-stagehand.com and puppet-stagehand.com, and 'unknown' for anything else — proven by a unit test, not just read."
    - "The Tester's Guide is trimmed to setup framing, currently-supported-feature manual QA scenarios, and a pointer to /support/ for reporting issues (D-07) — it contains no Go/TypeScript source-checkout test commands, no puppet-console-internal .planning/ or docs/qa/ file references, and no sections the source explicitly marks deferred-to-v2 or historical/dormant."
    - "The new content entry builds as a normal /docs/testers-guide/ route, and npm run test:unit / npm run check:routes both pass with the closed-enumeration contracts (docs-collection.test.ts, check-built-routes.ts) updated to include it."
    - "On testpilots/beta hostnames the guide's body renders normally; on any other hostname (stable, an unrecognized preview domain) a client script hides the body and shows an honest, non-alarming 'not available on this site' message instead — with zero per-environment build difference (the same static bytes ship everywhere)."
  artifacts:
    - src/lib/site-channel.ts
    - src/content/docs/testers-guide.md
    - src/content.config.ts
  key_links:
    - "src/content/docs/testers-guide.md's visibleOn frontmatter -> src/content.config.ts's schema (must accept the field or Zod silently strips it) -> src/layouts/DocsLayout.astro's conditional render"
    - "src/layouts/DocsLayout.astro's client <script> -> src/lib/site-channel.ts's resolveSiteChannel(window.location.hostname) -> toggling the [data-tester-guide]/[data-tester-guide-unavailable] elements"
    - "testers-guide.md's slug 'testers-guide' -> the exact '/docs/testers-guide' prefix Plan 04.1-01 declared in gated-paths.json — the edge gate and this content route must reference the identical path"
---

<objective>
Port `puppet-console`'s Tester's Guide into this site as a curated, tester-relevant docs entry
(D-01), and add the client-side hostname-detection utility (D-02 — the identical static bundle
ships to all three environments; per PROJECT.md's immutable-promotion constraint this cannot be a
per-environment build, so a runtime script reads `window.location.hostname` instead; shared with
Phase 04.2's downloads page per CONTEXT.md's discretion note) that makes the guide visible only on
testpilots/beta. This is layered on top of, not a replacement for, AUTH-05's single-source-of-truth
gated-path declaration (D-03) — the edge gate from Plan 04.1-01/02 still protects this exact route
identically on all three environments; only client-side *visibility* differs.

Purpose: DOC-01 (the guide exists on this site, trimmed for its actual audience), AUTH-06
(testpilots/beta-only visibility via client-side detection, never a per-environment build).
Output: `src/lib/site-channel.ts` (a small, reusable pure function), `src/content/docs/testers-guide.md`,
and `src/layouts/DocsLayout.astro` extended with the visibility toggle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-gated-tester-access/04.1-CONTEXT.md
@.planning/phases/04.1-gated-tester-access/04.1-RESEARCH.md
@.planning/phases/04.1-gated-tester-access/04.1-PATTERNS.md
@src/content.config.ts
@src/layouts/DocsLayout.astro
@src/components/CompatibilityMatrix.astro
@src/content/docs/security.md
@tests/unit/docs-collection.test.ts
@scripts/check-built-routes.ts
@scripts/deploy-site.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test the site-channel resolution contract</name>
  <files>src/lib/site-channel.ts, tests/unit/site-channel.test.ts</files>
  <behavior>
    - `resolveSiteChannel('testpilots.puppet-stagehand.com')` returns `'testpilots'`.
    - `resolveSiteChannel('beta.puppet-stagehand.com')` returns `'beta'`.
    - `resolveSiteChannel('www.puppet-stagehand.com')` and `resolveSiteChannel('puppet-stagehand.com')` both return `'stable'`.
    - `resolveSiteChannel('localhost')` and `resolveSiteChannel('d111111abcdef8.cloudfront.net')` both return `'unknown'` — no partial/substring matching, exact hostname equality only.
  </behavior>
  <action>
    Create `src/lib/site-channel.ts` exporting a `SiteChannel` union type
    (`'testpilots' | 'beta' | 'stable' | 'unknown'`) and a pure function
    `resolveSiteChannel(hostname: string): SiteChannel` performing exact-string comparisons against
    the four real hostnames named in the behavior above (no case-folding, no wildcard/substring
    matching — CloudFront's real hostnames are already lowercase and exact). This module has no DOM
    dependency and takes the hostname as a plain string argument specifically so it is unit-testable
    without a browser environment and reusable by Phase 04.2's downloads page without modification.

    Create `tests/unit/site-channel.test.ts` using this repository's existing `describe`/`it`/`expect`
    Vitest convention (mirror `tests/unit/docs-collection.test.ts`'s import style), covering exactly
    the four cases in the behavior list above.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/site-channel.test.ts</automated>
  </verify>
  <done>resolveSiteChannel exists, is pure and DOM-free, and all 4 test cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Curate and wire the Tester's Guide content entry</name>
  <files>
    src/content.config.ts, src/content/docs/testers-guide.md, tests/unit/docs-collection.test.ts,
    scripts/check-built-routes.ts, scripts/deploy-site.sh
  </files>
  <read_first>
    /Users/matthew/Code/puppet-console/docs/TESTER-GUIDE.md (the full 1375-line source — read it
    completely before curating; do not curate from a partial read), src/content.config.ts,
    src/content/docs/security.md (frontmatter shape to match exactly), tests/unit/docs-collection.test.ts,
    scripts/check-built-routes.ts, scripts/deploy-site.sh
  </read_first>
  <action>
    In `src/content.config.ts`, add `visibleOn: z.array(z.enum(['testpilots', 'beta', 'stable'])).optional()`
    to the `docs` collection's Zod schema, alongside the existing `title`/`description`/`order`/`updated`
    fields — Zod silently strips unrecognized frontmatter keys by default, so this must land in the
    same change as the content file below or the field is a silent no-op.

    Curate `src/content/docs/testers-guide.md` from the full source per D-07 ("trim to
    tester-relevant content — setup, how to test, how to report issues"). Concretely:
    - Frontmatter: `title: "Tester's Guide"`, a one-sentence `description`, `order: 4` (the next
      unused value after the existing `getting-started`=1/`security`=2/`first-run`=3), `updated` set
      to today's date, and `visibleOn: ['testpilots', 'beta']`.
    - Opening/setup section: adapt the source's "who this is for" framing for an external tester of a
      *deployed* Stagehand instance holding the shared password — not a source checkout. State the
      assumption plainly (a running instance + the shared password given to you by the Stagehand
      team) and link to `/support/` for how to report anything found, rather than duplicating
      `/support/`'s existing public-tracker-vs-private-advisory content.
    - Feature-boundary section: port the "what's supported in 1.0 vs. deferred" framing in plain
      prose, with every Go/TypeScript automated-test command block removed — a tester never runs
      those.
    - Manual-QA content: port the per-feature "Scenario / Expected result" tables for currently
      *supported*, *non-deferred* features only (Compliance, Code Management's control-repo-only
      scope, Bolt vulnerability scans, the Deploy environment picker and Deployments-tab checklists,
      the scoped task & plan launcher, Bytnar dependency graphs, NodeDetail/Activity/Classes/
      Configuration Coverage, Visual Bolt Designer and its dry-run/publish/command-palette checklists,
      the novice-friendly Bolt UX checklist, the EYAML profile wizard, Estate Viewer, the Console SBOM
      checklist, the guided Hierascope+EYAML walkthrough, the PDCTNG connection checklist, the
      Playbooks feature refresh, and the Patchbot-only production boundary / package-protection
      checklist) — strip every embedded Go/TS command block, drop every heading the source itself
      marks "(deferred to v2, ...)" (self-update lifecycle, signature-verification banners, the ENC
      discovery/import wizard) or "historical/dormant" (the Phase 37 automated patch-provider gate
      blocks), and drop every dated "Ad hoc (2026-08-19...)" bug-fix-changelog entry — those document
      one-off historical fixes, not durable test procedures a current tester needs.
    - Deliberately exclude: the "Manual QA — when and how" section (internal `.planning/`/`docs/qa/`
      process, not applicable outside the source repo), the empty "Manual QA checklist template"
      heading, "Phase 999.5 — Identifier retirement" (a one-time upgrade-migration check, not
      relevant to a first-time external tester), "Per-phase requirement (standing rule)" (an internal
      development-process rule), and "Where things live" (internal repository file paths that don't
      exist in this repo).
    - Where a scenario table references a `.planning/HUMAN-TESTS-DEFERRED.md`-style internal
      tracking artifact or a Cytoscape/jsdom implementation detail, drop that sentence but keep the
      scenario/expected-result row itself if the feature is otherwise in scope.
    - Preserve the source's plain-language tone; do not invent test scenarios the source doesn't
      already describe.

    Wire the new entry into every place a new doc must be registered in the same change (mirroring
    CONT-05's established convention from Phase 3): add `'testers-guide'` in alphabetically-sorted
    position to `tests/unit/docs-collection.test.ts`'s closed slug array; add
    `'docs/testers-guide/index.html'` to `scripts/check-built-routes.ts`'s `requiredOutputs` array;
    add `'/docs/testers-guide/index.html'` to `scripts/deploy-site.sh`'s `create-invalidation --paths`
    list, matching the existing entries' exact quoting style.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/docs-collection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/content.config.ts`'s schema accepts `visibleOn`.
    - `src/content/docs/testers-guide.md` exists with the frontmatter shape specified above, and
      contains no Go/TypeScript command block, no reference to a `puppet-console`-internal
      `.planning/`/`docs/qa/` path, and no section the source marks deferred-to-v2 or
      historical/dormant.
    - `tests/unit/docs-collection.test.ts` passes with `'testers-guide'` present in the sorted slug array.
    - `scripts/check-built-routes.ts` and `scripts/deploy-site.sh` both reference `docs/testers-guide/index.html` in the same shapes as the three existing docs entries.
  </acceptance_criteria>
  <reversibility rating="reversible">The curation depth (trim vs. verbatim) is a content judgment call per D-07, not a locked byte-for-byte requirement — future phases can expand or further trim this entry without any structural change.</reversibility>
  <done>The Tester's Guide exists as a normal, wired-in docs entry, curated to its actual external-tester audience.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the testpilots/beta-only visibility toggle and confirm the build</name>
  <files>src/layouts/DocsLayout.astro</files>
  <read_first>
    src/layouts/DocsLayout.astro (full file — the exact `docs-content`/`slot` insertion point),
    src/components/CompatibilityMatrix.astro lines 247-279 (the proven, CSP-safe non-inline
    `<script>` pattern to mirror — this site's CSP is `script-src 'self'` with no
    `'unsafe-inline'`/nonce/hash, so an `is:inline` script or inline event handler is silently blocked
    at runtime with no build error)
  </read_first>
  <action>
    In `DocsLayout.astro`, change the existing `<div class="docs-content"><slot /></div>` block so
    the `data-tester-guide` attribute is present only when `entry.data.visibleOn` is set (Astro
    renders a boolean attribute expression as present/absent, not `"true"`/`"false"` text — use the
    existing file's conditional-render idiom already present for `updatedIso && updatedLabel`,
    applied to the attribute value rather than the whole element, so the div's structure stays
    identical in both the gated and non-gated cases). Immediately after that div, conditionally
    render one `<p>` with `data-tester-guide-unavailable` and a `hidden` attribute, only when
    `entry.data.visibleOn` is set, containing an honest, non-alarming message that this guide isn't
    shown on this site.

    Immediately after the closing `</article>` tag, conditionally emit (only when
    `entry.data.visibleOn` is set — do not load this script on every docs page) a plain `<script>`
    block (never `is:inline`) that imports `resolveSiteChannel` from `'../lib/site-channel'`, reads
    `window.location.hostname`, and — only when the resolved channel is *not* `'testpilots'` and not
    `'beta'` — sets the `hidden` attribute on the `[data-tester-guide]` element and removes it from
    the `[data-tester-guide-unavailable]` element. Leave both elements in their default rendered
    state (guide visible, fallback hidden) when the channel *is* `testpilots`/`beta`, or when
    JavaScript never runs at all — this means a visitor to `stable` with JavaScript disabled would
    still see the guide's markup; this is the same acknowledged, deliberate trade-off RESEARCH.md
    already reasons through (the content is password-gated identically on every environment, so
    seeing it on `stable` requires the same legitimate access as seeing it on `testpilots`/`beta` —
    it is not a new exposure). State this trade-off explicitly in the SUMMARY so it is never mistaken
    for an overlooked bug later.

    Run `npm run build` and `npm run check:routes` to confirm `/docs/testers-guide/` builds and is
    recognized as a required output.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run check:routes</automated>
  </verify>
  <acceptance_criteria>
    - The new script is emitted only on the `testers-guide` page, not on every `/docs/[slug]/` route.
    - No `is:inline` directive or inline event-handler attribute is introduced.
    - `npm run build` and `npm run check:routes` both pass.
    - The SUMMARY explicitly states the "identical bytes ship to stable, JS toggles visibility only"
      trade-off and why it is accepted, not overlooked.
  </acceptance_criteria>
  <done>The guide renders on testpilots/beta and shows an honest unavailable state everywhere else, via one shared client-side utility and zero per-environment build difference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `puppet-console` source repository -> this public repository | The port must not carry over any internal secret, real DSN, or credential-shaped string from the source's example commands. |
| Client-side hostname read -> visibility decision | A purely cosmetic control, not a security boundary — the edge gate (Plan 04.1-01/02) is the actual access control; this toggle only affects what a *legitimately authenticated* visitor sees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-09 | Information Disclosure | Curated content copied from `puppet-console/docs/TESTER-GUIDE.md` | medium | mitigate | Task 2's curation explicitly drops every internal file-path reference and command block; the source's `PSH_TEST_DATABASE_URL=<dsn>` occurrences are placeholder syntax, not real values, but the curation pass double-checks nothing DSN-shaped, token-shaped, or credential-shaped survives into the ported file. |
| T-04.1-10 | Spoofing | A visitor on `stable` viewing the Tester's Guide's raw HTML source despite the client-side hide | low | accept | RESEARCH.md's explicit, reasoned trade-off: the content is password-gated identically on every environment (Plan 04.1-01/02's edge gate applies to `/docs/testers-guide` regardless of hostname), so this is not a new exposure — a visitor able to see it already had legitimate access to the identical content on `testpilots`/`beta`. |
</threat_model>

<verification>
- `npx vitest run tests/unit/site-channel.test.ts` and `npx vitest run tests/unit/docs-collection.test.ts` both pass.
- `npm run build && npm run check:routes` passes with `/docs/testers-guide/` present.
- `git grep -in "dsn\|token\|password"` over `src/content/docs/testers-guide.md` finds nothing but generic prose mentions.
</verification>

<success_criteria>
The Tester's Guide is live as a curated, wired-in docs entry, visible only on testpilots/beta via a
small, reusable, unit-tested hostname-resolution utility, with zero per-environment build
difference.
</success_criteria>

<output>
Create `.planning/phases/04.1-gated-tester-access/04.1-03-SUMMARY.md` when done
</output>
