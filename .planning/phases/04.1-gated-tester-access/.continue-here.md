---
context: phase
phase: 04.1-gated-tester-access
task: null
total_tasks: null
status: paused
last_updated: 2026-09-01T00:43:06.386Z
---

<current_state>
Nothing is mid-task. This session closed out Phase 04.2 entirely (CR-01 gap closure + two
code-review findings + re-verification + the human UAT read), executed Phase 5's final two plans
(05-10, 05-11 — LAUN-05 is now proven, Phase 5 is marked Complete), did a large amount of ad hoc
homepage/design work outside the formal roadmap, and corrected a stale assumption about phase
04.1's own AUTH-06 blocker. Every thread is committed and pushed to `origin/main`.

04.1 is the phase this handoff is filed under only because it's the earliest phase without a
`passed` verification status — not because there's active work here to resume. Its one remaining
UAT item (AUTH-06's live cross-environment proof) is genuinely, intentionally blocked until the
maintainer does a real future public launch (removes `enable_basic_auth`) — it is NOT something to
attempt to unblock or re-test right now.
</current_state>

<completed_work>

This session (chronological):
- Closed CR-01 (`loadDownloads()`'s uncaught-throw-crashes-the-build gap) via TDD — commits
  `38b1eb5` (RED), `8e74a43` (GREEN).
- Closed WR-01 (`<noscript>` links invisible to the built-link checker) — commit `f2b1c58`.
- Closed WR-02 (no DOM test for the channel-reveal script) — commit `6d184ba`.
- Re-verified phase 04.2 end to end — 6/6 must-haves, `human_needed` only for the reader-voice
  read, which the user then performed and passed — commits `4582e9b`, `48424ac`.
- Moved the docs-style writing guide from root `CLAUDE.md` into a project skill
  (`skills/docs-style/`) so it stops loading into every session.
- Added `src/content/docs/why-stagehand.md` (mission page) — commit `c1d393f`.
- Iterated the homepage H1 tagline extensively (including outside critique from Opus and Fable),
  landed on "Manage Puppet Environments With Less Complexity." — commit `86b0b2a`.
- Added a "Who It's For" homepage section — commit `99cd260`.
- Fixed a real layout bug the shorter H1 exposed (forced `min-height` stretching empty space) —
  commits `c6fea26`, `3c3b270`.
- Replaced the hero rail's jargon-only "CONTROL MODE"/"EXECUTION" labels, and added a new
  top-level `/features/` page, both showing real screenshots of the running `puppet-console`
  (seeded with synthetic demo data — 24 nodes, compliance results, Bolt runs, audit log entries,
  classification groups) — commit `4ec27b5`.
- Captured all of the above (untracked against REQUIREMENTS.md) in
  `.planning/notes/homepage-marketing-refresh.md` — commit `c6129d0`.
- Discovered local `main` was 54 commits ahead of `origin/main` (nothing pushed since 2026-08-28);
  pushed with explicit user confirmation since it triggers a real production deploy.
- Executed 05-10 (promoted a genuinely newer SHA to `beta` through the protected dispatch path) —
  commit `ed1e097`.
- Executed 05-11 (proved the rollback end to end — independently verified restoration even though
  the workflow's own smoke-test step failed on a stale CI secret; filed that as WINDOWS entry 4) —
  commit `defdc04`. **LAUN-05 closed, Phase 5 marked Complete.**
- Clarified the vendored-modules todo: no content port needed, just a Forge link once
  `stagehand-stagehand` is actually published (confirmed it isn't yet) — commit `da659b8`.
- Corrected AUTH-06's blocker framing: it was never a Phase 5 dependency — the maintainer
  confirmed `enable_basic_auth` stays up intentionally until the rest of the release ships. Waived
  WINDOWS entry 2, updated `04.1-UAT.md` test 1 to match — commit `8fb17c6`.

All 18 commits are pushed; testpilots auto-deployed each one successfully.
</completed_work>

<remaining_work>

Nothing forced. If there's a next GSD-tracked step, it routes through 04.1's UAT state, but test
1 (AUTH-06) should NOT be re-attempted — it's correctly `blocked`/waived, not resumable. The only
other UAT item (test 2, reviewing `testers-guide.md` for audience fit) is `skipped`, deferred by
the user's own earlier choice to batch it with a later content review.

Open, non-blocking items for whenever convenient:
- WINDOWS entry 3 — `downloads.ts` live verification, blocked on `stagehand-release` actually
  publishing releases.
- WINDOWS entry 4 — stale GitHub Actions basic-auth secrets; maintainer needs to refresh them.
- Vendored-modules page — blocked on `stagehand-stagehand` actually publishing to the Forge.
- The homepage/Features work is real but untracked against `REQUIREMENTS.md`/`ROADMAP.md` — worth
  folding into formal tracking if it needs to survive a future audit.
</remaining_work>

<decisions_made>

- Docs-style guide lives in a project skill, not root CLAUDE.md (loads only for doc-writing work).
- Homepage H1: "Manage Puppet Environments With Less Complexity." — chosen after independent
  critique from both Opus and Fable flagged earlier candidates as flat.
- Hero rail and `/features/` page show real product screenshots (seeded demo data) instead of
  empty jargon labels or no visual proof.
- AUTH-06's blocker is an intentional maintainer decision (basic auth stays up until the rest of
  the release ships), not an engineering gap — waived accordingly, not "fixed."
- Did not touch the stale CI basic-auth secrets directly — needs a maintainer-confirmed value,
  filed as an open WINDOWS item instead of guessed at.
</decisions_made>

<blockers>
- AUTH-06 live proof: blocked on the maintainer's own future public-launch action. Not something
  this session or a resuming session can unblock.
- `downloads.ts` live verification: blocked on an external cross-repo release.
- Vendored-modules page: blocked on an external Forge publication.
</blockers>

## Required Reading (in order)
1. `.planning/notes/homepage-marketing-refresh.md` — context for why the homepage/hero/Features
   page look the way they do; none of it is requirements-driven.
2. `.planning/WINDOWS.md` — entries 3 and 4 are the two genuinely open, non-blocking items.
3. `.planning/phases/04.1-gated-tester-access/04.1-UAT.md` — test 1's corrected `blocked_by`/
   `reason` before assuming AUTH-06 needs work.

## Infrastructure State
- `origin/main` is fully in sync with local `main` (0 commits ahead either direction as of this
  session's end).
- `beta` currently serves `18967ca1f806cca030173f7d0f7f16d61940b20c` (rolled back, per 05-11) —
  NOT the latest `main`. Worth noting if a future session expects `beta` to reflect current `main`.
- All three environments (testpilots/beta/stable) still have `enable_basic_auth = true` —
  intentional, per the maintainer.
- A `puppet-console` dev stack (Docker) was seeded with synthetic demo data this session for
  screenshot purposes — that stack and its seed data are the maintainer's own, outside this repo,
  not something this repo's tooling manages.

<context>
Long, wide-ranging session covering formal GSD phase-closure work (04.2, Phase 5) and a large
amount of ad hoc homepage/design iteration alongside it. Everything that could be closed was
closed. The remaining open items are all genuinely external — nothing is stuck due to unfinished
work in this repo.
</context>

<next_action>
No specific forced action. If picking GSD work back up, run `/gsd-progress` to get current
routing (it will likely point at 04.1's UAT state again, but AUTH-06's test should be left as
blocked/waived, not re-attempted). Otherwise, formalizing the homepage/Features work against
REQUIREMENTS.md, or picking up either open WINDOWS item, are both reasonable next steps.
</next_action>
