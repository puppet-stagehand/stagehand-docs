---
phase: 04.1-gated-tester-access
verified: 2026-08-28T13:34:39Z
status: human_needed
score: 6/6 must-haves verified (5 fully live-proven, 1 PASSED via accepted override)
behavior_unverified: 0
overrides_applied: 1
overrides:
  - must_have: "The Tester's Guide (DOC-01) is added to the gated-path set and is rendered only on testpilots and beta — stable renders an honest 'not available here' state for a real tester session (Roadmap SC 6 / AUTH-06)."
    reason: "Live-tested (not just code-level) via an ad hoc Playwright session using only the tester-gate credential against beta and www (stable). Root cause isolated and documented: a separate, temporary, out-of-phase whole-site `enable_basic_auth` lockdown (commit 44b56c9, added the evening before this phase closed) shares the same WWW-Authenticate realm as the new tester gate. gated-paths.json only declares `/docs/testers-guide`, so the gated page's own static assets (CSS/JS/images, including the visibility-toggle script itself) fall to the whole-site lockdown, which a tester-credential-only browser session cannot also satisfy — so the toggle script never loads/runs and stable incorrectly renders the guide body instead of the fallback. User reviewed three remediation options (04.1-04-SUMMARY.md 'Decision Resolved') and explicitly selected Option 1: accept as a pre-launch-only limitation, expected to self-resolve with zero code change when Phase 05 removes the temporary whole-site lockdown at public launch. No code fix was authorized, and none was applied to gated-paths.json, redirect.js, or any environment's enable_basic_auth setting, because doing so would change the deliberately documented D-05/D-06 credential-separation boundary. Tracked as an open item in .planning/WINDOWS.md (id 2) for mandatory re-verification once Phase 05 ships."
    accepted_by: "user (via 04.1-04-SUMMARY.md 'Decision Resolved' record, matches task-provided context confirming this is a deliberate, user-approved deferral)"
    accepted_at: "2026-08-28"
human_verification:
  - test: "Confirm the WINDOWS.md ledger entry (id 2) is re-verified against the live testpilots/beta/stable environments once Phase 05 removes the temporary whole-site enable_basic_auth lockdown — repeat the same ad hoc Playwright, tester-credential-only session against beta and stable's /docs/testers-guide/ used in 04.1-04, and confirm stable now shows the fallback while beta shows the guide body."
    expected: "beta: [data-tester-guide] visible, [data-tester-guide-unavailable] hidden, for real reasons (toggle script loads and resolves 'beta'). stable: [data-tester-guide] hidden, [data-tester-guide-unavailable] visible, because the toggle script now loads (no competing basic-auth challenge) and resolves 'stable'."
    why_human: "Requires a live browser session against the real CloudFront distributions post-Phase-05 cutover; cannot be verified from the current codebase state because the blocking whole-site lockdown is still active as of this verification."
  - test: "Read `src/content/docs/testers-guide.md` end to end and confirm the D-07 curation (trimmed from puppet-console's 1375-line TESTER-GUIDE.md to setup framing, 17 currently-supported non-deferred manual-QA scenarios, and a /support/ pointer) reads correctly and completely for an external tester audience."
    expected: "The curated content is accurate, unambiguous, and does not reference internal puppet-console tooling, deferred-to-v2 features, or dead file paths."
    why_human: "Curation depth/accuracy and audience-fit is a content-judgment call the 04.1-03-SUMMARY.md itself flags as `human_judgment: true` and not yet performed by a human reviewer."
---

# Phase 04.1: Gated Tester Access (INSERTED) Verification Report

**Phase Goal:** A tester with the shared password reaches the testing guides — including a ported
Tester's Guide, visible only on testpilots/beta — and anyone without the password is refused at the
edge; the password is nowhere in the public repository.

**Verified:** 2026-08-28T13:34:39Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria 1-6)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A request to a gated path without credentials is refused by the CloudFront viewer-request function with `401` and a `WWW-Authenticate` challenge, before any S3 origin is reached; a request carrying the shared password is served normally. | ✓ VERIFIED | **Independently re-executed against live infrastructure, not from SUMMARY claims.** `curl -sI https://{testpilots,beta,www}.puppet-stagehand.com/docs/testers-guide/` on all three real hosts today returns `HTTP/2 401`, `www-authenticate: Basic realm="Restricted", charset="UTF-8"`, `cache-control: no-store`, `x-cache: FunctionGeneratedResponse from cloudfront` — proving the CloudFront Function itself (not S3, not a CDN error page) generated the refusal. `redirect.js` read directly confirms the `isGatedPath()` guard clause runs first and fails closed on any `Authorization` mismatch or KVS error. |
| 2 | The shared password exists only in a CloudFront KeyValueStore and in the operator runbook's rotation procedure — `git grep` over the working tree and history finds no credential, no hash of one, and no placeholder that is actually live, and the repository stays public. | ✓ VERIFIED | Independently re-ran the credential sweep: `git grep -n "gate_expected_header"` across the tracked tree returns only the KVS *key name* in code/tests/docs, never a value; `grep -rn aws_cloudfrontkeyvaluestore_key` across all `.tf` files returns zero matches (Terraform never manages the credential's value — `kvs.tf` declares the empty container only). No plaintext password or its base64 `Basic` form found anywhere in the tracked tree. (See note below on an untracked, gitignored `.claude/settings.local.json` local artifact — does not affect this truth; detailed under Anti-Patterns.) |
| 3 | Rotating the credential is a KeyValueStore write documented in a runbook, and does not require republishing the CloudFront function or redeploying the site. | ✓ VERIFIED | `docs/operations/tester-access.md` exists and documents the exact `aws cloudfront-keyvaluestore put-key` rotation procedure with no `tofu apply` step. `04.1-04-SUMMARY.md` records a real rotate-then-restore performed live against testpilots as part of this phase's own close-out: the KVS key's ETag changed three times (`KV1PA6795UKMFR9` → `KV13V1IB3VIYZZH` → `KV1VC38T7YXB528`) while the CloudFront Function's own ETag/LastModifiedTime (`E1PA6795UKMFR9`, `2026-08-28T10:35:55.13Z`) stayed identical across all three checks — a real state-transition proof, not just a documentation claim. |
| 4 | `npm run verify` is green with the gate live: `check:links` and the Playwright e2e suite either carry the credential or exclude the gated paths by an explicit, recorded rule — neither silently reports success because it stopped crawling, and neither is weakened for unrelated paths. | ✓ VERIFIED | **Independently re-ran `npm run verify` from a clean invocation in this session** (not trusted from SUMMARY): exit 0, format/lint/typecheck/unit/redirect-tests/two builds/route-check/invalidation-check/link-check all pass, 27/27 Playwright e2e tests pass, `check:e2e-isolation` reports `production=0 (0 fixture-derived)`. Separately confirmed `test:redirect` (`node --test infra/modules/static-site/tests/redirect.test.mjs`) passes 16/16, including the 6 new gate tests. `scripts/serve-static-build.ts` carries a discoverable comment (grep-confirmed) explaining why local tooling structurally cannot simulate the CloudFront-only gate, cross-referencing the runbook — matching AUTH-04's explicit-recorded-rule requirement. |
| 5 | Which paths are gated is declared in exactly one place that both the function and the verification tooling read, so an ungated testing guide is a build-visible error rather than a discovery in production. | ✓ VERIFIED | `infra/modules/static-site/gated-paths.json` contains exactly `["/docs/testers-guide"]`. Independently grepped for any other hardcoded gated-path literal: only consumers found are `cloudfront.tf`'s `file()`/`replace()` chain (confirmed at cloudfront.tf:144-148, wired to `__GATED_PATH_PREFIXES__` and `key_value_store_associations`) and `redirect.test.mjs`'s direct JSON read. No second, independently-typed gated-prefix literal exists in any `.ts`/`.js`/`.tf` source file. |
| 6 | The Tester's Guide, ported from `puppet-console`'s `docs/TESTER-GUIDE.md`, sits behind the gate and renders on `testpilots`/`beta`; the identical, immutably-promoted build served on `stable` shows an honest "not available here" state instead, decided by client-side hostname detection — not a separate build. | ✓ PASSED (override) | **Code-level artifacts verified as real and substantive:** `src/content/docs/testers-guide.md` (236 lines, curated content — read directly, confirms real setup framing and 17 manual-QA scenarios, not a stub); `src/lib/site-channel.ts`'s `resolveSiteChannel()` (unit-tested, 6/6 pass, independently re-run); `TesterGuideVisibility.astro` + `DocsLayout.astro` wiring confirmed by direct read (conditional `data-tester-guide`/`data-tester-guide-unavailable` pair, script only emitted when `visibleOn` is set). **Live cross-environment behavior does NOT currently hold pre-launch**, per 04.1-04's own live Playwright evidence: a real tester-credential-only session sees the guide body on `stable` too (should see the fallback), because the gated page's own static assets fall outside `gated-paths.json`'s one declared prefix and hit a separate, temporary, out-of-phase whole-site `enable_basic_auth` lockdown sharing the same auth realm — so the toggle script never loads. This is a deliberate, user-approved, explicitly-decided deferral (see override in frontmatter), not an oversight, and is accurately tracked in `.planning/WINDOWS.md` (entry id 2, confirmed against the real commit `44b56c9` it cites). |

**Score:** 6/6 truths verified (5 fully live-proven this session, 1 carried as PASSED via an explicit, user-accepted override).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `infra/modules/static-site/functions/redirect.js` | KVS-backed gate guard clause, mutually exclusive with pre-existing basic-auth | ✓ VERIFIED | Read directly; matches SUMMARY description exactly — `isGatedPath()` helper, async `cf.kvs().get('gate_expected_header')`, fail-closed try/catch, `if`/`else-if` mutual exclusion with `basicAuthEnabled`. |
| `infra/modules/static-site/gated-paths.json` | Single declared source of truth for gated prefixes | ✓ VERIFIED | Contains exactly `["/docs/testers-guide"]`. |
| `infra/modules/static-site/kvs.tf` | KVS container resource, no value management | ✓ VERIFIED | `aws_cloudfront_key_value_store.tester_gate` only; no key resource. |
| `infra/modules/static-site/outputs.tf` | `tester_gate_kvs_arn` output | ✓ VERIFIED | Present at line 26. |
| `docs/operations/tester-access.md` | Rotation runbook with D-06 credential separation | ✓ VERIFIED | 119 lines, real rotation commands, explicit separate section for the pre-existing whole-site lockdown. |
| `src/content/docs/testers-guide.md` | Curated Tester's Guide content | ✓ VERIFIED | 236 lines of real, specific content (setup framing, manual QA scenarios) — not a placeholder. |
| `src/lib/site-channel.ts` | Pure hostname→channel resolver | ✓ VERIFIED | 4-branch exact-match resolver; 6/6 unit tests independently re-run and passing. |
| `src/components/TesterGuideVisibility.astro` + `src/layouts/DocsLayout.astro` | Client-side visibility toggle | ✓ VERIFIED (wired) | Script conditionally emitted; DocsLayout imports and conditionally renders it; confirmed via direct read. Live-browser confirmation is exactly what Truth 6's override covers. |
| `infra/modules/static-site/tests/redirect.test.mjs` | Gate proven by real test suite | ✓ VERIFIED | 16/16 tests pass (independently re-run), including 6 new gate-specific tests. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `cloudfront.tf` | `gated-paths.json` | `file()`/`trimspace()`/`replace("__GATED_PATH_PREFIXES__", ...)` | ✓ WIRED | Confirmed at cloudfront.tf:144-145. |
| `cloudfront.tf` | `kvs.tf` | `key_value_store_associations = [aws_cloudfront_key_value_store.tester_gate.arn]` | ✓ WIRED | Confirmed at cloudfront.tf:148. |
| `redirect.js` | live KVS store | `cf.kvs().get('gate_expected_header')` | ✓ WIRED (live) | Confirmed via live `curl` 401 test carrying the CloudFront-function-generated response header, and via 04.1-02/04.1-04's recorded live KVS rotate/restore. |
| `DocsLayout.astro` | `TesterGuideVisibility.astro` | `{entry.data.visibleOn && <TesterGuideVisibility />}` | ✓ WIRED | Confirmed at DocsLayout.astro:94. |
| `TesterGuideVisibility.astro` | `src/lib/site-channel.ts` | `import { resolveSiteChannel } from '../lib/site-channel'` | ✓ WIRED | Confirmed in component source. |
| Real tester browser session | `TesterGuideVisibility.astro`'s compiled script | HTTP fetch of `/assets/TesterGuideVisibility.*.js` | ⚠️ BLOCKED (pre-launch only) | The script itself is correctly built and referenced, but a tester-credential-only session cannot fetch it live today — see Truth 6 override. This is the one link this verification found genuinely broken in the live pre-launch environment, and it is the documented, accepted gap. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Unauthenticated request to gated path is refused at the edge | `curl -sI https://testpilots.puppet-stagehand.com/docs/testers-guide/` | `401`, `www-authenticate: Basic realm="Restricted"`, `x-cache: FunctionGeneratedResponse from cloudfront` | ✓ PASS |
| Same check on beta | `curl -sI https://beta.puppet-stagehand.com/docs/testers-guide/` | `401` | ✓ PASS |
| Same check on stable (real alias `www.puppet-stagehand.com`) | `curl -sI https://www.puppet-stagehand.com/docs/testers-guide/` | `401` | ✓ PASS |
| Node gate unit tests pass | `node --test infra/modules/static-site/tests/redirect.test.mjs` | 16/16 pass | ✓ PASS |
| site-channel/docs-collection/deploy-scripts unit tests pass | `npx vitest run tests/unit/site-channel.test.ts tests/unit/docs-collection.test.ts tests/unit/deploy-scripts.test.ts` | 3 files, 29/29 tests pass | ✓ PASS |
| Full standing gate is green | `npm run verify` | exit 0; 27/27 e2e; isolation check clean | ✓ PASS |
| ESLint clean | `npm run lint` | exit 0, no output | ✓ PASS |
| No debt markers in any phase key file | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across 11 key files | no matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| AUTH-01 | 04.1-01, 04.1-02, 04.1-04 | Shared-password edge gate, refuses unauth 401, serves auth normally | ✓ SATISFIED | Live curl proof (Truth 1), redirect.js code (Artifacts). |
| AUTH-02 | 04.1-01, 04.1-04 | Credential only in KVS, never in repo | ✓ SATISFIED | git grep sweep independently re-run, clean (Truth 2). |
| AUTH-03 | 04.1-02, 04.1-04 | Rotation is a KVS write, no republish/redeploy | ✓ SATISFIED | Runbook exists; live rotate/restore ETag evidence (Truth 3). |
| AUTH-04 | 04.1-02, 04.1-04 | check:links/e2e stay green, exclusion reason recorded | ✓ SATISFIED | `npm run verify` independently green; comment confirmed in serve-static-build.ts (Truth 4). |
| AUTH-05 | 04.1-01, 04.1-04 | Gated-path set declared in exactly one place | ✓ SATISFIED | gated-paths.json single source, grep-confirmed no second literal (Truth 5). |
| AUTH-06 | 04.1-03, 04.1-04 | Tester's Guide visible testpilots/beta only, stable shows fallback | ⚠️ SATISFIED (override) | Code/wiring verified real; live pre-launch behavior blocked by a documented, accepted, unrelated temporary lockdown (Truth 6). |
| DOC-01 | 04.1-03 | Tester's Guide ported and curated, gated by AUTH-01/06 | ✓ SATISFIED | Real 236-line curated content confirmed by direct read; human curation-quality read still pending (see Human Verification). |
| GATE-06 | 04.1-01, 04.1-04 | Test asserts unauth refused 401, auth succeeds | ✓ SATISFIED | redirect.test.mjs's 6 gate tests, wired into `npm run verify` via `test:redirect`. |

No orphaned requirements found — REQUIREMENTS.md's Phase 04.1 mapping (AUTH-01..06, DOC-01, GATE-06) matches exactly what the four plans declare as `requirements-completed` across their frontmatter.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.claude/settings.local.json` | 21-22 | Live tester-gate plaintext password embedded in a Claude Code local permission-allowlist entry (`curl -sI -u :HQZgd8GuZrP2snJa1746l13oHy97bH ...`) | ℹ️ Info | **Not a phase failure** — this file is untracked and covered by the user's global `~/.config/git/ignore` (confirmed via `git check-ignore -v` and `git ls-files`), so it is not part of the public repository and does not violate AUTH-02's "nowhere in the public repository" requirement, which is scoped to the repo's working tree/history as tracked by git. Flagged only as an operational-hygiene note: this file sits in the working tree today and would need to be cleaned/regenerated (or the credential rotated) if this machine's local Claude settings were ever shared or accidentally force-added. No action required for this phase's goal. |

No `TODO`/`FIXME`/`HACK`/`PLACEHOLDER`/stub patterns found in any of the phase's key files (redirect.js, gated-paths.json, kvs.tf, outputs.tf, tester-access.md, testers-guide.md, site-channel.ts, TesterGuideVisibility.astro, DocsLayout.astro, serve-static-build.ts, eslint.config.js).

### Human Verification Required

### 1. Re-verify Truth 6 / AUTH-06 live cross-environment behavior after Phase 05's public launch

**Test:** Once Phase 05 removes the temporary whole-site `enable_basic_auth` lockdown, repeat the ad hoc Playwright check from `04.1-04-SUMMARY.md`: authenticate as a real tester (tester-gate credential only, no other credential) and load `/docs/testers-guide/` on `beta.puppet-stagehand.com` and `www.puppet-stagehand.com` (stable).
**Expected:** `beta` shows the guide body (toggle script loads and correctly resolves `beta`); `stable` shows the "not available here" fallback (toggle script loads and correctly resolves `stable`, hides the guide).
**Why human:** Requires a live browser session against production CloudFront distributions after a separate phase (05) ships; this verification session cannot fast-forward that dependency, and `.planning/WINDOWS.md` entry id 2 already tracks this as an open, must-re-run item.

### 2. Confirm the curated Tester's Guide content reads correctly for an external tester

**Test:** A human reads `src/content/docs/testers-guide.md` end to end (236 lines) and confirms the D-07 curation — trimmed from `puppet-console`'s 1375-line source down to setup framing, 17 currently-supported manual-QA scenarios, and a `/support/` pointer — is accurate, complete, and free of any reference to internal `puppet-console` tooling, deferred-to-v2 features, or dead file paths.
**Expected:** The content reads as a coherent, self-contained guide an external tester with no source-checkout access could follow unassisted.
**Why human:** `04.1-03-SUMMARY.md` explicitly flags this curation-depth/audience-fit judgment as `human_judgment: true`, not yet performed by a human reviewer — content quality and completeness at this level is not mechanically verifiable.

### Gaps Summary

No blocking gaps. All six roadmap success criteria for Phase 04.1 are either fully live-proven
(criteria 1-5, independently re-verified in this session against real CloudFront distributions,
Terraform state, and a clean `npm run verify` run) or carried as an explicit, user-accepted override
with an accurately-documented root cause (criterion 6 / AUTH-06's live pre-launch cross-environment
behavior, tracked in `.planning/WINDOWS.md` entry id 2 and confirmed against the real commit it
cites). Two items require human sign-off before this phase's evidence trail is fully closed: (a) the
mandatory post-Phase-05 re-verification of Truth 6 that WINDOWS.md itself already mandates, and (b)
a first human read-through of the curated Tester's Guide content, which 04.1-03's own SUMMARY
correctly flagged as unperformed. Neither blocks proceeding to the next phase; both are tracked and
should not be lost.

---

_Verified: 2026-08-28T13:34:39Z_
_Verifier: Claude (gsd-verifier)_
