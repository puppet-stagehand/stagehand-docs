---
phase: 04.1-gated-tester-access
verified: 2026-08-30T19:52:00Z
status: human_needed
score: 6/6 must-haves verified (5 fully live-proven at initial verification, unchanged this run; 1 PASSED via a still-open, previously-accepted override)
behavior_unverified: 0
overrides_applied: 1
overrides:
  - must_have: "The Tester's Guide (DOC-01) is added to the gated-path set and is rendered only on testpilots and beta — stable renders an honest 'not available here' state for a real tester session (Roadmap SC 6 / AUTH-06)."
    reason: "Carried forward unchanged from the initial 2026-08-28 verification. Live-tested (not just code-level) via an ad hoc Playwright session using only the tester-gate credential against beta and www (stable). Root cause isolated and documented: a separate, temporary, out-of-phase whole-site `enable_basic_auth` lockdown (commit 44b56c9) shares the same WWW-Authenticate realm as the new tester gate, so the gated page's own static assets (including the visibility-toggle script) 401 for a tester-credential-only session — the toggle script never loads/runs pre-launch. Re-confirmed this run: `infra/environments/{testpilots,beta,stable}/main.tf` still declare `enable_basic_auth = true`, and WINDOWS.md ledger id 2 is still `open` — the lockdown has not yet been removed by Phase 05 (which is itself still mid-flight: waves 8-9 pending). User previously selected Option 1: accept as a pre-launch-only limitation, expected to self-resolve with zero code change when Phase 05 removes the temporary whole-site lockdown. No code change was made to gated-paths.json, redirect.js, or any environment's enable_basic_auth setting this run either — this gap-closure plan (04.1-05) explicitly scoped itself out of this item (confirmed: `git diff --stat` shows zero changes under infra/ between the prior close-out commit and HEAD)."
    accepted_by: "user (via 04.1-04-SUMMARY.md 'Decision Resolved' record; re-confirmed unchanged this run, no new user decision required since nothing about this item changed)"
    accepted_at: "2026-08-28"
re_verification:
  previous_status: human_needed
  previous_score: 6/6
  gaps_closed:
    - "UAT test 3 / code-review CR-01: the Tester's Guide visibility toggle failed OPEN (guide server-rendered visible by default, fallback hidden by default; only a successfully-executing client script narrowed visibility for a disallowed channel). Any visitor whose browser never ran that script — JS disabled, blocked by policy/extension, slow/incomplete hydration, or any other cause — would have seen the guide body on `stable`. Closed by gap-closure plan 04.1-05 (commits c8679d3, d728754): DocsLayout.astro now server-renders `.docs-content` with `hidden={entry.data.visibleOn ? true : undefined}` and the fallback paragraph with no default `hidden`; TesterGuideVisibility.astro's guard clause now only ever reveals-on-allow, never hides-on-disallow. Independently re-verified in this session, not trusted from SUMMARY/REVIEW claims (see Behavioral Spot-Checks and Data-Flow Trace below)."
  gaps_remaining: []
  regressions: []
gaps: []
human_verification:
  - test: "Re-verify Truth 6 / AUTH-06 live cross-environment behavior after Phase 05 removes the temporary whole-site enable_basic_auth lockdown."
    expected: "beta shows the guide body (toggle script loads and correctly resolves 'beta'); stable shows the 'not available here' fallback (toggle script loads and correctly resolves 'stable', hides the guide) — for a tester holding only the tester-gate credential, not the whole-site basic-auth credential."
    why_human: "Requires a live browser session against production CloudFront distributions once Phase 05's remaining waves (8-9) ship and the temporary lockdown is removed. Re-confirmed this run that `enable_basic_auth = true` is still set in all three environments' Terraform, so this cannot be verified from the current codebase/infra state. Tracked in .planning/WINDOWS.md (id 2, still open) and .planning/phases/04.1-gated-tester-access/04.1-UAT.md (test 1, result: blocked, blocked_by: prior-phase)."
  - test: "Read src/content/docs/testers-guide.md end to end and confirm the D-07 curation (trimmed from puppet-console's 1375-line TESTER-GUIDE.md to setup framing, 17 currently-supported manual-QA scenarios, and a /support/ pointer) reads accurately and completely for an external tester audience."
    expected: "The curated content is accurate, unambiguous, and free of references to internal puppet-console tooling, deferred-to-v2 features, or dead file paths."
    why_human: "Content-judgment/audience-fit call the phase's own artifacts flag as human_judgment: true. User explicitly deferred this in 04.1-UAT.md test 2 (result: skipped) to a later batched content review once the User Guide, installer guide, and module guides are also ported, rather than reviewing this one page in isolation now. Still unperformed as of this run."
---

# Phase 04.1: Gated Tester Access (INSERTED) Verification Report

**Phase Goal:** A tester with the shared password reaches the testing guides — including a ported
Tester's Guide, visible only on testpilots/beta — and anyone without the password is refused at the
edge; the password is nowhere in the public repository.

**Verified:** 2026-08-30T19:52:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (04.1-UAT.md test 3 / code-review CR-01)

## Goal Achievement

### Gap Closure Verification (this run's primary focus)

**Claim under test:** 04.1-UAT.md test 3 was updated from a blocker to `result: passed`, and its
corresponding Gaps entry marked `status: resolved`, on the basis that gap-closure plan 04.1-05
(commits `c8679d3`, `d728754`) fixed the Tester's Guide visibility toggle to fail closed instead of
fail open (04.1-REVIEW.md CR-01). This claim was independently re-verified end to end, not accepted
from SUMMARY.md/REVIEW.md/UAT.md text:

1. **Code read directly** — `src/components/TesterGuideVisibility.astro` and
   `src/layouts/DocsLayout.astro` were read in full. The guard clause in
   `TesterGuideVisibility.astro` is now `if (allowed) { guide.removeAttribute('hidden');
   fallback.setAttribute('hidden', '') }` with no `else` branch — matching the claimed
   reveal-on-allow inversion. `DocsLayout.astro`'s `.docs-content` div carries
   `hidden={entry.data.visibleOn ? true : undefined}` and the `data-tester-guide-unavailable`
   paragraph carries no default `hidden` attribute — matching the claimed fail-closed default.
2. **Built the site independently** (`npm run build:e2e`) and inspected
   `dist/docs/testers-guide/index.html` directly: `<div class="docs-content" data-tester-guide="true"
   hidden>` and `<p data-tester-guide-unavailable>` (no `hidden`) are present in the literal static
   HTML that ships — the safe default does not depend on client JS running.
3. **Ran the new Playwright spec independently**
   (`npx playwright test tests/e2e/tester-guide-visibility.spec.ts --project=production`): 3/3 pass
   — Test A (JS disabled, no-JS default is fallback-visible/guide-hidden), Test B (real
   `beta.puppet-stagehand.com` hostname via `--host-resolver-rules`, working script reveals the
   guide), Test C (real `www.puppet-stagehand.com` hostname, working script keeps the guide hidden).
4. **Confirmed non-gated pages are unaffected**: `grep -o 'class="docs-content"[^>]*>'` across
   `dist/docs/{getting-started,security,first-run}/index.html` shows no `hidden` attribute on any of
   them — the new default-hidden behavior is scoped exactly to pages with `visibleOn` set, as
   claimed.
5. **Ran `npm run verify` from a clean invocation** in this session: exit 0. format/lint/typecheck
   (`astro check`)/`validate:data`/98 unit tests/16 redirect tests/build/route-check/
   invalidation-check/link-check all pass; 30/30 Playwright e2e tests pass (including all 3 new
   tester-guide-visibility tests); `check:e2e-isolation` reports clean isolation
   (`production=0 (0 fixture-derived)`).
6. **Confirmed scope containment**: `git diff --stat` for the two 04.1-05 commits touches exactly
   the three files the plan declared (`src/layouts/DocsLayout.astro`,
   `src/components/TesterGuideVisibility.astro`, `tests/e2e/tester-guide-visibility.spec.ts`) — no
   changes under `infra/` or to `gated-paths.json`/`redirect.js`, confirming this fix did not touch
   the edge-gate mechanism itself.

**Conclusion: the gap closure is real and the UAT status update was warranted.** UAT test 3's
`result: passed` and its Gaps entry's `status: resolved` both hold up against direct code
inspection, an independent build, and an independent test run — not just SUMMARY.md narration.

### Observable Truths (Roadmap Success Criteria 1-6)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A request to a gated path without credentials is refused by the CloudFront viewer-request function with `401` and a `WWW-Authenticate` challenge, before any S3 origin is reached; a request carrying the shared password is served normally. | ✓ VERIFIED | Unchanged by this run's gap closure (`infra/` diff is empty for the 04.1-05 commits). `infra/modules/static-site/functions/redirect.js`'s `isGatedPath()` guard and mutual exclusion with `basicAuthEnabled` re-confirmed by direct read; `node --test infra/modules/static-site/tests/redirect.test.mjs` independently re-run this session, 16/16 pass including 6 gate-specific tests (`gated path with no authorization header is refused with a Basic challenge`, `...wrong credential is refused`, `...correct credential passes through`, `...fails closed when the KVS lookup fails`, etc). Live-CloudFront curl proof from the initial 2026-08-28 verification not independently re-run this session (no infrastructure change occurred that would affect it). |
| 2 | The shared password exists only in a CloudFront KeyValueStore and in the operator runbook's rotation procedure — `git grep` over the working tree and history finds no credential, no hash of one, and no placeholder that is actually live, and the repository stays public. | ✓ VERIFIED | Independently re-ran the credential sweep this session: `git grep -n "gate_expected_header"` across the tracked tree returns only the KVS *key name* in code/tests/docs/plan files, never a value; `git grep -n "gate_expected_header="` (assignment form) returns zero matches. `.claude/settings.local.json` re-confirmed gitignored (`git check-ignore -v` succeeds) and untracked (`git ls-files` returns nothing for it) — same info-only note as the initial verification, not a phase failure. |
| 3 | Rotating the credential is a KeyValueStore write documented in a runbook, and does not require republishing the CloudFront function or redeploying the site. | ✓ VERIFIED | `docs/operations/tester-access.md` unchanged by this run (not in the 04.1-05 diff); rotation procedure and live rotate/restore evidence carried forward unchanged from the initial verification. |
| 4 | `npm run verify` is green with the gate live: `check:links` and the Playwright e2e suite either carry the credential or exclude the gated paths by an explicit, recorded rule — neither silently reports success because it stopped crawling, and neither is weakened for unrelated paths. | ✓ VERIFIED | **Independently re-ran `npm run verify` from a clean invocation this session**: exit 0, all stages pass including 30/30 e2e (3 more than the initial verification's 27, accounting for the 3 new tester-guide-visibility tests), `check:e2e-isolation` clean. |
| 5 | Which paths are gated is declared in exactly one place that both the function and the verification tooling read, so an ungated testing guide is a build-visible error rather than a discovery in production. | ✓ VERIFIED | `infra/modules/static-site/gated-paths.json` re-confirmed unchanged, still exactly `["/docs/testers-guide"]`; not touched by the 04.1-05 gap closure. |
| 6 | The Tester's Guide, ported from `puppet-console`'s `docs/TESTER-GUIDE.md`, sits behind the gate and renders on `testpilots`/`beta`; the identical, immutably-promoted build served on `stable` shows an honest "not available here" state instead, decided by client-side hostname detection — not a separate build. | ✓ PASSED (override) | **Code-level and local-build mechanism now verified stronger than at initial verification** — the toggle now fails closed (this run's core finding, see Gap Closure Verification above), closing 04.1-REVIEW.md's CR-01 information-disclosure risk (T-04.1-12) for every failure-to-execute cause. **Live cross-environment behavior against real CloudFront still does not hold pre-launch**, unchanged from initial verification: `enable_basic_auth = true` is still set in all three environments' Terraform, so a tester-credential-only session still cannot fetch the toggle script's own asset and cannot be live-proven until Phase 05 removes that temporary lockdown. Carried forward as the same user-accepted override (WINDOWS.md id 2, still `open`; UAT test 1, still `result: blocked`). |

**Score:** 6/6 truths verified (5 unchanged and re-confirmed this run, 1 carried as PASSED via a
still-open, previously user-accepted override). The gap-closure-specific truths (04.1-05's own
must_haves D1-D4) are additionally verified below.

### Gap-Closure Plan's Own Must-Haves (04.1-05-PLAN.md)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| D1 | A visitor whose browser never executes the visibility-toggle script (JS disabled, blocked, slow hydration, or any other cause) is served the fallback and never the guide body — fails closed. | ✓ VERIFIED | Test A (`javaScriptEnabled: false`) independently re-run, passes. Static `dist/docs/testers-guide/index.html` inspected directly: `hidden` attribute present on `.docs-content`, absent on the fallback paragraph, in the literal built HTML. |
| D2 | A tester on testpilots/beta whose toggle script runs successfully still sees the full guide body, unchanged from before this fix. | ✓ VERIFIED | Test B (real `beta.puppet-stagehand.com` hostname via `--host-resolver-rules`) independently re-run, passes. |
| D3 | A visitor on stable whose toggle script runs successfully still sees the fallback, unchanged from before this fix. | ✓ VERIFIED | Test C (real `www.puppet-stagehand.com` hostname) independently re-run, passes. |
| D4 | Non-gated documentation pages render exactly as before — the new default-hidden state is conditional on `entry.data.visibleOn`. | ✓ VERIFIED | Independently grepped built `dist/docs/{getting-started,security,first-run}/index.html`: no `hidden` attribute on any `.docs-content` div. `npm run verify`'s full unit/e2e suite (98 unit + 30 e2e) independently re-run, all pass, zero regressions. |

**Score:** 4/4 gap-closure truths verified.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/layouts/DocsLayout.astro` | `.docs-content` defaults hidden when `visibleOn` set; fallback defaults visible | ✓ VERIFIED | Read directly; `hidden={entry.data.visibleOn ? true : undefined}` present on the div (line 62); no `hidden` on the fallback `<p>` (line 68). |
| `src/components/TesterGuideVisibility.astro` | Reveal-on-allow guard, no else branch | ✓ VERIFIED | Read directly; `if (allowed) { ... }` with no `else`, comment rewritten to state the fail-closed contract. |
| `tests/e2e/tester-guide-visibility.spec.ts` | 3 tests proving no-JS default, beta-reveal, stable-hide | ✓ VERIFIED | 68-line spec, 3 tests, independently re-run, 3/3 pass. |
| `infra/modules/static-site/gated-paths.json` | Unchanged single source of truth | ✓ VERIFIED | Confirmed unchanged, `["/docs/testers-guide"]`. |
| `infra/modules/static-site/functions/redirect.js` | Unchanged gate guard clause | ✓ VERIFIED | Confirmed unchanged (not in 04.1-05 diff); 16/16 redirect tests independently re-run. |
| `src/content/docs/testers-guide.md` | Curated Tester's Guide content, `visibleOn: ['testpilots', 'beta']` | ✓ VERIFIED | 236 lines, unchanged; frontmatter confirms `visibleOn: ['testpilots', 'beta']`. |
| `docs/operations/tester-access.md` | Rotation runbook | ✓ VERIFIED | Unchanged, not touched by gap closure. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `DocsLayout.astro`'s default markup | `TesterGuideVisibility.astro`'s reveal-on-allow script | Both sides agree the safe default is guide-hidden/fallback-visible | ✓ WIRED | Confirmed by direct read of both files and by the built static HTML matching the server-rendered default exactly. |
| `TesterGuideVisibility.astro` | `src/lib/site-channel.ts`'s `resolveSiteChannel` | `import { resolveSiteChannel } from '../lib/site-channel'` | ✓ WIRED | Unchanged; import present, used to compute `channel`/`allowed`. |
| `tests/e2e/tester-guide-visibility.spec.ts` | `playwright.config.ts`'s `production` project | `baseURL 127.0.0.1:4321`, `--project=production` flag | ✓ WIRED | Test run this session used exactly this project and passed against the real built `dist/`. |
| `cloudfront.tf` | `gated-paths.json` | `file()`/`replace()` chain | ✓ WIRED | Unchanged, not touched by gap closure. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| No-JS visitor sees fallback, not guide | `npx playwright test tests/e2e/tester-guide-visibility.spec.ts --project=production` (Test A) | passed (107ms) | ✓ PASS |
| Beta hostname with working script reveals guide | same file, Test B | passed (163ms) | ✓ PASS |
| Stable hostname with working script keeps fallback | same file, Test C | passed (140ms) | ✓ PASS |
| Full standing gate is green post-fix | `npm run verify` | exit 0; 30/30 e2e; isolation clean | ✓ PASS |
| Redirect/gate unit tests unaffected | `node --test infra/modules/static-site/tests/redirect.test.mjs` | 16/16 pass | ✓ PASS |
| No credential leaked in tracked tree | `git grep -n "gate_expected_header="` (assignment form) | zero matches | ✓ PASS |
| Non-gated docs pages unaffected | `grep -o 'class="docs-content"[^>]*>' dist/docs/{getting-started,security,first-run}/index.html` | no `hidden` attribute on any | ✓ PASS |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `dist/docs/testers-guide/index.html` `.docs-content` `hidden` attribute | server-rendered default visibility | `entry.data.visibleOn` (Astro content collection frontmatter) via `DocsLayout.astro`'s conditional | Yes — verified by direct inspection of the built static HTML, not a mock or fixture | ✓ FLOWING |
| Client-revealed guide state | `allowed` boolean | `resolveSiteChannel(window.location.hostname)` in `TesterGuideVisibility.astro`, exercised against real hostnames in Tests B/C | Yes — real hostname resolution via Chromium `--host-resolver-rules`, not a stubbed function | ✓ FLOWING |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| AUTH-01 | 04.1-01, 04.1-02, 04.1-04 | Shared-password edge gate, refuses unauth 401, serves auth normally | ✓ SATISFIED | Unchanged, 16/16 redirect.test.mjs pass this session (Truth 1). |
| AUTH-02 | 04.1-01, 04.1-04 | Credential only in KVS, never in repo | ✓ SATISFIED | git grep sweep independently re-run this session, clean (Truth 2). |
| AUTH-03 | 04.1-02, 04.1-04 | Rotation is a KVS write, no republish/redeploy | ✓ SATISFIED | Runbook unchanged (Truth 3). |
| AUTH-04 | 04.1-02, 04.1-04 | check:links/e2e stay green, exclusion reason recorded | ✓ SATISFIED | `npm run verify` independently re-run this session, green, 30/30 e2e (Truth 4). |
| AUTH-05 | 04.1-01, 04.1-04 | Gated-path set declared in exactly one place | ✓ SATISFIED | gated-paths.json unchanged, single source (Truth 5). |
| AUTH-06 | 04.1-03, 04.1-04, 04.1-05 | Tester's Guide visible testpilots/beta only, stable shows fallback, fails closed for any reason the toggle doesn't run | ⚠️ SATISFIED (override on live cross-environment leg only) | **CR-01's fail-open defect fully closed and independently re-verified this session** (code, build, and 3/3 test evidence above). Live pre-launch cross-environment proof against real CloudFront distributions remains blocked by the still-open, unrelated temporary whole-site lockdown (Truth 6). |
| DOC-01 | 04.1-03 | Tester's Guide ported and curated, gated by AUTH-01/06 | ✓ SATISFIED | Content unchanged, 236 lines, real; human curation-quality read-through still pending (deferred by user, see Human Verification). |
| GATE-06 | 04.1-01, 04.1-04, 04.1-05 | Test asserts unauth refused 401, auth succeeds; gate correctness proven by automated test | ✓ SATISFIED | redirect.test.mjs's 6 gate tests (16/16 total independently re-run) plus the new tester-guide-visibility.spec.ts's 3/3 tests proving the visibility mechanism itself is now test-proven, not asserted. |

No orphaned requirements found — REQUIREMENTS.md's Phase 04.1 mapping (AUTH-01..06, DOC-01, GATE-06)
matches exactly what the five plans (04.1-01 through 04.1-05) declare across their `requirements`
frontmatter, and REQUIREMENTS.md's traceability table marks all eight as `Complete | Phase 04.1`.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.claude/settings.local.json` | n/a | Live tester-gate plaintext password embedded in a local, gitignored Claude Code permission-allowlist entry (carried forward, unchanged, from the initial verification) | ℹ️ Info | Not a phase failure — untracked and gitignored, confirmed again this session via `git check-ignore -v` and `git ls-files`. Does not violate AUTH-02, which is scoped to the tracked repository. No action required for this phase's goal. |

No `TODO`/`FIXME`/`HACK`/`PLACEHOLDER`/`TBD`/`XXX` debt markers found in any of the gap-closure's
key files (`DocsLayout.astro`, `TesterGuideVisibility.astro`,
`tests/e2e/tester-guide-visibility.spec.ts`) or in any of the phase's other key files re-checked
this session.

04.1-REVIEW.md's re-review (dated 2026-08-31T00:47:00Z, superseding the prior report in full)
independently found the CR-01 fix correct and complete via its own build inspection and test run,
and flagged one pre-existing, out-of-scope, non-blocking warning (WR-01: the client script hardcodes
its allowed-channel set rather than reading `entry.data.visibleOn`'s actual array contents — present
since the toggle's original introduction, untouched by this fix, and explicitly out of scope for
04.1-05). WR-01 does not affect any of this phase's success criteria or must-haves today (the one
gated document's `visibleOn` value happens to match the hardcoded pair exactly) and is not a gap
against this phase's goal; it is a latent design note for whoever adds a second gated page.

### Human Verification Required

### 1. Re-verify Truth 6 / AUTH-06 live cross-environment behavior after Phase 05 removes the temporary whole-site lockdown

**Test:** Once Phase 05's remaining waves ship and `enable_basic_auth` is removed from all three
environments, repeat a live Playwright/curl check authenticating as a real tester (tester-gate
credential only, no other credential) against `/docs/testers-guide/` on `beta.puppet-stagehand.com`
and `www.puppet-stagehand.com` (stable).
**Expected:** `beta` shows the guide body; `stable` shows the fallback — both because the toggle
script now loads (no competing basic-auth challenge on its own asset request) and correctly resolves
its hostname.
**Why human:** Requires a live browser session against production CloudFront distributions once a
separate, still-in-progress phase (05) ships its remaining waves. Re-confirmed this session that
`enable_basic_auth = true` is still set in all three environments' Terraform and WINDOWS.md id 2 is
still `open` — this cannot be verified from the current codebase/infra state.

### 2. Confirm the curated Tester's Guide content reads correctly for an external tester

**Test:** A human reads `src/content/docs/testers-guide.md` end to end (236 lines) and confirms the
D-07 curation is accurate, complete, and free of any reference to internal `puppet-console` tooling,
deferred-to-v2 features, or dead file paths.
**Expected:** The content reads as a coherent, self-contained guide an external tester with no source
checkout could follow unassisted.
**Why human:** Content-judgment/audience-fit call the phase's own artifacts flag as
`human_judgment: true`. User explicitly deferred this in `04.1-UAT.md` test 2 (`result: skipped`) to
a later batched content review alongside the User Guide, installer guide, and module guides, rather
than reviewing this page in isolation now — still unperformed as of this run.

### Gaps Summary

No blocking gaps remain. The gap this re-verification run exists to check — UAT test 3 /
code-review CR-01's fail-open Tester's Guide visibility toggle — is independently confirmed closed:
the code was read directly, the site was independently built and its static HTML inspected, and the
new Playwright spec was independently re-run (3/3 pass), alongside a full clean `npm run verify` run
(exit 0, 30/30 e2e, zero regressions). The UAT status update to `result: passed` and the Gaps entry's
`status: resolved` are both warranted.

All six roadmap success criteria hold: five are fully verified with no changes since the initial
verification, and the sixth (AUTH-06) is stronger than before (the fail-open design defect is now
closed) but still carries the same, unchanged, previously user-accepted override for one specific
leg — live cross-environment proof against real CloudFront, which remains blocked by an unrelated,
still-open, temporary whole-site lockdown tracked in WINDOWS.md id 2 pending Phase 05's remaining
waves. Two items require human sign-off before this phase's evidence trail is fully closed, both
carried forward unchanged from the initial verification: (a) the mandatory post-Phase-05
re-verification of Truth 6, and (b) a first human read-through of the curated Tester's Guide content,
deliberately deferred by the user to a later batched review. Neither blocks proceeding; both are
tracked and should not be lost.

---

_Verified: 2026-08-30T19:52:00Z_
_Verifier: Claude (gsd-verifier)_
