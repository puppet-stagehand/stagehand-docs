---
phase: 04.1
slug: gated-tester-access
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-27
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (unit), Playwright (e2e), `node --test` (redirect/CloudFront-function unit tests) |
| **Config file** | `vitest.config.ts`, `playwright.config.ts`, `infra/modules/static-site/tests/*.test.mjs` (no separate config — plain `node:test`) |
| **Quick run command** | `npm run test:unit` |
| **Full suite command** | `npm run verify` |
| **Estimated runtime** | ~60-120s (unit) / several minutes (full verify incl. e2e/build) |

---

## Sampling Rate

- **After every task commit:** Run `npm run test:unit` (and `npm run test:redirect` for CloudFront Function logic changes)
- **After every plan wave:** Run `npm run verify`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

Filled in by the planner per task once PLAN.md tasks are authored — this file is seeded here to
satisfy the Wave 0 gate; the planner completes the table.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | AUTH-01..06, DOC-01, GATE-06 | — | Unauthenticated request to a gated path is refused with 401 | unit | `npm run test:redirect` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Extend `infra/modules/static-site/tests/redirect.test.mjs` (or a sibling `*.test.mjs`) with
      gate-logic test cases (401 without credential, pass-through with credential) — GATE-06.
- [ ] `tests/unit/docs-collection.test.ts` — must be updated for the new Tester's Guide content
      entry (research flagged this test's closed slug-array assertion breaks otherwise).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| KeyValueStore credential rotation | AUTH-03 | Requires a real AWS apply + KVS write; no local emulation | Follow the operator runbook: write a new value via `aws cloudfrontkeyvaluestore-key put-key` (or Terraform-managed KVS entry), confirm old credential rejected and new one accepted against the live CloudFront distribution, without a function republish. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
