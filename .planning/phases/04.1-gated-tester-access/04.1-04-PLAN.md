---
phase: 04.1-gated-tester-access
plan: 04
type: execute
wave: 3
depends_on: ["04.1-02", "04.1-03"]
files_modified: []
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, DOC-01, GATE-06]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "npm run verify is fully green with the gate, the KVS terraform, and the Tester's Guide content all live together in the working tree."
    - "Each of the 6 roadmap success criteria for Phase 04.1 is demonstrated true against the real, deployed environments — not merely asserted from code review."
    - "A git grep sweep over the full working tree and history finds no plaintext password, no base64-encoded expected-header value, and no live placeholder credential anywhere."
  artifacts: []
  key_links:
    - "Plan 04.1-02's live gate + Plan 04.1-03's live content -> the same real /docs/testers-guide/ route on testpilots/beta/stable, proven together for the first time in this plan"
  prohibitions:
    - requirement_id: AUTH-02
      category: values
      status: resolved
      verification: automated
      resolution: null
      reason: null
      statement: "MUST NOT find any committed plaintext password, encoded expected-header value, or live placeholder credential anywhere in the working tree or git history — verified by an explicit git grep sweep, mirroring PUB-07's established convention from Phase 2."
---

<objective>
Close Phase 04.1: run the full standing verification gate with every change from this phase present
together for the first time, and prove each of the roadmap's six success criteria against the real,
live environments — not just against code and mocked tests.

Purpose: confirm AUTH-01 through AUTH-06, DOC-01, and GATE-06 are genuinely true in production, and
that no credential ever leaked into the repository across the whole phase.
Output: a green `npm run verify`, and a SUMMARY recording the live cross-environment proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.1-gated-tester-access/04.1-01-SUMMARY.md
@.planning/phases/04.1-gated-tester-access/04.1-02-SUMMARY.md
@.planning/phases/04.1-gated-tester-access/04.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full npm run verify with every phase change present</name>
  <files>(none — verification only; any fix is mechanical drift in files already owned by prior plans)</files>
  <action>
    Run `npm run verify` from a clean working tree with all of Plans 04.1-01 through 04.1-03's
    changes present. If anything fails, fix the specific drift (e.g. a formatting or lint issue from
    combining independently-authored files) without reopening any of this phase's design decisions —
    this task closes the phase, it does not replan it. Re-run until green.
  </action>
  <verify>
    <automated>npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify` exits 0.
    - No fix in this task touches the mutually-exclusive gate/basic-auth branch structure, the KVS
      key-naming scheme, or the content curation scope decided in prior plans — only mechanical
      integration drift (formatting, lint, closed-enumeration lists) is in scope here.
  </acceptance_criteria>
  <done>npm run verify is green with the complete phase's changes present together.</done>
</task>

<task type="auto">
  <name>Task 2: Live cross-environment proof and the credential-leak sweep</name>
  <files>(none — real-AWS/git-history verification only, no repository files modified)</files>
  <read_first>.planning/ROADMAP.md (Phase 04.1's six numbered success criteria, verbatim)</read_first>
  <action>
    Against the three real, live hostnames, demonstrate each roadmap success criterion with a
    concrete command and its output, recorded in the SUMMARY:
    1. `curl -sI "https://testpilots.puppet-stagehand.com/docs/testers-guide/"` with no credential
       returns 401 with a `www-authenticate` header; the same request with `-u ":$PASSWORD"` does not.
       Repeat for `beta.puppet-stagehand.com` and `www.puppet-stagehand.com`.
    2. `git grep -i` (working tree) and `git log --all -p | grep -i` (full history) for the chosen
       password's literal value, its base64-encoded expected-header form, and the string
       `gate_expected_header=` followed by anything non-empty — all must find nothing (AUTH-02). This
       mirrors PUB-07's established sweep convention from Phase 2 (REQUIREMENTS.md).
    3. Confirm rotating the credential requires no function republish: describe the KVS key's current
       `LastModifiedTimestamp` via `aws cloudfront-keyvaluestore-get-key` (or equivalent describe
       call) before and after a test rotation on one environment, and confirm the function's own
       `ETag`/`LastModifiedTime` (via `aws cloudfront describe-function`) is unchanged across that
       rotation (AUTH-03). Restore the original password afterward.
    4. Re-confirm `npm run check:links` and `npm run test:e2e` both still pass with the gate live
       (AUTH-04 — they never traverse CloudFront, so this is expected, not a coincidence).
    5. `git grep -n "docs/testers-guide"` across `infra/modules/static-site/gated-paths.json`,
       `infra/modules/static-site/tests/redirect.test.mjs`, and `src/content/docs/testers-guide.md`
       to show the one declared path is what every consumer actually uses (AUTH-05).
    6. Load `https://beta.puppet-stagehand.com/docs/testers-guide/` and
       `https://www.puppet-stagehand.com/docs/testers-guide/` (each with the correct credential) in a
       real browser context or via a Playwright ad hoc script, and confirm the former shows the guide
       body and the latter shows the "not available on this site" fallback (AUTH-06/DOC-01).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' "https://www.puppet-stagehand.com/docs/testers-guide/" | grep -q '^401$' &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - All six numbered checks above are recorded in the SUMMARY with their real command output, not
      just a claim they were run.
    - The credential-leak sweep (check 2) finds nothing.
  </acceptance_criteria>
  <done>Every one of Phase 04.1's roadmap success criteria is demonstrated true against the real, deployed environments, and no credential ever leaked into the repository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Full repository history -> public GitHub | The final, phase-closing point at which a credential leak must be caught if it ever happened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-11 | Information Disclosure | Full git history across all three of this phase's plans | high | mitigate | Task 2's explicit `git log --all -p` sweep, mirroring PUB-07's established convention, is the last line of defense if an earlier plan's discipline slipped. |
</threat_model>

<verification>
- `npm run verify` is green.
- All six roadmap success criteria are demonstrated against real, live environments with recorded command output.
- The credential-leak sweep finds nothing.
</verification>

<success_criteria>
Phase 04.1 is complete: the gate genuinely refuses/admits at the edge on all three real
environments, the credential never touched the repository, rotation needs no republish, the gated
path is declared once and used consistently everywhere, and the Tester's Guide is live only where
intended — all proven against reality, not just against code.
</success_criteria>

<output>
Create `.planning/phases/04.1-gated-tester-access/04.1-04-SUMMARY.md` when done
</output>
