---
status: partial
phase: 04.1-gated-tester-access
source: [04.1-VERIFICATION.md, 04.1-REVIEW.md]
started: 2026-08-28T14:15:57Z
updated: 2026-08-29T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Re-verify AUTH-06 live post-launch
expected: beta shows the guide body; stable shows the honest "not available on this site"
  fallback — using a live browser session that holds only the tester-gate credential (no
  whole-site basic-auth credential), against the real testpilots/beta/stable distributions,
  once Phase 05 removes the temporary enable_basic_auth lockdown (commit 44b56c9). Tracked in
  .planning/WINDOWS.md (id 2).
result: blocked
blocked_by: prior-phase
reason: "User confirmed: still prompted for both passwords (tester-gate credential AND the
  whole-site basic-auth credential) to reach a fully-working page — the whole-site lockdown
  hasn't been removed yet, so AUTH-06's tester-credential-only behavior can't be verified live
  until Phase 05 ships. Matches the documented gap exactly; not a new issue."

### 2. Read the curated Tester's Guide end-to-end for audience fit
expected: src/content/docs/testers-guide.md (D-07 curation, trimmed from puppet-console's
  1375-line TESTER-GUIDE.md to setup framing, 17 currently-supported non-deferred manual-QA
  scenarios, and a /support/ pointer) reads accurately and completely for an external tester
  audience — no reference to internal puppet-console tooling, deferred-to-v2 features, or dead
  file paths.
result: skipped
reason: "Deferred follow-up: user will review content once all remaining content (user guide,
  installer guide, module guides) is pushed up, rather than reviewing this one page in
  isolation now."

### 3. Tester's Guide toggle hides pre-release content only when its own script runs (code-review CR-01)
expected: A `stable` visitor with no working page script (JS disabled, script blocked by
  policy/extension, slow hydration, or any other reason the toggle script fails to execute)
  should see the same honest "not available on this site" fallback as a working visitor —
  the mechanism should fail closed for the content it's meant to hide, not fail open.
result: issue
reported: "Code review (04.1-REVIEW.md, CR-01) found `TesterGuideVisibility.astro:20-26`
  server-renders the guide body visible and the fallback hidden by default, and only
  hides the guide if the client-side script runs and resolves the channel. This is
  independent of the already-accepted D7/AUTH-06 asset-blocking gap (which explains one
  specific cause of the script not running and is expected to self-resolve at Phase 05
  launch) — the fail-open *design* persists regardless of that fix: any visitor whose
  browser skips the script for any other reason still sees the guide on stable."
severity: blocker

## Summary

total: 3
passed: 0
issues: 1
pending: 0
skipped: 1
blocked: 1

## Deferred Follow-Ups

- test: 2
  idea: "Review src/content/docs/testers-guide.md for audience fit once all remaining content
    (user guide, installer guide, module guides) is pushed up — batching the content review
    rather than doing it page by page."
  deferred_at: 2026-08-28

## Gaps

- truth: "A stable visitor whose browser never runs the visibility-toggle script (JS disabled,
    blocked, slow hydration, or any other cause) still sees the honest fallback, not the
    tester-guide body — the mechanism fails closed for content it exists to hide."
  status: failed
  reason: "Code review (04.1-REVIEW.md, CR-01): TesterGuideVisibility.astro defaults to
    guide-visible/fallback-hidden and only flips state if its own script runs successfully.
    Fails open, not closed."
  severity: blocker
  test: 3
  root_cause: "src/components/TesterGuideVisibility.astro:20-26 — DocsLayout.astro server-renders
    the guide visible and fallback hidden by default; the client script only hides the guide for
    a disallowed channel, never the reverse. No server-side or no-JS fallback path defaults to
    hidden."
  artifacts:
    - path: "src/components/TesterGuideVisibility.astro"
      issue: "Fail-open default visibility; script only narrows visibility, never establishes it"
    - path: "src/layouts/DocsLayout.astro"
      issue: "Server-renders guide visible / fallback hidden by default (lines 59-69)"
  missing:
    - "Invert the default so the guide is hidden and the fallback is shown server-side (or via a
      no-JS state) unless the toggle script explicitly proves an allowed channel — see
      04.1-REVIEW.md CR-01 for a worked fix (Option A: hidden-by-default with reveal-on-allow)."
  debug_session: ""
